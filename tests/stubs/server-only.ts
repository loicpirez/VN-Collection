// Vitest stub for Next.js's `server-only` directive package. The real
// package throws at build time if it's pulled into a client bundle;
// in tests we don't have that distinction, so we noop.
export {};
