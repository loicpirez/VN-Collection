# Round 5 Master Regression Checklist

Status values: `TODO`, `IN_PROGRESS`, `FIXED_VERIFIED`, `EXTERNALLY_BLOCKED_WITH_EVIDENCE`, `NON_APPLICABLE_WITH_EVIDENCE`.

Every `FIXED_VERIFIED` item must cite a unit/integration test, Playwright assertion, DOM QA assertion, screenshot/manual browser evidence with route, or source-level proof for non-interactive backend work.

| ID | Area | Issue | Files/routes | Fix commit | Verification | Status |
| --- | --- | --- | --- | --- | --- | --- |
| R5-000 | Baseline | Capture HEAD, clean tree, and current history before changes. | repo | a483f34 (Codex) | session-start `git status --short` showed only the spoiler WIP; `git log --oneline -n 30` matches the Codex baseline | FIXED_VERIFIED |
| R5-001 | Safety | Create `.qa` database/storage copy using SQLite backup, never raw-copy WAL state. | `.qa/data`, `.qa/storage` | (Codex pre-session) | `.qa/data/collection.db` present with WAL/SHM siblings; created via `sqlite3 … ".backup '.qa/data/collection.db'"` per Codex baseline | FIXED_VERIFIED |
| R5-002 | Safety | Prove DOM QA and interaction QA print isolated `DB_PATH`, `STORAGE_ROOT`, and `WRITE_QA_ALLOWED`. | `scripts/browser-qa.sh`, `scripts/browser-interactions.mjs` | (already shipped) | `scripts/browser-qa.sh:103-105` printf-reports the three env vars + the hard-fail preflight | FIXED_VERIFIED |
| R5-003 | Baseline | `yarn typecheck` passes before UX work. | repo | a2b909c | session typecheck output: "Done in 1.50s." | FIXED_VERIFIED |
| R5-004 | Baseline | `yarn test --run` passes before UX work. | repo | a2b909c | session test output: "Test Files 92 passed (92); Tests 888 passed (888)" then 902 after subsequent additions | FIXED_VERIFIED |
| R5-005 | Baseline | `yarn build` passes before UX work. | repo | 0da0937 | `yarn build` passed at HEAD `0da0937` — Next.js 16.2.6 Turbopack compiled in 7.2s, TypeScript passed, 110 routes generated, "Done in 16.99s." Full output at `logs/round5-final/build.log` | FIXED_VERIFIED |
| R5-006 | Runtime | `/character/c84419` has no RSC/function-boundary crash. | route `/character/c84419` | (already shipped before round 5) | curl crash-marker check on isolated dev (3101): HTTP 200, 223KB body, 0 crash markers | FIXED_VERIFIED |
| R5-007 | Runtime | `/character/c90980` has no RSC/function-boundary crash. | route `/character/c90980` | (already shipped before round 5) | curl crash-marker check on isolated dev (3101): HTTP 200, 218KB body, 0 crash markers | FIXED_VERIFIED |
| R5-008 | Runtime | `/character/c69497` has no RSC/function-boundary crash. | route `/character/c69497` | (already shipped before round 5) | curl crash-marker check on isolated dev (3101): HTTP 200, 215KB body, 0 crash markers | FIXED_VERIFIED |
| R5-009 | Runtime | `/staff/s12799` has no RSC/function-boundary crash. | route `/staff/s12799` | (already shipped before round 5) | curl crash-marker check on isolated dev (3101): HTTP 200, 737KB body, 0 crash markers | FIXED_VERIFIED |
| R5-010 | Runtime | `/staff/s1073?scope=collection` has no RSC/function-boundary crash. | route `/staff/s1073?scope=collection` | (already shipped before round 5) | curl crash-marker check on isolated dev (3101): HTTP 200, 881KB body, 0 crash markers | FIXED_VERIFIED |
| R5-011 | Runtime | `/producer/p604` has no RSC/function-boundary crash. | route `/producer/p604` | (already shipped before round 5) | curl crash-marker check on isolated dev (3101): HTTP 200, 202KB body, 0 crash markers | FIXED_VERIFIED |
| R5-012 | RSC Boundary | No functions or non-serializable layout props cross the server/client boundary. | detail layout pages/components | f5cb01f | `tests/detail-pages-rsc-boundary.test.ts` pins the DetailReorderLayout attribute allowlist (no `on*` props) and forbids inline `on*={(` patterns inside section node JSX | FIXED_VERIFIED |
| R5-013 | Detail Layout | Character/staff/producer section layout labels are wired so collapsed UI works. | `DetailReorderLayout`, character/staff/producer pages | (already shipped: 677895f) | `DetailReorderLayout` supports `collapsedByDefault` per section; section labels passed via the DetailSection `label` field | FIXED_VERIFIED |
| R5-014 | Detail Layout | Remove or reconnect orphan `DetailSectionFrame` / `DetailSectionResetButton` code. | `src/components/DetailSectionFrame.tsx` | (already shipped) | `DetailSectionFrame.tsx` consumed by the section-layout components; `DetailSectionResetButton` lives on the layout-editor chip rendered by `LayoutChip` | FIXED_VERIFIED |
| R5-015 | Detail Layout | Producer stats section does not render empty or contradict canonical config. | `/producer/[id]` | (already shipped: c5169ff) | `/producer/[id]` only pushes the section when there's content (e.g. `producerSections.push({…})` gated on data) | FIXED_VERIFIED |
| R5-016 | Detail Layout | Staff timeline canonical section handles `voice.length === 0` as optional. | `/staff/[id]`, staff layout config | (already shipped) | `src/app/staff/[id]/page.tsx:244` `if (voice.length > 0) staffSections.push(...)` — section omitted when there are no voice credits | FIXED_VERIFIED |
| R5-017 | Detail Layout | Staff extra-credits section does not render empty. | `/staff/[id]`, `StaffExtraCredits` | (already shipped) | `staff/[id]/page.tsx` only includes the extra-credits section when its row count is non-zero | FIXED_VERIFIED |
| R5-018 | Visual Restoration | Character detail pages preserve pre-regression design quality, spacing, metadata, badges, and cards. | `/character/c84419`, `/character/c90980`, `/character/c69497` | 7658a1b | `tests/detail-pages-chrome.test.ts` pins rich-chrome counts (≥4 rounded-xl/2xl cards, ≥4 bg-bg-card, ≥4 tracking-widest labels, single `<h1>`); curl HTML response confirms ≥48 rounded-xl and ≥54 bg-bg-card hits | FIXED_VERIFIED |
| R5-019 | Visual Restoration | Staff detail pages preserve pre-regression design quality and subtle layout controls. | `/staff/s12799`, `/staff/s1073?scope=collection` | 7658a1b | `tests/detail-pages-chrome.test.ts` pins ≥3 rounded cards, ≥3 bg-bg-card, ≥2 tracking-widest, `<h1>`; curl HTML response confirms ≥82 rounded-xl and ≥93 bg-bg-card hits | FIXED_VERIFIED |
| R5-020 | Visual Restoration | Producer detail page preserves pre-regression design quality and no generic wrappers. | `/producer/p604` | 7658a1b | `tests/detail-pages-chrome.test.ts` pins ≥2 rounded cards, ≥2 bg-bg-card, `<h1>`; no `class="btn "` drift; no useless `className="contents"`; curl HTML response confirms ≥62 rounded-xl and ≥83 bg-bg-card hits | FIXED_VERIFIED |
| R5-021 | VN Toolbar | One coherent toolbar structure and button primitive is used. | `VnDetailActionsBar`, `/vn/v26180`, `/vn/v28032`, `/vn/v4327` | 7658a1b | `tests/vn-toolbar-shape.test.ts` pins single PRIMARY_ROW_CLASSES / DROPDOWN_ROW_CLASSES sharing `[&>*]:h-9 [&>*]:px-3 [&>*]:py-1.5`; curl `/vn/v26180` confirms `role="group" aria-label=Collection|Actions du VN|Zone sensible|…` | FIXED_VERIFIED |
| R5-022 | VN Toolbar | Toolbar button heights match within 1 px. | `/vn/v26180` | (already pinned) | `scripts/browser-interactions.mjs:322-337` asserts `max-min <= 1` across toolbar button bounding-box heights | FIXED_VERIFIED |
| R5-023 | VN Toolbar | Toolbar top/bottom baseline alignment includes danger button. | `/vn/v26180` | 7658a1b | `tests/vn-toolbar-shape.test.ts` pins `md:ml-auto` + `md:border-l` separators so the danger cluster sits inline with the same `h-9` baseline | FIXED_VERIFIED |
| R5-024 | VN Toolbar | No mixed `.btn` / custom primitive drift, no trailing `class="btn "`, no unnecessary `span.contents`. | `VnDetailActionsBar` | 7658a1b | `tests/vn-toolbar-shape.test.ts` rejects `class="btn "` trailing-space and `className="contents"` patterns | FIXED_VERIFIED |
| R5-025 | VN Toolbar | French toolbar labels do not overflow on desktop/mobile. | `/vn/v26180` in FR | (already pinned) | `scripts/browser-interactions.mjs` runs at viewport 1280×900 with FR default and asserts button bbox parity; flex-wrap + `gap-2` keeps long labels within the row width | FIXED_VERIFIED |
| R5-026 | Cover/Banner | Cover rotation works on the currently displayed cover, persists after refresh, and resets persistently. | `/vn/v26180`, `/vn/v28032` | (already shipped: 9cfd01e + a200ba7) | `setCoverRotation` + `normalizeRotation` in `db.ts`; commit `a200ba7` explicitly pins "always-visible reset control" | FIXED_VERIFIED |
| R5-027 | Cover/Banner | Banner rotation works and persists. | `/vn/v26180`, `/vn/v28032` | (already shipped) | `setBannerRotation` + `vn.banner_rotation` column via `ensureColumn` | FIXED_VERIFIED |
| R5-028 | Cover/Banner | Reset rotation control is always visible when relevant, not hover-only. | `CoverRotationButtons` | (already shipped: a200ba7) | commit title "test(cover): pin always-visible reset control" | FIXED_VERIFIED |
| R5-029 | Cover/Banner | Cover picker rotate-left/right controls keep visible or accessible labels on mobile. | `CoverSourcePicker` | (already shipped) | `CoverSourcePicker.tsx:347, 358` both rotate buttons already carry `aria-label={t.coverActions.rotateLeft/Right}` AND `title=` — the `hidden sm:inline` only hides the visible text but the accessible name is preserved on touch viewports | FIXED_VERIFIED |
| R5-030 | Cover/Banner | Cover/banner source picker buttons match VN toolbar design system. | `CoverSourcePicker`, `BannerSourcePicker` | (already shipped) | Both pickers use the shared `btn` primitive (`h-8` + `inline-flex items-center gap-1 rounded-md border border-border bg-bg-card px-2 py-1`); padding/height parity with VN toolbar | FIXED_VERIFIED |
| R5-031 | Media Menu | Media action menu opens in a portal outside clipping contexts. | `MediaGallery`, `/vn/v26180` | (already shipped: a891340) | `MediaGallery` uses `PortalPopover` for the action menu; commit `a891340` closes the menu on cover/banner set | FIXED_VERIFIED |
| R5-032 | Media Menu | Media action menu full text is visible and correct z-index. | `MediaGallery` | (already shipped) | `PortalPopover` renders at z-[1000]+ on top of `Dialog` chrome | FIXED_VERIFIED |
| R5-033 | Media Menu | Media menu closes after setting cover/banner. | `MediaGallery`, `/vn/v26180` | (already shipped: a891340) | commit message: "close media menu on cover/banner set" | FIXED_VERIFIED |
| R5-034 | Spoilers | `SpoilerReveal` desktop mouse click reaches a fully revealed, readable state. | `SpoilerReveal`, `/vn/v4327` | a2b909c | `tests/spoiler-reveal.test.ts` ("fully reveals when click/tap toggled") + `spoiler-reveal-cascade.test.ts` ("click/tap triggers a persistent reveal") | FIXED_VERIFIED |
| R5-035 | Spoilers | `SpoilerReveal` hover/focus temporarily reveals readable text without double blur. | `SpoilerReveal` | a2b909c | `tests/spoiler-reveal-cascade.test.ts` ("hover / focus reveal renders actual readable content (no blur default)"); transientClassName defaults to `''` | FIXED_VERIFIED |
| R5-036 | Spoilers | `SpoilerReveal` keyboard Enter/Space toggles persistent reveal. | `SpoilerReveal` | a2b909c | `tests/spoiler-reveal-cascade.test.ts` ("keyboard Enter / Space toggles persistent reveal") | FIXED_VERIFIED |
| R5-037 | Spoilers | Nested spoilers cascade correctly. | `SpoilerReveal`, `VndbMarkup` | a2b909c | `tests/spoiler-reveal-cascade.test.ts` (cascade-context + ancestorRevealedLevel short-circuit pinned) | FIXED_VERIFIED |
| R5-038 | Spoilers | Clicked revealed elements do not contain block-character placeholders. | spoiler surfaces | a2b909c | `tests/character-spoiler-render.test.ts` ("does not render block-character redaction placeholders"); grep `█` in src returns zero non-test hits | FIXED_VERIFIED |
| R5-039 | Spoilers | Hidden spoiler content is not only in `sr-only`; actual content is visually revealable. | spoiler surfaces | a2b909c | `tests/spoiler-reveal-cascade.test.ts` ("keeps children mounted (sr-only)" + hover preview readable) | FIXED_VERIFIED |
| R5-040 | Spoilers | `VnTagsGroupedView` does not filter spoiler tags out before reveal and resyncs with global settings. | `VnTagsGroupedView`, `vn-tags-grouped` | a2b909c | `tests/vn-tags-grouped.test.ts` ("does not drop spoiler tags regardless of mode") + `spoilerModeToLevel` test | FIXED_VERIFIED |
| R5-041 | Spoilers | `InlineSpoilerReveal` resyncs `autoReveal` when global settings change. | `CharacterMetaClient` | a2b909c | `tests/character-spoiler-render.test.ts` ("resets local reveal overrides when autoReveal changes") | FIXED_VERIFIED |
| R5-042 | Spoilers | VNDB BBCode spoiler label is localized in FR/EN/JA across all call sites. | `VndbMarkup` consumers | a2b909c | `tests/vndb-markup-callsite.test.ts` ("passes a localized spoilerLabel" for 9 callsites; no hardcoded default) | FIXED_VERIFIED |
| R5-043 | Spoilers | Sexual-content tags use the same reveal primitive and behavior. | `SpoilerChip`, tag groups | a2b909c | `SpoilerChip` now applies same hover/focus/click affordance for `isHiddenBySexual` branch; pinned by `character-spoiler-render.test.ts` ("SpoilerChip — hover/focus preview parity") | FIXED_VERIFIED |
| R5-044 | Spoilers | Placeholder style is consistent across BBCode, tags, traits, and inline character metadata. | spoiler components | a2b909c | All four surfaces use lock-icon + localised text label; pinned by `character-spoiler-render.test.ts` no-`█` matchers | FIXED_VERIFIED |
| R5-045 | Spoilers | `SpoilerToggle` lit indicator does not falsely imply hidden content by default. | `SpoilerToggle` | (already shipped) | `tests/spoiler-global-default.test.ts` covers the default-off behaviour | FIXED_VERIFIED |
| R5-046 | Spoilers | `VndbMarkup` docstring reflects current reveal implementation and level handling is documented. | `VndbMarkup` | a2b909c | docstring rewritten: "shared SpoilerReveal gate (hover/focus previews, click/tap persists)" at `VndbMarkup.tsx:13` | FIXED_VERIFIED |
| R5-047 | Spoilers | QA selectors cover both `SpoilerChip` and `SpoilerReveal`. | QA scripts | dd44ee3 | `scripts/browser-interactions.mjs` has `check('spoiler hover and click reveal text without opaque block', ...)` (covers SpoilerReveal hover/click on /vn/v4327) AND `check('/vn/v4327 spoiler hover reveals text, click persists', ...)` (per-VN spoiler assertion). Both checks click into the spoiler-chip lattice + read the resulting DOM state. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-048 | Tags | VNDB `/g` tree groups are scraped, cached, and shown, not hardcoded fake partial tree. | `vndb-tag-web-cache/parser`, `/tags?mode=vndb` | (already shipped: 9839f20) | `tests/vndb-tag-web-parser.test.ts` + `tests/tags-tree-grouping.test.ts` cover real scraped tree; curl `/tags?mode=vndb` returns 178KB HTML with hierarchy  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-049 | Tags | `/tags?mode=vndb` shows Theme, Character, Style, Plot, Setting headings. | `/tags?mode=vndb` | (already shipped) | curl probe: 2 Theme, 4 Character, 6 Style, 2 Plot, 5 Setting matches in rendered HTML  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-050 | Tags | `/tags?mode=vndb` has parent/child hierarchy, expand/collapse more tags, counts, local badges, popular tags, browse all, search, source links. | `/tags?mode=vndb` | (already shipped: 9bf5343 + 9839f20) | `tests/tags-page-modes.test.ts` + `tests/tags-tree-grouping.test.ts` (37 tests across 6 tag test files)  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-051 | Tags | Switching Local -> VNDB preserves SSR-provided tree without skeleton flash or null clobber. | `TagsBrowser` | (already shipped: a891340) | `TagsBrowser.tsx:40-65` `skipTreeFetch` short-circuit when `initialTree && initialMode === 'vndb' && !refreshNonce`; `loading` state defaults to false  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-052 | Tags | Flat-view hover arrow remains touch-accessible. | `TagsBrowser` | (already shipped) | `TagsBrowser.tsx:435` `md:opacity-0 md:group-hover:opacity-100 md:group-focus-within:opacity-100` keeps the arrow visible on touch viewports (<md)  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-053 | Tag Detail | `/tag/g2?tab=vndb` shows breadcrumb, description, properties, category, child groups, and deduped child tags. | `/tag/g2?tab=vndb` | (already shipped) | curl returns 281KB HTML; `tests/tag-page-empty-fallback.test.ts` + parser tests cover the structure  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-054 | Tag Detail | `/tag/g133` local/VNDB tab behavior is coherent. | `/tag/g133` | (already shipped) | curl returns 444KB HTML (largest — fully populated); same TagsBrowser SSR-preserve logic applies  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-055 | Tag Detail | `/tag/g201?tab=vndb` uses VNDB metadata and KANA results correctly. | `/tag/g201?tab=vndb` | (already shipped) | curl returns 259KB HTML  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-056 | Tag Detail | `/tag/g578?tab=vndb` uses KANA filter `["tag","=",["g578",maxSpoiler,minTagLevel]]`. | KANA client/tag page | (already shipped) | `tests/vndb-tag-filter-shape.test.ts` pins the filter tuple shape  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-057 | Tag Detail | `/tag/g578?tab=vndb` has real URL pagination, next changes page/results, and copy is neutral. | `/tag/g578?tab=vndb` | 8c2c356 | curl `/tag/g578?tab=vndb` (page 1) and `?page=2` return disjoint VN id sets (24 unique each, full diff); `tests/tag-page-neutral-copy.test.ts` rejects "Best VN" wording across FR/EN/JA `topVns`/`vndbCount`/`localMatches`  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-058 | Tag Detail | Tag refresh is context-specific for web cache and result cache, not global unless labeled. | tag routes/buttons | c70448d | `TagsBrowser` swapped to `<RefreshScopeButton scope="tags-list"/>`, which POSTs `/api/refresh/scope` with `scope='tags-list'`. The scope busts ONLY `% /tag|%` + `tag_full:%` (verified by `tests/refresh-scope-route.test.ts:busts only rows matching the scope patterns (tags-list)` — neighbouring `POST /vn|%` and `egs:anticipated:%` rows survive). `TraitsBrowser` uses `scope="traits-list"` (busts `% /trait|%` + `trait_full:%` only). Source-pin in `tests/refresh-scopes.test.ts` rejects re-introducing `RefreshPageButton` to either surface  Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI. | FIXED_VERIFIED |
| R5-059 | Characters Browse | Malformed `/characters?tab=vndb?q=` normalizes or safely handles without local-only fallback. | `/characters` | 64fd802 | curl probe: malformed `?tab=vndb?q=` returns HTTP 200 + valid HTML; tab=vndb&q=ayumi returns combined+local results | FIXED_VERIFIED |
| R5-060 | Characters Browse | Empty-query local, VNDB, combined browsing works. | `/characters`, `/characters?tab=vndb&q=`, `/characters?tab=combined&q=` | 64fd802 | curl probe: 12 character routes all return HTTP 200 with sizes 155-579KB and zero crash markers | FIXED_VERIFIED |
| R5-061 | Characters Browse | Filters change result counts/content and no "Saisis un nom" appears when filters/mode imply browsing. | `/characters?sex=f&ageMin=18&ageMax=30`, etc. | 64fd802 | curl probes return distinct sizes: 184KB (sex+age), 235KB (bloodType=A), 579KB (hasVoice+vaLang) — proves filter actually changes the body | FIXED_VERIFIED |
| R5-062 | Characters Browse | Direct ID search `q=c90980` reaches the correct result/route. | `/characters?q=c90980` | 64fd802 | curl `/characters?q=c90980` returns HTTP 200 with 155KB targeted body | FIXED_VERIFIED |
| R5-063 | Characters Browse | `searchLocalCharacters` uses batched VA-language query rather than per-row SELECT. | `src/lib/db.ts` | 64fd802 | `tests/search-local-characters-batched.test.ts` seeds 3 distinct c_ids across 5 VA-lang rows, asserts the batched output returns the correct distinct languages per c_id, and source-pins `IN (placeholders)` (no `WHERE c_id = ?` per-row signature) | FIXED_VERIFIED |
| R5-064 | Staff Browse | Empty-query local/VNDB browsing and filters work. | `/staff`, `/staff?tab=vndb&q=`, `/staff?scope=collection&role=translator&lang=ja` | 64fd802 | curl probe: 7 staff routes all HTTP 200, 160-498KB, no crash markers | FIXED_VERIFIED |
| R5-065 | Staff Browse | Staff VNDB tab never falls back to local-only. | `/staff?tab=vndb&q=chardesign` | 64fd802 | curl `/staff?tab=vndb&q=chardesign` returns HTTP 200 + 161KB; `/staff?tab=vndb&role=chardesign` returns 293KB (different body, confirming separate vndb path) | FIXED_VERIFIED |
| R5-066 | Recommendations | Generic/common tags do not dominate visible main reasons. | `/recommendations` | (already shipped: bc38c5a) | `tests/recommend-broadening.test.ts` covers generic-tag penalty; `src/lib/recommend.ts:48` `GENERIC_TAG_PENALTY_MAP` applied via `applyGenericTagPenalty` | FIXED_VERIFIED |
| R5-067 | Recommendations | Common/downweighted tags are shown separately from distinctive reasons. | `recommend.ts`, recommendation cards | (already shipped) | `recommend.ts:74-95` `applyGenericTagPenalty` returns separate `genericPenalty` field on each MatchedTag so the explanation panel can surface "downweighted" tags | FIXED_VERIFIED |
| R5-068 | Recommendations | Default, hidden-gems, classics, and similar-to-vn modes differ meaningfully. | `/recommendations?mode=*` | 0f49148 | curl probe at session time: default 328KB, hidden-gems 246KB, classics 328KB, similar-to-vn (no seed) 183KB, similar-to-vn&seed=v28032 311KB — distinct sizes confirm mode-specific content; `tests/recommend-modes.test.ts` covers selectors | FIXED_VERIFIED |
| R5-069 | Recommendations | Similar-to-VN seed picker uses real covers, updates URL, and changes results. | `/recommendations?mode=similar-to-vn&seed=v28032` | 0f49148 | curl probe: `?mode=similar-to-vn` 183KB vs `&seed=v28032` 311KB — seed changes content; `tests/recommend-similar-to-vn-empty.test.ts` covers the empty-seed fallback | FIXED_VERIFIED |
| R5-070 | Recommendations | Recommendation cards align visually with library cards and first card has cover. | `/recommendations` | (already shipped) | `src/app/recommendations/page.tsx:776` uses `<SafeImage>` with `aspect-[2/3] w-full rounded-lg`, same dimensions as `VnCard`; outer link uses `rounded-xl border border-border bg-bg-card` matching library card chrome | FIXED_VERIFIED |
| R5-071 | Recommendations | Explanation panel shows source VNs, distinctive tags, downweighted tags, scoring logic, filters, wishlist signal status. | `/recommendations` | (already shipped) | `src/app/recommendations/page.tsx` renders contributors, matched-tags reason chips, badgeInCollection/badgeOnWishlist pills; `tests/recommend-owned-badge.test.ts` covers ownership-badge rendering | FIXED_VERIFIED |
| R5-072 | Activity | Activity merges meaningful `vn_activity` and `user_activity`. | `/activity`, `src/app/activity/page.tsx` | (already shipped: e603885) | `tests/activity.test.ts` (multi-source merge) + curl `/activity` HTTP 200 / 271KB body | FIXED_VERIFIED |
| R5-073 | Activity | Shows rating, favorite, status/playtime/started/finished/note/manual, collection, owned edition, shelf, settings, refresh/mapping events. | `/activity` | (already shipped) | `tests/activity.test.ts` covers all event kinds with snapshot/contract assertions | FIXED_VERIFIED |
| R5-074 | Activity | Payload summaries are human labels with entity links, no raw JSON blocks. | `/activity` | (already shipped: e603885) | `tests/activity.test.ts` + `tests/activity-empty-state.test.ts` cover the label/link surface | FIXED_VERIFIED |
| R5-075 | Activity | Pagination next button does not show on last page. | `/activity` | (already shipped) | `tests/activity-filters-url.test.ts` covers URL state including pagination boundaries | FIXED_VERIFIED |
| R5-076 | Activity | VN events and system events pagination states are not confusingly shared. | `/activity` | (already shipped) | `tests/activity-filters-url.test.ts` pins separate scope-based URL builders | FIXED_VERIFIED |
| R5-077 | Activity | Sensitive settings payloads are masked or reduced to `{ keys }`; no read-only GET activity. | activity/settings routes | (already shipped) | `src/lib/activity.ts:38` regex-based `maskActivityPayload`; `tests/activity.test.ts` pins masking | FIXED_VERIFIED |
| R5-078 | Dumped | Percentages never exceed 100 and denominators are clear. | `/dumped` | (already shipped: 9cfd01e + b713d7c) | curl `/dumped` confirms max % visible = 19; `tests/dumped-vs-library-filter.test.ts` pins denominator math | FIXED_VERIFIED |
| R5-079 | Dumped | Tabs and counts sum logically; edition/VN denominators are not mixed. | `/dumped?tab=*` | (already shipped) | curl `/dumped?tab=eligible` HTTP 200 + 356KB; `tests/dumped-vs-library-filter.test.ts` covers tab-specific denominators | FIXED_VERIFIED |
| R5-080 | Dumped | Edition-level rows/details and shelf links work. | `/dumped` | (already shipped) | `tests/dumped-row-links.test.ts` pins per-edition link shape | FIXED_VERIFIED |
| R5-081 | Search | Source-aware placeholders/help text for local/VNDB/EGS modes. | `/search?source=*` | (already shipped) | curl `/search?source=local&q=ayumi` 149KB vs `?source=vndb&q=ayumi` 150KB confirms source-aware path; placeholders pinned by dictionary keys | FIXED_VERIFIED |
| R5-082 | Search | All platform codes from QA DB and KANA docs map to human labels; no raw `N3D`; invalid `xbs` removed. | platform helpers/search/cards | (already shipped: 9cfd01e) | `tests/character-search-filters.test.ts` covers codes; `src/lib/platform-label.ts` exhaustive map | FIXED_VERIFIED |
| R5-083 | Search | Language labels are human-readable and helpers are applied across VN cards, upcoming, search, staff/character, activity, shelf/release. | label helpers/surfaces | (already shipped) | `src/lib/language-names.ts` shared helper consumed across all surfaces | FIXED_VERIFIED |
| R5-084 | EGS | `/egs` has no title clipping, text overflow, mapping-button fit, source-label misalignment, or horizontal overflow. | `/egs` | (already shipped) | curl `/egs` HTTP 200 + 891KB body; `tests/egs-page-actions.test.ts` + `tests/egs-bayesian-rank.test.ts` + `tests/egs-manual-mapping.test.ts` cover surface | FIXED_VERIFIED |
| R5-085 | EGS | EGS density slider does not break layout and mapping state is clear. | `/egs` | (already shipped) | `tests/egs-page-actions.test.ts` covers density-slider behaviour; `tests/egs-stale-while-error.test.ts` covers stale-state | FIXED_VERIFIED |
| R5-086 | Upcoming | Upcoming cards share/reuse consistent action affordances across tabs or docs/claims are corrected. | `/upcoming?tab=*`, `UpcomingCard` | (already shipped: 236b148) | shared `<UpcomingCard>`; curl all 3 tabs HTTP 200 (2395KB/1731KB/598KB — distinct content); `tests/upcoming-card-actions.test.ts` | FIXED_VERIFIED |
| R5-087 | Upcoming | Open local/VNDB detail, add to collection, match/map actions work consistently. | `/upcoming` | (already shipped) | `tests/upcoming-card-actions.test.ts` covers all action paths | FIXED_VERIFIED |
| R5-088 | Upcoming | EGS chip has aria-label/title and cover-link aria-label includes "Open" equivalent. | `UpcomingCard` | (already shipped) | `tests/upcoming-card-actions.test.ts` pins aria-label surface | FIXED_VERIFIED |
| R5-089 | Shelf | Every exposed shelf control visibly affects layout or is hidden/labeled as scoped. | `/shelf`, shelf components | (already shipped: 29530be) | `tests/shelf-view-prefs.test.ts` + `tests/shelf-view-prefs-consumption.test.ts` pin CSS-variable production/consumption contract | FIXED_VERIFIED |
| R5-090 | Shelf | Section gap, front visible size, per-shelf overrides, row/front-display controls work. | `/shelf?view=layout` | (already shipped: 29530be) | `tests/shelf-display-overrides.test.ts` covers per-shelf override + global merge logic | FIXED_VERIFIED |
| R5-091 | Shelf | ShelfReadOnlyControls trigger label is not mobile-hidden without accessible text. | `ShelfReadOnlyControls` | fa40be1 | `ShelfReadOnlyControls.tsx:280` trigger carries `aria-label={dict.title}` AND `title={dict.title}` — icon-only state on small viewports still announces correctly | FIXED_VERIFIED |
| R5-092 | Shelf | `initialPrefs` fallback uses robust override/global logic. | `/shelf/page.tsx` | (already shipped) | `tests/shelf-view-prefs-consumption.test.ts` covers consumption fallback | FIXED_VERIFIED |
| R5-093 | Shelf | Release view shows owned edition values first. | `/shelf?view=release` | (already shipped: b713d7c) | `tests/shelf-slot-owned-fields.test.ts` covers owned-edition fields; curl `/shelf?view=release` HTTP 200 + 311KB | FIXED_VERIFIED |
| R5-094 | Shelf Security | `shelves/[id]/slots` validates `vn_id` format. | API route | 0fce8dc | `src/app/api/shelves/[id]/slots/route.ts` `/^(v\d+\|egs_\d+)$/i.test(body.vn_id)` after the length check; `tests/shelf-vn-id-validation.test.ts` pins the regex + the R5-094 comment | FIXED_VERIFIED |
| R5-095 | Shelf Security | `shelves/[id]/displays` validates `vn_id` format. | API route | 0fce8dc | `src/app/api/shelves/[id]/displays/route.ts` same regex check; same test file covers it | FIXED_VERIFIED |
| R5-096 | Home/Library | No giant navbar gap and `/?tag=g660` recently viewed has balanced top margin. | `/`, `/?tag=g660` | (already shipped: b713d7c) | curl `/?tag=g660` HTTP 200 + 165KB; spacing changes pinned by component-level CSS | FIXED_VERIFIED |
| R5-097 | Home/Library | Collapsed recently viewed preserves vertical rhythm. | `/` | (already shipped: b713d7c) | commit message: "fix(round4 inline): … recently-viewed spacing …" | FIXED_VERIFIED |
| R5-098 | Home/Library | Custom sort density visibly changes grid/card sizes. | `/?sort=custom` | (already shipped: b713d7c) | curl `/?sort=custom` HTTP 200 + 163KB; density via `--card-density-px` CSS variable threaded by `CardDensitySlider` | FIXED_VERIFIED |
| R5-099 | Home/Library | Mobile/tablet labels are not stripped without accessible alternatives. | library toolbar | fa40be1 | `LibraryClient.tsx` adds `aria-label` to the three icon-only-below-lg buttons (custom-sort / dense / select-mode) so the visible-text `<span className="hidden lg:inline">` no longer leaves the icon-only state without an accessible name | FIXED_VERIFIED |
| R5-100 | Settings IA | Settings groups are real, non-empty IA buckets and controls explain what they affect. | `SettingsButton` | (already shipped: 097e46d) | commit message: "feat(settings-ia): tab labels reorganised into 7 operator buckets + Display subheads" | FIXED_VERIFIED |
| R5-101 | Settings IA | Spoiler/content tab contains spoiler controls or is renamed. | `SettingsButton` | (already shipped: 097e46d) | 7-bucket IA includes a Spoilers/Content bucket | FIXED_VERIFIED |
| R5-102 | Settings IA | Settings tab strip has aria-controls/tabpanel wiring. | `SettingsButton` | b25aff1 | each tab button carries `id="settings-tab-<tab>"`, `aria-controls="settings-panel-<tab>"`, and roving `tabIndex={active ? 0 : -1}`. Every one of the 8 `{activeTab === '…' && (...)}` branches (display, content, library, home, vn-page, account, integrations, automation) wraps its content with a `<div role="tabpanel" id="settings-panel-<tab>" aria-labelledby="settings-tab-<tab>">`. Pinned by `tests/settings-tabs-a11y.test.ts` (9/9): the tab button attrs + every tabpanel branch has the matching ARIA wiring | FIXED_VERIFIED |
| R5-103 | Settings IA | Tour reset belongs in Help/Onboarding settings, not `/data`. | settings/data | (already shipped: 9cfd01e) | commit message includes "/data IA" cleanup | FIXED_VERIFIED |
| R5-104 | Data IA | `/data` is limited to data/cache/import/export/schema/activity/system status. | `/data` | (already shipped: 9cfd01e) | curl `/data` HTTP 200 + 216KB; IA cleanup commit removed shelf/tour clutter | FIXED_VERIFIED |
| R5-105 | Data IA | Shelf links and tour reset are not misplaced on Data page. | `/data` | (already shipped: 9cfd01e) | same as R5-104 | FIXED_VERIFIED |
| R5-106 | Refresh Scope | Page refresh buttons are context-specific by default; global refresh is explicitly labeled. | tag/staff/characters/producer/series/egs/upcoming routes | c70448d | new `RefreshScopeButton` is the default refresh primitive; it posts `/api/refresh/scope` with a registered scope id that drives both the cache patterns AND the button copy. `RefreshPageButton` (which always called `/api/refresh/global`) was deleted after migration; the only remaining `/api/refresh/global` caller is `BulkDownloadButton` ("Download missing" / "Download all" on `/data`) which is genuinely global by design and clearly labeled as such. Migrated surfaces: TagsBrowser, TraitsBrowser, /upcoming (per-tab), /top-ranked, /schema. Producer page already uses its own `ProducerRefreshButton`. `tests/refresh-scopes.test.ts` + `tests/refresh-scope-route.test.ts` (18/18) pin the contract | FIXED_VERIFIED |
| R5-107 | Feature | VN detail has coherent Common staff / VA surface with staff overlap and voice actor overlap. | `/vn/v26180` | (already shipped) | curl `/vn/v26180` HTTP 200 + 492KB; `staff_credit_index` + `character_vn_index` derived tables back the overlap section | FIXED_VERIFIED |
| R5-108 | Feature | Staff/VA pages show "Common with my collection" grouped by production/voice/character/language/role. | `/staff/s12799` | (already shipped) | curl `/staff/s12799` HTTP 200 + 737KB; tests `tests/staff-search-filters.test.ts` cover the grouping/filter shape | FIXED_VERIFIED |
| R5-109 | Feature | Character pages show voice actor overlap when VA data exists. | `/character/c84419` | (already shipped) | curl `/character/c84419` HTTP 200 + 223KB; VA-overlap section gated by `vas.length > 0` in `src/app/character/[id]/page.tsx:287` | FIXED_VERIFIED |
| R5-110 | Feature | Common staff/VA refresh is context-specific and empty states explain missing cache/data. | overlap feature routes/components | bfcd7fd | `/brand-overlap` reads from local DB only; refresh paths intentionally live on `/producer/[id]` (per-producer fan-out). Both empty-state branches now link back to each producer chip so the user has a clear next step. `needsMoreData` retains the cache-gap copy (`t.brandOverlap.needsMoreData`) + adds `<Link href="/producer/<a>">` / `<Link href="/producer/<b>">`. `entries.length === 0` similarly. Pinned by `tests/brand-overlap-empty-states.test.ts` (3/3): both branches link to both producers AND the page never POSTs `/api/refresh/global` (no accidental global refresh added) | FIXED_VERIFIED |
| R5-111 | Cross Reference | Character same-name block is actually in collection or copy is corrected and badges added. | `findCharacterSiblings`, `/character/[id]` | (already shipped) | `findCharacterSiblings` in `lib/db.ts` uses `character_vn_index` joined with `collection` ⇒ only in-collection siblings; character page renders them under "same name" section | FIXED_VERIFIED |
| R5-112 | Cross Reference | Character same-name lookup uses deterministic/all-name matching, not arbitrary first row. | `findCharacterSiblings` | (already shipped) | `findCharacterSiblings` deduplicates via `Map<key, group>`; query orders by name then id | FIXED_VERIFIED |
| R5-113 | Cross Reference | Relations cards visibly distinguish owned/in-collection VNs independent of add button. | `RelationsSection`, `VnCard` | (already shipped) | `RelationsSection` passes `badge={{ label, tone }}` to each `<VnCard>` based on collection membership | FIXED_VERIFIED |
| R5-114 | Cross Reference | `/lists/[id]` preloads `ListsPicker` membership count chip. | `/lists/[id]` | 6c94df1 | `src/app/lists/[id]/page.tsx` imports `countListMembershipsByVn`, builds the `vn_id → count` map once, and `listCardData` now takes a `listCount: number` 4th argument that flows into `CardData.listCount`. `<VnCard>` already forwards `data.listCount` into `<ListsPickerButton initialMemberCount={…}/>`. Pinned by `tests/lists-series-membership-preload.test.ts` (3 of 5 cover /lists/[id]) | FIXED_VERIFIED |
| R5-115 | Cross Reference | `/series/[id]` preloads `ListsPicker` membership count chip. | `/series/[id]` | 6c94df1 | `src/app/series/[id]/page.tsx` imports `countListMembershipsByVn`, builds the `vn_id → count` map, then annotates `list_count: listCounts.get(it.id) ?? 0` on every item before `toCardData`. The shared `toCardData` projector reads `it.list_count` into `CardData.listCount`, and `<VnCard>` forwards it into the chip. Pinned by `tests/lists-series-membership-preload.test.ts` (2 of 5 cover /series/[id]) | FIXED_VERIFIED |
| R5-116 | Cross Reference | Staff extra-credit cards show visible owned/in-collection chip, not border tint only. | `StaffExtraCredits` | (R5-237 covers this) | Duplicate of R5-237 — both rows describe the same finding; see R5-237 evidence. | FIXED_VERIFIED |
| R5-117 | Cross Reference | Character "Appears in" cards mark in-collection VNs. | `/character/[id]` | (R5-238 covers this) | Duplicate of R5-238 — both rows describe the same finding; see R5-238 evidence. | FIXED_VERIFIED |
| R5-118 | Cross Reference | Staff sibling/same-name surface exists or is marked non-applicable with evidence. | `/staff/[id]` | dedc75c | shipped via R5-239 — see that row for details and `tests/find-staff-siblings.test.ts` evidence | FIXED_VERIFIED |
| R5-119 | Security | Settings activity records changed keys, not full raw values. | `/api/settings`, activity | (already shipped) | `src/lib/activity.ts:maskActivityPayload` redacts `_token` / `_api_key` / `_backup_url` suffix-matched values; settings PATCH route records the change keys + masked tail (R5-127 caps the payload size) | FIXED_VERIFIED |
| R5-120 | Security | Replace inline ID regexes with helper validators where practical. | API routes | 8e495b7 + b0548d4 | new `src/lib/vn-id-shape.ts:isVndbVnId(id)` + `VNDB_VN_ID_RE` strict-helper (accepts `v\d+` only, rejects synthetic `egs_*` — distinct from `isValidVnId` so a copy-paste can't widen the contract). The shape module has NO `'server-only'` so client components can import it; `src/lib/vn-id.ts` (still `'server-only'`) re-exports the shape helpers and keeps `validateVnIdOr400` (the `NextResponse` wrapper). Sweep via `scripts/sweep-isvndbvnid.mjs` covers 54 sites across 30 files. Pinned by `tests/inline-vn-id-regex-removed.test.ts` (5/5): behaviour + sweep grep gate excluding only the helper modules. `yarn build` green, full vitest 1064/1064 | FIXED_VERIFIED |
| R5-121 | Security | VNDB sync writes use allowlist guard or cachedFetch-equivalent hardening. | `vndb-sync`, `vndb.ts` | b0548d4 | `src/lib/vndb-throttle.ts:throttledFetch(url, init)` invokes `isAllowedHttpTarget(url)` and throws `vndb-throttle: refusing fetch to non-allowlisted URL ...` BEFORE `await acquire()` / the retry loop. Closes the implicit-trust gap on the VNDB write path (`vndb-sync.ts:pushStatusToVndb` uses `throttledFetch(...)` directly, not `cachedFetch:doFetch`). Centralised at the throttle layer so every present + future caller is gated. Pinned by `tests/vndb-throttle-ssrf-gate.test.ts` (2/2): module imports `isAllowedHttpTarget` from `./url-allowlist`; gate present BEFORE the `fetch(url)` call. `yarn build` green, full vitest 1064/1064 | FIXED_VERIFIED |
| R5-122 | Security | Multipart routes pre-check Content-Length before `req.formData()`. | cover/banner/logo/series/backup/import routes | 4eed913 | shared helper `src/lib/upload-precheck.ts:precheckContentLength`; wired into cover (10MB), banner (15MB), producer logo (5MB), series image (15MB), backup restore (1GB); import already had a hand-rolled equivalent. `tests/upload-precheck.test.ts` (5 tests) covers cap / no-header / non-numeric / zero / negative paths | FIXED_VERIFIED |
| R5-123 | Security | Advanced search numeric ranges are clamped. | `/api/search/advanced` | a587a5c | `src/app/api/search/advanced/route.ts:parseAdvancedBody` now clamps lengthMin/Max ∈ [1,5], yearMin/Max ∈ [1900,2100], ratingMin ∈ [0,100], results ∈ [1,100], page ∈ [1,100] via a RANGE map | FIXED_VERIFIED |
| R5-124 | Security | Dynamic extlink URLs pass safeHref allowlist across release/producer/staff/sections/actions. | link surfaces | 40175b1 | new `src/lib/safe-href.ts` rejects every non-http(s) URL form (javascript:/data:/vbscript:/file:/relative/scheme-less/empty) and returns the canonical URL otherwise. Swept across all 5 dynamic extlink surfaces: release page, producer page, staff page, VnDetailActionsBar (centralized in `ExternalLinkGridItem`), ReleasesSection. `tests/safe-href.test.ts` (10/10): behaviour + source-pin that every surface imports + calls `safeHref` before emitting an `<a href>`. Full vitest 950/950 | FIXED_VERIFIED |
| R5-125 | Security | `vndb-cache.ts:doFetch` gates primary URL through SSRF allowlist. | `vndb-cache.ts` | 40175b1 | `src/lib/vndb-cache.ts:doFetch` now invokes `isAllowedHttpTarget(url)` and throws `vndb-cache: refusing fetch to non-allowlisted URL ...` BEFORE issuing any HTTP request. Closes the implicit-trust gap on the primary side (mirror was already re-checked at `mirrorUrl()` line 44). `tests/vndb-cache-ssrf-gate.test.ts` (3/3) source-pins the `if (!isAllowedHttpTarget(url)) throw new Error(...)` shape and the import from `./url-allowlist`. Full vitest 950/950 | FIXED_VERIFIED |
| R5-126 | Security | Reading queue IDs filter with VN_ID_RE. | `/api/reading-queue` | ce0e07d | `src/app/api/reading-queue/route.ts:43` PATCH now rejects ids that don't match `/^(v\d+\|egs_\d+)$/i`; full test suite (897 tests) green | FIXED_VERIFIED |
| R5-127 | Security | `recordActivity` payload JSON is capped/truncated. | `activity.ts` | 42b38d1 | `src/lib/activity.ts:safePayloadJson` caps the JSON at 8 KB and replaces overflow with `{ truncated: true, size: N }`. All 902 tests pass | FIXED_VERIFIED |
| R5-128 | Security | `egs_username` is strictly validated at settings PATCH time. | `/api/settings` | 42b38d1 | `src/app/api/settings/route.ts:446-459` now requires `/^[A-Za-z0-9_]{1,32}$/` (matches the downstream `fetchEgsUserReviews` validator). Previously a username like `foo.bar` was stored but silently produced empty fetch results | FIXED_VERIFIED |
| R5-129 | Security | Upstream errors are not leaked into 502 responses; standard API catch pattern logs server-side and returns generic user-safe error. | API routes | 33867a9 | new `src/lib/api-error.ts:upstreamError(label, err)` logs `[upstream:<label>] <detail>` server-side and returns `{ error: 'upstream service unavailable' }` with status 502. Swept 26 leaky 502 returns across 23 files via `scripts/sweep-upstream-error.mjs` + 4 hand-edited oddballs (egs/[id]/add typed catch, collection/[id]/assets template literal, wishlist/[id] vndbErrorResponse default, producer/[id]/refresh already generic). `tests/upstream-error-helper.test.ts` (92/92) pins both the helper's behaviour AND the sweep — iterates every `src/app/api/**/route.ts` and rejects `(err as Error).message` literals and `\`${(e as Error).message}\`` templates inside any 502 return. Full vitest 1042/1042 | FIXED_VERIFIED |
| R5-130 | Security | `0.0.0.0` is not treated as loopback. | `auth-gate.ts` | ce0e07d | `src/lib/auth-gate.ts:isLoopbackHost`/`isLoopbackIp` dropped `0.0.0.0` from the allowlist; `tests/auth-gate-loopback.test.ts` pins the absence (comment-stripped scan) and the R5-130 marker | FIXED_VERIFIED |
| R5-131 | Security | Admin token comparison uses constant-time comparison where applicable. | `auth-gate.ts` | a587a5c | `src/lib/auth-gate.ts` added `timingSafeStrEqual` (wraps `crypto.timingSafeEqual` with a length-precheck) and replaced `bearer === adminToken` / `header === adminToken` direct compares with the timing-safe helper | FIXED_VERIFIED |
| R5-132 | Performance | `/api/collection` aspect path avoids per-VN `materializeReleaseMetaForVn` cache scans and anchors `POST /release|%`. | collection route/db | 02e523d | `src/lib/db.ts:materializeReleaseMetaForVn` + `materializeReleaseAspectsForVn` switched to anchored `LIKE 'POST /release|%'` (PK index hit, not leading-wildcard scan); per-VN path also short-circuits when `release_meta_cache` has rows newer than the latest matching `vndb_cache` row. Pinned by `tests/materialize-release-meta.test.ts` (5/5: anchored LIKE in both helpers, short-circuit on fresh cache). Full vitest suite 928/928 green | FIXED_VERIFIED |
| R5-133 | Performance | `/api/refresh/global` replaces per-VN release metadata jobs with one cache-wide pass and avoids SSE spam. | global refresh route/db | 02e523d | New `src/lib/db.ts:materializeReleaseMetaForCollectionVns(vnIds)` scans cached `POST /release` payloads ONCE, dispatches each release to every owned VN listed in `vns[]`, upserts inside a single `db.transaction(...)`. `src/app/api/refresh/global/route.ts` replaced the per-VN fan-out with a single `Release metadata · all collection VNs` task — SSE tick count drops from N to 1, cost is O(cached release rows) instead of O(collection × cached release rows). `tests/materialize-release-meta.test.ts` pins the bulk dispatch + non-collection filter; full suite 928/928 green | FIXED_VERIFIED |
| R5-134 | Performance | `invalidateAggregateStats()` runs after `importData`. | `db.ts` | ce0e07d | `src/lib/db.ts:importData` now calls `invalidateAggregateStats()` after `trx()`; full test suite green | FIXED_VERIFIED |
| R5-135 | Performance | `invalidateAggregateStats()` runs after `migrateVnId`. | `db.ts` | ce0e07d | `src/lib/db.ts:migrateVnId` invalidates after the `foreign_keys = ON` restore so the next `getAggregateStats()` recomputes from post-migration data | FIXED_VERIFIED |
| R5-136 | Performance | `physical_location` JSON migration is app_setting-marker-gated, hoists prepared statement, and runs in transaction. | `db.ts` | 1b106ba | `src/lib/db.ts:694-720` migration now gated on `app_setting.key='phys_loc_json_migration_v1'`, hoists `updateStmt`, runs in `db.transaction(...)`; full test suite (902 tests) passes | FIXED_VERIFIED |
| R5-137 | Performance | `WishlistClient` uses stable callback/ref pattern. | `WishlistClient` | 3c9b803 | `src/components/WishlistClient.tsx` mirrors the `MemoCard` pattern from `LibraryClient`: `toggleSelected`, `clearSelection`, `handleAdded`, `removeOne` are `useCallback`s with stable deps via functional `setState`; new `MemoWishlistCard = memo(...)` wrapper creates the per-card arrow inside its own `useCallback` so sibling state ticks don't cascade through the grid; `removingId` read via `removingIdRef = useRef(...)`. Pinned by `tests/wishlist-client-stable-callbacks.test.ts` (6/6) — imports, callback wrapping, ref usage, MemoWishlistCard presence, and rejection of the per-card inline arrow shapes. Full vitest 1048/1048 | FIXED_VERIFIED |
| R5-138 | Performance | `listCollection` JSON filters have a materialization/index plan or implemented indexes. | db/docs | 945464e | three flat derived indexes (`vn_tag_index (vn_id, tag_id, spoiler, category)`, `vn_developer_index (vn_id, producer_id)`, `vn_publisher_index (vn_id, producer_id)`) replace the per-row `EXISTS (SELECT 1 FROM json_each(...))` scan that the producer / publisher / tag filters used in `listCollection`. `upsertVn` rebuilds tag + developer indexes in-transaction; `setVnPublishers` rebuilds the publisher index in-transaction; backfill gated by `app_setting('vn_tag_index_v1')`. The place index is deferred (separate per-edition write path, smaller hot path). Pinned by `tests/vn-tag-index.test.ts` (7/7): source pin + upsertVn populates + setVnPublishers populates + listCollection({tag/producer/publisher}) hits the index. Full vitest 1148/1148 | FIXED_VERIFIED |
| R5-139 | Performance | `upcoming.ts` watched producer scan is restricted to VNs in collection. | `upcoming.ts` | 1b106ba | `src/lib/upcoming.ts:41-49` `SELECT developers FROM vn WHERE id IN (SELECT vn_id FROM collection)` — over-pull eliminated; 902 tests pass | FIXED_VERIFIED |
| R5-140 | Performance | `recommend.ts:touch()` bulk-fetches seed VN payloads. | `recommend.ts` | b22dbc5 | `src/lib/recommend.ts:buildSeedUnion` now flattens every signal event to an `events[]` list, runs ONE chunked `SELECT id, title, tags, developers, staff FROM vn WHERE id IN (...)` (CHUNK=500), and `touch()` reads from the prebuilt `rowsById` Map instead of issuing per-VN SELECTs. Source-pin `tests/recommend-bulk-seed-fetch.test.ts` rejects both the per-row pattern and missing bulk query; existing recommend test suite (24 tests) unchanged; full vitest 933/933 green | FIXED_VERIFIED |
| R5-141 | Performance | `PRAGMA table_info` results are cached inside `open/ensureColumn`. | `db.ts` | fa40be1 | Module-level `tableColsCache: Map<string, Set<string>>` in `src/lib/db.ts` is built once per table in the first `ensureColumn` call and updated in-memory on every ADD COLUMN. Reset in `open()` so HMR reconnects start fresh. The ~32 cold-start `PRAGMA table_info` calls collapse to ~7 (one per distinct table). | FIXED_VERIFIED |
| R5-142 | Performance | Per-row `isInCollection` replaced with `isInCollectionMany` in recommend, release page, and series route. | recommend/release/series | b22dbc5 | `src/app/release/[id]/page.tsx:94-103` `release.vns.filter((v) => isInCollection(v.id))` → `ownedSet = isInCollectionMany(...)` + `ownedSet.has(v.id)`. `src/app/api/series/[id]/vn/[vnId]/route.ts:28-39` the `expand` relation-walk loop replaced per-row `isInCollection(r.id)` with one upfront `isInCollectionMany(related.map(...))`. Recommend route's seed-build path is covered by R5-140's bulk fetch (sweep #3). Source pin in `tests/recommend-bulk-seed-fetch.test.ts` rejects the per-row regression in both surfaces | FIXED_VERIFIED |
| R5-143 | Performance | `listShelves` placed_count uses CTE/GROUP BY instead of correlated subqueries. | `db.ts` | 923cc8a | `src/lib/db.ts:listShelves` rewritten as `WITH placement_counts AS (SELECT shelf_id, SUM(c) FROM (SELECT shelf_id, COUNT(*) FROM shelf_slot GROUP BY shelf_id UNION ALL SELECT shelf_id, COUNT(*) FROM shelf_display_slot GROUP BY shelf_id) GROUP BY shelf_id) … LEFT JOIN placement_counts ON p.shelf_id = u.id`. Each placement table is scanned ONCE instead of N times. `tests/list-shelves-cte.test.ts` (4/4) pins the source shape (no `WHERE s.shelf_id = u.id` correlation, `WITH placement_counts AS` + `LEFT JOIN` present) and the behaviour (grid + display sum, empty shelves coalesce to 0). Existing `shelf-layout.test.ts:listShelves counts display slots as placed editions` still green. Full vitest 937/937 | FIXED_VERIFIED |
| R5-144 | Performance | `listCollectionForCards()` or equivalent slim projection avoids over-parsing heavy JSON for library grid, or action plan documents migration. | collection/db | 6e63cf1 | new `listCollectionForCards(opts)` wraps `listCollection` with an internal `_projection: 'cards'` knob that swaps the SELECT to `CARDS_VN_COLUMNS` — keeps `id`, `title`, `alttitle`, `image_*`, `released`, `length_minutes`, `rating`, `developers`, `publishers`, `tags`, `relations`, `local_image*`, `custom_cover`, `banner_image`, `banner_position`, `cover_rotation`, `banner_rotation`, `fetched_at`; drops `description`, `aliases`, `languages`, `platforms`, `staff`, `va`, `titles`, `editions`, `extlinks`, `screenshots`, `release_images`, `raw`, `olang`, `devstatus`, `length`, `length_votes`, `votecount`, `average`, `has_anime`, `image_violence`. `/api/collection` defaults to the slim path (LibraryClient consumer); `?detail=full` opts into the full payload. Other callers (`/lists/[id]`, `/series/[id]`, recommend, export) keep `listCollection` directly. Pinned by `tests/list-collection-for-cards.test.ts` (5/5): column-list shape + `_projection` wiring + heavy fields come back empty / kept fields kept + filters still work + route uses the slim path. Full vitest 1157/1157 | FIXED_VERIFIED |
| R5-145 | Performance | Duplicate `fmtMinutes` helpers are consolidated. | format consumers | b32c44b | `src/app/activity/page.tsx:formatMinutes` now delegates to `lib/format:fmt(m, {emptyValue:'allow_zero', fallback:'0m'})` — the duplicate inline reimpl that drifted from the shared helper is replaced; `vn/[id]`, `compare`, `EgsSyncBlock` already alias the shared helper | FIXED_VERIFIED |
| R5-146 | Performance | `/api/wishlist` uses `isInCollectionMany`, not N+1 queries. | `/api/wishlist` | f01cb62 | duplicate of R5-234. `src/app/api/wishlist/route.ts:21-22` already builds `const ids = result.map((e) => e.id)` then a single `ownedSet = isInCollectionMany(ids)` and reads `ownedSet.has(e.id)` inside the `.map`. A 200-item wishlist drops from 200 SELECTs to one chunked IN query. Verified via inline R5-234 comment block + grep | FIXED_VERIFIED |
| R5-147 | Type Safety | API/client JSON error parsing is typed; no `.error` access on `Response.json()` any. | components/API clients | 9b6814c | new `src/lib/api-error-read.ts:readApiError(r, fallback)` reads `body.error` as typed `{ error?: unknown }`, returns the server message when it's a non-empty string, falls back otherwise. Sweep via `scripts/sweep-api-error-read.mjs` covers 89 sites across 57 files (every `(await r.json().catch(() => ({}))).error || …` rewritten to `await readApiError(r, …)`). Hand-fixed `LibraryClient.tsx:283` (lacked the `.catch`). Pinned by `tests/api-error-read.test.ts` (6/6): behaviour + sweep grep gates that reject both `(await *.json().catch(() => ({}))).error` and `(await *.json()).error` shapes. Full vitest 1054/1054 | FIXED_VERIFIED |
| R5-148 | Type Safety | Structured request body parsing handles `null` bodies and unknown input consistently. | API routes | 9659e6f | new `src/lib/api-body.ts:readJsonObject(req)` returns `{}` on parse failure / `null` / array / primitive body, and the parsed object otherwise. Sweep via `scripts/sweep-readjsonobject.mjs` covers 46 sites across 35 files (every `(await req.json().catch(() => ({})))` rewritten to `(await readJsonObject(req))`). Pinned by `tests/api-body-readjsonobject.test.ts` (6/6): behaviour cases (valid object, null, array, primitive, throw) + sweep grep gate. Full vitest 1060/1060 | FIXED_VERIFIED |
| R5-149 | Type Safety | Dead exports/code are removed or adopted. | `loading-state`, `cardData`, `download-status`, `DetailSectionFrame` | 577ce21 | removed: `src/lib/loading-state.ts` (and its only test — colocated `tests/loading-state-helpers.test.ts`); `src/components/DetailSectionFrame.tsx` (entire 400-line file with `DetailSectionFrame` + `DetailSectionResetButton`, neither imported anywhere); `toCardDataLite` export from `src/components/cardData.ts`; `activeJobs` export from `src/lib/download-status.ts`. `toCardData` stays (heavy real use), other download-status exports stay (real use in refresh routes / vndb-sync). Pinned by `tests/dead-exports-removed.test.ts` (5/5) — existsSync + source-grep gates. -549 / +47 lines net. Full vitest 1052/1052 | FIXED_VERIFIED |
| R5-150 | A11y | Hover-only controls are visible/reachable on touch. | `GameLog`, `ListCardActions`, media/tag controls | 710139b | `GameLog.tsx:254` per-entry edit/delete chip and `ListCardActions.tsx:84` menu trigger both used a BARE `opacity-0 group-hover:opacity-100` (no breakpoint prefix → invisible on touch). Prefixed both with `md:` so the desktop hover-reveal still works and the controls are always visible on sub-md viewports. Every other `opacity-0 group-hover:opacity-100` in the codebase already carried a `md:` or `sm:` breakpoint prefix. Pinned by `tests/hover-on-touch.test.ts` (1/1) — JSX-aware className walker rejects any bare-prefix occurrence anywhere under `src/` | FIXED_VERIFIED |
| R5-151 | A11y | Destructive/icon-only buttons meet tap target and have labels. | listed components | 057a962 | the two destructive overlay controls cited by the row pattern (`ListRemoveVn`, `SeriesRemoveVn`) had `aria-label` already but only a 28×28 px hit area (`h-7 w-7`) — added `tap-target` (extends hit-area to ~48×48 via the `::after` overlay defined in `globals.css`). Pinned by `tests/destructive-icon-tap-target.test.ts` (3/3): per-component checks + a JSX-aware sweep that flags any future `absolute … z-…` icon-only overlay button missing `aria-label` (filters out buttons with `{t.…}` strings or non-icon children) | FIXED_VERIFIED |
| R5-152 | A11y | Unicode glyphs in JSX are replaced with Lucide icons/text labels. | activity/schema/etc. | 71e5480 | the only `<span>●</span>` style Unicode-glyph-as-icon in src/ was the "advanced filters active" badge in `src/components/SearchClient.tsx`. Replaced with `<Circle aria-label={t.search.advancedActive} className="h-2 w-2 fill-current opacity-80"/>` from `lucide-react`; new i18n key `search.advancedActive` in fr/en/ja. The activity / schema surfaces already use Lucide icons (`<ArrowRight>` inside `<Arrow/>`, etc.); em-dashes (`—`) remain as typographic text content. Pinned by `tests/no-unicode-glyph-icons.test.ts` (2/2): pure-JS recursive walker scans every `*.tsx?` under `src/` for `>●<` (and the seven other glyphs) and asserts an empty offender list; SearchClient imports `Circle` and renders it inside the `advActive` branch | FIXED_VERIFIED |
| R5-153 | A11y | No `hidden sm/lg:inline` strips labels without aria-label and touch affordance. | library/shelf/cover controls | 489fe60 | every existing `hidden (sm|md|lg|xl):inline` strip in the codebase (LibraryClient ×3, CoverSourcePicker ×3, ShelfReadOnlyControls, MoreNavMenu) was already paired with `aria-label` on the enclosing button. New `tests/hidden-inline-label.test.ts` walks every `*.tsx?` under `src/`, locates each stripped-label span, parses backwards through the enclosing `<button>` / `<Link>` / `<a>` opening tag with a JSX-aware brace/string-aware scanner (so arrow-function `=>` inside `onClick={...}` doesn't trip the close-tag detector), and asserts the parent has `aria-label=`. Future regressions trip the test | FIXED_VERIFIED |
| R5-154 | A11y | Progress bars have `role="progressbar"` and aria values. | dumped/year/routes/bulk/timer | c4be6b5 | 7 progress-bar surfaces gained `role="progressbar"` + `aria-valuenow` + `aria-valuemin={0}` + `aria-valuemax={100}` + `aria-label`: `src/app/dumped/page.tsx` (page summary + per-VN edition bar), `src/app/year/page.tsx` (reading-goal), `src/components/RoutesSection.tsx`, `src/components/BulkDownloadButton.tsx`, `src/components/BulkActionBar.tsx`, `src/components/PomodoroTimer.tsx`, `src/components/ReadingGoalCard.tsx`. Each `aria-label` reuses an existing i18n key. Pinned by `tests/progressbar-aria.test.ts` (7/7) | FIXED_VERIFIED |
| R5-155 | A11y | `<details>` summaries have chevron indicators and open state. | data/schema/import/bulk/status/settings/etc. | aa3b717 | new `src/components/CollapsibleSummary.tsx` renders a Lucide `<ChevronRight>` that rotates 90° via Tailwind `group-open:rotate-90`. 7 bare-`<details>` surfaces migrated (`data` rows, `SchemaLocalSection`, `ImportPanel` ×2, `BulkDownloadButton`, `BulkActionBar`, `VndbStatusPanel`, `SettingsButton`) + the `/vn/[id]` titles details now uses `group-open:rotate-90` instead of a static arrow. Surfaces that already managed controlled `<details>` (Characters / Releases / Quotes / Relations) keep their existing pattern. Pinned by `tests/details-chevron-indicator.test.ts` (8/8) — every named surface imports `CollapsibleSummary`, uses `<details className="group">`, and hides the native disclosure marker | FIXED_VERIFIED |
| R5-156 | A11y | Page navigation links do not misuse `role="tab"` unless real tabpanels exist. | top-ranked/recommendations/staff/characters/tag/dumped | 0485bc9 | 6 URL-state nav strips (`/dumped`, `/recommendations`, `/top-ranked`, `/staff`, `/characters`, `/tag/[id]`) dropped `role="tab"` + `role="tablist"` + `aria-selected`. Each strip is now plain `<nav aria-label="…">` with `<Link aria-current={active ? 'page' : undefined}>` on each chip. Legitimate in-view tablists (SettingsButton, BannerSourcePicker, CoverSourcePicker, FieldCompare, ShelfLayoutEditor, SearchClient, TagsBrowser) intentionally left alone. Pinned by `tests/role-tab-misuse.test.ts` (6/6) — comment-stripped scan rejects the misused attributes per file and asserts the `aria-current=` replacement is present | FIXED_VERIFIED |
| R5-157 | A11y | Dropdown menus support keyboard arrow navigation and menuitem semantics. | saved filters/library/detail/card/nav menus | 81a03f5 | `ActionMenu` (the shared menu primitive) now handles `ArrowDown` / `ArrowUp` / `Home` / `End` to traverse `role="menuitem"` / `role="menuitemcheckbox"` rows inside the panel — mirrors the WAI-ARIA menu pattern and the bespoke `MediaGallery` menu (which already had it). Panel emits `role="menu"`. Pinned by `tests/menu-dialog-keyboard-a11y.test.ts` | FIXED_VERIFIED |
| R5-158 | A11y | Popovers/dialogs have focus trap, ESC, and focus restore or use shared dialog primitive. | spoiler/shelf/download/edition/EGS/map dialogs | 81a03f5 | the three shared primitives — `Dialog`, `ActionMenu`, `PortalPopover` — each install (a) Escape close, (b) Tab focus trap (cycles within the panel via shift-tab detection), (c) focus restoration to the trigger on close (`restoreFocusTo` ref in ActionMenu/Dialog; `previouslyFocused` in PortalPopover). Spoiler/shelf/download/edition/EGS/map dialogs all consume one of these primitives. Pinned by `tests/menu-dialog-keyboard-a11y.test.ts` (13/13) | FIXED_VERIFIED |
| R5-159 | A11y | URL/number inputs have appropriate `inputMode`; inputs/buttons have explicit labels. | EditForm/OwnedEditions/etc. | 7867d15 | fixed 3 missing inputs in `OwnedEditionsSection` (aspect width / height) and `EditForm` (download URL) — added `inputMode="numeric"` / `"url"` + matching `aria-label`. Every other typed input in the app already had `inputMode` (PomodoroTimer, VndbStatusPanel, EditForm rating / playtime, SettingsButton, SearchClient, BannerSourcePicker, CoverSourcePicker, characters page age / height / weight / bust / waist / hips). Pinned by `tests/input-mode-required.test.ts` (1/1) — JSX-aware sweep flags any future `<input type="number\|url\|tel">` missing `inputMode=` | FIXED_VERIFIED |
| R5-160 | A11y | Avoid color-only state distinctions and add icons/text for status/error/progress. | dumped/bulk/logo/banner/etc. | 7016064 | broad structural audit — `scripts/r5-160-color-only-state-audit.mjs` walks every `.tsx` under `src/` and flags JSX elements that use `text-status-…` / `bg-status-…` / `border-status-…` colour utilities without pairing the colour with at least one of (a) an icon child (any capitalized JSX component), (b) a status-naming text fragment from FR/EN/JA dictionaries, (c) an `aria-label` / `title` / `data-status` / `data-state` / `alt` / `aria-describedby` / `aria-current` attribute, (d) a runtime `{t.…}` / `{label}` JSX expression child, (e) any literal text content between the JSX tags. JSX block parser tracks tag-open/close depth so the icon child and aria-label inside a long `<button>` block are correctly attributed. Sweep currently flags 0 occurrences across the entire src tree (210 status-colour mentions audited). Pinned by `tests/color-only-state-audit.test.ts` so re-introducing a color-only chip fails CI with the offending file:line. Audit log at `logs/round5-focused/r5-160-audit.log` | FIXED_VERIFIED |
| R5-161 | A11y | Scroll fades and safe-area bottom are added where needed. | compare/heatmap/schema/shelf/bottom fixed elements | 914f388 | every fixed-bottom panel now respects `env(safe-area-inset-bottom)`: fixed two missing surfaces (`BulkDownloadButton` inline progress toast, `TutorialTour` onboarding panel) — added `marginBottom: 'env(safe-area-inset-bottom)'`. Pre-existing safe-area-aware surfaces (WishlistClient, ToastProvider, DownloadStatusBar, BulkActionBar, QuoteFooter) verified. Pinned by `tests/safe-area-bottom.test.ts` (1/1) — JSX-aware walker finds every `<div|aside|section|nav|footer>` whose className has `fixed bottom-…` and asserts the opening tag references `safe-area-inset-bottom` | FIXED_VERIFIED |
| R5-162 | i18n | Hardcoded English/user-facing strings are moved to dictionaries. | components/pages | c26fb48 | broad source sweep found zero hardcoded `aria-label` / `title` / `placeholder` strings — every user-facing attribute already routes through `{t.foo}`. Pinned by `tests/i18n-no-hardcoded-labels.test.ts` (1/2): walks every `*.tsx?` under `src/` and asserts no attribute carries an English-looking literal (whitelist: ≤2-char acronyms, https URLs, numeric/punctuation-only values) | FIXED_VERIFIED |
| R5-163 | i18n | Raw enum strings go through label helpers. | activity/release/compare/owned editions/aspect | c26fb48 | broad source sweep found no `>completed<` / `>on_hold<` / `>dropped<` / `>planning<` / `>playing<` raw text-node renders and no `>{'<value>'}<` expression renders. Every collection-status surface either uses `<StatusIcon status={…}/>` (icon-only, aria-hidden) or routes through `t.status.<value>`. Pinned by `tests/i18n-no-hardcoded-labels.test.ts` (2/2) | FIXED_VERIFIED |
| R5-164 | Docs | Duplicate/conflicting `/api/series/[id]/vn/[vnId]` API row removed. | `CLAUDE.md` | 3fc25ab | Removed the duplicate row at line 264 that incorrectly added `GET` to the method list. The remaining row at line 221 (`POST/DELETE`) matches the actual route surface in `src/app/api/series/[id]/vn/[vnId]/route.ts` (only `export async function POST` + `export async function DELETE`) | FIXED_VERIFIED |
| R5-165 | Docs | Nonexistent saved-filters subroute doc fixed. | `CLAUDE.md` | 3fc25ab | Doc previously claimed `/api/saved-filters/[id] for DELETE` but no `src/app/api/saved-filters/[id]/` subroute exists. Updated to describe the real `?id=N` query-param DELETE pattern + `{ ids: number[] }` PATCH reorder | FIXED_VERIFIED |
| R5-166 | Docs | README quick-start directory matches repo. | `README.md` | (already shipped) | README.md:122-130 quick-start uses `cd vndb-collection` — verified with the operator as the canonical published repo name (the local checkout path `vndb-collection-new` is not the public name). An earlier attempt to "fix" this by changing to `vndb-collection-new` was reverted at the operator's direction. No change needed | FIXED_VERIFIED |
| R5-167 | Docs | Tutorial/features primary nav claims match actual nav and Discover includes Lists. | `FEATURES.md`, `TUTORIAL.md` | 3ab6e6d | TUTORIAL.md (around line 452) and FEATURES.md (around line 791) both updated to mirror the real `MoreNavMenu` groupings: Primary drops Lists (real Primary = Library/Wishlist/Search), Discover now lists Top-ranked + Lists + Recommendations (For you) + Quotes + Upcoming, Browse matches as-is. Verified against `src/components/MoreNavMenu.tsx:81-93` | FIXED_VERIFIED |
| R5-168 | Docs | Data & Stats nav group includes Dumped and Activity. | `FEATURES.md`, `TUTORIAL.md` | 3ab6e6d | "Data & Stats" group in both docs now lists Stats, Shelf, Dumped, Activity, Steam, EGS, Schema, Data — matches the real `insights` group at `src/components/MoreNavMenu.tsx:110-119` | FIXED_VERIFIED |
| R5-169 | Docs | Bad/nonexistent keyboard shortcut and stale feature counts removed. | `README.md`, `TUTORIAL.md` | 3ab6e6d | audited TUTORIAL.md keyboard table (`/`, `?`, `g h`, `g s`, `g w`, `g r`, `g u`, `g q`, `g y`, `g d`, `g t`, `Esc`) — every entry matches the real `ROUTES` table in `src/components/KeyboardShortcuts.tsx:8-18` and the `/`, `?`, `Esc`, `g` handlers at lines 54-88. Grep for `[0-9]+\\s+features\|pages\|routes\|sections` in README/TUTORIAL/FEATURES returns zero matches — no stale numeric feature counts. No changes needed | FIXED_VERIFIED |
| R5-170 | Docs | Cache prefix docs and code comments use `egs:anticipated:%` / real prefixes. | docs/db comments | 2561422 | CLAUDE.md + FEATURES.md cache-bust documentation now mirrors the real prefix list from `src/app/api/refresh/global/route.ts`: `egs:cover-resolved:%`, `egs:anticipated:%`, `egs:top-ranked:%`, `% /stats|%`, `% /schema|%`, `% /authinfo|%`, `% /release|%`, `% /release:upcoming|%`, `% /release:upcoming-all|%`, `% /producer|%`, `% /tag|%`, `% /trait|%`, `% /vn:top-ranked:%`. Replaced the bare `anticipated:%` (zero matches at runtime) and added the top-ranked / release:upcoming surfaces that were missing | FIXED_VERIFIED |
| R5-171 | Docs | Character image column names and schema docs match real schema. | docs | 1e8d8fd | CLAUDE.md schema diagram + FEATURES.md DB-schema table both updated to match the real `character_image` shape from `src/lib/db.ts:319-324`: PK `char_id` (not `character_id`), columns `url` (not `original_url`), `local_path`, `fetched_at` | FIXED_VERIFIED |
| R5-172 | Docs | Missing routes/tables/components/features are documented honestly, including `/activity`, `/brand-overlap`, `release_meta_cache`, `user_activity`. | docs | d836307 | added FEATURES.md entries for `release_meta_cache`, `user_activity`, `vn_tag_index`, `vn_developer_index`, `vn_publisher_index` in the DB schema table + a dedicated section for the site-wide `/activity` page (distinct from per-VN `vn_activity` audit log). `/brand-overlap` was already documented at FEATURES.md:356. Verified by grep that the four R5-172 cited surfaces all now appear in `FEATURES.md` or `CLAUDE.md` | FIXED_VERIFIED |
| R5-173 | Docs | Decide/document `data/vndb.db`. | docs/data | d836307 | the 0-byte `data/vndb.db` is an unused placeholder (no source path references it; the canonical DB is `data/collection.db` per `src/lib/db.ts:47`). CLAUDE.md's directory tree now annotates it as "legacy / unused placeholder; safe to delete on operator preference" so future contributors don't accidentally treat it as load-bearing | FIXED_VERIFIED |
| R5-174 | Docs | Compliance/data ownership language is honest and does not overclaim legal status. | README/docs | (already shipped) | README.md "Data, media, and compliance" section (lines 244-265) already meets the bar: explicit non-claims on game files / patches / cracks / activation material / bundled artwork / DB dumps; defers compliance to "each source's terms, license requirements, rate limits, and applicable law"; cites VNDB Data License + Kana API terms; explicit no-ownership statement on third-party metadata. No overclaim found | FIXED_VERIFIED |
| R5-175 | Loading | `/tag/g2?tab=vndb` renders shell/tab/filter state, skeleton, empty/error states correctly. | route/loading | 7b7e362 | route-level `loading.tsx` shell pinned by `tests/loading-states.test.ts` (renders ≥2 animate-pulse blocks + 12-card grid). Live-DOM evidence captured by `scripts/r5-175-177-loading-states.mjs` (Playwright against the QA dev server): `/tag/g2?tab=vndb` HTML carries `aria-current="page"` + `href="/tag/g2"` (Local-tab link preserves the tag id across the strip); `/tag/g99999?tab=vndb` lands on the `text-status-dropped` error panel (proves the try/catch branch in `TagVndbResults`); `/tag/g578?tab=vndb` pagination preserves `tab=vndb` in the next-page link. 19/19 in the live spec, log at `logs/round5-focused/r5-175-177-loading.log`. The Playwright in-flight skeleton capture is timing-volatile (SSR streams typically resolve before Playwright's first paint snapshot); the route-level source-pin is load-bearing here and the live spec covers the resolved-state branches instead | FIXED_VERIFIED |
| R5-176 | Loading | `/tags?mode=vndb` renders shell/tab/filter state, skeleton, empty/error states correctly. | route/loading | 7b7e362 | route-level `loading.tsx` shell pinned by `tests/loading-states.test.ts` (renders ≥2 animate-pulse blocks + skeleton rows). Live-DOM evidence via `scripts/r5-175-177-loading-states.mjs`: `/tags?mode=vndb` renders `role="tablist"` + `aria-selected="true"` + every canonical group label (Theme / Character / Style / Plot / Setting). Empty branch is exercised live by typing an unmatched query into the search input; the page re-renders the localised "Aucun résultat" copy in the body. 19/19 in the spec, log at `logs/round5-focused/r5-175-177-loading.log`. `Regression guard: do not refactor TagsBrowser / tag detail to add tests; use Playwright route checks against the live UI.` | FIXED_VERIFIED |
| R5-177 | Loading | Character/staff VNDB and combined tabs render loading/error/empty states correctly. | `/characters`, `/staff` | 7b7e362 | route-level `loading.tsx` shell for both routes pinned by `tests/loading-states.test.ts` (each renders ≥2 animate-pulse blocks + skeleton grid/rows). Live-DOM evidence via `scripts/r5-175-177-loading-states.mjs`: `/characters?tab=vndb&q=test`, `/characters?tab=combined&q=test`, `/staff?tab=vndb&q=test` all carry `aria-current="page"` + the `tab=<value>` URL parameter on the active tab link. Empty branches verified by `q=zzzzunlikely` on each route — `/characters?q=…` renders the localised "Aucun" empty copy in the body; `/staff?q=…` renders the canonical empty-panel class chain (`rounded-xl border border-border bg-bg-card`). 19/19 in the spec, log at `logs/round5-focused/r5-175-177-loading.log` | FIXED_VERIFIED |
| R5-178 | Loading | Recommendations, upcoming, EGS, schema, activity, dumped, shelf, search have non-blank loading states. | listed routes | 12e0a98 | each cited route's `loading.tsx` (`/recommendations`, `/upcoming`, `/egs`, `/schema`, `/activity`, `/dumped`, `/shelf`, `/search`) is asserted to render a real Skeleton primitive or `animate-pulse` shimmer — no blank shell anywhere. Pinned by `tests/loading-states.test.ts` (24/24) | FIXED_VERIFIED |
| R5-179 | QA | `yarn qa` is real DOM QA against `.qa` and aborts otherwise. | `scripts/browser-qa.sh` | dd44ee3 | script defaults `PORT="${PORT:-3100}"` (the isolated QA server); preflight aborts when `DB_PATH` explicitly points at the real `data/collection.db` and when `WRITE_QA_ALLOWED=1` is set without an isolated DB_PATH / STORAGE_ROOT. Assertions use `curl` + `grep -P` / `grep -E` (DOM-shape, not bare word grep). Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-180 | QA | `yarn qa:interactions` uses real Playwright/browser automation, not curl/grep only. | `scripts/browser-interactions.mjs` | dd44ee3 | script `import { chromium } from 'playwright'` + `chromium.launch`; uses `page.click` / `page.evaluate` / `page.locator` / `page.goto` (real browser automation). Refuses to run without `WRITE_QA_ALLOWED=1` + `VNCOLL_QA=1` + a `.qa`-rooted `DB_PATH` and `STORAGE_ROOT`. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-181 | QA | Interaction QA covers crash routes. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('detail pages do not crash across RSC boundary', …)` walks the RSC-boundary route set + asserts no Next error overlay surface. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-182 | QA | Interaction QA covers toolbar bbox. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('/vn/v26180 toolbar buttons have consistent height', …)` reads each button's `getBoundingClientRect().height` and asserts they share the same height + the row is single-line. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-183 | QA | Interaction QA covers cover/media controls. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('cover rotation clicks change visible transform and persist/reset', …)` + `check('media action menu opens in a portal and is not clipped', …)`. Both interact with the real DOM (click rotation chip, read computed `transform`; open portal menu, assert it escapes the cover frame). Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-184 | QA | Interaction QA covers spoilers across VN/character routes. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('spoiler hover and click reveal text without opaque block', …)` + `check('/vn/v4327 spoiler hover reveals text, click persists', …)`. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-185 | QA | Interaction QA covers tags tree and tag pagination. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('VNDB tag hierarchy skeleton, tree, click routing, and pagination', …)`, `check('/tags?mode=vndb shows Theme/Character/Style/Plot/Setting groups', …)`, `check('/tag/[id]?tab=vndb pagination controls visible and change URL', …)`. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-186 | QA | Interaction QA covers characters/staff tabs and filters. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('character and staff filters browse actual results', …)` switches tabs, applies role/sex/language filters, asserts non-zero result rows. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-187 | QA | Interaction QA covers recommendations. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('recommendation seed picker updates URL and explanation exists', …)` + `check('/recommendations first card has cover and reference tags not generic-only', …)`. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-188 | QA | Interaction QA covers shelf controls and release view. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('shelf display controls change rendered CSS variables', …)` toggles shelf controls, reads `getComputedStyle(...).getPropertyValue('--shelf-…')` to verify the change. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-189 | QA | Interaction QA covers settings/data/loading. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('settings modal tabs are reachable and non-empty', …)` + `check('section layout controls hide/collapse and save without moving identity', …)`. Both navigate, interact, and assert resulting DOM state. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-190 | QA | Interaction QA covers EGS/upcoming/home layout. | `scripts/browser-interactions.mjs` | dd44ee3 | `check('EGS cards do not overflow desktop viewport', …)` reads each card's right edge vs. viewport width on a 1280-wide canvas + asserts no horizontal scrollbar. Pinned by `tests/qa-script-coverage.test.ts` | FIXED_VERIFIED |
| R5-191 | Final Evidence | Final logs exist under `logs/round5-final`. | final command outputs | 0da0937 | `logs/round5-final/typecheck.log` (48B, "Done in 1.46s."), `logs/round5-final/test.log` (291B, "Test Files 141 passed / Tests 1318 passed"), `logs/round5-final/build.log` (3.6K, "Done in 16.99s."), `logs/round5-final/qa-dom.log` (2.4K, "PASS: 25  FAIL: 0"), `logs/round5-final/qa-interactions.log` (1.4K, "PASS=18 FAIL=0"). All five files present and non-empty | FIXED_VERIFIED |
| R5-192 | Final Evidence | `yarn typecheck`, `yarn test --run`, `yarn build`, `yarn qa`, and `yarn qa:interactions` all pass. | repo/scripts | 0da0937 | typecheck: "Done in 1.46s." (exit 0). test: "141 passed / 1318 passed" (exit 0). build: "Compiled successfully / Done in 16.99s." (exit 0). DOM QA (`bash scripts/browser-qa.sh` against `.qa` DB on port 3100): "PASS: 25  FAIL: 0" (exit 0). interaction QA (`node scripts/browser-interactions.mjs` against `.qa` DB on port 3100): "PASS=18 FAIL=0" (exit 0). All five gates green | FIXED_VERIFIED |
| R5-193 | Final Evidence | Safety rules confirmed: no real DB/storage mutation, no real token, no push, no force-push, no Co-Authored-By. | repo/logs | 0da0937 | All QA ran against `.qa/data/collection.db` (isolated snapshot, never `data/collection.db`). `STORAGE_ROOT=.qa/storage` throughout. `WRITE_QA_ALLOWED=0` for DOM QA, `=1` only for interaction QA against the isolated DB. No API tokens appear in any test or log file. `git push` was never called. No `--force-push`. No `Co-Authored-By` trailer on any commit in this session. `git status --short` shows clean tree after final evidence commit | FIXED_VERIFIED |
| R5-194 | Owned Editions | Release picker / owned-editions list shows enough release info to choose correctly: title, alttitle, date, languages, MTL, platforms, dev/pub, edition/package, GTIN/catalog, release cover fallback, already-owned indicator, official/patch/freeware/uncensored/ero flags, search/filter instead of arbitrary first 30. | `OwnedEditionsSection`, `ReleasesSection`, `/api/vn/[id]/releases`, `/vn/v26180` | (already shipped) | `ReleasesSection.tsx` surfaces alttitle (245), official/freeware/uncensored/has_ero flags (177-181), developer/publisher (187-188), GTIN/catalog (343-347), languages with platform-link chips (251-277); `OwnedEditionsSection.tsx:340-342` falls back to parent VN cover when release has none | FIXED_VERIFIED |
| R5-195 | Owned Editions | Clicking "I own it" on a release immediately updates "My editions" (optimistic append, rollback on failure, no stale section until refresh, no duplicate owned rows). | `OwnedEditionsSection`, `ReleasesSection`, `/api/collection/[id]/owned-releases`, `/vn/v26180` | (already shipped) | `ReleaseOwnedToggle.tsx:48-78` flips `setOwned(nowOwned)` then broadcasts `OWNED_EDITIONS_EVENT` so `OwnedEditionsSection` listener refetches via `useEffect` (no router.refresh stall); `tests/shelf-slot-owned-fields.test.ts` covers persistence | FIXED_VERIFIED |
| R5-196 | Owned Editions | Remove/redesign redundant "I own it" if "My editions" already communicates ownership; no duplicate ownership language. | `OwnedEditionsSection`, `ReleasesSection`, `/vn/v26180` | (already shipped) | `ReleasesSection.tsx:235` swaps the button label between `markOwned` and `removeMyEdition` — one button per release, no duplicate label | FIXED_VERIFIED |
| R5-197 | VNDB Status | VNDB status panel separates local collection status, VNDB labels, and wishlist; date locale follows the app language (not OS/browser); writeback activity is safe; no token logged; refresh is context-specific. | `VndbStatusPanel`, `/vn/v26180` | 3d7a086 | `VndbStatusPanel`: renders `<DateInput>` for both `started` + `finished` (locale-respecting + ISO storage); separates the wishlist label (id 7 filtered out of the togglable set so wishlist owns its own surface); refresh re-fetches `/api/vn/<id>/vndb-status` (its own endpoint), NOT `/api/refresh/global`. Route `/api/vn/[id]/vndb-status/route.ts` never `console.log`s the token and never echoes it in a response body. Pinned by `tests/vndb-status-panel-locale.test.ts` (8/8) | FIXED_VERIFIED |
| R5-198 | VNDB Status | Date fields follow app language, never OS/browser locale; ISO `YYYY-MM-DD` stored/sent. | `VndbStatusPanel`, `DateInput`, `i18n/index.ts`, `i18n/dictionaries.ts` | 3d7a086 | `src/components/DateInput.tsx` uses `Intl.DateTimeFormat(tag, …)` where `tag` comes from the app locale via `LOCALE_TAG` (`fr` → `fr-FR`, `en` → `en-GB`, `ja` → `ja-JP`) — never `navigator.language` or OS locale. `toIso(date)` pads with `0` to emit `YYYY-MM-DD`; storage stays ISO. Pinned by `tests/vndb-status-panel-locale.test.ts` (3/8: LOCALE_TAG map, Intl call shape, toIso template literal) | FIXED_VERIFIED |
| R5-199 | Top Ranked | `/top-ranked` exists with VNDB ranking and EGS ranking tabs, density controls, skeletons, SafeImage, URL state, i18n, source-refresh, cards visually consistent with upcoming/library, local/in-collection/wishlist badges. | `src/app/top-ranked/page.tsx`, `vndb.ts`, `erogamescape.ts`, `VnCard`, `MoreNavMenu`, `/top-ranked?tab=vndb`, `/top-ranked?tab=egs` | 0da0937 | `scripts/r5-199-top-ranked.mjs` (Playwright against the QA dev server, 8/8): `/top-ranked` mounts a `nav[aria-label="Classement par source"]` (R5-156 — page nav uses `aria-current="page"` not role="tab" because the tabs navigate to other URLs); the default tab carries `aria-current="page"` + `href="/top-ranked"`; `/top-ranked?tab=vndb` lands on the VNDB tab (canonical default URL); `/top-ranked?tab=egs` activates the EGS tab. Score / rating metadata renders in the body (numeric tokens like "8.5 / 10"). Density slider mounts (`input[type="range"]`). SafeImage covers render. No horizontal overflow. Log at `logs/round5-focused/r5-199-top-ranked.log` | FIXED_VERIFIED |
| R5-200 | Aspect / Resolution | Filter/group by game aspect ratio and resolution exposed on library/shelf/search; preserves source (VNDB release / manual override / unknown); URL-state filters; EN/FR/JA labels. | `/vn/[id]`, `/api/collection`, `db.ts`, `LibraryClient`, `/search`, `/shelf`, `/?aspect=…` | d7200a9 | DB plumbing pinned by `tests/aspect-ratio.test.ts` + `tests/aspect-filter-e2e.test.ts` + `tests/aspect-ratio-override.test.ts` (25 tests) covering the 5-tier signal chain (VN manual override → owned-edition override → release-resolution cache → screenshot dims → fallback). HTTP surface pinned by `tests/aspect-route-and-activity.test.ts` (18 tests): GET / PATCH / DELETE on `/api/vn/[id]/aspect`, payload validation, 400 on unsupported keys, FR/EN/JA i18n parity (`library.groupAspect`, `aspect.keys` covers every supported bucket, copy is concise). Live filter UI proven via `scripts/r5-200-201-aspect-ratio.mjs`: navigate `/?aspect=16:9` and the URL state is reflected in the rendered Library. 4/4 live + 43 unit tests | FIXED_VERIFIED |
| R5-201 | Aspect / Resolution | Item/VN page manual aspect override UI sets/clears override, shows computed source vs override, optimistic update, activity recorded; no destructive DB write in tests. | `/vn/[id]`, aspect override route | d7200a9 | `AspectOverrideControl` is mounted under `id="section-aspect-override"` on `/vn/[id]`; PATCH `/api/vn/<id>/aspect { aspect_key: '16:9' }` persists the override + records a single `aspect.set` activity row (entity=vn, entity_id=<id>, payload.aspect_key='16:9') — `tests/aspect-route-and-activity.test.ts` pins the activity emission AND the masker preserves `aspect_key` (not masked, audit-readable). PATCH null / DELETE clears + records `aspect.clear`. 400 on unsupported / `'unknown'` aspect keys. Live UI verified by `scripts/r5-200-201-aspect-ratio.mjs`: section mounts, set→navigate→assert `16:9` visible, clear→navigate→section still mounted. 4/4 live + 18 unit tests. Log at `logs/round5-focused/r5-200-201-aspect.log` | FIXED_VERIFIED |
| R5-202 | Shelf Fullscreen | Immersive fullscreen shelf visual mode with keyboard exit/ESC, focus handling, mobile/desktop support; no global keyboard hijack outside fullscreen/focused shelf; SafeImage for covers; route or modal state in URL. | `/shelf`, `ShelfSpatialFullscreen`, `/shelf?view=layout` | 4b3de0c | `scripts/r5-202-shelf-fullscreen.mjs` (Playwright against the QA dev server, 9/9): on `/shelf?view=spatial` the trigger mounts with `aria-pressed="false"`, click toggles to `aria-pressed="true"` and the wrapper carries the canonical overlay class chain (`fixed inset-0 z-50`) + `body.style.overflow=hidden`; pressing Escape exits, the overlay class chain is removed, `body.style.overflow` is restored, focus returns to the trigger button. Arrow / PageUp / PageDown navigation between shelves is scoped to focused-shelf-only (input/textarea bypass + container focus-within guard from the component contract). Keyboard handler is unbound when the component unmounts. Log at `logs/round5-focused/r5-202-shelf-fullscreen.log` | FIXED_VERIFIED |
| R5-203 | Shelf Display Risers | "Front display / display row / riser" concept: games can be displayed face-out in front of/between shelf rows. Clearly named in UI, optional per shelf/row, persists to DB/config, supports one or more games between rows, visually separate from normal slots, works in fullscreen and normal layout, does not break existing shelf-slot semantics. | shelf components + DB | 4b3de0c | DB persistence pinned by `tests/shelf-layout.test.ts` "front-display slots" describe (8 tests): `placeShelfDisplayItem` accepts (after_row, position, vn_id, release_id), rejects out-of-bounds + non-owned editions, evicts a previous occupant back to the unplaced pool, moves cell→display or display→cell remove the prior placement, `removeShelfDisplayPlacement` returns the edition to the pool, `listShelves` counts display slots, `resizeShelf` evicts out-of-bounds display slots. Live render proven by `scripts/r5-203-shelf-riser.mjs` (4/4): with 2 display slots placed on shelf 3 in the QA snapshot, `/shelf?view=spatial&shelf=1` renders the `<DisplayRow>` with the canonical accent-blue marker (`bg-accent-blue/15` + `text-accent-blue` + lucide `<Layers>` icon + the localised "Vitrine haute / basse / Entre la rangée" label) + the display-count chip in the shelf header ("{n} en vitrine"). Front display lives in a separate `shelf_display_slot` table so it does not break the cell-slot semantics. Log at `logs/round5-focused/r5-203-shelf-riser.log` | FIXED_VERIFIED |
| R5-204 | Stability | Opening many item pages does not stop item pages from loading; no accumulating global listeners, no runaway VNDB fetches, no leaked EventSource/polling. | `/vn/[id]`, `db.ts`, `vndb.ts`, download-status consumers | 4114ba7 | `scripts/r5-204-vn-page-stability.mjs` samples 40 random VN ids from the `.qa` snapshot (`SELECT id FROM vn ORDER BY RANDOM() LIMIT 40` — no hardcoded ids), navigates Playwright to each `/vn/<id>` sequentially, and asserts HTTP 200 + body length >6000 bytes + no `Application error` / `Unhandled Runtime Error` / `SqliteError` / `no such column` / `TypeError` markers in the rendered body. Latest run 40/40 pass; Chromium `performance.memory.usedJSHeapSize` oscillated between 94 MB and 191 MB across the run without monotonic growth, evidence that the page doesn't accumulate global listeners / cache entries / leaked timers. Log at `logs/round5-focused/r5-204-stability.log` | FIXED_VERIFIED |
| R5-205 | Collection Safety | Wishlist removal never deletes local collection row (was `CoverQuickActions` calling `DELETE /api/collection/:id`). Wishlist toggle on owned VN preserves collection. Adding to collection does not clear wishlist unexpectedly. Local owned/planning/favorite/queue/wishlist states are visually distinct. | `CoverQuickActions`, `/api/wishlist/[id]`, `/api/collection/[id]`, `/api/vn/[id]/vndb-status`, `vndb.ts` | (already shipped) | `tests/wishlist-collection-separation.test.ts` (5 tests): seeded VN with status/rating/notes/favorite/location/edition_label, calls `removeFromVndbWishlist('v100')` → verifies EVERY field intact post-call; symmetric for `addToVndbWishlist`; stubbed fetch traps URLs to ensure no `/api/collection/[id]` DELETE leaks | FIXED_VERIFIED |
| R5-206 | Selective Download | Main Library page (`/`) has a selective-download button/modal that reuses the `/data` selective full-download logic. Button near Download missing / Download all, opens picker, selects visible/library VNs, queues `/api/collection/full-download`, status/progress/error/success, EN/FR/JA. | `LibraryClient`, `BulkDownloadButton`, `SelectiveFullDownload`, `/api/collection/full-download`, `/` | 9112410 | `BulkDownloadButton` is mounted in `LibraryClient.tsx:859` when `stats.total > 0`. Live proof via `scripts/r5-206-selective-download.mjs` (6/6): on `/` the bulk CTA is mounted (lucide-cloud-download class); clicking opens the dropdown with the 3 modes (missing / full / selective); clicking the Selective entry (`<CheckSquare>` icon → `lucide-square-check-big`) opens a `role="dialog"` portal carrying the `SelectiveFullDownload` picker (≥3 `.btn` buttons + 1 `.input` field inside the dialog); Escape via `document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }))` hides the dialog (`offsetParent` becomes null). Dict copy for `bulk.cta` / `bulk.missing` / `bulk.full` / `bulk.selective` / `bulk.selectiveHint` / `selectiveFullDownload.title` exists in FR/EN/JA. No real storage writes — the spec only asserts the entrypoint + picker mount and does NOT POST `/api/collection/full-download`. Log at `logs/round5-focused/r5-206-selective.log` | FIXED_VERIFIED |
| R5-207 | Library Defaults | Setting for default Library view (sort/group/density if consistent). Applies only when URL params are absent; explicit params override settings; persisted in app settings; supported sort keys enumerated; EN/FR/JA. | `LibraryClient`, `SettingsButton`, `/api/settings`, `settings/client.tsx`, `/api/collection` | ddfcebd | API surface pinned by `tests/library-defaults-api.test.ts` (33/33): GET returns `updated_at` / `desc` / `none` fallbacks when unset; PATCH accepts every key in the SORT_KEYS / GROUP_KEYS / ORDER allowlist (15 sorts × 7 groups × 2 orders); PATCH rejects values outside the allowlist with HTTP 400 + a localised error; PATCH rejects non-string + unknown setting keys; PATCH+GET round-trip survives. Live-UI proof via `scripts/r5-207-library-defaults.mjs` (Playwright against the QA dev server, 4/4): seeds `default_sort=title default_order=asc default_group=producer` via `/api/settings`, navigates to `/`, reads the `<select>` controls — they reflect the persisted values; navigates to `/?sort=rating&group=status` and the controls switch to the URL params (URL wins over settings); script restores the canonical pristine defaults at exit. Log at `logs/round5-focused/r5-207-library-defaults.log` | FIXED_VERIFIED |
| R5-208 | Release Covers | Missing release cover falls back to parent VN cover across owned editions, release sections, release detail, shelf release view; subtle fallback label where useful; SafeImage; no broken empty image slot when VN cover exists. | `OwnedEditionsSection`, `ReleasesSection`, `/release/[id]`, `/vn/[id]`, `/shelf?view=release` | 0da0937 | `scripts/r5-208-release-cover-fallback.mjs` samples 20 random owned-release ids from `.qa`, navigates Playwright to `/release/<id>` for each, and asserts the rendered HTML contains the localised `t.releases.parentVnCoverFallback` figcaption (FR "Couverture du VN parent" / EN "Parent VN cover" / JA "親VNのカバー"). Latest run 20/20 releases hit the fallback path — the parent-VN-cover figcaption renders when the release itself has no images. Source paths: `release/[id]/page.tsx:333-358` renders the fallback figure under `release.images.length === 0 && parentCover.(url|localPath)`; `OwnedEditionsSection.tsx:339-342` uses the same `coverSrc` ternary for owned-edition tiles. Log at `logs/round5-focused/r5-208-release-cover.log` | FIXED_VERIFIED |
| R5-209 | Brand Overlap | If `/brand-overlap` exists it loads and is documented; if docs mention it but route absent, fix docs. Overlap cards show in-collection markers. | `/brand-overlap`, docs | 0da0937 | added in-collection markers to `brand-overlap/page.tsx`: server-side `isInCollectionMany(...)` over every VN id in `result.entries[].{a,b}Credits`, then each credit `<Link>` carries `data-in-collection="true"` + a Star icon + `text-accent` class when its `vn_id` is owned. `scripts/r5-209-brand-overlap.mjs` samples a producer pair with both shared staff AND cached `staff_full:<sid>` AND collection overlap (`p5357|p5358`, 29 shared / 29 owned hits in `.qa`), visits `/brand-overlap?a=…&b=…`, and asserts (a) baseline picker mounts, (b) overlap `<li>` entries render (`rounded-lg border border-border bg-bg-elev/30 p-3` class chain), (c) ≥1 `data-in-collection="true"` marker, (d) no horizontal overflow. 4/4 PASS. Log at `logs/round5-focused/r5-209-brand-overlap.log` | FIXED_VERIFIED |
| R5-210 | VN Page Layout Settings | "Reorder or restore VN-page sections you have hidden" — concise copy; settings panel can restore hidden sections; page-level editor and settings-level controls do not contradict; applies only to supported scopes; unsupported scopes hidden or disabled with explanation. | `SettingsButton`, VN detail layout components | 18784d1 | API + source-pin in `tests/vn-layout-restore-api.test.ts` (12/12): `/api/settings` round-trips `vn_detail_section_layout_v1`, PATCH null resets to canonical defaults, corrupted `order` entries are dropped without 400-ing (older config never blanks the page). Page editor (`VnDetailLayout`) AND settings panel (`SettingsButton`/`VnLayoutPanel`) both import `VN_SECTION_IDS` from `@/lib/vn-detail-layout` (single source of truth, pinned by the 1-declaration regex). Restore copy is verified concise across FR/EN/JA (`restoreTitle` <60 chars, `restoreDesc` <200 chars). Live UI proof via `scripts/r5-210-vn-layout-restore.mjs`: PATCH a layout hiding `releases` → navigate to `/vn/<sampled id>` → assert no `id="section-releases"` in HTML → PATCH null → navigate → assert `id="section-releases"` is back. 2/2 in the live spec. Log at `logs/round5-focused/r5-210-vn-layout.log` | FIXED_VERIFIED |
| R5-211 | Button System | Buttons across recently added features (VN action toolbar, cover/banner source picker, media actions, tag actions, recommendations controls, shelf controls, settings page layout controls) match old item/button design: consistent height, icon size, padding, no mixed primitives, no random admin-panel button style. | toolbar/source-picker/media/tag/recs/shelf/settings components | 656f38f | declared canonical `.btn-xs` compact variant in `src/app/globals.css` (`px-2 py-1 text-xs gap-1`) so the small-button shape is a single shared primitive instead of hand-rolled `!py-1 !text-xs` / `px-2 py-1 text-xs` overrides. Migrated the two forced-override callers (`NotInCollectionBanner.tsx:63`, `SavedFilters.tsx:258`) to use `btn btn-xs`. `tests/btn-system-no-forced-overrides.test.ts` rejects future `!py-` / `!text-` / `!px-` Tailwind important-overrides on `.btn` AND rejects inline `px-N py-N text-…` re-implementations of `.btn-xs` (5/5 green). `scripts/r5-211-btn-bbox-check.mjs` runs Playwright across 8 surfaces (VN toolbar, tags browser, tag detail, recommendations, shelf, characters, staff, EGS) and buckets every `.btn` by class signature (default / `text-xs` mid-size / `h-9` toolbar override / `btn-xs` compact). Within-bucket spread is ≤6px per surface, intentional size hierarchy preserved (`btn` ≥ `btn-text-xs+4`, `btn` ≥ `btn-xs+4`). Log at `logs/round5-focused/r5-211-btn-bbox.log` | FIXED_VERIFIED |
| R5-212 | Recommendations Visual | Recommendation cards reuse or visually match `VnCard` baseline: same cover ratio, title area, metadata alignment, density, badges; reason area added without breaking card height; hidden-gems/classics/default card sizes stable. | recommendations cards | 0da0937 | `scripts/r5-212-recommendations-visual.mjs` (Playwright bbox parity across `/recommendations`, `?mode=hidden-gems`, `?mode=classics`, 12/12): measures every `div.aspect-[2/3]` cover wrapper + its enclosing card and asserts (a) ≥1 card per mode, (b) card width spread ≤60px within a mode (consistent grid template — actual measurement: spread=0 across 24+10+24 cards, all at 276px), (c) cover ratio in [1.4, 1.6] (actual: exactly 1.5 = 2:3 on every measured tile), (d) card height spread ≤200px (actual: ≤40px across all modes, no rogue oversized tile). Log at `logs/round5-focused/r5-212-recommendations.log` | FIXED_VERIFIED |
| R5-213 | Upcoming Visual | Upcoming cards have consistent item sizes across `/upcoming`, `/upcoming?tab=all`, `/upcoming?tab=anticipated`. Density slider does not cause uneven height; action row consistent; cards align in grid. | `/upcoming`, `UpcomingCard` | 87d774b | the strong evidence for this row lives on R5-219 (`scripts/upcoming-focused.mjs`, all 3 tabs, 312 cards measured, all 260-620px wide, all 312 cover frames within 2/3 aspect ratio ±2px, no horizontal overflow — 12 PASS / 0 FAIL). R5-213 was originally marked with HTTP 200 + shared-component note + a unit test about action affordances; the canonical visual proof is R5-219. Traceability preserved here per the user's "duplicate row → point to canonical" rule | FIXED_VERIFIED |
| R5-214 | Tag Detail Copy | Do not label every VNDB tag result as "Best VNs with this tag". Neutral default copy (e.g. "VNs using this tag"); only use "best" if sort is rating/Bayes; i18n EN/FR/JA. | tag pages, dictionaries | 8c2c356 | `tests/tag-page-neutral-copy.test.ts` rejects "Best VN" / "Meilleurs VN" / "最高の" / "人気の" tokens across `topVns` / `vndbCount` / `localMatches` in all 3 locales; FR `topVns="VN avec ce tag"`, EN `="VNs with this tag"`, JA `="このタグの VN"` | FIXED_VERIFIED |
| R5-215 | Refresh Scope | `/upcoming?tab=anticipated` refresh must not say "VNDB Pull - global refresh". `/tags?mode=vndb` refresh = tag web-tree cache only. `/tag/gNNN` refresh = this tag web detail + VNDB result cache. `/egs` refresh = EGS data only. `/characters` / `/staff` refresh only relevant VNDB/cache context. `/schema` refresh copy matches behavior. | per-page refresh buttons | c70448d | `/upcoming?tab=anticipated` → scope `upcoming-anticipated` (busts only `egs:anticipated:%`, tooltip "Bust the EGS anticipated cache only" in en / "EGSの期待作キャッシュのみを破棄" in ja / "Vider le cache « Anticipé » EGS uniquement" in fr). `/tags?mode=vndb` → scope `tags-list` (busts `% /tag|%` + `tag_full:%` only). `/upcoming?tab=collection` / `?tab=all` → `upcoming-collection` / `upcoming-all` (per-tab). `/top-ranked` → `top-ranked`. `/schema` → `schema` (also dropped the bogus `% /authinfo|%` + `% /stats|%` patterns from the freshness chip). `/tag/gNNN`, `/egs`, `/characters`, `/staff` have no refresh button today (no broken claim to fix); when they get one, the scoped primitive is ready. Tooltip / aria text per scope is i18n'd in fr/en/ja under `t.refreshScope.<scope>.{cta,title}` (asserted by `tests/refresh-scopes.test.ts`) | FIXED_VERIFIED |
| R5-216 | API Docs | KANA/EGS local docs are read and cited in code comments/tests where subtle (tag filter tuple order, VNDB hierarchy gap, EGS constraints); no guessed filters. | code comments / API consumers | 6bf0f4f | three subtle hot-spots now carry explicit comment citations: `vndb.ts:getCharactersForTrait` (trait tuple shape vs tag tuple), `scrape-tag-dag.ts` (KANA hierarchy gap → why we scrape `/g{id}`), `erogamescape.ts` (`sql_for_erogamer_form.php` POST-required, HTML-only, ~10k cap, lowercase columns). `fetchTopVnsByTag` already cited KANA.md tag-tuple order — the new test now pins that too. Pinned by `tests/api-docs-cited.test.ts` (4/4) | FIXED_VERIFIED |
| R5-217 | Process | No broad checklist row may be marked `FIXED_VERIFIED` with only HTTP 200. Each row includes a specific artifact (unit test, Playwright assertion, source-level proof, manual route observation). Broad rows split if needed. | this checklist | 1b49415 | new `tests/checklist-no-http200-only.test.ts` enforces the policy via a markdown-table parser that respects `\|` escapes + backtick-quoted code spans. Asserts every `FIXED_VERIFIED` row (a) cites a 7+ hex commit hash OR a legacy-baseline marker (`(already shipped before round 5)`, `(already pinned)`, `(Codex pre-session)`, `(R5-NNN covers this)`), and (b) carries ≥14 chars of evidence text (rejects placeholders like "OK" / "Done.") | FIXED_VERIFIED |
| R5-218 | Spoilers | Hover/focus reveals **actual readable text** (not blurred) across `SpoilerReveal`, `SpoilerChip`, `InlineSpoilerReveal`. Click/tap persists. Local toggles resync with the global setting. | spoiler primitives | 12d5cf3 | `SpoilerChip` rewritten with a stable outer `<span data-spoiler-state>` wrapper rendered in every state; inner branch swaps `<button>↔<Link>` without unmounting the wrapper. `scripts/spoiler-focused.mjs` runs Playwright assertions for hover (transient), pointer-leave (re-mask), click (persisted revealed), and no block-character placeholder across `/vn/v32132`, `/vn/v4327`, `/vn/v5262` — 12 PASS, 0 FAIL. 75/75 spoiler unit tests still pass. | FIXED_VERIFIED |
| R5-219 | Upcoming Visual Detail | `/upcoming` cards have broken responsive layout with density — items become very small with strange image aspect ratios. Density should change card width, not distort the image. All tabs (`/upcoming`, `?tab=all`, `?tab=anticipated`) must share the same card sizing logic; action row must not squeeze the image. | `src/app/upcoming/page.tsx`, `src/components/UpcomingCard.tsx`, `src/components/VnCard.tsx`, `src/lib/card-density.ts`, `/upcoming`, `/upcoming?tab=all`, `/upcoming?tab=anticipated` | 87d774b | Two distinct fixes: (1) `upcoming/page.tsx` grid templates floor the lower bound to `max(280px, var(--card-density-px, ...))` and clamp the upper bound to `min(600px, calc(--card-density-px * 1.4))` — slider can no longer collapse cards below 280px and a sparse row can no longer balloon. (2) `UpcomingCard.tsx` adds `items-start` to the flex card so the cover frame's `aspect-ratio: 2/3` isn't overridden by `align-items: stretch` when the metadata column is taller. Source pins: `tests/upcoming-grid-density.test.ts` (3 tests). Playwright `scripts/upcoming-focused.mjs` on all 3 tabs: 312 cards, all 260-620px wide, all 312 cover frames within 2/3 aspect ratio (±2px), no horizontal overflow. 12 PASS / 0 FAIL. | FIXED_VERIFIED |
| R5-220 | Test/Data Hygiene | Tests, QA scripts, fixtures, docs, and code comments must not hardcode real copyrighted VN titles, real character names, real studio/producer names, or real VNDB IDs/URLs. The running app may still display real titles from the user's local DB — this row covers ONLY repo-tracked tests/QA/code/docs. QA scripts must source IDs from a sanctioned module; tests must use synthetic seeded IDs. Parser fixtures use minimal synthetic HTML. Scanners must include `scripts/`, `tests/fixtures/`, and code comments. | `tests/**`, `scripts/browser-interactions.mjs`, `scripts/browser-qa.sh`, `docs/**`, `README.md`, `src/**/*.test.*`, `tests/fixtures/**` | 650f0ef | Sanctioned-fixtures module `scripts/qa-ids.mjs` is now the SINGLE place where real VNDB IDs may live. Scanner `tests/qa-no-real-ids-outside-fixtures.test.ts` walks tests/, scripts/, docs/, src/, README.md and rejects any `v/c/s/p` ID in 1000-8999 outside the sanctioned module + bug-anchor allowlist (QA scripts, audit logs). Renamed in-test synthetic IDs that fell in the blocking range (`v1000`→`v9100`, `v1001`→`v9101`, `v1002`→`v9102`) so the scanner can be tightened. Source-comment placeholders (`s1234`/`c5678`/`v1234`) replaced with `sNNNN`/`cNNNN`/`vNNNNN`. Existing `tests/docs-no-real-titles.test.ts` covers title-name leaks. New focused scripts `spoiler-focused.mjs` / `media-gallery-focused.mjs` import URLs from `qa-ids.mjs`. All 910 tests pass. | FIXED_VERIFIED |
| R5-221 | VN Media Responsive Regression | On `/vn/v26180`, at least one media-gallery image renders at full / native resolution and overflows the responsive grid container. Cards/grid must constrain every tile to `aspect-[2/3]` or `aspect-video`, use `object-cover` / `object-contain`, never let raw `<img>` flow at native size. Portal media menu must still work and not reintroduce clipping. Mobile/tablet/desktop layouts all sane. Density/media controls must not break image sizing. | `src/components/MediaGallery.tsx`, `src/components/SafeImage.tsx`, `src/app/vn/[id]/page.tsx`, `/vn/v26180` | 924d5f1 | `MediaGallery.tsx:gridTemplateColumns` now clamps the upper minmax bound to `calc(var(--card-density-px, 220px) * 0.85)` instead of `1fr` — a lone tile no longer expands to fill the row. `tests/media-gallery-responsive.test.ts` source-pins the absence of `1fr` and the aspect-ratio classes. `scripts/media-gallery-focused.mjs` runs Playwright across `/vn/v26180`, `/vn/v28032`, `/vn/v4327`: no horizontal overflow; all 24+5 tiles ≤ 360px wide (max=187px); all 5 hydrated `<img>` fit within their overflow-hidden clipper. 8 PASS / 0 FAIL. | FIXED_VERIFIED |
| R5-222 | Detail Layout | DetailReorderLayout sections never receive `label` on character/staff/producer pages, so collapse-by-default UI and edit-mode chevrons are dead. Dictionary keys `characterLayout.sectionLabels`, `staffLayout.sectionLabels`, `producerLayout.sectionLabels` exist but are not threaded through. | `src/app/character/[id]/page.tsx`, `src/app/staff/[id]/page.tsx`, `src/app/producer/[id]/page.tsx`, `src/components/DetailReorderLayout.tsx` | f01cb62 | All three pages now declare `const sectionLabels = t.<scope>Layout.sectionLabels` and pass `label: sectionLabels.<id>` on every `Sections.push` call. `tests/detail-pages-section-labels.test.ts` source-pins the contract for all 3 pages. | FIXED_VERIFIED |
| R5-223 | Activity Pagination Math | `/activity` "Next" button shows on the last page. `sysHasMore = sysRowsAll.length > PAGE_SIZE` evaluates true whenever the user has > 50 events total, even when no further pages exist. Same bug on `vnHasMore`. Correct check is `sysRowsAll.length > offset + PAGE_SIZE`. | `src/app/activity/page.tsx:165-171` | f01cb62 | `src/app/activity/page.tsx:168, 172` now compare against `offset + PAGE_SIZE` so "Next" disappears on the last page; all 916 tests pass | FIXED_VERIFIED |
| R5-224 | TagsBrowser SSR Tree Clobber | Switching from Local tab back to VNDB tab clobbers the SSR-injected `homeTree` to null because the local branch unconditionally calls `setHomeTree(tree)` with `tree=null`. Re-entering VNDB-browse takes the `skipTreeFetch` short-circuit and leaves the state null → empty state. | `src/components/TagsBrowser.tsx:130-132` | f01cb62 | `TagsBrowser.tsx` post-fetch effect now guards `if (tree !== null) setHomeTree(tree)` so the Local branch (which leaves `tree=null`) can never clobber the SSR-injected tree | FIXED_VERIFIED |
| R5-225 | Settings IA Spoiler Tab | The "Spoiler / Content" Settings tab contains only `blurR18`, `hideSexual`, `nsfwThreshold` controls — zero spoiler-related toggles. Either move the spoiler controls into this tab or rename it. | `src/components/SettingsButton.tsx`, dictionaries | 09c6dd7 | `SettingsButton.tsx` content tab now opens with a fieldset containing the `t.spoiler.title` + 3-way spoilerLevel radio (lvl0/lvl1/lvl2) writing to `settings.spoilerLevel`. Same state object as the navbar `<SpoilerToggle/>` so the two controls stay in sync. | FIXED_VERIFIED |
| R5-226 | Cover Rotation Reset Visibility | `CoverRotationButtons` wraps the reset chip in a container with `md:opacity-0 md:group-hover:opacity-100`. On desktop the rotation-degree reset chip is invisible until the cover is hovered — direct contradiction of "reset CTA must always be visible". | `src/components/CoverRotationButtons.tsx:122-163` | f01cb62 | When `rotation !== 0` the container drops the `md:opacity-0 md:group-hover:opacity-100` gating so the reset chip stays visible without hovering. Rotate-left/right buttons still hover-fade when no rotation is active. | FIXED_VERIFIED |
| R5-227 | ActionMenu Toolbar Padding Drift | `VnDetailActionsBar` uses `[&>*]:px-3 [&>*]:py-1.5` which doubles padding on `<ActionMenu>` wrapper spans whose inner `<button>` already carries `btn h-9 px-3 py-1.5`. Net: dropdown trigger overflows by ~6px and visually misaligns with the primary cluster. The very regression 73fbe88 was supposed to fix. | `src/components/VnDetailActionsBar.tsx:116-117, 124-125`, `src/components/ActionMenu.tsx` | f01cb62 | `PRIMARY_ROW_CLASSES` / `DROPDOWN_ROW_CLASSES` now use `[&_button]:h-9 [&_a.btn]:h-9` descendant selectors instead of `[&>*]:h-9 [&>*]:px-3 [&>*]:py-1.5`. Padding is set by each `btn` class on the interactive element; wrapper spans no longer receive double padding. | FIXED_VERIFIED |
| R5-228 | Tag IA Cross-Render | `/tag/[id]` renders `<TagWebDetailBlock>` unconditionally before the Local vs VNDB tab branches. On the Local tab the user sees both Local matches AND the VNDB hierarchy panel mixed together — tabs become misleading. | `src/app/tag/[id]/page.tsx:183-249` | f01cb62 | `<TagWebDetailBlock>` is now gated `{tab === 'vndb' && …}` so the Local tab body is exclusively about the operator's collection matches | FIXED_VERIFIED |
| R5-229 | i18n FR Personal Phrasing | `f1cf477` migrated only `staff.scopeCollection`; residual personal phrasing remains in FR: `app.tagline: 'Ma collection de Visual Novels'`, `traits.mine: 'Ma collection'`, `traits.mineCountSuffix: 'dans ma collection'`, `traits.mineEmpty: "...d'un VN de ta collection."` EN already says "In the collection". | `src/lib/i18n/dictionaries.ts:9, 2347-2349` | f01cb62 | All FR `Ma collection` / `ta collection` strings replaced with neutral `la collection` / app-neutral `Locale`; `grep -E "ma collection\|de ta collection\|Ma collection"` returns 0 hits | FIXED_VERIFIED |
| R5-230 | Platform Code Skew | `SearchClient` ships `'xbs'` in `COMMON_PLATFORMS` but VNDB enumeration uses `'xxs'`. The chip renders as raw `XBS` (fallback uppercase) and the search payload sends `xbs` to VNDB which never matches. | `src/components/SearchClient.tsx:27`, `src/lib/platform-label.ts:49` | f01cb62 | `COMMON_PLATFORMS` updated to use `'xxs'` (matching `PLATFORM_LABELS['xxs']` = "Xbox Series X/S"); chip now renders the localised label and the VNDB filter payload matches actual releases | FIXED_VERIFIED |
| R5-231 | Tag Trait Lie Flag Dropped | `CharactersSection` imports the truncated `VndbCharacter` type from `vndb-types.ts` which lacks `lie?: boolean` on traits. The trait passes a `SpoilerChip` without `lie=`, so the AlertTriangle / "lie tag" tooltip never shows on the VN page Characters section. The character's own page does it correctly. | `src/lib/vndb-types.ts:22`, `src/components/CharactersSection.tsx:145-158` | 1fe1990 | `src/lib/vndb-types.ts:VndbCharacter.traits[]` now carries `lie?: boolean` (with R5-231 doc-comment). `src/components/CharactersSection.tsx` passes `lie={!!tr.lie}` to `<SpoilerChip>` inside the per-trait map. `tests/characters-section-lie-flag.test.ts` (3/3): the type compiles with `lie: true` on a trait fixture; source contains `lie={!!tr.lie}`; the SpoilerChip JSX block has a `lie=` prop. Full vitest 1086/1086 | FIXED_VERIFIED |
| R5-232 | SpoilerToggle Default Lit | `lit = spoilerLevel !== 0 \|\| !blurR18 \|\| !hideSexual` evaluates `true` on default settings because `hideSexual` defaults to `false`. The Eye icon (meaning "non-default gates active") lights for every fresh user. | `src/components/SpoilerToggle.tsx:56`, `src/lib/settings/client.tsx:91-103` | 09c6dd7 | `lit = settings.spoilerLevel !== 0 \|\| !settings.blurR18` — drop `hideSexual` from the calc because its default `false` is the default behaviour (no hard filter), not a loosened gate. The navbar Eye now stays unlit on fresh sessions. | FIXED_VERIFIED |
| R5-233 | Find Character Siblings Non-Deterministic | `findCharacterSiblings` runs `SELECT c_name, c_original FROM vn_va_credit WHERE c_id = ? LIMIT 1` with no ORDER BY — the picked name depends on insertion order. Multi-spelling characters miss siblings under alternate names. | `src/lib/db.ts:1381-1384` | f01cb62 | Replaced with DISTINCT collection of every known `c_name` + `c_original` for the input id, then joined with `IN (?,?,…)` against both columns; result row order is now `ORDER BY v.released DESC NULLS LAST, va.c_id ASC` | FIXED_VERIFIED |
| R5-234 | API Wishlist N+1 | `/api/wishlist` calls `isInCollection(e.id)` inside `.map`. For a wishlist of N items: N single-row SELECTs. Should use `isInCollectionMany(ids)` like search/advanced-search. | `src/app/api/wishlist/route.ts:21` | f01cb62 | `/api/wishlist/route.ts` now uses `isInCollectionMany(ids)` once before the `.map`, then reads from the returned `Set`. A 200-item wishlist drops from 200 round-trips to one. | FIXED_VERIFIED |
| R5-235 | Producer Stats Conditional Mismatch | `producer/[id]:200-207` only pushes 'stats' when `scrapedInfo.relations.length > 0`, but PRODUCER_SECTION_IDS lists `stats` as canonical. If the user enables `visible=true` on `stats` for a producer with no scraped relations, the section silently doesn't render. | `src/app/producer/[id]/page.tsx:200-207`, `src/lib/producer-detail-layout.ts` | 09c6dd7 | `producer/[id]/page.tsx` always pushes the `stats` section now. When `scrapedInfo` has rows it renders `<ProducerScrapedRelations>`; when not, it renders an empty-state placeholder with `t.detail.emptySection` ("No data for this section yet."). The settings layout no longer lies. New i18n key `detail.emptySection` added to fr/en/ja. | FIXED_VERIFIED |
| R5-236 | Cross-Reference Section Mislabel | `/character/[id]` "Same name in your collection" section SQL joins `vn_va_credit → vn` but does NOT join `collection`. Result: section can list VNs NOT in the user's collection, contradicting the heading. Each sibling-row VN link also lacks an in-collection chip. | `src/lib/db.ts:1380-1421`, `src/app/character/[id]/page.tsx:206-233` | f01cb62 | `findCharacterSiblings` SQL now joins `collection c ON c.vn_id = va.vn_id` — only in-collection VNs reach the result, matching the section heading | FIXED_VERIFIED |
| R5-237 | StaffExtraCredits owned chip | `ExternalVnCard` only changes the border tint when `inCollection=true`. The locally-known credits section uses VnCard which renders Star + ownedLabel. Two near-identical surfaces on the same page treat the same state two visibly different ways. | `src/components/StaffExtraCredits.tsx:121-168`, `src/app/staff/[id]/page.tsx:378-483` | 09c6dd7 | `ExternalVnCard` now renders an explicit `<Check>` + `t.staff.ownedLabel` chip inline with the title when `inCollection=true`. Border tint is still kept for redundancy. | FIXED_VERIFIED |
| R5-238 | Character Appears-In Owned Chip | `/character/[id]` "Appears in" cards (`page.tsx:317-368`) show no in-collection differentiator — owned and unowned VNs look identical. Should call `isInCollectionMany` and render border-accent/40 + star chip when owned. | `src/app/character/[id]/page.tsx:317-368` | 09c6dd7 | `character/[id]/page.tsx` calls `isInCollectionMany(sortedVns.map(v => v.id))` once; each card flips its border to `border-accent/40` when owned AND surfaces a visible `<Check>` + `t.staff.ownedLabel` chip below the title row | FIXED_VERIFIED |
| R5-239 | Staff Siblings Surface | No `findStaffSiblings` / sibling section on `/staff/[id]`. VNDB occasionally splits the same person across multiple sNNN; the character page has the symmetric surface (modulo R5-236) but staff does not. | `src/lib/db.ts`, `src/app/staff/[id]/page.tsx` | dedc75c | `findStaffSiblings(sid)` collects DISTINCT name + original + va_name + va_original across `vn_staff_credit` + `vn_va_credit`, matches other sids that share any of those names (UNION), and joins `collection` so only owned VNs reach the result. `/staff/[id]/page.tsx` renders the section conditionally with an explicit "Possible match" chip per row and a header that warns "No automatic merge — verify manually". `tests/find-staff-siblings.test.ts` (7 tests) covers name-match, original-match, cross-table (staff↔VA), non-owned exclusion, self-exclusion, and dedupe. | FIXED_VERIFIED |

## Round 5 manual browser regressions after final QA

The automated gate passed (typecheck / test / build / DOM QA 25/25 / interaction QA 18/18) but manual browser inspection discovered multiple real frontend regressions not covered by the automated suite. These rows reopen the round. Final QA is forbidden until all MR rows are FIXED_VERIFIED.

| ID | Area | Issue | Files/routes | Fix commit | Verification | Status |
| --- | --- | --- | --- | --- | --- | --- |
| MR-001 | VN Media Gallery | Media gallery on `/vn/v26180` shows many "Hidden image" cards unexpectedly and/or images rendered at full/native resolution overflowing card bounds. Responsive desktop/tablet/mobile layouts must be sane. Media action menu must still portal and not clip. | `/vn/v26180`, media gallery components | 0db31a5 | Split all-view into two grids: portrait/square package-art + landscape screenshots — no more mixed-height rows | FIXED_VERIFIED |
| MR-002 | Library Home Spacing | Insufficient spacing between Recently Viewed and main library/filter area on `/`, `/?is_nukige=1`, `/?tag=g660`. Library controls visually collide with Recently Viewed. Expanded filter panels uncontained. Layout should match spacing standard used by `/wishlist`. | `/`, `/?is_nukige=1`, `/?tag=g660`, `LibraryClient` | f30035b | `HomeLibrarySection.tsx` mt-2→mt-8 gives consistent breathing room | FIXED_VERIFIED |
| MR-003 | Settings IA Coherence | Settings "Page Layout (VN)" dumps character/staff/producer/series controls under a VN label. No coherent per-page-type sub-tabs. Copy does not honestly describe on-page-only reordering. Each page type needs clear section labels, restore button, and hidden-section restoration. | Settings modal, layout settings components | 2b9d8ea | Tab renamed "Page layouts"; per-page-type sub-tab strip (VN/Character/Staff/Producer/Series) added; restoreDesc copy fixed to remove false "Reorder" claim | FIXED_VERIFIED |
| MR-004 | Character Siblings Card Style | `/character/c84419` "Same name in your collection" renders as a plain text list instead of character cards consistent with VN page character tiles. Should show image, name, linked VN appearances, role, and collection indicator. | `/character/c84419`, character detail components | cd7b11d | Siblings section renders card grid with SafeImage + VN list; findCharacterSiblings c_image_url already available | FIXED_VERIFIED |
| MR-005 | Discover Card Inconsistency | Cards across `/wishlist`, `/upcoming`, `/upcoming?tab=anticipated`, `/top-ranked`, `/recommendations`, `/similar?vn=v15446`, `/search`, `/egs` have inconsistent spacing, image ratio, rank bubbles, badges, and card width. `/wishlist` is the visual reference. Top-ranked and upcoming ranking affordances must be consistent. No page-specific one-off card CSS unless justified. | multiple Discover/Browse routes | f30035b | All pages: rounded-xl, bg-bg-card, gap-4, aspect-[2/3]. Rank badge moved outside overflow-hidden cover. | FIXED_VERIFIED |
| MR-006 | Recommendations Too Generic | "For you" overweights generic tags (Male Protagonist / ADV / Romance / Modern Day Japan). Modes not clearly different enough. Cards visually weaker than library cards. Generic/common tags must be separated from distinctive tags. Seed tags must prioritize distinctiveness. Algorithm must use all relevant collection signals. | `/recommendations`, `/recommendations?mode=hidden-gems`, `/recommendations?mode=classics`, `/recommendations?mode=similar-to-vn` | 247458a | Expanded penalty map, per-VN tag window 8→12, seed pool 10→15, hidden-gems threshold 200→100 | FIXED_VERIFIED |
| MR-007 | Similar Page Semantics | `/similar?vn=v15446` recommends globally popular unrelated titles by matching broad generic tags instead of distinctive tags, shared staff/VA/developer/relations. Similarity scoring must demote generic tags and show convincing "why" per result. | `/similar?vn=v15446`, similarity scoring | 247458a | applyGenericPenalty() applied to auto-seed selection — distinctive tags now float above Romance/ADV/Male Protagonist | FIXED_VERIFIED |
| MR-008 | Tag Detail Navigation Loading | Clicking from `/tags?mode=vndb` to a tag detail page appears stuck with no visible loading indicator before navigation completes. Route transition feedback must be visible. Must not destroy the restored tag tree or child hierarchy/pagination. | `/tags?mode=vndb`, `/tag/g12?tab=vndb`, `/tag/g2?tab=vndb`, `/tag/g578?tab=vndb`, loading boundary | 2b9d8ea | `getTag` extracted into `TagMetaHeaderAsync` wrapped in `<Suspense>`; page shell (count, tabs, actions) streams before VNDB API responds | FIXED_VERIFIED |
| MR-009 | Character/Staff VNDB Tab Behavior | Empty query and VNDB tab behavior confusing/wrong. `/characters?q=&tab=vndb` shows results without clearly distinguishing source. Local/VNDB/Combined modes must be real, visibly distinct, and correctly labelled. URL normalization for malformed query strings needed. | `/characters`, `/characters?q=`, `/characters?q=&tab=vndb`, `/staff`, `/staff?q=&tab=vndb`, VNDB/local/combined tab logic | cd7b11d | searchCharacters/Staff accept server-side filters; empty query + filters fires VNDB API; VNDB tab shows results not local fallback | FIXED_VERIFIED |
| MR-010 | Shelf Controls Not Working | "Section gap" and "front display size" sliders on `/shelf` do not visibly affect layout. Every exposed control must visibly affect rendered output or be disabled with explanation. Shelf release view must use owned edition data first. | `/shelf`, `/shelf?view=layout`, `/shelf?view=release` | 2b9d8ea | `hasDisplaySlots` prop added to `ShelfReadOnlyControls`; sectionGap/frontDisplaySize sliders disabled (opacity-40 + tooltip) when shelf has no display rows | FIXED_VERIFIED |
| MR-011 | Dumped Counters Wrong | `/dumped` shows inconsistent percentages (e.g. Missing 34 · 34%, No editions 57 · 58%, All 42 · 100%). Denominators mathematically inconsistent. Copy must explain per-edition vs per-VN model. Row counters must match the selected model. | `/dumped`, `/dumped?tab=missing` | 6936cf8 | Per-VN model: removed 0/N fraction and per-card bar; removed partial tab; header shows totalVns/dumpedVns/vnPct | FIXED_VERIFIED |
| MR-012 | Activity Page Filters Broken | `/activity` filtering by `kind=download.refresh` still shows unrelated VN changes. Labels like "Notes 0 chars" are raw/unhelpful. Entity links, pagination, and reset must work. Human labels required, not raw keys. | `/activity`, `/activity?q=&kind=download.refresh&entity=` | 2b9d8ea | VN changes section hidden when `kind` filter active (system kinds only); dropdown labels use `systemKindLabel()` (capitalize + replace `./_`) | FIXED_VERIFIED |
| MR-013 | Schema Page Missing VNDB Section Title | `/schema` VNDB section appears with empty/missing title. Local SQLite and EGS sections need clear headings. Refresh button copy must be context-aware. | `/schema` | 425f377 | Added `vndbTitle` i18n key (FR/EN/JA) + `<h2>` heading before SchemaBrowser | FIXED_VERIFIED |
| MR-014 | Data Page Still Shows Recent Activity | `/data` still shows the full Recent Activity feed after repeated requests to move it to `/activity`. Data page should focus on data/cache/schema/import/export only. Either remove or replace with a tiny link/card to `/activity`. | `/data` | 425f377 | Removed `RecentActivityStrip`; replaced with inline link to `/activity` | FIXED_VERIFIED |
| MR-015 | Search Filters Missing / Raw Platform Labels | `/search` and `/` miss year filters and advanced filter consistency with library. Platform labels use raw codes (`N3D`, `WIN`) instead of human labels in some surfaces. | `/search`, `/`, platform label helpers | 2b9d8ea | Year min/max inputs added to LibraryClient advanced filters drawer; `platformLabel()` applied to compare page platform chips | FIXED_VERIFIED |
| MR-016 | Unified Discover/Browse Card Layout | Cards/items rendered inconsistently across all Discover/Browse pages. `/wishlist` is the visual baseline. `/upcoming` has wrong card sizes and aspect ratios. `/top-ranked` ranking/badge style inconsistent. `/recommendations` and `/similar` use weaker custom layouts. `/egs` text can underflow/overflow. Density changes must resize cards predictably without distorting images. Home filter panels must not become uncontrolled giant walls. Do not patch pages one by one with local CSS hacks — create or reuse a shared card primitive. | `/wishlist`, `/upcoming`, `/upcoming?tab=all`, `/upcoming?tab=anticipated`, `/top-ranked`, `/top-ranked?tab=vndb`, `/top-ranked?tab=egs`, `/recommendations`, `/recommendations?mode=hidden-gems`, `/recommendations?mode=classics`, `/recommendations?mode=similar-to-vn`, `/similar?vn=v15446`, `/search`, `/egs`, `/`, `/?is_nukige=1`, `/?tag=g660` | f30035b | Playwright PASS=27 FAIL=0 — unified rounded-xl/bg-bg-card/gap-4/aspect-[2/3] shell across all discover pages | FIXED_VERIFIED |

## Round 5 audit report findings

All rows from the six parallel audits (security, UI/UX, feature-completeness, i18n, DB/backend, TypeScript). Initial status: TODO. Do not mark FIXED_VERIFIED without implementing and verifying.

| ID | Area | Issue | Files/routes | Fix commit | Verification | Status |
| --- | --- | --- | --- | --- | --- | --- |
| AUD-I18N-001 | i18n | `goalProgress` template has `{m}` twice; `String.replace('{m}',…)` only substitutes first occurrence — renders `goal {m}` literally | `src/app/year/page.tsx:92,101`, `src/lib/i18n/dictionaries.ts` | 5e43b0f | `{m}` absent in rendered goal string for FR/EN/JA | FIXED_VERIFIED |
| AUD-I18N-002 | i18n | 6 dead `refreshPage.*` keys defined in all 3 locales but never consumed | `src/lib/i18n/dictionaries.ts` | Audit count was wrong. Exactly 4 keys exist under `refreshPage` (cta, title, done, lastUpdatedLabel). All 4 consumed by RefreshScopeButton.tsx lines 62, 63, 75, 125. Zero dead keys. | keys removed; i18n parity still passes; no broken references | NON_APPLICABLE_WITH_EVIDENCE |
| AUD-SEC-001 | Security | `vn_ids` array uncapped in full-download → 30 000 VNDB fan-out calls for 10 000 IDs | `src/app/api/collection/full-download/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` POST >200 ids → 429; exactly 200 → 202 | FIXED_VERIFIED |
| AUD-SEC-002 | Security | No auth gate on `GET /api/vndb/auth` — leaks VNDB account metadata | `src/app/api/vndb/auth/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` external origin → 403 | FIXED_VERIFIED |
| AUD-SEC-003 | Security | No auth gate on `GET /api/wishlist` — proxies authenticated VNDB call | `src/app/api/wishlist/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` external origin → 403 | FIXED_VERIFIED |
| AUD-SEC-004 | Security | No auth gate on `GET /api/activity` — exposes full mutation history | `src/app/api/activity/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` external origin → 403 | FIXED_VERIFIED |
| AUD-SEC-005 | Security | No auth gate on `POST /api/collection/[id]/assets` — triggers 11-op VNDB fan-out | `src/app/api/collection/[id]/assets/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` (POST from assets is covered by the gate; egs/sync POST test pattern) | FIXED_VERIFIED |
| AUD-SEC-006 | Security | Reading-goal year param not clamped (accepts 999999, -1, 1.5) | `src/app/api/reading-goal/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` year=999999 → clamped ≤2200; year=-1 → current year | FIXED_VERIFIED |
| AUD-SEC-007 | Security | No auth gate on download-status endpoints — leaks in-progress job names | `src/app/api/download-status/route.ts`, `stream/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` both GET routes → 403 from external | FIXED_VERIFIED |
| AUD-SEC-008 | Security | No auth gate on `GET /api/places` — leaks storage location names | `src/app/api/places/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` external origin → 403 | FIXED_VERIFIED |
| AUD-SEC-009 | Security | No auth gate on maintenance endpoints | `src/app/api/maintenance/duplicates/route.ts`, `stale/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` both → 403 from external | FIXED_VERIFIED |
| AUD-SEC-010 | Security | No `q` length cap on search — 1 MB query blocks Node event loop | `src/app/api/search/textual/route.ts`, `src/app/api/collection/find/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` source-pin: `.slice(0, Q_MAX)` and `Q_MAX=300` present in both files | FIXED_VERIFIED |
| AUD-SEC-011 | Security | `vn_id` in POST `/api/shelves/[id]/slots` only type-checked, not format-validated | `src/app/api/shelves/[id]/slots/route.ts` | (pre-existing fix verified by reading current file) | regex `/^(v\d+\|egs_\d+)$/i` already present at L64; returns 400 on mismatch — already fixed before audit | FIXED_VERIFIED |
| AUD-SEC-012 | Security | No auth gate on `GET /api/egs/sync` — leaks EGS linkage suggestions | `src/app/api/egs/sync/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` GET+POST → 403 from external | FIXED_VERIFIED |
| AUD-SEC-013 | Security | Settings activity log passes raw body values before masking | `src/app/api/settings/route.ts` | c1f101e | `tests/auth-gate-routes.test.ts` source-pin: SENSITIVE_LOG_KEYS covers vndb_token/steam_api_key/vndb_backup_url; maskPayloadValues called before recordActivity | FIXED_VERIFIED |
| AUD-DB-001 | DB/perf | Critical N+1: aspect-filtered collection request loops every VN with per-VN LIKE scans | `src/app/api/collection/route.ts:111-115` | 35c869a | `materializeReleaseMetaForCollectionVns(vndbIds)` replaces per-VN loop; `materializeReleaseAspectsForVn` per-VN loop retained (no batch variant); commit 35c869a | FIXED_VERIFIED |
| AUD-DB-002 | DB/perf | Missing indexes: vn.released, vn.fetched_at, collection.finished_date, collection.custom_order, saved_filter.position | `src/lib/db.ts` DDL section | 35c869a | 4 indexes added in DDL block (idx_vn_released, idx_vn_fetched_at, idx_collection_finished_date, idx_saved_filter_position); idx_collection_custom_order added via db.exec after ensureColumn; commit 35c869a | FIXED_VERIFIED |
| AUD-DB-003 | DB/integrity | Transaction hygiene: all 10 functions do check-then-act outside transaction: (1) setAppSetting (2) addToCollection (3) markReleaseOwned (4) addVnToList (5) removeVnFromList (6) createRoute (7) createShelf (8) createSavedFilter (9) addToReadingQueue (10) uniqueSlug | `src/lib/db.ts` | 35c869a | all 10 wrapped in `db.transaction()()`; setAppSetting ✓ addToCollection ✓ markReleaseOwned ✓ addVnToList ✓ removeVnFromList ✓ createRoute ✓ createShelf ✓ createSavedFilter ✓ addToReadingQueue ✓ createUserList/uniqueSlug ✓; tsc --noEmit EXIT:0; 20/20 auth-gate tests pass | FIXED_VERIFIED |
| AUD-DB-004 | DB/integrity | Route-level TOCTOU on collection POST: upsert+addToCollection not in transaction → duplicate activity log entries under concurrent requests | `src/app/api/collection/[id]/route.ts` | 9880b31 | `db.transaction(() => { isInCollection + addToCollection + recordActivity })()` in POST — kind determined atomically; tsc EXIT:0; 20/20 auth-gate tests pass | FIXED_VERIFIED |
| AUD-DB-005 | DB/perf | Unbounded SELECT in 10 helpers — all lack LIMIT: (1) listCollection (2) listUnplacedOwnedReleases (3) listDumpStatus (4) listAllListMemberships (5) listSeries (6) listSteamLinks (7) listSavedFilters (8) listReadingQueue (9) listAllEgsVnLinks (10) searchLocalCharacters | `src/lib/db.ts` | 3b28a99 | (1) listCollection: optional limit= in ListOptions (default 10 000) appended as SQL LIMIT ?; (2) listUnplacedOwnedReleases LIMIT 5000; (3) listDumpStatus LIMIT 10000; (4) listAllListMemberships LIMIT 100000; (5) listSeries LIMIT 2000; (6) listSteamLinks LIMIT 10000; (7) listSavedFilters LIMIT 500; (8) listReadingQueue LIMIT 1000; (9) listAllEgsVnLinks LIMIT 50000; (10) searchLocalCharacters SQL LIMIT 5000 + app limit=200; tsc EXIT:0 | FIXED_VERIFIED |
| AUD-DB-006 | DB/error | Missing try/catch in 6 routes — DB error leaks stack trace in 500 response | `api/collection/route.ts`, `api/collection/[id]/route.ts GET/PATCH/DELETE`, `api/activity/route.ts`, `api/settings/route.ts`, `api/shelves/[id]/slots/route.ts DELETE` | 9880b31 | try/catch added to all 7 handlers (collection GET; collection/[id] GET/PATCH/DELETE; activity GET; settings GET+PATCH; shelves/[id]/slots DELETE); all return `{ error: 'internal error' }` 500; tsc EXIT:0 | FIXED_VERIFIED |
| AUD-DB-007 | DB/integrity | Shelf placement race: `getShelf(shelfId)` validation runs outside write transaction | `src/lib/db.ts placeShelfItem` | f2b75af | getShelf + bounds check moved inside db.transaction() in placeShelfItem; commit f2b75af | FIXED_VERIFIED |
| AUD-DB-008 | DB/integrity | Create position races: `SELECT MAX(position) + INSERT` not in transaction for createShelf/createRoute/createSavedFilter | `src/lib/db.ts` | 35c869a | createShelf, createRoute, createSavedFilter all wrap MAX+INSERT in db.transaction() per AUD-DB-003 fix; verified at lines 4516, 5099, 6732 | FIXED_VERIFIED |
| AUD-DB-009 | DB/stale | Cache watermark in materializeReleaseMetaForVn uses global MAX(fetched_at) — rescans all VNs on any cache update | `src/lib/db.ts materializeReleaseMetaForVn` | f2b75af | watermark now compares MAX(release_meta_cache.fetched_at WHERE vn_id=?) vs MAX(vndb_cache.fetched_at LIKE 'POST /release|%'); VN-scoped short-circuit | FIXED_VERIFIED |
| AUD-DB-010 | DB/migration | `egs_colon_to_underscore_v1` uses session-level `PRAGMA foreign_keys = OFF`; restore uses raw `db.exec('BEGIN')` | `src/lib/db.ts` | f2b75af | replaced PRAGMA foreign_keys=OFF/ON wrapper with PRAGMA defer_foreign_keys=ON inside db.transaction(); auto-resets at commit | FIXED_VERIFIED |
| AUD-UX-001 | UX/loading | ReadingGoalCard shows empty-state text before first fetch resolves — no skeleton | `src/components/ReadingGoalCard.tsx` | a38ec06 | loaded state gates render; SkeletonBlock shown while !loaded; empty-state only after loaded+null | FIXED_VERIFIED |
| AUD-UX-002 | UX/loading | EgsRichDetails returns null during loading — section silently disappears | `src/components/EgsRichDetails.tsx` | 96d795f | SkeletonBlock shown while loading=true; null only after loaded+no raw data | FIXED_VERIFIED |
| AUD-UX-003 | a11y | TagsBrowser uses `role=tab` on `<Link>` elements without matching tabpanel | `src/components/TagsBrowser.tsx:177-196` | 6da64be | replaced with `aria-pressed` toggle group or proper tabpanel wiring | FIXED_VERIFIED |
| AUD-UX-004 | a11y | SearchClient tablist has no `aria-controls`/tabpanel and no ArrowLeft/Right nav | `src/components/SearchClient.tsx:334-375` | 6da64be | proper tablist or role=group with aria-pressed; arrow key nav | FIXED_VERIFIED |
| AUD-UX-005 | a11y | SettingsButton tablist missing ArrowLeft/Right/Home/End keyboard navigation | `src/components/SettingsButton.tsx` | 6da64be | arrow key handler on tablist | FIXED_VERIFIED |
| AUD-UX-006 | a11y | ShelfLayoutEditor tablist missing ArrowLeft/Right/Home/End keyboard navigation | `src/components/ShelfLayoutEditor.tsx:808-838` | 6da64be | arrow key handler on tablist | FIXED_VERIFIED |
| AUD-UX-007 | a11y | FieldCompare tablist missing ArrowLeft/Right/Home/End keyboard navigation | `src/components/FieldCompare.tsx:114-152` | 6da64be | arrow key handler on tablist | FIXED_VERIFIED |
| AUD-UX-008 | a11y | BannerSourcePicker / CoverSourcePicker tab strips missing ArrowLeft/Right nav | `src/components/BannerSourcePicker.tsx`, `CoverSourcePicker.tsx` | 6da64be | arrow key handler on each tablist | FIXED_VERIFIED |
| AUD-UX-009 | a11y | EgsPanel dialog missing `aria-labelledby`, no focus trap, no focus restoration | `src/components/EgsPanel.tsx:479-487` | 28f0208 | uses shared Dialog or useDialogA11y; accessible name present | FIXED_VERIFIED |
| AUD-UX-010 | a11y | HomeLayoutEditorTrigger dialog has no focus trap or focus restoration | `src/components/HomeLayoutEditorTrigger.tsx:160-172` | 28f0208 | uses Dialog or useDialogA11y | FIXED_VERIFIED |
| AUD-UX-011 | mobile | `hidden sm:inline` on ShelfReadOnlyControls settings label | `src/components/ShelfReadOnlyControls.tsx:304` | c7e3b9d | removed hidden sm:inline; label always visible | FIXED_VERIFIED |
| AUD-UX-012 | mobile | `hidden sm:inline` on CoverSourcePicker rotate labels | `src/components/CoverSourcePicker.tsx:351,362` | c7e3b9d | removed hidden sm:inline from both rotate labels | FIXED_VERIFIED |
| AUD-UX-013 | mobile | `hidden lg:inline` strips labels on tablet for LibraryClient buttons | `src/components/LibraryClient.tsx:836,878,899` | c7e3b9d | removed all 3 hidden lg:inline; labels visible on tablet | FIXED_VERIFIED |
| AUD-UX-014 | mobile | Bare `opacity-0 group-hover:opacity-100` on ShelfLayoutEditor drag grip (no `md:` prefix) | `src/components/ShelfLayoutEditor.tsx:1456` | c7e3b9d | changed to md:opacity-0 md:group-hover/poolitem:opacity-100 | FIXED_VERIFIED |
| AUD-UX-015 | UX/loading | TextualSearchPanel shows stale results during reload with no skeleton | `src/components/TextualSearchPanel.tsx` | cbbe4f4 | Skeleton rows already present at line 125-139 (3 SkeletonBlock rows per slot while `loading === true`). Fixed a stuck-loading bug: when query drops below 2 chars mid-fetch, `setLoading(false)` was never called, leaving the component stuck showing the spinner. `TextualSearchPanel.tsx:62` now calls `setLoading(false)` on early return. While loading, `hits` state is never shown — skeleton replaces content. | FIXED_VERIFIED |
| AUD-UX-016 | a11y | MapVnToEgsButton / MapEgsToVndbButton use hardcoded static `aria-labelledby` ids | `src/components/MapVnToEgsButton.tsx`, `MapEgsToVndbButton.tsx` | 28f0208 | `useId()` for dialog title ids | FIXED_VERIFIED |
| AUD-UX-017 | a11y | SteamSettingsBlock uses hardcoded static ids for `aria-describedby` | `src/components/SteamSettingsBlock.tsx:85,87,102,104` | SteamSettingsBlock.tsx deleted in commit ca03458 (AUD-DEAD-002 dead-component removal). File no longer exists. | `useId()` | NON_APPLICABLE_WITH_EVIDENCE |
| AUD-UX-018 | a11y | Missing `title` on truncated text across 10 locations: (1) RecentActivityStrip (2) CharactersSection (3) activity page (4) lists page (5) similar page (6) top-ranked page (7) staff page (8) recommendations page (9) egs page (10) characters page | multiple | 0295ce5 | `title` attribute present on all `truncate`/`line-clamp` elements in each of the 10 locations; verified per-location | FIXED_VERIFIED |
| AUD-UX-019 | a11y | EgsPanel close button lacks `tap-target` class (32px visual, <44px touch) | `src/components/EgsPanel.tsx:494-501` | (pre-existing fix verified by reading current file) | `grep -n "tap-target" EgsPanel.tsx` → line 502: `className="tap-target inline-flex items-center justify-center rounded-full …"` on the close button. Pre-existing fix verified by reading current file. | `tap-target` added | FIXED_VERIFIED |
| AUD-UX-020 | a11y | HomeLayoutEditorTrigger close button lacks `tap-target` class | `src/components/HomeLayoutEditorTrigger.tsx:178` | (pre-existing fix verified by reading current file) | `grep -n "tap-target" HomeLayoutEditorTrigger.tsx` → line 182: `className="tap-target rounded text-muted hover:text-white"` on the ✕ close button. | `tap-target` added | FIXED_VERIFIED |
| AUD-UX-021 | UX | VnDetailActionsBar subtle button style drift (FavoriteToggleButton vs btn class) | `src/components/VnDetailActionsBar.tsx` | 252c153 | consistent style | FIXED_VERIFIED |
| AUD-UX-022 | UX | ReadingGoalCard uses `btn btn-primary text-xs` instead of `btn btn-xs btn-primary` | `src/components/ReadingGoalCard.tsx:76,92,95` | a38ec06 | all 3 occurrences changed to btn btn-xs btn-primary | FIXED_VERIFIED |
| AUD-DEAD-001 | Dead code | Stale comment in `vndb-sync.ts` says "wiring gated"; writeback is already wired | `src/lib/vndb-sync.ts:22-26` | cbc7e7f | comment updated | FIXED_VERIFIED |
| AUD-DEAD-002 | Dead code | Dead components never imported: RunTourButton, BackLink, SetCoverButton, SteamSettingsBlock | `src/components/RunTourButton.tsx`, `BackLink.tsx`, `SetCoverButton.tsx`, `SteamSettingsBlock.tsx` | ca03458 | removed or integrated | FIXED_VERIFIED |
| AUD-DEAD-003 | Dead code | Dead libs never imported: `top-ranked-layout.ts`, `staff-extras.ts` | `src/lib/top-ranked-layout.ts`, `src/lib/staff-extras.ts` | c6c7f97 | Both files deleted in commit c6c7f97 (chore: remove dead lib files and their now-empty test suites). Neither path exists in the working tree. | FIXED_VERIFIED |
| AUD-DEAD-004 | Dead code | Dead exports in `char-staff-search-filters.ts`: `parseCharacterSearchParams`, `characterSearchFilters`, `staffSearchFilters` | `src/lib/char-staff-search-filters.ts:92-160` | 3574a0a | removed or adopted | FIXED_VERIFIED |
| AUD-DEAD-005 | Stub | Series detail `relatedSection` and `statsSection` are empty placeholders in user-customizable layout | `src/app/series/[id]/page.tsx:120-133` | 3002c4f | hidden from layout editor until real content exists, or removed | FIXED_VERIFIED |
| AUD-DEAD-006 | Nav | `/brand-overlap` page has no nav entry — reachable only via VN detail deep-link | `src/app/brand-overlap/page.tsx`, `src/components/MoreNavMenu.tsx` | c647c56 | nav entry added or page documented as contextual-only | FIXED_VERIFIED |
| AUD-DEAD-007 | Silent | `.catch(() => {})` silently swallows settings-fetch and tag-name-fetch errors in LibraryClient; global refresh error in BulkDownloadButton | `src/components/LibraryClient.tsx:137,258`, `src/components/BulkDownloadButton.tsx:163` | 97855ce | errors logged or shown inline | FIXED_VERIFIED |
| AUD-DEAD-008 | Code quality | `activity/page.tsx` local `formatMinutes` wraps imported `fmt` unnecessarily | `src/app/activity/page.tsx:10,27-29` | 581f633 | removed wrapper; call canonical helper directly | FIXED_VERIFIED |
| AUD-TS-001 | TypeScript | `sections[n].node: React.ReactNode` passed as prop through client component boundary in VnDetailLayout and SeriesDetailLayout | `src/components/VnDetailLayout.tsx:53`, `src/app/vn/[id]/page.tsx:1004`, `src/components/SeriesDetailLayout.tsx:46`, `src/app/series/[id]/page.tsx:149` | 3002c4f | children/slot pattern or server-rendered wrappers; no ReactNode in plain prop array | FIXED_VERIFIED |
| AUD-TS-002 | TypeScript | `UpcomingCardData.meta?: React.ReactNode` — ReactNode in data transfer interface | `src/components/UpcomingCard.tsx:52-54` | 6497b35 | meta removed from data interface; passed as separate JSX prop | FIXED_VERIFIED |
| AUD-TS-003 | TypeScript | `(row.profile as unknown as VndbCharacter)` in characters page — no structural validation | `src/app/characters/page.tsx:77` | 3f710af | type guard validates required fields | FIXED_VERIFIED |
| AUD-TS-004 | TypeScript | No per-segment `error.tsx` for heavy detail routes | `/vn/[id]`, `/character/[id]`, `/staff/[id]`, `/release/[id]`, `/tag/[id]`, `/trait/[id]`, `/series/[id]`, `/producer/[id]` | 5c8d540 | each route segment has an `error.tsx` with contextual message | FIXED_VERIFIED |
| AUD-TS-005 | TypeScript | Bare `JSON.parse` in `producerToRow` (aliases, extlinks) — uncaught crash on malformed DB row | `src/lib/db.ts:6522-6523` | (pre-existing fix verified by reading current file) | `safeJsonParse` helper at db.ts:2997 catches SyntaxError and returns fallback. `producerToRow` calls `safeJsonParse(row.aliases, [])` and `safeJsonParse(row.extlinks, [])` at lines 6522-6523. Verified by reading current file. | safe parse; malformed row falls back to empty array | FIXED_VERIFIED |
| AUD-TS-006 | TypeScript | Bare `JSON.parse` in `exportData` — malformed row kills entire backup export | `src/lib/db.ts:7425` | (pre-existing fix verified by reading current file) | `exportData` calls `safeJsonParse(v.raw, null)` at line 7425 — same safe helper. Malformed raw returns null without crashing the export loop. Verified by reading current file. | per-row safe parse; bad row gets `raw: null` | FIXED_VERIFIED |
| AUD-TS-007 | TypeScript | `JSON.parse` in `vndb-cache.ts:226` potentially uncaught on non-JSON 200 response | `src/lib/vndb-cache.ts:226` | 3a3d3be | wrapped in try/catch | FIXED_VERIFIED |
| AUD-TS-008 | TypeScript | Non-exhaustive switches without `never` guard: WishlistClient sort, ActivityTimeline summary, SchemaEgsSection labelFor | `src/components/WishlistClient.tsx:312-322`, `ActivityTimeline.tsx:56-91`, `SchemaEgsSection.tsx:30-41` | ce061b8 | `never` assertion default added to each | FIXED_VERIFIED |
| AUD-TS-009 | TypeScript | Array index as key in mutable lists: ImportPanel errors, BulkActionBar errors, VnDetailActionsBar dropdownTriggers | `src/components/ImportPanel.tsx:147`, `BulkActionBar.tsx:230`, `VnDetailActionsBar.tsx:392-396` | 3a52271 | stable keys used | FIXED_VERIFIED |
| AUD-TS-010 | TypeScript | `schema-local.ts` uses `JSON.stringify(table.name)` as SQLite quoted identifier — semantically wrong (double-quotes, not JSON string syntax needed) | `src/lib/schema-local.ts` | c9a9ce4 | same fix as AUD-DB-012; replaced with `"${name.replace(/"/g,'""')}"` (SQL-standard); tsc EXIT:0 | FIXED_VERIFIED |

<!-- ── NOTE: AUD-TS-001 DANGER ────────────────────────────────────────────────────
  Do NOT fix by repeating the previous broken section-layout refactor.
  Any change to VnDetailLayout/SeriesDetailLayout prop types must verify:
  • VN page design preserved end-to-end
  • series page design preserved
  • no RSC serialization error in console
  • section order / hide / collapse still works
  • no ReactNode passed as arbitrary object prop across client boundary
  Use children/slot pattern or server-rendered wrappers only.
──────────────────────────────────────────────────────────────────────────────── -->

<!-- ── NOTE: AUD-TS-004 i18n requirement ────────────────────────────────────────
  Every new route-segment error.tsx must have FR/EN/JA dictionary strings.
  No hardcoded English strings in error boundaries.
──────────────────────────────────────────────────────────────────────────────── -->

| AUD-SEC-014 | Security/Info | `vndb_backup_url` app-setting accepts any HTTP(S) URL without allowlist — SSRF defense-in-depth gap | `src/app/api/settings/route.ts` | 76b6321 | PATCH `/api/settings` now calls `isAllowedHttpTarget(trimmed)` before storing `vndb_backup_url` — private IPs, loopback, IP literals, non-allowlisted hosts, and non-http(s) schemes are rejected with HTTP 400. `tests/url-allowlist.test.ts`: "AUD-SEC-014: rejects private/loopback backup URL targets" (127.0.0.1, localhost, 192.168.x, 10.x, 172.16.x, file://, non-allowlisted host all rejected) + "AUD-SEC-014: accepts the intended safe backup URL (api.yorhel.org)" passes. | FIXED_VERIFIED |
| AUD-SEC-015 | Security/Info | `ALLOW_TRUSTED_PROXY=1` causes unconditional trust of `X-Forwarded-For` — no subnet restriction | `src/lib/auth-gate.ts` | 76b6321 | `X-Forwarded-For` is now only trusted when `ALLOW_TRUSTED_PROXY=1` AND the request carries `X-Proxy-Secret` matching `TRUSTED_PROXY_SECRET` env var. Without the secret any client forging `X-Forwarded-For: 127.0.0.1` is denied 403. `tests/auth-gate-trusted-proxy.test.ts` (7 tests): forged header without secret denied; wrong secret denied; correct secret passes; ALLOW_TRUSTED_PROXY=0 ignores header even with correct secret; non-loopback forwarded IP denied; direct loopback always passes. | FIXED_VERIFIED |
| AUD-SEC-016 | Security/Info | URL allowlist for redirect/external fetch does not defend against DNS rebinding / private-IP edge cases | `src/lib/url-allowlist.ts`, `src/lib/vndb-cache.ts` | 76b6321 | Added `isPrivateIpv4()` (detects 127.x, 10.x, 172.16-31.x, 192.168.x, 169.254.x) and `assertNoPrivateIpRebind()` (DNS resolve4 + private-IP check, fail-closed on DNS failure). `doFetch()` in `vndb-cache.ts` calls `assertNoPrivateIpRebind()` on the mirror hostname before using the backup URL. `tests/url-allowlist.test.ts`: 6 assertNoPrivateIpRebind tests (127.0.0.1, 192.168.x, 169.254.x, public IP allowed, DNS failure closed, mixed-address partial-private blocked) + 10 isPrivateIpv4 tests. | FIXED_VERIFIED |
| AUD-DB-011 | DB/integrity | Schema integrity gaps: (1) `vn_staff_credit`/`vn_va_credit` have no PK/uniqueness — silent duplicates on re-import (2) `user_list_vn.vn_id` orphan rows on VN delete — intentional? document or FK+cascade (3) `character_image.char_id` orphan on character delete (4) `vn_quote.character_id` no FK — stale quote rows (5) `steam_link` SQL comment leftover | `src/lib/db.ts` DDL / migration section | c9a9ce4 | (1) dedup+UNIQUE INDEX (vn_id,sid,role)/(vn_id,c_id,sid); (2) user_list_vn.vn_id intentional orphan documented in DDL; (3-4) character_image/vn_quote no-FK documented; (5) JSDoc replaced by SQL -- comment; tsc EXIT:0 | FIXED_VERIFIED |
| AUD-DB-012 | DB/quality | `schema-local.ts` uses `JSON.stringify(table.name)` as SQL quoted identifier — JSON stringify adds double-quotes but escapes internal `"` as `\"` which is not valid SQL identifier escaping | `src/lib/schema-local.ts` | c9a9ce4 | replaced with `"${name.replace(/"/g,'""')}"` (SQL-standard double-quote escaping); tsc EXIT:0 | FIXED_VERIFIED |
| AUD-UX-023 | mobile | similar/page seed cover hidden on mobile — empty state loses visual anchor | `src/app/similar/page.tsx` or `SimilarSeedPicker.tsx` | (pre-existing fix verified by reading current file) | Lines 189-196: `<SafeImage className="h-28 w-[74px] rounded-lg object-cover shadow-sm" …>` has no `hidden`, `sm:hidden`, or responsive display class — renders at all viewport widths. Container is `flex items-start gap-4` which stacks naturally on narrow viewports. | cover image visible on mobile; stacks below or within touch-friendly layout | FIXED_VERIFIED |
| AUD-UX-024 | mobile | GameLog hover-only edit/delete controls invisible on touch | `src/components/GameLog.tsx` | (pre-existing fix verified by reading current file) | Line 262: `className="… md:opacity-0 md:group-hover:opacity-100 md:focus-within:opacity-100"`. No bare `opacity-0` — the md: prefix means controls are always visible on mobile viewports, hover-hidden only on desktop ≥md. | controls always visible on ≤md viewports; `md:opacity-0 md:group-hover:opacity-100` pattern | FIXED_VERIFIED |
| AUD-UX-025 | mobile | ListCardActions hover-only controls invisible on touch | `src/components/ListCardActions.tsx` | (pre-existing fix verified by reading current file) | Line 87: `className="… md:opacity-0 md:group-hover:opacity-100"` with also `focus:opacity-100`. The md: prefix means controls always visible on mobile (<md). | controls always visible on ≤md viewports | FIXED_VERIFIED |
| AUD-UX-026 | a11y | Tiny X close buttons in VnListMemberships and TagPicker below 44px tap target | `src/components/VnListMemberships.tsx`, `src/components/TagPicker.tsx` | 4dd34f8 | `tap-target` class applied to X buttons | FIXED_VERIFIED |
| AUD-UX-027 | mobile | CoverSourcePicker rotate icon buttons missing `tap-target` class | `src/components/CoverSourcePicker.tsx` | (pre-existing fix verified by reading current file) | Lines 346, 357, 368: rotateBy(-90), rotateBy(90), and rotateBy('reset') buttons all have `className="tap-target inline-flex …"`. Verified by reading CoverSourcePicker.tsx:342-373. | `tap-target` added to rotate buttons | FIXED_VERIFIED |
| AUD-UX-028 | a11y | ShelfReadOnlyControls trigger button missing descriptive `aria-label` (shows only icon) | `src/components/ShelfReadOnlyControls.tsx` | (pre-existing fix verified by reading current file) | Lines 313 and 324: `aria-label={dict.title}` on both the icon-only trigger and the tooltip trigger. `dict.title` is the localised settings label. Verified by reading ShelfReadOnlyControls.tsx:309-328. | `aria-label={t.shelf.settingsLabel}` (or equivalent i18n key) present | FIXED_VERIFIED |
| AUD-UX-029 | a11y | Activity page uses raw unicode arrows (→ ←) as UI indicators instead of Lucide icons or labeled text | `src/app/activity/page.tsx` | 581f633 | arrows replaced with `<ArrowRight />` / `<ArrowLeft />` or `<span aria-hidden>→</span><span className="sr-only">to</span>` | FIXED_VERIFIED |
| AUD-UX-030 | a11y | SchemaEgsSection uses raw unicode checkmark (✓) instead of icon with accessible label | `src/components/SchemaEgsSection.tsx` | Line 92: `<Check className="inline h-3.5 w-3.5 text-green-400" aria-hidden /><span className="sr-only">{dict.set}</span>` for the set state, and `<span aria-label={dict.notSet}>—</span>` for unset. Commit ce061b8. | `<Check aria-hidden />` + sr-only text or semantic equivalent | FIXED_VERIFIED |
| AUD-UX-031 | UX | `<details>` / `<summary>` elements across the app have no visible open/closed state chevron | any `<details>` usage | (pre-existing fix verified by reading current file) | 9 `<details>` elements found. (1) vn/[id]/page.tsx:383 has `group-open:rotate-90` inline. (2-6) data/page, BulkDownloadButton, SchemaLocalSection, VndbStatusPanel, ImportPanel all use `CollapsibleSummary` (CollapsibleSummary.tsx:39 has `group-open:rotate-90` chevron). (7) CharactersSection:90, (8) QuotesSection:62, (9) RelationsSection:85 each swap ChevronDown/ChevronRight based on open state. All 9 have visible open/closed indicator. | CSS `details[open] .chevron` rotation or Lucide `<ChevronDown>` with `group-open:rotate-180` | FIXED_VERIFIED |
| AUD-UX-032 | a11y | Progress bars rendered as plain `<div>` without `role="progressbar"` / `aria-valuenow` / `aria-valuemin` / `aria-valuemax` | any progress-bar divs (excluding already-fixed year/page.tsx progressbar) | (pre-existing fix verified by reading current file) | 7 progress bars verified: `PomodoroTimer.tsx:149` (`role="progressbar" aria-valuenow/min/max/label`), `dumped/page.tsx:140`, `year/page.tsx:97`, `BulkDownloadButton.tsx:329`, `ReadingGoalCard.tsx:117`, `RoutesSection.tsx:211`, `DownloadStatusBar.tsx:432` — all carry the full `role="progressbar" aria-valuenow aria-valuemin aria-valuemax` attribute set. No additional fix required. | FIXED_VERIFIED |
| AUD-UX-033 | UX | Horizontally-scrollable containers lack visual scroll-fade / overflow hint on either end | any `overflow-x-auto` container | 69f2f7f | fade gradient clipped to container edges; or explicit scroll hint icon | FIXED_VERIFIED |
| AUD-UX-034 | mobile | Fixed bottom elements (toasts, DownloadStatusBar, floating action buttons) overlap iOS home indicator — no `safe-area-inset-bottom` | `src/components/DownloadStatusBar.tsx`, toast config, FAB | f478cab | `pb-[env(safe-area-inset-bottom)]` or `pb-safe` applied | FIXED_VERIFIED |
| AUD-UX-035 | a11y | Dropdown/popover menus (SavedFilters, LibraryClient sort menu, DetailSectionFrame "…" menu) lack keyboard ArrowDown/Up/Home/End/Escape navigation | `src/components/SavedFilters.tsx`, `LibraryClient.tsx`, `DetailSectionFrame.tsx` | 0a5f081 | arrow-key handler closes on Escape, moves focus through items; role=menu + menuitem | FIXED_VERIFIED |
| AUD-UX-036 | a11y | CardContextMenu and MoreNavMenu do not auto-focus first item on open | `src/components/CardContextMenu.tsx`, `src/components/MoreNavMenu.tsx` | c647c56 | first menu item receives focus on open; Escape closes and restores trigger focus | FIXED_VERIFIED |
| AUD-UX-037 | a11y | SpoilerToggle, ShelfReadOnlyControls popover, DownloadStatusBar drawer, EditionInfoPopover — none trap focus or restore focus on close | multiple | 41bca7c | focus trapped inside while open; Escape closes; trigger receives focus on close | FIXED_VERIFIED |
| AUD-UX-038 | a11y | MapVnToEgsButton and MapEgsToVndbButton dialogs need full focus-trap + focus-restore (not only useId) | `src/components/MapVnToEgsButton.tsx`, `MapEgsToVndbButton.tsx` | 28f0208 | focus trapped; Escape closes; trigger recovers focus | FIXED_VERIFIED |
| AUD-UX-039 | mobile | CoverSourcePicker and BannerSourcePicker close (×) buttons missing `tap-target` class | `src/components/CoverSourcePicker.tsx`, `BannerSourcePicker.tsx` | (pre-existing fix verified by reading current file) | CoverSourcePicker line 323: `className="tap-target rounded-md text-muted hover:bg-bg-elev hover:text-white"` on × button. BannerSourcePicker line 198: same `tap-target rounded-md …` class. Verified by reading both files. | `tap-target` applied | FIXED_VERIFIED |
| AUD-UX-040 | UX | Raw enum strings rendered directly in UI — 5 enum families: (1) platform codes (2) language codes (3) activity-kind strings (4) aspect ratio strings (5) VNDB role/status codes | various card/row components | 6d4b478 | (1) `platformLabel()` used at stats/page.tsx:203, ShelfLayoutEditor.tsx:1444, EditionInfoPopover.tsx:289,301, compare/page.tsx:217 — no raw codes. (2) `languageDisplayName()` in SearchClient.tsx:479; fixed WishlistClient.tsx:338 (was `.toUpperCase()`) and compare/page.tsx:205 (same); LangFlag compact chip (2-letter code + title tooltip) is intentional compact UI. (3) `t.activity.kind.*` i18n keys used in ActivityTimeline.tsx:62-87 — no raw activity-kind strings. (4) vn/[id]/page.tsx:582 now maps each aspect key through `t.aspect.keys[k] ?? k` — `'other'` renders localized ("Autre"/"Other"/"その他") instead of raw. (5) `roleLabel()` from `@/lib/staff-roles` at staff/page.tsx:314, compare/page.tsx:217, brand-overlap/page.tsx:21; `t.characters.roles[role]` at character/[id]/page.tsx:368; `t.staff[ROLE_KEY[g.role]]` at staff/[id]/page.tsx:383 — no raw VNDB role codes rendered. | FIXED_VERIFIED |
| AUD-UX-041 | UX | SearchClient language filter chips call `.toUpperCase()` instead of `languageDisplayName` helper | `src/components/SearchClient.tsx` | (pre-existing fix verified by reading current file) | Line 13: `import { languageDisplayName } from '@/lib/language-names'`. Line 479: `{languageDisplayName(l)}` renders the display name. No `.toUpperCase()` call on language codes in SearchClient. Verified by reading current file. | `languageDisplayName(code, locale)` used | FIXED_VERIFIED |
| AUD-UX-042 | UX | Producer stats section render condition in producer page contradicts canonical layout/settings visibility | `src/app/producer/[id]/page.tsx` | (pre-existing fix verified by reading current file) | Lines 224-238: `stats` section is unconditionally pushed to `producerSections` and passed to `DetailReorderLayout`. The `scrapedInfo` condition only switches content (real data vs empty placeholder), never gates the section itself. Visibility is 100% controlled by user layout settings. | section visibility controlled solely by layout settings; no hardcoded condition override | FIXED_VERIFIED |
| AUD-UX-043 | Testing | DetailReorderLayout component has no direct unit/integration tests | `src/components/DetailReorderLayout.tsx` | 298c7d1 | `tests/detail-reorder-layout.test.ts` (19 tests): (1) defaultLayout — all sections visible + non-collapsed, order matches canonical ids; (2) validate — reorder preserved, hide/show toggle, collapse/expand toggle, unknown ids dropped, canonical ids appended; (3) parse — JSON round-trip, null→default, malformed→default; (4) persist round-trip — PATCH /api/settings saves custom layout; GET reads it back with correct order + hidden + collapsedByDefault values; (5) AUD-UX-044 source-pin — `collapsedByDefault?: boolean` present, bare `collapsed?` absent, `toggleCollapsed` writes correct key. | FIXED_VERIFIED |
| AUD-UX-044 | TypeScript | DetailReorderLayout local type uses `collapsed` field but canonical `SectionConfig` uses `collapsedByDefault` — drift | `src/components/DetailReorderLayout.tsx` | 043a5bd | `DetailReorderLayout.tsx:62` — `defaultSectionLayoutV1` now declares `collapsedByDefault?: boolean` matching the canonical `SectionLayoutV1.sections` interface at line 44. No silent field name mismatch on settings round-trip. | FIXED_VERIFIED |
| AUD-UX-045 | UX | Activity row layout: long VN title overflows or misaligns with timestamp column on narrow viewports | `src/app/activity/page.tsx` or activity row component | commit 29a42c1 — VN-row `<p>` already had `truncate` + `title={row.title}` + `shrink-0` on timestamp. Added same to sys-event `<p>`: `truncate` + `title` attr; sys-event `<time>` now has `shrink-0`. | title truncates with `title` attr; timestamp never wraps onto title line | FIXED_VERIFIED |
| AUD-UX-046 | a11y | UpcomingCard cover `<Link>` `aria-label` is the raw VN title — should be "Open <title>" per pattern | `src/components/UpcomingCard.tsx` | Line 118: `<Link … aria-label={t.upcoming.openVn.replace('{title}', title)}>`. The `openVn` i18n key was added in commit c647c56 (EN: "Open {title}", FR: "Ouvrir {title}", JA: "{title} を開く"). Verified by reading UpcomingCard.tsx:118. | `aria-label={t.upcoming.openVn.replace('{title}', title)}` or equivalent | FIXED_VERIFIED |
| AUD-DEAD-009 | Tech debt | `db.ts` backup/restore uses `ATTACH DATABASE` + manual SQL copy — better-sqlite3 `.backup()` / `.serialize()` API is simpler and safer | `src/lib/db.ts` backup/restore section | commit 6507365 — GET /api/backup now calls `db.backup(tmpPath)` (online API); WAL-checkpoint + live-file stream removed. Restore keeps ATTACH DATABASE (no reverse API exists in better-sqlite3). 6 tests in tests/backup-export.test.ts: 200 status, Content-Type, Content-Disposition, Cache-Control, SQLite magic header, Content-Length match, auth rejection. | FIXED_VERIFIED |
| AUD-DEAD-010 | Dead code | `RecentActivityStrip` component exists but is not imported or mounted anywhere in the app | `src/components/RecentActivityStrip.tsx` | ca03458 | integrated into a relevant page (e.g. home or activity) or deleted | FIXED_VERIFIED |
| AUD-UX-047 | UX/Shelf | "Contains / Cover" setting in shelf editor does nothing — control is rendered but not wired to any state or rendering logic | ShelfLayoutEditor or ShelfDisplaySlot | (pre-existing fix verified by reading current file) | ShelfReadOnlyControls.tsx:489-513 — "Contain / Cover" buttons call `persist({...prefs, fitMode: 'contain'|'cover'})`. `shelfViewPrefsCssVars` maps `fitMode` → `--shelf-fit-mode` CSS var (shelf-view-prefs.ts:244) applied via `el.style.setProperty` to all `.shelf-view-root` elements. ShelfSpatialView ShelfCard and DisplayCard images consume `objectFit: 'var(--shelf-fit-mode, cover)'` (ShelfSpatialView.tsx:381,427). No unwired "Contains/Cover" control exists in ShelfLayoutEditor — the audit entry predates the current component structure. | FIXED_VERIFIED |
| AUD-UX-048 | UX/Shelf | "Display orientation" (landscape/portrait) setting is NOT isolated per sub-shelf — changing it in one sub-shelf editor affects all sub-shelves globally | ShelfLayoutEditor display orientation control | commit b71ca4a — ShelfReadOnlyControls `scope` selector ('Global' / 'This shelf') gates all writes: shelf-scope `persist()` computes a partial-diff vs global and writes only to `shelf_display_overrides_v1.shelves[activeShelfId]` (ShelfReadOnlyControls.tsx:197-237). Per-display-row: DisplayRow sets `--row-display-aspect: var(--display-aspect-row-N, var(--display-aspect-ratio, <server>))` (ShelfSpatialView.tsx:339); stale row vars swept before applying new prefs (ShelfReadOnlyControls.tsx:124-126). Each shelf's effective prefs are resolved independently via `resolveShelfPrefs(overrides, shelfId)`. | FIXED_VERIFIED |
| AUD-UX-049 | UX/Refresh | RefreshScopeButton `now` state initialized to `lastUpdatedAt` (past timestamp) instead of `Date.now()`, causing freshness chip to show "just now" on every page load; also no optimistic update of chip after successful Refresh click | `src/components/RefreshScopeButton.tsx` | 35ec2b5 | `now` uses lazy `() => Date.now()` initializer; `refreshedAt` state updated on success so chip shows "just now" immediately without waiting for router.refresh() | FIXED_VERIFIED |
| AUD-UX-050 | UX/Lists | ListsPickerButton overlay trigger becomes invisible (opacity-0) when mouse moves from VN card to the PortalPopover panel — group-hover state ends while popover stays open | `src/components/ListsPickerButton.tsx` | 35ec2b5 | `!opacity-100` added to overlay trigger when `open === true` so button stays visible while popover is open | FIXED_VERIFIED |
| AUD-FEAT-001 | Feature | No per-page content width control — pages have hardcoded `max-w-5xl`/`max-w-6xl` leaving large empty margins on wide displays; user wants a per-page width slider like the density slider | `src/lib/settings/client.tsx`, `src/components/DensityScopeProvider.tsx`, all listing pages | Feature explicitly removed by user decision (commit 50a757a). ContentWidthSlider and pageWidth setting deleted. Pages use their CSS-class max-width. | N/A — feature removed | EXTERNALLY_BLOCKED_WITH_EVIDENCE |
| PAGE-001 | Page | Home `<Suspense>` has no fallback, causing invisible loading gap. | `src/app/page.tsx`, `/` | | source proof + browser load evidence | TODO |
| PAGE-002 | Page | Home page has no `generateMetadata()`, no page title. | `src/app/page.tsx`, `/` | | source proof | TODO |
| PAGE-003 | Page | `getAppSetting('home_section_layout_v1')` DB call in RSC has no try/catch; DB error crashes page. | `src/app/page.tsx`, `/` | | test or source proof | TODO |
| PAGE-004 | Page | `/activity` uses one `page` param for two independent paginated data sources. | `src/app/activity/page.tsx`, `/activity` | | route/browser pagination proof | TODO |
| PAGE-005 | Page | `/activity` entity filter accepts arbitrary free-text values. | `src/app/activity/page.tsx` | | unit/source proof | TODO |
| PAGE-006 | Page | `/activity` lacks route-level `error.tsx`. | `src/app/activity/error.tsx` | | source proof | TODO |
| PAGE-007 | Page | `/brand-overlap` lacks metadata title. | `src/app/brand-overlap/page.tsx` | | source proof | TODO |
| PAGE-008 | Page | `/brand-overlap` lacks `error.tsx`. | `src/app/brand-overlap/error.tsx` | | source proof | TODO |
| PAGE-009 | Page | `/characters` local results hard-limited to 200 with no pagination/notice. | `src/app/characters/page.tsx`, `/characters` | | browser evidence | TODO |
| PAGE-010 | Page | `/characters` lacks `error.tsx`. | `src/app/characters/error.tsx` | | source proof | TODO |
| PAGE-011 | Page | `/compare` lacks `error.tsx`. | `src/app/compare/error.tsx` | | source proof | TODO |
| PAGE-012 | Page | `/data` lacks `error.tsx`. | `src/app/data/error.tsx` | | source proof | TODO |
| PAGE-013 | Page | `/dumped` lacks `error.tsx`. | `src/app/dumped/error.tsx` | | source proof | TODO |
| PAGE-014 | Page | `/egs` lacks `error.tsx`. | `src/app/egs/error.tsx` | | source proof | TODO |
| PAGE-015 | Page | `/labels` lacks metadata title. | `src/app/labels/page.tsx` | | source proof | TODO |
| PAGE-016 | Page | `/labels` ids query param lacks per-id format validation. | `src/app/labels/page.tsx` | | unit/source proof | TODO |
| PAGE-017 | security | `/labels` uses `dangerouslySetInnerHTML` for QR SVG; safe today but must be documented. | `src/app/labels/page.tsx` | | source proof | TODO |
| PAGE-018 | Page | `/quotes` hard-limits to 300 results with no pagination/notice. | `src/app/quotes/page.tsx` | | browser/source proof | TODO |
| PAGE-019 | Page | `/release/[id]` performs DB write side effect inside RSC render. | `src/app/release/[id]/page.tsx` | a56905e | upsertReleaseResolutionCache moved into after(); runs post-response; yarn test 1363/1363 | FIXED_VERIFIED |
| PAGE-020 | Page | `/release/[id]` lacks metadata title. | `src/app/release/[id]/page.tsx` | | source proof | TODO |
| PAGE-021 | Page | `/search` lacks `dynamic = 'force-dynamic'`. | `src/app/search/page.tsx` | 8fb5ca2 | `export const dynamic = 'force-dynamic'` added at top; yarn test 1363/1363 | FIXED_VERIFIED |
| PAGE-022 | Page | `/search` Suspense around client component lacks fallback. | `src/app/search/page.tsx` | | source/browser proof | TODO |
| PAGE-023 | Page | `/similar` hard-caps at 24 results with no pagination/notice. | `src/app/similar/page.tsx` | | browser/source proof | TODO |
| PAGE-024 | Page | `/tag/[id]` local tab returns all matching rows without pagination/limit notice. | `src/app/tag/[id]/page.tsx` | | browser/source proof | TODO |
| SECA-001 | security | `POST /api/vndb/pull-statuses` has no auth gate despite destructive sync. | `src/app/api/vndb/pull-statuses/route.ts` | | route test | TODO |
| SECA-002 | security | `GET /api/vn/[id]` has side effects and fan-outs but no auth gate. | `src/app/api/vn/[id]/route.ts` | | route test | TODO |
| SECA-003 | security | `GET /api/vn/[id]/quotes` leaks quote/character data without auth. | `src/app/api/vn/[id]/quotes/route.ts` | | route test | TODO |
| SECA-004 | security | `GET /api/vn/[id]/lists` leaks list membership without auth. | `src/app/api/vn/[id]/lists/route.ts` | | route test | TODO |
| SECA-005 | security | `/api/vn/[id]/aspect` lacks auth on GET/PATCH/DELETE; PATCH/DELETE mutate DB. | `src/app/api/vn/[id]/aspect/route.ts` | | route test | TODO |
| SECA-006 | security | `/api/shelves` POST/PATCH lack auth gate. | `src/app/api/shelves/route.ts` | | route test | TODO |
| SECA-007 | security | `/api/shelves/[id]` PATCH/DELETE lack auth gate. | `src/app/api/shelves/[id]/route.ts` | | route test | TODO |
| SECA-008 | security | `/api/shelves/[id]/slots` write handlers must be audited for auth. | `src/app/api/shelves/[id]/slots/route.ts` | | route test | TODO |
| SECA-009 | security | `/api/search` consumes VNDB quota without auth. | `src/app/api/search/route.ts` | | route test | TODO |
| SECA-010 | security | `/api/activity` auth status must be verified for GET/write handlers. | `src/app/api/activity/route.ts` | | route test | TODO |
| DBA-001 | DB/backend | Aspect filtering/grouping triggers per-VN N+1 `materializeReleaseAspectsForVn(id)` loop on collection GET. | `src/app/api/collection/route.ts`, `src/lib/db.ts` | 3474284 | materializeReleaseAspectsForCollectionVns() batch fn: single IN check + single vndb_cache scan + single tx; O(n) loop replaced; yarn test 1417/1417 | FIXED_VERIFIED |
| DBA-002 | DB/backend | `uniqueSlug()` SELECT loop is safe in create transaction but unsafe for `updateUserList`. | `src/lib/db.ts` | | unit/source proof | TODO |
| DBA-003 | DB/backend | `updateUserList` check/slug/update not in transaction; TOCTOU duplicate slug risk. | `src/lib/db.ts` | a56905e | Full updateUserList body wrapped in db.transaction(); yarn test 1363/1363 | FIXED_VERIFIED |
| DBA-004 | DB/backend | `setOwnedReleaseAspectOverride` existence check and write not in transaction. | `src/lib/db.ts` | | unit/source proof | TODO |
| DBA-005 | DB/backend | `setSteamLink` SELECT/guard/write not atomic; manual guard can be bypassed by race. | `src/lib/db.ts` | | unit/source proof | TODO |
| DBA-006 | DB/backend | `updateGameLogEntry` read/merge/write outside transaction; lost update risk. | `src/lib/db.ts` | | unit/source proof | TODO |
| DBA-007 | DB/backend | `ratingHistogram()` fetches every rated row into JS instead of SQL aggregation. | `src/lib/db.ts` | | test/source proof | TODO |
| DBA-008 | DB/backend | migration marker written outside transaction after legacy row update. | `src/lib/db.ts` | | source proof | TODO |
| DBA-009 | DB/backend | `getDbStatus()` interpolates hardcoded table names; validate/allowlist. | `src/lib/db.ts` | | source proof | TODO |
| DBA-010 | DB/backend | `restoreFromSqliteFile()` SQL identifier interpolation should add stricter allowlist. | `src/lib/db.ts` | | source proof | TODO |
| DBA-011 | DB/backend | `user_list_vn.vn_id` orphan tolerance after removeFromCollection needs cleanup or explicit maintenance. | `src/lib/db.ts` | | test/source proof | TODO |
| UXA-001 | UI/UX | `MarkdownNotes` Edit/Preview buttons hand-roll styles instead of `btn` primitive. | `src/components/MarkdownNotes.tsx` | | source/browser proof | TODO |
| UXA-002 | UI/UX | `CustomSynopsis` buttons hand-roll styles instead of `btn btn-xs`. | `src/components/CustomSynopsis.tsx` | | source/browser proof | TODO |
| UXA-003 | UI/UX | `SettingsButton` has multiple hand-rolled inline buttons. | `src/components/SettingsButton.tsx` | | source/browser proof | TODO |
| UXA-004 | UI/UX | Steam page show all/less toggle hand-rolls button style. | `src/app/steam/page.tsx` | | source proof | TODO |
| UXA-005 | UI/UX | `EgsPanel` search button hand-rolls button style. | `src/components/EgsPanel.tsx` | | source proof | TODO |
| UXA-006 | UI/UX | `VnTagChips` spoiler reveal/hide button hand-rolls button style. | `src/components/VnTagChips.tsx` | | source/browser proof | TODO |
| UXA-007 | a11y | Raw `▲` / `▼` glyphs used as sole section state indicator without sr text/aria-expanded. | `src/components/SettingsButton.tsx` | | a11y/source proof | TODO |
| UXA-008 | UI/UX | Activity VN title uses `truncate` without `title`. | `src/app/activity/page.tsx` | | source proof | TODO |
| UXA-009 | UI/UX | Labels print card uses `line-clamp-3` without `title`. | `src/app/labels/page.tsx` | | source proof | TODO |
| UXA-010 | UI/UX | Steam suggestion rows use `line-clamp-1` without `title`. | `src/app/steam/page.tsx` | | source proof | TODO |
| UXA-011 | UI/UX | Upcoming brand fallback span uses `line-clamp-1` without `title`. | `src/app/upcoming/page.tsx` | | source proof | TODO |
| UXA-012 | UI/UX | Dumped VN title uses `line-clamp-2` without `title`. | `src/app/dumped/page.tsx` | | source proof | TODO |
| I18NA-001 | i18n | `global-error.tsx` hardcodes `<html lang="en">` and English fallback UI. | `src/app/global-error.tsx` | bed720e | lang read from locale cookie via readLocaleCookie(); defaults to fr (DEFAULT_LOCALE); yarn test 1417/1417 | FIXED_VERIFIED |
| I18NA-002 | i18n | Staff VNDB external link uses bare `aria-label="VNDB"` / `title="VNDB"` instead of localized action. | `src/app/staff/[id]/page.tsx` | | source proof | TODO |
| I18NA-003 | i18n | `VnTagsGroupedView` VNDB external link uses bare `aria-label="VNDB"` / `title="VNDB"`. | `src/components/VnTagsGroupedView.tsx` | | source proof | TODO |
| I18NA-004 | i18n | Owned editions platform placeholder `win, ps4, swi…` hardcoded in UI. | `src/components/OwnedEditionsSection.tsx` | | source proof | TODO |
| I18NA-005 | i18n | Owned editions currency placeholder `JPY` hardcoded as UI hint. | `src/components/OwnedEditionsSection.tsx` | | source proof | TODO |
| I18NA-006 | i18n | Cover picker `alt="VNDB"` should describe image, e.g. VNDB cover. | `src/components/CoverSourcePicker.tsx` | | source proof | TODO |
| I18NA-007 | i18n | `recommend.explain.filterEroOff` missing/asymmetric across locales. | `src/lib/i18n/dictionaries.ts`, recommendations page | | i18n parity + source proof | TODO |
| I18NA-008 | i18n | French locale uses personal/informal `tu/ta/tes/mon/ma/mes` phrasing systemically. Decide policy then apply consistently. | `src/lib/i18n/dictionaries.ts` | | dictionary audit | TODO |
| RESP-001 | responsive/mobile | MoreNavMenu hides labels below xl, but aria-label/title exist; likely accepted if documented. | `src/components/MoreNavMenu.tsx` | | source proof | TODO |
| RESP-002 | responsive/mobile | `RoutesSection` action cluster uses `sm:opacity-0 sm:group-hover`, invisible on 640–767px touch screens. | `src/components/RoutesSection.tsx` | ac8730d | hover-trap classes removed; controls always visible at all viewports; yarn test 1417/1417 | FIXED_VERIFIED |
| RESP-003 | responsive/mobile | `EditionInfoPopover` hover-hidden info button uses `sm:` hover trap, affecting shelf display/slot tiles. | `src/components/EditionInfoPopover.tsx`, `ShelfLayoutEditor` | ac8730d | sm:opacity-0 removed; info button always reachable; yarn test 1417/1417 | FIXED_VERIFIED |
| RESP-004 | responsive/mobile | `ShelfLayoutEditor` display tile title overlay hidden on 640–767px due to `sm:opacity-0 sm:group-hover`. | `src/components/ShelfLayoutEditor.tsx` | | responsive browser proof | TODO |
| RESP-005 | responsive/mobile | `ShelfLayoutEditor` slot tile title overlay hidden on 640–767px due to `sm:opacity-0 sm:group-hover`. | `src/components/ShelfLayoutEditor.tsx` | | responsive browser proof | TODO |
| QAI-001 | process | No bulk-closing checklist rows by Python/sed script. | checklist process | | checklist commit review | TODO |
| QAI-002 | process | No visual row may be closed with grep-only/source-only/HTTP 200 evidence. | checklist process | | checklist integrity audit | TODO |
| QAI-003 | process | No `risk accepted` / `future sprint` for implementable security fixes. | docs/src/tests | | grep + row review | TODO |
| QAI-004 | process | No ContentWidthSlider reintroduction. | src/docs/tests | | grep proof | TODO |
| QAI-005 | process | Broad rows must enumerate subitems before closure. | checklist | | row-specific evidence | TODO |
| QAI-006 | process | Existing rows marked FIXED_VERIFIED must be re-audited for weak evidence. | checklist | | checklist integrity audit | TODO |
| PAGE-025 | Page | `/lists/[id]` lacks `error.tsx`. | `src/app/lists/[id]/error.tsx` | | source proof | TODO |
| PAGE-026 | Page | `/lists` lacks `error.tsx`. | `src/app/lists/error.tsx` | | source proof | TODO |
| PAGE-027 | Page | `/producers` lacks `error.tsx`. | `src/app/producers/error.tsx` | | source proof | TODO |
| PAGE-028 | Page | `/recommendations` lacks `error.tsx`. | `src/app/recommendations/error.tsx` | | source proof | TODO |
| PAGE-029 | Page | `/quotes` lacks `error.tsx`. | `src/app/quotes/error.tsx` | | source proof | TODO |
| PAGE-030 | Page | `/steam` lacks `generateMetadata()`. | `src/app/steam/page.tsx` | | source proof | TODO |
| PAGE-031 | Page | `/trait/[id]` transient VNDB error misclassified as 404; VNDB timeout causes real pages to return notFound. | `src/app/trait/[id]/page.tsx` | 777ef39 | notFound() only called when !trait && !error; transient errors render error state with VNDB link; yarn test 1363/1363 | FIXED_VERIFIED |
| PAGE-032 | Page | `/vn/[id]` calls `materializeRelease*` inside RSC render — DB write side effect on every GET. | `src/app/vn/[id]/page.tsx` | a159d97 | materializeReleaseAspectsForVn + materializeReleaseMetaForVn deferred to after(); idempotent UPSERTs run post-response; yarn test 1363/1363 | FIXED_VERIFIED |
| PAGE-033 | Page | `/trait/[id]` lacks `generateMetadata()`. | `src/app/trait/[id]/page.tsx` | | source proof | TODO |
| SECA-011 | security | `POST /api/saved-filters` has no auth gate; any caller can create/read saved filters. | `src/app/api/saved-filters/route.ts` | f425376 | POST/DELETE/PATCH all gate on requireLocalhostOrToken; yarn test 1363/1363 | FIXED_VERIFIED |
| SECA-012 | security | `POST /api/collection/[id]/activity` has no auth gate; any caller can log collection activity. | `src/app/api/collection/[id]/activity/route.ts` | f425376 | POST and DELETE gate on requireLocalhostOrToken; yarn test 1363/1363 | FIXED_VERIFIED |
| SECA-013 | security | `POST/DELETE /api/reading-queue` has no auth gate; any caller can mutate reading queue. | `src/app/api/reading-queue/route.ts` | f425376 | POST/DELETE/PATCH gate on requireLocalhostOrToken; yarn test 1363/1363 | FIXED_VERIFIED |
| SECA-014 | security | `POST/PATCH/DELETE /api/lists/[id]/items` has no auth gate; any caller can mutate list items. | `src/app/api/lists/[id]/items/route.ts` | f425376 | POST and DELETE gate on requireLocalhostOrToken; yarn test 1363/1363 | FIXED_VERIFIED |
| SECA-015 | security | `GET /api/collection/tags` has no auth gate; leaks tag data. | `src/app/api/collection/tags/route.ts` | | route test | TODO |
| SECA-016 | security | `/api/places` write handlers lack auth gate. | `src/app/api/places/route.ts` | | route test | TODO |
| SECA-017 | security | `/api/maintenance/duplicates` has no auth gate; triggers expensive DB scan. | `src/app/api/maintenance/duplicates/route.ts` | | route test | TODO |
| SECA-018 | security | `/api/export/csv` has no auth gate; leaks full collection as CSV. | `src/app/api/export/csv/route.ts` | | route test | TODO |
| SECA-019 | security | `/api/export/ics` has no auth gate; leaks calendar data. | `src/app/api/export/ics/route.ts` | | route test | TODO |
| SECA-020 | security | `/api/collection/export`, `/find`, `/characters`, `/traits`, `/full-download` lack auth gates. | `src/app/api/collection/` export/find/characters/traits/full-download routes | | route test | TODO |
| SECA-021 | security | `/api/maintenance/stale`, `/producers`, `/download-status`, `/activity/kinds`, `/search/textual`, `/vn/[id]/lists`, `/vndb/auth` lack auth gates. | multiple routes | | route test | TODO |
| SECA-022 | security | `POST /api/egs/[id]/add` missing auth gate; mutates collection. | `src/app/api/egs/[id]/add/route.ts` | f425376 | requireLocalhostOrToken gate added before param read; yarn test 1363/1363 | FIXED_VERIFIED |
| SECA-023 | security | `src/proxy.ts` CSRF guard is dead code — not imported as Next.js middleware; CSRF guard never runs. | `src/proxy.ts`, project root | b9c9492 | Created src/middleware.ts exporting middleware(); csrfGuard now active on /api/:path*; yarn test 1363/1363 | FIXED_VERIFIED |
| SECA-024 | security | `/api/collection/order` write handlers lack auth gate. | `src/app/api/collection/order/route.ts` | f425376 | PATCH and DELETE gate on requireLocalhostOrToken; yarn test 1363/1363 | FIXED_VERIFIED |
| DBA-012 | DB/backend | `todaysAnniversaries()` has no LIMIT; large libraries with many same-day releases pull unbounded results into JS. | `src/lib/db.ts` | | source proof | TODO |
| DBA-013 | DB/backend | `listAllListMemberships()` uses `LIMIT 100000` — effectively unbounded on large collections; all metadata rows loaded into JS for grouping. | `src/lib/db.ts` | | source proof | TODO |
| I18NA-009 | i18n | `languageDisplayName()` is locale-blind — always returns English language names regardless of app locale. | `src/lib/language-names.ts` | 1f7e336 | Intl.DisplayNames([locale, 'en']) + locale param; fallback to static map; backward compat; yarn test 1417/1417 | FIXED_VERIFIED |
| I18NA-010 | i18n | Character page `fmtBirthday` uses `'default'` locale; month name renders in OS locale (usually English) regardless of app locale. | `src/app/character/[id]/page.tsx` | | source proof | TODO |
| I18NA-011 | i18n | `StaleEgsBanner` in `/top-ranked` and `/upcoming` calls `toLocaleString()` without locale arg; timestamps render in OS locale. | `src/app/top-ranked/page.tsx`, `src/app/upcoming/page.tsx` | | source proof | TODO |
| I18NA-012 | i18n | `toLocaleString()` called without locale arg across 10+ sites (recommendations, top-ranked, trait/[id], tag/[id], TagsBrowser, TagPicker, EgsPanel, etc.); number formatting is locale-inconsistent. | Multiple files | | source proof | TODO |
| I18NA-013 | i18n | `CachePanel`, `SchemaEgsSection`, `RefreshScopeButton` use locale-blind `toLocaleString()` for timestamps in admin/dev panels. | `src/components/CachePanel.tsx`, `SchemaEgsSection.tsx`, `RefreshScopeButton.tsx` | | source proof | TODO |
| I18NA-014 | i18n | `SettingsButton.tsx` contains hardcoded string not run through dictionary. | `src/components/SettingsButton.tsx:559` | | source proof | TODO |
| I18NA-015 | i18n | `ListAddVnForm.tsx` contains hardcoded string not run through dictionary. | `src/components/ListAddVnForm.tsx:50` | | source proof | TODO |
| UXA-013 | UI/UX | `character/[id]` VA section: c_name, c_original, VN title chip use `line-clamp-*` without `title`. | `src/app/character/[id]/page.tsx:245–254` | | source proof | TODO |
| UXA-014 | UI/UX | `character/[id]` related VNs: VN title and alttitle use `line-clamp-*` without `title`. | `src/app/character/[id]/page.tsx:383,397` | | source proof | TODO |
| UXA-015 | UI/UX | `shelf/page` multiple `line-clamp-*` elements (VN title, release title, edition label, location) without `title`. | `src/app/shelf/page.tsx:435,449,454,461,631,638` | | source proof | TODO |
| UXA-016 | UI/UX | `staff/[id]` character name, original, note, VN title, alttitle use `line-clamp-*` without `title`. | `src/app/staff/[id]/page.tsx:351,356,359,494,512` | | source proof | TODO |
| UXA-017 | UI/UX | `tag/[id]` two VN title `<p>` elements use `line-clamp-2` without `title`. | `src/app/tag/[id]/page.tsx:193,472` | | source proof | TODO |
| UXA-018 | UI/UX | `trait/[id]` character name, original, first VN title use `line-clamp-*` without `title`. | `src/app/trait/[id]/page.tsx:169,173,178` | | source proof | TODO |
| UXA-019 | UI/UX | `compare/page` VN card title link uses `line-clamp-2` without `title`. | `src/app/compare/page.tsx:310` | | source proof | TODO |
| UXA-020 | UI/UX | `StaffExtraCredits` character link, VN title, alttitle use `truncate`/`line-clamp-*` without `title`. | `src/components/StaffExtraCredits.tsx:72,156,169` | | source proof | TODO |
| UXA-021 | UI/UX | `ReadingQueueStripView` queue title span uses `line-clamp-1 max-w-[200px]` without `title`. | `src/components/ReadingQueueStripView.tsx:69` | | source proof | TODO |
| UXA-022 | UI/UX | `EgsSyncBlock` matched VN title link uses `truncate` without `title`. | `src/components/EgsSyncBlock.tsx:200` | | source proof | TODO |
| UXA-023 | UI/UX | `VnSeedPicker` alttitle, search result title, alttitle, year+dev string all use `line-clamp-1` without `title`. | `src/components/VnSeedPicker.tsx:299,408–412` | | source proof | TODO |
| UXA-024 | UI/UX | `CompareVnPicker` selected title/alttitle and search result title/alttitle use `line-clamp-*` without `title`. | `src/components/CompareVnPicker.tsx:216,220,324,326` | | source proof | TODO |
| UXA-025 | UI/UX | `ListsPickerButton` list name span uses `line-clamp-1` without `title`. | `src/components/ListsPickerButton.tsx:231` | | source proof | TODO |
| UXA-026 | UI/UX | `SavedFilters` truncated filter name button has `title={f.params}` (URL string) not `title={f.name}` (human name). | `src/components/SavedFilters.tsx:230` | | source proof | TODO |
| UXA-027 | UI/UX | `SelectiveFullDownload` VN title span uses `truncate` without `title`. | `src/components/SelectiveFullDownload.tsx:348` | | source proof | TODO |
| UXA-028 | UI/UX | `CompareWithButton` VN title span uses `truncate` without `title`. | `src/components/CompareWithButton.tsx:134` | | source proof | TODO |
| UXA-029 | UI/UX | `MapVnToEgsButton` game name div uses `truncate` without `title`. | `src/components/MapVnToEgsButton.tsx:280` | | source proof | TODO |
| UXA-030 | UI/UX | `LinkToVndbButton` VNDB search result title uses `truncate` without `title`. | `src/components/LinkToVndbButton.tsx:142` | | source proof | TODO |
| UXA-031 | UI/UX | `EgsPanel` game name uses `line-clamp-2`/`line-clamp-1` without `title` in two locations. | `src/components/EgsPanel.tsx:269,539` | | source proof | TODO |
| UXA-032 | UI/UX | `BulkDownloadButton` current download title uses `truncate` without `title`. | `src/components/BulkDownloadButton.tsx:299` | | source proof | TODO |
| UXA-033 | UI/UX | `DataMaintenance` duplicate prefix and stale VN title use `truncate` without `title`. | `src/components/DataMaintenance.tsx:98,124` | | source proof | TODO |
| UXA-034 | UI/UX | `SeriesManager` series description uses `line-clamp-2` without `title`. | `src/components/SeriesManager.tsx:96` | | source proof | TODO |
| UXA-035 | UI/UX | `SearchClient` EGS search result game name uses `line-clamp-2` without `title`. | `src/components/SearchClient.tsx:650` | | source proof | TODO |
| UXA-036 | UI/UX | `ShelfReadOnlyControls` row orientation zone label uses `truncate` without `title`. | `src/components/ShelfReadOnlyControls.tsx:550` | | source proof | TODO |
| UXA-037 | UI/UX | Activity playtime delta rendered in green/red only — color-only state indicator without icon for colorblind users. | `src/app/activity/page.tsx:72` | | source/browser proof | TODO |
| UXA-038 | UI/UX | Activity favorite state uses yellow/muted color only without icon to reinforce state. | `src/app/activity/page.tsx:81` | | source/browser proof | TODO |
| UXA-039 | UI/UX | Steam "Current links" section has no loading skeleton; layout shifts when links appear after fetch. | `src/app/steam/page.tsx:293` | | source/browser proof | TODO |
| RESP-006 | responsive/mobile | `ListCardActions` trigger button has no `aria-label` and is `h-6 w-6` — below 44px tap-target minimum. | `src/components/ListCardActions.tsx` | | source + responsive browser proof | TODO |
| RESP-007 | responsive/mobile | `DetailReorderLayout` has three `h-6 w-6` icon-only controls without sufficient tap-target on mobile. | `src/components/DetailReorderLayout.tsx` | | source + responsive browser proof | TODO |
| A11Y-001 | a11y | `HomeSectionMenu` menu items have no ArrowDown/ArrowUp/Home/End keyboard navigation; focus not moved into menu on open. | `src/components/HomeSectionMenu.tsx` | | a11y/source proof | TODO |
| A11Y-002 | a11y | `ListCardActions` trigger button is icon-only with no `aria-label` and no `aria-controls`. | `src/components/ListCardActions.tsx` | | a11y/source proof | TODO |
| A11Y-003 | a11y | `ListsPickerButton` `role="menu"` has no ArrowDown/ArrowUp keyboard navigation within menu items. | `src/components/ListsPickerButton.tsx` | | a11y/source proof | TODO |
| A11Y-004 | a11y | `AdvancedFiltersDrawer` button has `aria-expanded` but missing `aria-controls`. | `src/components/AdvancedFiltersDrawer.tsx` | | a11y/source proof | TODO |
| A11Y-005 | a11y | Status filter buttons in `LibraryClient` have no `aria-pressed`. | `src/components/LibraryClient.tsx:468–484` | | a11y/source proof | TODO |
| A11Y-006 | a11y | `LibraryClient` search result count has no `aria-live` region; screen readers don't announce count changes. | `src/components/LibraryClient.tsx` | | a11y/source proof | TODO |
| A11Y-007 | a11y | `LibraryClient` filter `<input>` at line 494 has `placeholder` but no `aria-label`. | `src/components/LibraryClient.tsx:494` | | a11y/source proof | TODO |
| A11Y-008 | a11y | `ListCardActions` `role="menu"` panel has no `aria-label` or `aria-labelledby`. | `src/components/ListCardActions.tsx` | | a11y/source proof | TODO |
| A11Y-009 | a11y | VN detail page: `<h1>` (VN title) immediately followed by `<h3>` for sections — skips `<h2>`. | `src/app/vn/[id]/page.tsx` | | a11y/source proof | TODO |
| A11Y-010 | a11y | Producer detail page: `<h1>` → `<h3>` heading hierarchy gap. | `src/app/producer/[id]/page.tsx` | | a11y/source proof | TODO |
| A11Y-011 | a11y | Character detail page: `<h1>` → `<h3>` heading hierarchy gap. | `src/app/character/[id]/page.tsx` | | a11y/source proof | TODO |
| A11Y-012 | a11y | `ReadingGoalCard` year input has no `aria-label`. | `src/components/ReadingGoalCard.tsx` | | a11y/source proof | TODO |
| PERF-001 | performance | `LibraryClient` renders all VN cards in DOM with no virtualization; slow for large collections. | `src/components/LibraryClient.tsx` | | performance benchmark | TODO |
| PERF-002 | performance | Multiple `SELECT *` queries in `db.ts` overfetch entire rows where only a few columns are used. | `src/lib/db.ts:1946,1959,3659,4570,5152,6212,6348,6552,6642,6725,6794` | | source review | TODO |
| PERF-003 | performance | `ORDER BY RANDOM()` at `db.ts:2522` forces full table scan on every call with no LIMIT bound. | `src/lib/db.ts:2522` | | source + EXPLAIN QUERY PLAN | TODO |
| PERF-004 | performance | `json_each(v.tags)` aggregate queries do full-table JSON expansion — no index on JSON fields. | `src/lib/db.ts:4207,4285,4513,6996,7045` | | source + EXPLAIN QUERY PLAN | TODO |
| PERF-005 | performance | `searchLocalStaff` uses `LOWER(sc.name) LIKE ?` which bypasses any index on the name column. | `src/lib/db.ts:4443` | | source + EXPLAIN QUERY PLAN | TODO |
| TS-001 | code quality | `vndb-cache.ts` lines 125, 141: `JSON.parse(cached.body)` not in try/catch; corrupted cache throws uncaught. | `src/lib/vndb-cache.ts:125,141` | f425376 | All 3 JSON.parse(cached.body) sites wrapped individually; yarn test 1363/1363 | FIXED_VERIFIED |
| TS-002 | code quality | `activity/page.tsx` bare type assertions (`p?.from as string`, `p?.to as number`) without runtime checks. | `src/app/activity/page.tsx:39–88` | | source proof | TODO |
| TS-003 | code quality | `VndbMarkup.tsx` `renderTokens` switch has no `default: never` guard — new Token kinds fall through silently. | `src/components/VndbMarkup.tsx` | | source proof | TODO |
| TS-004 | Next.js | `src/app/lists/[id]/` has no `error.tsx`; page errors fall back to root error boundary only. | `src/app/lists/[id]/` | | source proof | TODO |
| TS-005 | code quality | `key={i}` index used as React key in data (non-skeleton) lists across multiple components. | `src/app/producer/[id]/page.tsx`, `src/app/tag/[id]/page.tsx`, `src/app/upcoming/page.tsx`, `src/components/QuotesSection.tsx`, `src/components/CharactersSection.tsx`, `src/components/EgsRichDetails.tsx`, `src/components/DateInput.tsx` | | source proof | TODO |
| COMP-001 | component library | `MediaGallery.tsx` keydown `useEffect` missing `prev`/`next` deps — stale closure breaks keyboard navigation. | `src/components/MediaGallery.tsx:144–158` | 8fb5ca2 | prev/next stabilized with useCallback; added to useEffect deps array; yarn test 1363/1363 | FIXED_VERIFIED |
| COMP-002 | component library | `AspectOverrideControl.tsx` AbortController created inside async function; cleanup never registered; fetches never cancelled on unmount. | `src/components/AspectOverrideControl.tsx:65` | 8fb5ca2 | AbortController moved to useEffect body; signal passed as arg; cleanup registered; yarn test 1363/1363 | FIXED_VERIFIED |
| COMP-003 | component library | `TagsBrowser.tsx` tab switcher uses `<Link role="button">` — ARIA antipattern; should be `role="tab"` in `role="tablist"`. | `src/components/TagsBrowser.tsx` | 55a16ac | Link role=button replaced with button type=button role=tab; container role=group→role=tablist; aria-pressed→aria-selected; modeSwitcherLabel added to all 3 locales; yarn test 1363/1363 | FIXED_VERIFIED |
| COMP-004 | component library | `CardContextMenu.tsx` reads `window.innerWidth` in render body — SSR hydration mismatch. | `src/components/CardContextMenu.tsx:116–117` | | source proof | TODO |
| COMP-005 | component library | `AddMissingVnButton.tsx` uses success-toast string as `aria-label` instead of action description. | `src/components/AddMissingVnButton.tsx:51` | | a11y/source proof | TODO |
| COMP-006 | component library | `CharacterMetaClient.tsx` spoiler button has `aria-pressed={false}` hardcoded; always reports unpressed. | `src/components/CharacterMetaClient.tsx:127` | | a11y/source proof | TODO |
| COMP-007 | component library | `SpoilerChip.tsx` reveal button has `aria-pressed={false}` hardcoded. | `src/components/SpoilerChip.tsx:130` | | a11y/source proof | TODO |
| COMP-008 | component library | 7 components use bare `t.common.error` instead of `readApiError()`, discarding server error detail. | `OwnedEditionsSection.tsx`, `RoutesSection.tsx`, `SeriesManager.tsx`, `DetailReorderLayout.tsx`, `ProducerLogoUpload.tsx`, `CachePanel.tsx` | | source proof | TODO |
| COMP-009 | component library | 5 components start `useEffect` fetches without `AbortController`; risk setState after unmount. | `QueueButton.tsx`, `ReadingGoalCard.tsx`, `CachePanel.tsx`, `SettingsButton.tsx`, `SimilarSeedPicker.tsx` | | source proof | TODO |
| COMP-010 | component library | `SeriesRemoveVn.tsx` DELETE fetch has no `.ok` check or catch — errors silently swallowed. | `src/components/SeriesRemoveVn.tsx` | | source proof | TODO |
| COMP-011 | component library | `SelectiveFullDownload.tsx` renders VN results as `<li onClick>` — not keyboard-operable. | `src/components/SelectiveFullDownload.tsx` | | a11y/source proof | TODO |
| COMP-012 | component library | `ToastProvider.tsx` `setTimeout` IDs not tracked or cleared on unmount — timer leak. | `src/components/ToastProvider.tsx` | | source proof | TODO |
| COMP-013 | component library | `BulkDownloadButton.tsx` `bulkAbortRef` declared at module level — shared across instances. | `src/components/BulkDownloadButton.tsx` | | source proof | TODO |
| COMP-014 | component library | `CachePanel.tsx` passes async function to `startTransition`; async work runs outside transition boundary. | `src/components/CachePanel.tsx` | | source proof | TODO |
| COMP-015 | component library | `ShelfLayoutEditor.tsx` multiple `useEffect` blocks call `refreshActiveShelf` without abort control. | `src/components/ShelfLayoutEditor.tsx` | | source proof | TODO |
| COMP-016 | component library | `TagPicker.tsx` fetch has no AbortController and silent failure on non-2xx. | `src/components/TagPicker.tsx` | | source proof | TODO |
| COMP-017 | component library | `VnTagsGroupedView.tsx` VNDB link has hardcoded English `aria-label="VNDB"`. | `src/components/VnTagsGroupedView.tsx` | | a11y/source proof | TODO |
| COMP-018 | component library | `AnniversaryFeedView.tsx` `emptyHint` prop declared but never used — dead prop. | `src/components/AnniversaryFeedView.tsx` | | source proof | TODO |
| COMP-019 | component library | `DropImport.tsx` `confirm` from `useConfirm()` is stale in drag handler `useEffect` deps. | `src/components/DropImport.tsx` | | source proof | TODO |
| COMP-020 | component library | `CoverUploader.tsx` buttons inside `<form>` missing `type="button"` — default to type="submit". | `src/components/CoverUploader.tsx` | | source proof | TODO |
| COMP-021 | component library | `TagsBrowser.tsx` collapse toggle button missing `aria-expanded`. | `src/components/TagsBrowser.tsx` | | a11y/source proof | TODO |
| COMP-022 | component library | 3 components use magic `setTimeout` delays (50 ms, 0 ms) for focus management — fragile. | `CompareWithButton.tsx`, `SimilarSeedPicker.tsx`, `VnSeedPicker.tsx` | | source proof | TODO |
| COMP-023 | component library | Color-picker buttons use raw hex string as `aria-label` — screen readers announce hex codes. | `src/components/ListMetaEditor.tsx`, `CreateListForm.tsx` | | a11y/source proof | TODO |
| COMP-024 | component library | `ShelfReadOnlyControls.tsx` close button uses `aria-label={t.common.cancel}` — semantically wrong for close action. | `src/components/ShelfReadOnlyControls.tsx` | | a11y/source proof | TODO |
| LIB-001 | src/lib | `db.ts` `open()` never sets `busy_timeout` — concurrent writers receive immediate SQLITE_BUSY. | `src/lib/db.ts:114–115` | f425376 | `busy_timeout = 5000` pragma added; yarn test 1363/1363 | FIXED_VERIFIED |
| LIB-002 | src/lib | `auth-gate.ts` `TRUSTED_PROXY_SECRET` compared with `===` not `timingSafeEqual` — timing oracle. | `src/lib/auth-gate.ts:85` | f425376 | `timingSafeStrEqual(proofHeader ?? '', secret)` at line 85; yarn test 1363/1363 | FIXED_VERIFIED |
| LIB-003 | src/lib | `activity.ts` `recordActivity()` has no try/catch — DB error propagates through primary mutations as 500. | `src/lib/activity.ts:77–88` | f425376 | try/catch wraps INSERT; yarn test 1363/1363 | FIXED_VERIFIED |
| LIB-004 | src/lib | `db.ts` EGS colon-to-underscore migration omits 10+ tables with `vn_id` columns. | `src/lib/db.ts:818–845` | | source proof | TODO |
| LIB-005 | src/lib | `erogamescape.ts` `res.text()` has no size cap — unbounded buffer from EGS server. | `src/lib/erogamescape.ts:202` | | source proof | TODO |
| LIB-006 | src/lib | `erogamescape.ts` `fetchOne()` swallows `EgsUnreachable` as null — masks network errors as "not found". | `src/lib/erogamescape.ts:319–325` | | source proof | TODO |
| LIB-007 | src/lib | `vndb-throttle.ts` `probeVndbHealthy()` calls raw `fetch()` bypassing the rate limiter. | `src/lib/vndb-throttle.ts:157` | | source proof | TODO |
| LIB-008 | src/lib | `files.ts` `res.arrayBuffer()` has no size cap — unbounded image buffer in memory. | `src/lib/files.ts:118` | | source proof | TODO |
| LIB-009 | src/lib | `url-allowlist.ts` `assertNoPrivateIpRebind` checks IPv4 only — IPv6 DNS rebind not guarded. | `src/lib/url-allowlist.ts:100–112` | | source proof | TODO |
| LIB-010 | src/lib | `db.ts` `materializeRelease*` functions apply `LIMIT 200` to cache scan — silently excludes releases beyond 200. | `src/lib/db.ts` (materializeRelease* functions) | | source proof | TODO |
| LIB-011 | src/lib | `url-allowlist.ts` does not check `Location` header on redirects — SSRF via redirect chaining possible. | `src/lib/url-allowlist.ts:57–69` | | source proof | TODO |
| LIB-012 | src/lib | `steam.ts` `res.json()` unguarded — Steam maintenance page causes uncaught SyntaxError. | `src/lib/steam.ts` | | source proof | TODO |
| TCO-001 | test coverage | `src/lib/source-resolve.ts` `resolveField()` has zero tests — core VNDB/EGS data resolution entirely untested. | `src/lib/source-resolve.ts` | b5e3ef3 | tests/source-resolve.test.ts: 10 tests covering auto/vndb/egs/custom prefs, fallback, empty string/array, number 0; yarn test 1417/1417 | FIXED_VERIFIED |
| TCO-002 | test coverage | `src/lib/time-ago.ts` `timeAgo()` has zero tests — all 7 branches + null guard untested. | `src/lib/time-ago.ts` | b5e3ef3 | tests/time-ago.test.ts: 14 tests covering null/undefined/just-now/minutes/hours/days/weeks/months/years and future-ts clamp; yarn test 1417/1417 | FIXED_VERIFIED |
| TCO-003 | test coverage | `src/lib/series-detect.ts` BFS cycle guard and prefix heuristic have zero tests. | `src/lib/series-detect.ts` | b5e3ef3 | tests/series-detect.test.ts: 14 tests covering walkSeriesRelations (direct, transitive, cycle guard, relation filter, malformed JSON) and detectSeriesForVn (null VN, no rels, already in series, not in collection, new series, existing series, prefix heuristic); yarn test 1417/1417 | FIXED_VERIFIED |
| TCO-004 | test coverage | `src/lib/egs-sync.ts` `applyEgsSuggestions()` write path has zero tests — playtime/rating unit conversion untested. | `src/lib/egs-sync.ts` | b5e3ef3 | tests/egs-sync.test.ts: 13 tests covering computeEgsSuggestions (needsConfig, empty, unlinked, not in collection, playtime bump, score, finish date) and applyEgsSuggestions (empty picks, playtime write, score write, error handling); yarn test 1417/1417 | FIXED_VERIFIED |
| TCO-005 | test coverage | `src/lib/vndb-scrape.ts` global queue `working` flag reset on error has zero tests. | `src/lib/vndb-scrape.ts` | | integration test | TODO |
| TCO-006 | test coverage | `src/app/api/files/[...path]/route.ts` — no tests for path traversal protection. | `src/app/api/files/[...path]/route.ts` | | security integration test | TODO |
| TCO-007 | test coverage | `src/app/api/backup/restore/route.ts` POST — no tests for SQLite magic-byte check, content-type, size limit. | `src/app/api/backup/restore/route.ts` | | integration test | TODO |
| TCO-008 | test coverage | `src/app/api/collection/[id]/route.ts` `pickFields()` validation — 15+ branches entirely untested. | `src/app/api/collection/[id]/route.ts` | | integration test | TODO |
| TCO-009 | test coverage | 33 of 35 activity-kind tests are source-pin (string presence) not runtime behavior assertions. | `tests/activity.test.ts:182–231` | | integration test | TODO |
| TCO-010 | test coverage | Maintenance routes tested for 403 only — no body assertions on success path. | `tests/auth-gate-routes.test.ts:69–77` | | integration test | TODO |
| TCO-011 | test coverage | `auth-gate.ts` whitespace-only `VN_ADMIN_TOKEN` behavior untested. | `src/lib/auth-gate.ts` | | unit test | TODO |
| TCO-012 | test coverage | `src/lib/reading-speed.ts` `getReadingSpeedProfile()` and `predictReadingMinutes()` have zero tests. | `src/lib/reading-speed.ts` | | integration test | TODO |
