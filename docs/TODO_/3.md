OK, relations are stored as JSON. The rowToItem parses and returns them as relations on the CollectionItem. They flow to vn.relations on /vn/[id].

The final report is comprehensive. Let me finalize.

I should also do a final pass to check the inCollectionBadge placement at top-right collision. Looking at VnCard:

StatusBadge: absolute right-2 top-2 z-10 (line 226)showAddedBadge (CheckCheck pill): absolute right-2 top-2 z-10 (line 231)These have the SAME position. If both render, they overlap. But they're mutually exclusive in practice: StatusBadge renders if data.status, showAddedBadge renders if enableAdd && (data.inCollectionBadge || addedLocal). If data.status is set, inCollectionBadge = !!data.status from toCardData, but enableAdd is only passed by search/wishlist. Search results from the API don't have status (since they're from VNDB search), so they'd show showAddedBadge. Wishlist items from /api/wishlist don't have status either (fetchAuthenticatedWishlist's result is VNDB's ulist data, not user collection — even if owned). So no overlap.

OK. Now final report time.

Audit complete. Findings below.

Visual Regression Audit ReportRepo: /Users/loicpirez/Perso/vndb-collection-newBranch: main (HEAD d3b343d)Scope: in-collection / cross-reference / same-name surfaces

Finding #1 — /character/[id] "Same name in your collection" mislabels its own dataFiles

/Users/loicpirez/Perso/vndb-collection-new/src/lib/db.ts lines 1380–1421 (findCharacterSiblings)/Users/loicpirez/Perso/vndb-collection-new/src/app/character/[id]/page.tsx lines 206–233 (siblings section render)i18n keys t.characters.sameName / sameNameHintsrc/lib/i18n/dictionaries.ts 1114–1115 (fr), 3564–3565 (en), 6011–6012 (ja)SymptomThe section title reads "Same name in your collection" / "コレクション内で同名" / "Même nom dans la collection". Its hint says "VNDB sometimes splits a recurring character… Here are other entries sharing this display name." The SQL however joins vn_va_credit → vn only — it does NOT join collection. Because vn_va_credit is populated whenever upsertVn runs (relation fan-out at relations-full.ts:53, character full-download, staff fan-out at staff-full.ts, plain /vn/[id] visit at src/app/vn/[id]/page.tsx:119), the table holds rows for VNs that are not in the user's collection. So the section can list "siblings" whose VNs the user has never owned — directly contradicting the heading.

Each sibling row also renders its VN list as a bare link with no in-collection chip (src/app/character/[id]/page.tsx:221–226), so the user cannot even fact-check the heading visually.

Type: SQL bug + i18n / data-model mismatch.

Fix sketchEither tighten the SQL (JOIN collection c ON c.vn_id = va.vn_id) so the section only shows characters whose VNs the user actually owns — matching the existing copy — or rewrite the heading to "Same name in cached records" and add a per-VN in-collection chip inside the sibling rows.

Finding #2 — RelationsSection: in-collection related VNs render without any visible "you own this" indicatorFiles

/Users/loicpirez/Perso/vndb-collection-new/src/components/RelationsSection.tsx lines 30–47 (relationCardData), lines 98–107 ( usage)/Users/loicpirez/Perso/vndb-collection-new/src/components/VnCard.tsx lines 215–235 (badge gating)/Users/loicpirez/Perso/vndb-collection-new/src/app/vn/[id]/page.tsx lines 850–863 (in_collection flow)SymptomFor each related VN, /api/.../vn/[id]/page.tsx:854 correctly computes ownedSet = isInCollectionMany(...) and relationCardData sets inCollectionBadge: r.in_collection. But VnCard gates its visible "in collection" markers like this:

StatusBadge (top-right pill) — requires data.status. Related VNs have no status (the VnRelation shape doesn't carry status; the type only includes title/cover/relation/etc.). → never shown.showAddedBadge (CheckCheck pill, top-right) — requires enableAdd && (data.inCollectionBadge || addedLocal). RelationsSection never passes enableAdd. → never shown.FavoriteToggleButton does mount (line 215 condition is satisfied via data.inCollectionBadge), but it's opacity-0 md:group-hover (FavoriteToggleButton.tsx:106). → invisible on desktop until hover.Net effect: on /vn/[id], a user looking at the Relations grid cannot tell which related VNs they already own. Owned and unowned cards are visually identical.

Type: prop-not-passed bug. inCollectionBadge is set but its visible chip is gated behind enableAdd.

Fix sketchPick one:

Pass enableAdd to  in RelationsSection.tsx:99 (also lets the user one-click-add an unowned related VN, matching search/wishlist UX).Decouple the "in collection" pill from enableAdd in VnCard.tsx:141 — make showAddedBadge = !selectable && (data.inCollectionBadge || addedLocal) and gate only showAddButton on enableAdd.Finding #3 — /lists/[id] and /series/[id]: ListsPicker count chip never preloads → always blank until clickedFiles

/Users/loicpirez/Perso/vndb-collection-new/src/app/lists/[id]/page.tsx lines 27–54 (listCardData), 77–95 (loadCards)/Users/loicpirez/Perso/vndb-collection-new/src/app/series/[id]/page.tsx lines 36 + 105 (listCollection({series}) → toCardData(it))/Users/loicpirez/Perso/vndb-collection-new/src/lib/db.ts line 2745 (listCollection — does not annotate list_count)/Users/loicpirez/Perso/vndb-collection-new/src/components/cardData.ts line 46 (listCount: it.list_count ?? 0)Compare with the canonical wiring in /Users/loicpirez/Perso/vndb-collection-new/src/app/api/collection/route.ts:143–144SymptomThe Library (via /api/collection) annotates each item with list_count, so each VnCard's ListsPickerButton shows its membership-count chip at the corner (e.g. "3"). On /lists/[id], listCardData never sets listCount. On /series/[id], listCollection never annotates list_count. So VnCard.tsx:222 mounts <ListsPickerButton ... initialMemberCount={0}>, the count chip is hidden, and the user only sees a real number after clicking the popover (which then fetches /api/vn/[id]/lists).

Type: Prop-not-passed / data-not-annotated bug. Same per-card chip works correctly on Library, Wishlist, Search; missing on Series and Lists.

Fix sketch

For /series/[id]: after listCollection({series}), call countListMembershipsByVn() once and map items.map(it => ({ ...it, list_count: counts.get(it.id) ?? 0 })) before toCardData.For /lists/[id]: either add a (SELECT COUNT(*) FROM user_list_vn WHERE vn_id = v.id) subselect to loadCards's SQL and surface it on VnRow, or call countListMembershipsByVn() after loadCards and merge into the row. Then set listCount inside listCardData.Finding #4 — StaffExtraCredits (the "More credits" section on /staff/[id]): in-collection VNs aren't visually distinct enoughFiles

/Users/loicpirez/Perso/vndb-collection-new/src/components/StaffExtraCredits.tsx lines 121–168 (ExternalVnCard)compare /Users/loicpirez/Perso/vndb-collection-new/src/app/staff/[id]/page.tsx lines 378–483 (local VnCard used for known credits) — specifically lines 470–477 which render an explicit Star + ownedLabel chipSymptomThe locally-known-credits section uses a VnCard that renders an explicit Star + t.staff.ownedLabel pill for in-collection VNs (line 470–477). The "extra credits" section (the VNDB fan-out result, in StaffExtraCredits) uses ExternalVnCard which only changes the border tint to border-accent/40 when inCollection is true. There is no badge, no star, no label. Two near-identical surfaces on the same page treat the same state two visibly different ways — easy to miss the subtler one.

Type: Visual inconsistency / underweighted indicator.

Fix sketchMirror the local VnCard's chip in ExternalVnCard (insert a star + t.staff.ownedLabel near line 156–163 when inCollection).

Finding #5 — /staff/[id] has no "same-name staff" / sibling section at allFiles

/Users/loicpirez/Perso/vndb-collection-new/src/app/staff/[id]/page.tsx — no equivalent to findCharacterSiblings/Users/loicpirez/Perso/vndb-collection-new/src/lib/db.ts — findStaffSiblings does not existSymptomVNDB occasionally splits the same person across multiple sNNN ids (rename, region split, etc.). The character page surfaces sibling records (modulo Finding #1's bugs); the staff page does not. The "Aliases" block on /staff/[id] (lines 161–178) only shows intra-staff aliases (fullProfile.aliases.filter(a => !a.ismain)) — i.e. other names this same sNNN has used. There is no cross-sid same-display-name surface.

Type: Missing feature (relative to the symmetric character page surface).

Fix sketchAdd findStaffSiblings(sid) modeled on findCharacterSiblings (using vn_staff_credit.name = ? AND sid != ?, plus vn_va_credit.va_name if you want VA siblings too) and render an analogous section on /staff/[id]. Apply Finding #1's lesson: if the section title says "in your collection", actually join collection.

Finding #6 — /character/[id] "Appears in" cards don't mark in-collection VNsFile

/Users/loicpirez/Perso/vndb-collection-new/src/app/character/[id]/page.tsx lines 317–368SymptomThe section header says "Appears in · {N}" — accurate, not claiming "owned". But each card in the grid (lines 330–362) shows year + rating + role + an optional "N editions" chip — and nothing else. There's no visual differentiator between VNs the user owns and VNs they don't. The cards already have v.id so a single batched isInCollectionMany(sortedVns.map(v=>v.id)) call could drive a border + chip.

Type: Missing in-collection chip on a high-traffic card grid.

Fix sketchCompute ownedSet = isInCollectionMany(sortedVns.map(v=>v.id)) server-side; in each card, add border-accent/40 + a small star chip when ownedSet.has(v.id). Same pattern as staff/[id]'s local VnCard.

Finding #7 — findCharacterSiblings uses only the FIRST c_name for a char_idFile

/Users/loicpirez/Perso/vndb-collection-new/src/lib/db.ts lines 1381–1384Symptom

const me = db.prepare('SELECT c_name, c_original FROM vn_va_credit WHERE c_id = ? LIMIT 1').get(charId)When the same character (c_id) has multiple c_name spellings across VNs (different translations / romanizations / variant forms), only siblings of the first-row name are found. The LIMIT 1 is also non-deterministic (no ORDER BY) — the picked name depends on row insertion order.

Type: SQL correctness edge case.

Fix sketchEither use every known c_name (SELECT DISTINCT c_name FROM vn_va_credit WHERE c_id = ?) as match candidates, or extend the matcher to also try c_original. Add an ORDER BY so the choice is deterministic when the LIMIT 1 path is kept.

Finding #8 — /api/wishlist does N+1 isInCollection queriesFile

/Users/loicpirez/Perso/vndb-collection-new/src/app/api/wishlist/route.ts line 21Symptomin_collection: isInCollection(e.id) runs inside the .map. For a wishlist of N items that's N single-row SELECTs vs one batched IN(...). The exact pattern that isInCollectionMany exists to replace; the search and advanced-search routes already use it.

Type: Performance regression in the wishlist render path (not visually wrong, but in scope per the audit request mentioning WishlistClient).

Fix sketch

const ids = result.map(e => e.id);const ownedSet = isInCollectionMany(ids);const items = result.map(e => ({ ...e, vn: { ...e.vn, id: e.id }, in_collection: ownedSet.has(e.id), ... }));Findings out-of-scope but related (note only, not bugs to fix here)CastSection.tsx and StaffSection.tsx (/vn/[id] rendering): no in-collection / cross-credit chips on cast or staff chips. Bare links only. Compared with /producer/[id]'s ProducerVnsSections, which uses an explicit check icon when owned. Symmetry would call for a small "owns N other titles by this person" subscript per chip, but this is a feature-request, not a regression./similar, /top-ranked: card grids have no in-collection indicator. Same omission, not in scope.RelationsSection's self-section: relations carry no status, playtime, or per-source rating. Cards are deliberately spartan. The Finding #2 fix would land alongside this naturally.What I did NOT findNo SQL crash, JSON parse error, or null-deref in the audited files.No broken i18n key fallback (every sameName etc. exists in fr/en/ja).No missing-import or missing-export.findCharacterSiblings data shape, dedupAppearances, isInCollectionMany, relationCardData's plumbing, and the EnrichedRelation/VnRelation type chain are all internally consistent. The bugs are at the render-condition and SQL-filter layers, not in the data plumbing.Verification suggestions (not run — read-only audit)For each finding, the minimal reproduction:

#1: Visit /character/[id] for a character whose vn_va_credit rows include at least one VN NOT in the user's collection. The "Same name in your collection" section will list VNs the user doesn't own.#2: Visit /vn/[id] where relations includes at least one in-collection VN. Inspect the Relations grid — the owned card looks identical to the unowned cards.#3: Open /series/[id] or /lists/[id] for a list/series containing VNs that ARE members of at least one user list. The Bookmark icon shows but no count number.#4: Open /staff/[id] for a staff with VNDB credits the user doesn't own AND VNDB credits the user does own (so the "extra credits" section has mixed state). The owned ones are subtly tinted; only the border changes.#5: Search VNDB for a popular pen name shared across multiple sNNN records; the staff page won't surface the alternates.#6: Open /character/[id] for a multi-appearance character where some VNs are owned; cards in "Appears in" are visually uniform.#7: Engineer a case where vn_va_credit has the same c_id with c_name='Saori' and c_name='さおり'; only one branch matches.#8: Profile /api/wishlist with a 200-item wishlist; observe the per-row SELECTs in SQL logs.agentId: a99deada407a48e3d (use SendMessage with to: 'a99deada407a48e3d' to continue this agent)total_tokens: 248358tool_uses: 163duration_ms: 1260643
