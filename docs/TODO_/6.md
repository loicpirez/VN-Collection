Non, la liste Codex est **très large**, mais elle ne couvre pas encore absolument tout ce que tu as demandé dans toute la conversation. Elle couvre déjà énormément de choses non-critiques, notamment sécurité, perf, a11y, docs, loading, QA, cross-reference, common staff/VA, etc.  Mais elle rate encore plusieurs points précis venus de tes anciennes notes et des audits.

Voici ce que tu peux envoyer au nouvel agent pour compléter la checklist Codex sans repartir de zéro.

````md id="c1z0qx"
Codex created `docs/round5-master-regression-checklist.md`. It is a strong base, but it is NOT complete yet.

Do not treat only “Critical” items. Every severity level and every user-requested feature must be represented.

Before implementing, append the missing rows below to `docs/round5-master-regression-checklist.md`.

Rules:
- Do not delete Codex’s checklist.
- Add these as new R5 rows after the current last row.
- Renumber cleanly if needed, or use suffixes like `R5-194A` if renumbering is risky.
- Each row must have the same columns:
  `ID | Area | Issue | Files/routes | Fix commit | Verification | Status`
- Every new item starts as `TODO`.
- Do not mark fixed until tested or proven.

## Missing checklist items to add

### Owned editions / release picker

Add rows for these missing requirements:

1. `Owned Editions` — The “My editions” / owned release picker must show enough release info to choose correctly:
   - release title
   - alternate title if available
   - release date
   - languages
   - MTL flag if available
   - platforms
   - developer/publisher when available
   - edition/package info
   - GTIN/catalog if available
   - release cover fallback
   - already-owned indicator
   - official/patch/freeware/uncensored/ero flags if available
   - search/filter instead of arbitrary first 30

Files/routes:
```text
src/components/OwnedEditionsSection.tsx
src/components/ReleasesSection.tsx
src/app/api/vn/[id]/releases/route.ts
src/lib/vndb.ts
/vn/v26180
````

Verification:

* unit/component tests for release option formatting
* Playwright check on `/vn/v26180`
* manual QA confirms the user can choose the correct edition

2. `Owned Editions` — Clicking “I own it” on a release must immediately update “My editions”:

   * optimistic append to My editions
   * rollback on failure
   * no stale section until full page refresh
   * no duplicate owned rows

Files/routes:

```text
OwnedEditionsSection
ReleasesSection
/api/collection/[id]/owned-releases
/vn/v26180
```

Verification:

* Playwright click “I own it”, assert My editions row appears
* test rollback path if API fails

3. `Owned Editions` — Remove or redesign redundant “I own it” if “My editions” already communicates ownership.
   Verification:

* visual QA before/after
* no duplicate ownership language

### VNDB status / date locale bug

Add rows:

4. `VNDB Status` — Date fields must follow app language, not OS/browser locale.
   Current issue:

* if computer/browser locale is Japanese, native `input type=date` can show Japanese UI even when app language is English.

Files:

```text
src/components/VndbStatusPanel.tsx
src/components/DateInput.tsx
src/lib/i18n/index.ts
src/lib/i18n/dictionaries.ts
```

Requirements:

* Display dates according to app language.
* Store/send ISO `YYYY-MM-DD`.
* Started/finished dates in VNDB status panel use centralized locale-aware display.
* Avoid OS-locale leakage where possible.

Verification:

* unit test for formatting by app language
* Playwright set app language EN/FR/JA, assert displayed date copy follows app language
* confirm payload sent to VNDB remains ISO

### Top Ranked VN discovery page

Add rows:

5. `Top Ranked` — Add/repair Top Ranked VN discovery page.
   If `/top-ranked` already exists, audit it against requirements. If not, implement.

Requirements:

* page similar to `/upcoming`
* tabs:

  * VNDB ranking
  * EGS ranking/data
* VNDB ranking sorted by rating with vote-count thresholds
* EGS ranking uses available EGS score/median/average/Bayes fields
* density controls
* skeletons/loading/error states
* SafeImage
* URL tab/page state
* EN/FR/JA i18n
* source freshness/refresh only if remote-cache backed
* cards visually consistent with upcoming/library cards
* local/in-collection/wishlist badges

Files/routes:

```text
src/app/top-ranked/page.tsx
src/app/upcoming/page.tsx
src/lib/vndb.ts
src/lib/erogamescape.ts
src/components/VnCard.tsx
src/components/MoreNavMenu.tsx
/top-ranked
/top-ranked?tab=vndb
/top-ranked?tab=egs
```

Verification:

* route QA for all tabs
* Playwright tab switch + pagination
* tests for score sorting/thresholds

### Aspect ratio / resolution feature

Codex’s list mentions shelf/display/aspect in places, but it does not fully cover the user feature request.

Add rows:

6. `Aspect Ratio / Resolution` — Allow filtering/grouping by game aspect ratio and resolution.
   Requirements:

* use release resolution data where available
* expose filters/grouping on library/shelf/search where useful
* manual override on item page because games can have multiple resolutions
* preserve source:

  * VNDB release resolution
  * manual override
  * unknown
* URL-state filters
* EN/FR/JA labels

Files/routes:

```text
src/app/vn/[id]/page.tsx
src/app/api/collection/route.ts
src/lib/db.ts
src/components/LibraryClient.tsx
src/app/search/page.tsx
/shelf
/search
/?aspect=...
```

Verification:

* DB migration tests
* filter tests
* Playwright filter/grouping check
* manual override persists and affects filters

7. `Aspect Ratio / Resolution` — Item/VN page manual aspect override UI.
   Requirements:

* set/clear override
* show computed source vs override
* optimistic update
* activity recorded
* no destructive DB write in tests

### Shelf fullscreen / display risers

The current checklist has shelf controls, but the older requirement for fullscreen and visual display risers needs explicit rows.

Add rows:

8. `Shelf Fullscreen` — Fullscreen shelf visual mode.
   Requirements:

* immersive fullscreen shelf view
* keyboard exit / ESC
* focus handling
* mobile/desktop
* no global keyboard hijack outside fullscreen/focused shelf
* SafeImage for covers
* route or modal state in URL if appropriate

Files/routes:

```text
src/app/shelf/page.tsx
src/components/ShelfSpatialFullscreen.tsx
/shelf?view=layout
```

Verification:

* Playwright enter fullscreen, ESC exits, focus restored
* keyboard shortcuts scoped only to fullscreen/focused shelf

9. `Shelf Display Risers` — Front display / display riser concept.
   User meaning:

* games can be displayed face-out in front of/between shelf rows, like 1-2 highlighted physical boxes on a shelf.

Requirements:

* clear name in UI:

  * “Front display”
  * “Display row”
  * “Riser”
* optional per shelf/row
* toggle/hover friendly
* persists to DB/config
* supports one or more games between rows
* visually separate from normal shelf slots
* works in fullscreen and normal shelf layout
* does not break existing shelf slot semantics

Verification:

* DB tests for display/riser slot persistence
* Playwright place item into front display, reload, persists
* visual QA shows it between rows

### Item page crash after opening many items

Add row:

10. `Stability` — Opening many item pages must not make item pages stop loading.
    Requirements:

* audit DB handles
* cache growth
* EventSource/polling cleanup
* client effects cleanup
* server component fetch fan-out
* memory leak or stuck promise
* no accumulating global listeners
* no runaway VNDB fetches
* no hanging page after browsing many `/vn/[id]`

Files/routes:

```text
src/app/vn/[id]/page.tsx
src/lib/db.ts
src/lib/vndb.ts
download-status / EventSource consumers
```

Verification:

* Playwright opens 30-50 VN pages sequentially
* assert every page loads
* grep server log for unhandled errors
* if measurable, memory/listener count does not grow unbounded

### Wishlist/add-to-collection corruption

Codex’s checklist includes activity/security but not the older serious wishlist corruption bug explicitly enough. Add explicit rows.

11. `Wishlist / Collection Safety` — Wishlist removal must never remove local collection row.
    Known bug from earlier audit:

* `CoverQuickActions` treated local planning/collection status as wishlist state and called `DELETE /api/collection/:id`.

Requirements:

* local collection membership and VNDB wishlist label are separate concepts
* add/remove wishlist uses VNDB wishlist/status API
* remove wishlist never deletes local collection
* adding to collection never unsets wishlist unless explicitly intended
* if already owned, wishlist toggle does not remove metadata
* local owned/planning/favorite/queue/wishlist states visually distinct

Files:

```text
src/components/CoverQuickActions.tsx
src/app/api/wishlist/[id]/route.ts
src/app/api/collection/[id]/route.ts
src/app/api/vn/[id]/vndb-status/route.ts
src/lib/vndb.ts
```

Verification:

* regression tests:

  * local collection row survives wishlist removal
  * wishlist removal does not call collection DELETE
  * collection add does not clear VNDB wishlist unexpectedly
* Playwright: add/remove wishlist on owned VN, collection remains

### Selective download from Library home

The older draft explicitly asked for a button on `/`, not only `/data`. Add explicit rows if absent.

12. `Selective Download` — Main Library page has selective download button/modal.
    Requirements:

* button near Download missing / Download all
* opens modal/drawer picker
* can select visible/library VNs
* reuses existing selective full-download logic from `/data`
* queues `/api/collection/full-download`
* status/progress/error/success
* no global download label if context-specific
* EN/FR/JA strings

Files/routes:

```text
src/components/LibraryClient.tsx
src/components/BulkDownloadButton.tsx
src/components/SelectiveFullDownload.tsx
src/app/api/collection/full-download/route.ts
/
```

Verification:

* Playwright open modal, select 1-2 VNs, start download against `.qa`
* no real storage writes

### Default view on `/`

Add row:

13. `Library Defaults` — Setting for default Library view.
    Requirements:

* user can set default view/sort/group/density if consistent
* applies only when URL params are absent
* explicit URL params override settings
* persisted in app settings
* default sort options include all supported keys
* EN/FR/JA

Files:

```text
src/components/LibraryClient.tsx
src/components/SettingsButton.tsx
src/app/api/settings/route.ts
src/lib/settings/client.tsx
src/app/api/collection/route.ts
```

Verification:

* set default, visit `/`, applied
* visit `/?sort=title`, URL override wins
* test parsing/validation

### Release cover fallback

Add explicit row if not already present.

14. `Release Covers` — Missing release cover uses parent VN/item cover fallback.
    Requirements:

* owned editions
* release sections
* release detail page
* shelf release view if relevant
* label fallback subtly where useful
* SafeImage
* no broken empty image slot when VN cover exists

Files/routes:

```text
src/components/OwnedEditionsSection.tsx
src/components/ReleasesSection.tsx
src/app/release/[id]/page.tsx
src/app/vn/[id]/page.tsx
/shelf?view=release
```

Verification:

* fixture release without cover but VN with cover
* component test + route QA

### VNDB status writeback / locale / UX

Add rows:

15. `VNDB Status` — VNDB status panel separates local collection status, VNDB labels, wishlist, started/finished dates.
    Requirements:

* no status/wishlist confusion
* date locale follows app language
* writeback activity safe
* no token logged
* refresh context-specific

### Refresh scope, more precise

Codex has generic `R5-106`, but make page cases explicit.

Add rows:

* `/upcoming?tab=anticipated` EGS Anticipated refresh must not say “VNDB Pull - global refresh”.
* `/tags?mode=vndb` refreshes tag web tree cache, not all cache.
* `/tag/gNNN` refreshes this tag web detail and VNDB result cache.
* `/egs` refreshes EGS relevant data.
* `/characters` / `/staff` refresh only relevant VNDB/cache context if button exists.
* `/schema` refresh copy and button must match actual behavior.

### Brand overlap / cross-project overlap

Codex list has Common staff/VA, but if `/brand-overlap` exists or docs mention it, add explicit rows:

16. `Brand / Producer Overlap` — Brand overlap route exists or docs corrected.
    Requirements:

* if route exists, it loads and is documented
* if docs mention it but route absent, remove/fix docs
* overlap cards show in-collection markers

### VN page layout settings specific bug

Add row:

17. `VN Page Layout Settings` — “reorder or restore VN-page sections you have hidden…” settings text and behavior fixed.
    Current issue:

* wording too long/confusing
* some sections collapsed on VN page but other pages inconsistent
* cannot reorder from settings consistently
* hidden/restore logic inconsistent

Requirements:

* concise copy
* settings panel can restore hidden sections
* page-level editor and settings-level controls do not contradict
* applies only to supported scopes
* unsupported scopes hidden or disabled with explanation

Files:

```text
SettingsButton
VN detail layout components
detail layout settings modules
```

### Button design beyond VN toolbar

Codex covers VN toolbar, but user also said “buttons on new” and “Source de bannière” design. Add more explicit row:

18. `Button System` — New buttons across recently added features match old item/button design.
    Scope:

* VN action toolbar
* cover source picker
* banner source picker
* media actions
* tag actions
* recommendations controls
* shelf controls
* settings page layout controls

Requirements:

* consistent height
* consistent icon size
* consistent padding
* no mixed primitives
* no random admin-panel button style
* visual QA on desktop/mobile

### Recommendation cards match library cards

Codex has recommendations quality, but add explicit visual row:

19. `Recommendations Visual` — Recommendation cards reuse or visually match `VnCard` / library card baseline.
    Requirements:

* same cover ratio
* same title area
* same metadata alignment
* same density behavior
* same badges
* reason area added without breaking card height
* hidden-gems/classics/default card sizes stable

### Upcoming card size consistency

Add explicit row:

20. `Upcoming Visual` — Upcoming cards have consistent item sizes across tabs.
    Requirements:

* `/upcoming`
* `/upcoming?tab=all`
* `/upcoming?tab=anticipated`
* density slider not causing uneven height
* action row consistent
* cards align in grid

### “Best VN with this tag” copy

Codex includes neutral copy for tag pages, but add exact row:

21. `Tag Detail Copy` — Do not label every VNDB tag result as “Best VNs with this tag”.
    Requirements:

* neutral default copy:

  * “VNDB entries with this tag”
  * “VNs using this tag”
* only use “best” if explicit sort is rating/Bayes
* i18n EN/FR/JA

### Documentation / compliance exact requirement

Add row:

22. `README Compliance` — Data/media/compliance section links and language.
    Requirements:

* README data/media/compliance section has correct links to:

  * VNDB Data License
  * VNDB API / terms where relevant
  * ErogameScape terms/help/source page if available
* no invented links
* no overclaiming legal status
* clear that cached media/metadata rights remain with upstream/right holders
* screenshots/forks/hosted demos should not redistribute cached media/db dumps unless rights allow

Verification:

* source proof
* link checker or manual verified links

### VNDB/EGS docs consumption

Add row:

23. `API Docs` — KANA/EGS local docs are read and used before changing VNDB/EGS calls.
    Requirements:

* cite exact local docs section in code comments/tests where subtle:

  * tag filter tuple order
  * VNDB tag fields missing hierarchy
  * EGS query constraints
* no guessed filters

### Selective final priority management

Add process row:

24. `Process` — Do not mark broad checklist rows complete with only HTTP 200.
    Requirement:

* each `FIXED_VERIFIED` row includes a specific evidence artifact
* broad rows split if needed
* final report rejects “green summary” without proof

````

Also tell the agent:

```text
The checklist already covers many non-critical items, but append the missing rows above before implementation. Do not collapse them into existing broad rows. The user wants traceability for every feature/regression, not a vague “covered by Settings IA” or “covered by Shelf”.
````

My read: Codex’s checklist is a strong skeleton, but it still compresses or misses several old concrete asks. The biggest missing ones are **My editions picker details**, **VNDB date locale**, **Top Ranked VN**, **aspect ratio/resolution filtering**, **shelf fullscreen/display risers**, **wishlist/delete collection corruption**, **main-page selective download**, **default library view**, and **README compliance links**. Those came from your earlier feature/audit notes and are not fully represented as explicit rows in the current checklist. 

