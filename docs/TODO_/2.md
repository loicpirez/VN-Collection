So e5afe78 introduced SpoilerReveal as a replacement for the prior  wrapper. The "no JS needed " was the OLD behaviour, replaced by JS-driven hover-blur. The docstring in VndbMarkup is stale from that prior implementation.

Now I have everything. Let me write the final report.

Spoiler System Audit — vndb-collection-newAudit performed on the spoiler subsystem across src/components/Spoiler*.tsx, src/lib/spoiler-reveal.ts, src/lib/settings/client.tsx, src/components/VndbMarkup.tsx, src/components/VnTagsGroupedView.tsx, src/components/VnTagChips.tsx, src/components/CharactersSection.tsx, src/components/CharacterMetaClient.tsx, src/app/character/[id]/page.tsx, all VndbMarkup consumers, and the QA scripts scripts/browser-interactions.mjs + scripts/browser-qa.sh.

Findings ordered by user impact (critical first).

Finding 1 — Critical: There is no path to fully reveal a [spoiler]…[/spoiler] block from user interaction/Users/loicpirez/Perso/vndb-collection-new/src/components/SpoilerReveal.tsx:90-114/Users/loicpirez/Perso/vndb-collection-new/src/lib/spoiler-reveal.ts:40-52

The visibility function returns only three verdicts:

'revealed' — only when nodeLevel <= max(globalSetting, perSectionOverride ?? 0)'transient' — when hovered, focused, OR tapped'hidden' — defaultUser interaction (hover/focus/tap) only ever reaches 'transient', never 'revealed'. The transient state applies blur-sm (4px Tailwind blur) over the children — text is partially visible but unreadable. The only way to reach the clear 'revealed' state is to raise the global preference via the navbar SpoilerToggle.

User-visible symptom: On the VN synopsis (FieldCompare → VndbMarkup), character description, quotes, trait descriptions etc., a [spoiler]…[/spoiler] block shows a lock-icon placeholder. Hovering reveals heavily blurred text — almost unreadable. Clicking does nothing. Moving away re-hides. The user concludes "spoilers are broken — I can't read them."

Triggering state combination: any globalSetting < nodeLevel (VndbMarkup hardcodes nodeLevel=2, so globalSetting=0 or 1). Affects 100% of users with default settings on any page with [spoiler] markup.

Suggested fix: Either add a revealed-state flag separate from the transient gate (similar to SpoilerChip.tsx:50's const [revealed, setRevealed] = useState(false)) toggled by onClick/onPointerUp (without the touch/pen filter), or remove the blur-sm so transient = fully visible. The VNDB convention (and the stale docstring at VndbMarkup.tsx:13) is a clickable disclosure that fully shows content on activation.

Finding 2 — Critical: No desktop-mouse click handler on /Users/loicpirez/Perso/vndb-collection-new/src/components/SpoilerReveal.tsx:104-114, 144-160

The wrapper  has:

onPointerEnter / onPointerLeave (hover state)onPointerUp — gated on pointerType === 'touch' || 'pen' so it ignores mouseonFocus / onBlur / onKeyDown (keyboard)No onClickA desktop user clicking the placeholder triggers no state change. The element has role="button" and aria-pressed={false} while hidden, so assistive-tech users hear "toggle button, not pressed, Reveal this spoiler" — a contract the component cannot honour.

User-visible symptom: Desktop user clicks the placeholder, nothing happens. Hover reveals (transiently); they click expecting persistence; click is silently ignored.

Triggering state combination: any desktop session with globalSetting < nodeLevel.

Suggested fix: Add onClick={() => setTapped(v => !v)} (or a dedicated setRevealed/setPinned state). Drop the pointerType filter from onPointerUp, or add onClick alongside it.

Finding 3 — Critical (root cause of "spoiler not being visible" report):  filters spoiler tags out before they reach , with no live re-sync to the global toggle/Users/loicpirez/Perso/vndb-collection-new/src/components/VnTagsGroupedView.tsx:49-53, 56/Users/loicpirez/Perso/vndb-collection-new/src/lib/vn-tags-grouped.ts:39-58

const [spoilerMode, setSpoilerMode] = useState(() => {if (settings.spoilerLevel === 2) return 'all';if (settings.spoilerLevel === 1) return 'minor';return 'none';});// ...const grouped = filterAndGroupTags(tags, { spoilerMode, view });Two compounding problems:

useState(() => …) lazy initialiser fires once at mount. If the user toggles spoilerLevel later via the navbar SpoilerToggle, spoilerMode does NOT re-sync. (No useEffect to keep it aligned.)filterAndGroupTags with spoilerMode='none' (vn-tags-grouped.ts:44) completely drops tags with spoiler > 0 from the output. They never reach , so there is no blurred placeholder to click — they are simply invisible.User-visible symptom: User opens a VN detail page with default spoilerLevel=0. The Content/Sexual/Technical tag sections are missing some tags they expect (anything spoiler-flagged). They open the SpoilerToggle navbar popover and set "All". Other surfaces (VnTagChips, CharacterMetaClient trait chips) update live; the VnTagsGroupedView tag sections do NOT update — the tags remain absent. There is an inline "Spoil me"  row in the section (lines 82-90), but it only appears when the section detected hidden tags at the initial spoilerMode, and only flips the local spoilerMode, not the global one.

Additional sub-problem: the chip is then rendered with currentSpoilerLevel={settings.spoilerLevel} (VnTagsGroupedView.tsx:129), so once a tag passes the section-level filter, the chip itself gates again. The two gates are decoupled — when the user flips the inline spoilerMode='all' toggle, tags appear, but if globalSetting=0, they all immediately render as black-block placeholders. Either gate works in isolation, but together they make the user think they need to fight two different controls to actually see the tag.

Triggering state combination: globalSetting=0 (default) at mount, user later raises via navbar toggle → broken. Or globalSetting=0 at mount, no inline toggle interaction → broken.

Suggested fix: Either drop the local spoilerMode and read settings.spoilerLevel directly (the chip already gates correctly), or add useEffect(() => setSpoilerMode(map(settings.spoilerLevel)), [settings.spoilerLevel]) to keep them aligned. The single-source-of-truth approach is cleaner.

Finding 4 — High: InlineSpoilerReveal.autoReveal is captured at mount, never re-syncs/Users/loicpirez/Perso/vndb-collection-new/src/components/CharacterMetaClient.tsx:40-53, 85-95

const [revealed, setRevealed] = useState(autoReveal);autoReveal={level > 0} is passed from the parent on every render, but useState(autoReveal) only consumes it at first mount. If the user opens a character page with spoilerLevel=0 (autoReveal=false → revealed=false → black-block placeholder shown), then changes spoilerLevel to 1 or 2 via the navbar toggle, the parent re-renders with autoReveal=true but the child's revealed stays false. The user has to click each masked sex/gender row manually.

User-visible symptom: User on /character/cXXX page sees masked "Real sex" / "Real gender" rows. They open SpoilerToggle and set to "All". Trait chips reveal correctly (they read settings.spoilerLevel live via SpoilerChip). The sex/gender masks stay closed.

Triggering state combination: character has char.sex[1] !== char.sex[0] or char.gender[1] !== char.gender[0], user changes spoilerLevel from 0 → ≥1 after mount.

Suggested fix: drop the local useState, read revealed = autoReveal || tapped where tapped is a local override. Or add useEffect(() => { if (autoReveal) setRevealed(true); }, [autoReveal]) to escalate revealed state (but not lower it — the existing single-click "Hide" gesture should still work).

Finding 5 — High: VNDB BBCode [spoiler]…[/spoiler] placeholder label is the English literal 'spoiler' on nearly every page/Users/loicpirez/Perso/vndb-collection-new/src/components/VndbMarkup.tsx:186, 192-196

 accepts an optional spoilerLabel prop defaulting to 'spoiler'. Out of nine call sites that render BBCode that can contain [spoiler]:

Pass spoilerLabel={t.spoiler.markupSummary} (correct):

src/app/release/[id]/page.tsx:275src/app/trait/[id]/page.tsx:73Do NOT pass it (placeholder appears as 'spoiler' even in French/Japanese):

src/app/character/[id]/page.tsx:240 — character descriptionsrc/app/staff/[id]/page.tsx:183 — staff descriptionsrc/app/producer/[id]/page.tsx:158 — producer descriptionsrc/app/tag/[id]/page.tsx:125 — tag descriptionsrc/components/FieldCompare.tsx:253 — VN synopsis (Body component) — affects every VN pagesrc/components/CustomSynopsis.tsx:185 — user-authored synopsissrc/components/QuotesSection.tsx:94 — quote textUser-visible symptom: French user with spoilerLevel=0 viewing a VN synopsis with [spoiler]Le protagoniste meurt[/spoiler] sees a lock icon labelled spoiler (English). Japanese same. Locale-incoherent UI.

Triggering state combination: any locale ≠ default English on any of those 7 surfaces.

Suggested fix: change the default in VndbMarkup.tsx:186 to be derived from the i18n context (move the component to 'use client' and call useT(), or accept a required spoilerLabel prop and audit call sites). The latter is cleaner because VndbMarkup is currently usable from server components.

Finding 6 — High: SpoilerReveal tapped state cannot reach 'revealed' — only 'transient'/Users/loicpirez/Perso/vndb-collection-new/src/components/SpoilerReveal.tsx:104-114/Users/loicpirez/Perso/vndb-collection-new/src/lib/spoiler-reveal.ts:48-52

When tapped flips to true (touch/pen pointer-up or keyboard Enter/Space):

if (nodeLevel <= effective) return 'revealed';if (isTapped) return 'transient';A tap returns 'transient' (blurred), not 'revealed' (clear). So even mobile users who successfully tap-toggle a spoiler still get the blurred state — they never see the clear text. The Playwright test at scripts/browser-interactions.mjs:186 asserts data-spoiler-state === 'revealed' after a click, which is unreachable from any user interaction; the test only passes by skipping when spoiler.count() === 0 (no data-spoiler-state element on the page), masking the broken contract.

User-visible symptom: mobile user taps a spoiler placeholder — content becomes blurred but unreadable. Tap again to re-mask. Never gets a clear view.

Triggering state combination: any touch/pen user with globalSetting < nodeLevel. Affects 100% of mobile/tablet users on default settings.

Suggested fix: change line 49 of spoiler-reveal.ts so isTapped returns 'revealed' (the user has explicitly opted in to see this one). Hover/focus stay 'transient' for the "preview on hover" affordance, but commit → clear.

Finding 7 — Medium: VndbMarkup docstring claims  element that no longer exists/Users/loicpirez/Perso/vndb-collection-new/src/components/VndbMarkup.tsx:13

Docstring: [spoiler]…[/spoiler] → native  (no JS needed)

Actual rendering: … (line 175) — a JS-driven hover/blur  with no , no , no chevron. This was changed in commit e5afe78 ("feat(spoiler): consolidate reveal/hide rules behind SpoilerReveal") but the docstring was not updated.

User-visible symptom: none directly, but the docstring misleads anyone (including the user who reported this regression) into expecting a chevron/disclosure widget. The actual Lock icon + "spoiler" text is more subtle and easier to overlook.

Triggering state combination: N/A (docstring drift).

Suggested fix: update the docstring at line 13 to describe the actual  hover/focus/tap surface, or — better — restore the  element semantically (native disclosure is more discoverable than a role="button" ).

Finding 8 — Medium: BBCode [spoiler] is always treated as level=2, with no level distinction/Users/loicpirez/Perso/vndb-collection-new/src/components/VndbMarkup.tsx:174-178

return ({renderTokens(tok.children, spoilerLabel, key)});[spoiler] is hardcoded to level={2}. VNDB's BBCode does not encode a level — it's a binary flag. The choice of 2 means a user with spoilerLevel=1 (minor) still sees all in-text BBCode spoilers as masked. Whether this is a bug depends on policy; VNDB itself treats [spoiler] as major. But combined with Finding 6 (tapped only → transient blur), a spoilerLevel=1 user cannot read ANY [spoiler] block without changing the global setting to 2.

User-visible symptom: user sets spoilerLevel=1 expecting "minor spoilers visible". They still see lock icons over every [spoiler] block in markup, and cannot reveal them. Effectively the level=1 setting only affects trait/tag chips, not BBCode.

Triggering state combination: spoilerLevel=1, any markup with [spoiler]…[/spoiler].

Suggested fix: either document this (in the SpoilerToggle popover hint, e.g. "Level 1 reveals minor trait/tag spoilers but not BBCode [spoiler] blocks — VNDB convention"), or treat [spoiler] as level=1 so it reveals at minor. The current behaviour is consistent with VNDB.org but not communicated to users.

Finding 9 — Medium: Trait lie flag silently dropped on  (the VN detail "Characters" disclosure)/Users/loicpirez/Perso/vndb-collection-new/src/lib/vndb-types.ts:22/Users/loicpirez/Perso/vndb-collection-new/src/components/CharactersSection.tsx:10, 145-158

Two VndbCharacter types exist:

src/lib/vndb.ts:494-514 (used by character page, server-side fetch) — has traits: VndbCharacterTrait[] where VndbCharacterTrait includes lie?: booleansrc/lib/vndb-types.ts:3-24 (used by CharactersSection) — has traits: { id; name; group_name; spoiler; sexual }[] — no lie fieldCharactersSection imports the second shape. The API at src/app/api/vn/[id]/characters/route.ts returns the rich VNDB shape (with lie) over JSON, but the client TS sees the truncated shape, so lie is never destructured nor passed to  (CharactersSection.tsx:148-158 — no lie prop).

By contrast CharacterMetaClient.tsx:69 correctly passes lie={tr.lie} because it uses the full type.

User-visible symptom: On a VN detail page → Characters disclosure → trait chips, "lie" tags do NOT show the AlertTriangle warning icon (SpoilerChip.tsx:102) nor the "Disputed / lie tag" tooltip (SpoilerChip.tsx:98). On the character's own detail page, the same trait DOES show those affordances. Inconsistent UX.

Triggering state combination: VN page, "Characters" section open, any character with traits flagged lie: true in VNDB.

Suggested fix: unify the two VndbCharacter types (delete the truncated one in vndb-types.ts, import the full from vndb.ts), or add lie?: boolean to the trait shape in vndb-types.ts and pass it through in CharactersSection.tsx:151-158.

Finding 10 — Medium: SpoilerToggle's lit indicator lights by default/Users/loicpirez/Perso/vndb-collection-new/src/components/SpoilerToggle.tsx:56/Users/loicpirez/Perso/vndb-collection-new/src/lib/settings/client.tsx:91-103

const lit = settings.spoilerLevel !== 0 || !settings.blurR18 || !settings.hideSexual;Defaults: { spoilerLevel: 0, blurR18: true, hideSexual: false }. Evaluates to false || false || true = true. The Eye icon (which the comment describes as meaning "non-default safety gates active") fires for every fresh user because hideSexual defaults to false.

Combined with the button label showing "None" (labelByLevel[0]), the navbar reads [Eye icon] None. The Eye icon visually communicates "spoilers shown" while the text says "None". The contradiction confuses any user trying to understand whether the toggle is in default state.

User-visible symptom: navbar shows Eye + "None" on first session. User clicks expecting to TURN OFF whatever the Eye is signalling. Opens the popover, sees Hide sexual content is OFF — flips it ON — Eye becomes EyeOff. Now they wonder why "Hide sexual content" was previously off if the visual was claiming "loosened".

Triggering state combination: any fresh session (defaults).

Suggested fix: redefine lit to fire only when the user has actually loosened a gate — e.g. lit = spoilerLevel > 0 || !blurR18. hideSexual defaulting to false means "no hard filter", not "loosened" — it's the default behaviour. Or invert the icon: EyeOff for default, Eye for "spoilers explicitly enabled".

Finding 11 — Medium: Three visually-different placeholder treatments for "this is a hidden spoiler"/Users/loicpirez/Perso/vndb-collection-new/src/components/SpoilerReveal.tsx:165-175 (Lock + label text)/Users/loicpirez/Perso/vndb-collection-new/src/components/SpoilerChip.tsx:53-71 (Lock + ████ four block chars)/Users/loicpirez/Perso/vndb-collection-new/src/components/CharacterMetaClient.tsx:103-111 (EyeOff + █████ five block chars)

The three masked states render different placeholders. A user scanning a single page sees three different visual signals for the same "click here to reveal" affordance:

BBCode [spoiler] block in synopsis: dashed-border tile with Lock + word "spoiler" / localised labelTrait chip / tag chip: dashed-border pill with Lock + ████Sex/gender row: dashed-border pill with EyeOff + █████The █ (U+2588 FULL BLOCK) characters are arguably the same "opaque rectangle" visual that the SpoilerReveal redesign in commit 73fbe88 explicitly avoided (per the test at tests/spoiler-reveal-cascade.test.ts:52-59 checking for no bg-black). The test guards against the CSS class but not the Unicode block character.

User-visible symptom: visual fragmentation. User reports "the spoiler doesn't look like a spoiler" because the patterns don't match each other.

Triggering state combination: any page with masked spoilers across multiple types.

Suggested fix: standardise on the SpoilerReveal placeholder (Lock + localised label, no block chars). Update SpoilerChip and InlineSpoilerReveal to match. The block chars don't add information — they imitate redaction but the lock icon already says that.

Finding 12 — Medium: QA scripts probe for the wrong attribute, masking trait spoiler regressions/Users/loicpirez/Perso/vndb-collection-new/scripts/browser-qa.sh:383-389

The "character page renders + description" assertion (added in commit 6274cdf) gates on:

HAS_SPOILER_DATA=$(count_pattern "$CHAR_HTML" 'data-spoiler-level="[12]"')if [ "$HAS_SPOILER_DATA" -gt 0 ]; thenassert_at_least "spoiler-reveal triggers present" ...elseok "character spoiler reveals skipped" "no spoiler-tagged traits in fixture"fidata-spoiler-level is set only by  (src/components/SpoilerReveal.tsx:153).  — which renders trait spoilers — does NOT emit that attribute. So the gate is only triggered by BBCode [spoiler] blocks in char.description. Trait spoilers (which the assertion claims to cover, per the surrounding comment) will always hit the "skipped" branch.

Combined with the commit message "make character spoiler-reveal assertion conditional — only assert aria-pressed when the fixture character has spoiler-tagged traits", the fix achieves the opposite of its intent: it can never assert against trait spoilers because they don't set data-spoiler-level.

User-visible symptom: none directly. Implication: QA cannot regress on trait spoiler rendering — any of the bugs in Findings 3, 4, 9 could ship and the character page QA would pass.

Triggering state combination: any character page without [spoiler] BBCode in its description (the common case).

Suggested fix: either probe for the SpoilerChip class signature (border-dashed.*status-on_hold etc.) or have SpoilerChip emit data-spoiler-state="hidden" so both share a probe.

Finding 13 — Low: tabIndex={0} on  is unconditional/Users/loicpirez/Perso/vndb-collection-new/src/components/SpoilerReveal.tsx:148

The wrapper has tabIndex={0} regardless of effective state. A long description with several [spoiler] blocks rendered with globalSetting=2 (all revealed) produces N extra tab stops with no actionable interaction (the revealed state has no role, no onClick, and the keyboard handler at line 109-114 toggles tapped which is shadowed by 'revealed').

User-visible symptom: keyboard navigation has unnecessary tab stops on already-revealed spoilers.

Triggering state combination: globalSetting >= 2, page with multiple [spoiler] blocks.

Suggested fix: tabIndex={isHidden ? 0 : -1} (or only set tabIndex when there's something to do).

Finding 14 — Low:  wrapper resizes between hidden ↔ revealed states despite the comment claim/Users/loicpirez/Perso/vndb-collection-new/src/components/SpoilerReveal.tsx:130-142

The comment claims "the wrapper size does NOT change when we swap between hidden and revealed". In practice:

Hidden: placeholder span has inline-flex + padding + Lock + label → ~50–80px wideRevealed: placeholder is display: none, content span shows children → arbitrary width (often 100+ ch)Wrapper is inline-block and shrink-wraps to whichever child is visible. Going hidden → transient/revealed grows; going revealed → hidden shrinks. The fix in commit 73fbe88 keeps both spans in the DOM (avoiding unmount-related pointer-loss) but does NOT prevent visual size change. The comment overstates the fix.

User-visible symptom: layout shift when hovering / unhovering a spoiler in a sentence. Especially noticeable on tight wrapping.

Triggering state combination: any line of text with an inline [spoiler].

Suggested fix: either reflect the actual fix in the comment, or reserve the children's natural width on the wrapper (e.g. render children always with visibility: hidden to claim space, place placeholder absolutely on top) — but that's a bigger change.

Summary by areaSpoilerReveal.tsx: the largest cluster of breakage. No persistent reveal on desktop click (Finding 2). Tap state only reaches transient blur, never clear (Finding 6). Together these mean no user interaction can fully unblur an inline [spoiler] — only the navbar toggle can (Finding 1).VnTagsGroupedView.tsx: filter-out-then-don't-re-sync (Finding 3) — the most likely root cause of the user's "spoiler not being visible" report. Tags are completely absent rather than rendered as click-to-reveal chips, and toggling the global navbar doesn't help.CharacterMetaClient.tsx: stale useState(autoReveal) (Finding 4) breaks the global toggle for the sex/gender rows. Trait chips work because they read settings live.VndbMarkup.tsx callers: 7 out of 9 don't pass spoilerLabel (Finding 5) → English literal "spoiler" leaks into every other locale on the VN synopsis, character description, quotes, etc.CharactersSection.tsx: imports the wrong VndbCharacter type (Finding 9), silently dropping the lie flag.SpoilerToggle.tsx: lit icon lights by default (Finding 10), contradicting its own label.Three placeholder visual styles (Finding 11) for the same "click to reveal" affordance.QA test coverage: gates on wrong attribute (Finding 12), Playwright assertions that can only pass when skipped (Finding 6 corollary in browser-interactions.mjs:184-186).The user's "spoiler not being visible" report most plausibly traces to Finding 3 (tags filtered out of view in VnTagsGroupedView when global toggle is changed after mount) and/or Finding 1 (inline [spoiler] blocks in synopsis cannot be revealed by clicking, only by global toggle, and the blur on transient hover keeps them unreadable). Finding 4 also produces a "I changed the toggle but the spoiler is still masked" experience specifically for sex/gender rows.agentId: ae30a33fa6666f46a (use SendMessage with to: 'ae30a33fa6666f46a' to continue this agent)total_tokens: 159961tool_uses: 118duration_ms: 979912
