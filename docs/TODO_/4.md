Yes — the entire panel including reset button is md md:group-hover. The reset is hidden on desktop. Confirms Finding 15.

Now I have enough findings. Let me write the report.

Visual Regression Audit Reportvndb-collection-new, range 388e940..d3b343d (HEAD)Audit limited to files touched in the named commits plus their downstream consumers. Severity grades follow the requested scale.

Critical

DetailReorderLayout sections never receive labels — collapse-by-default UI is dead on character/staff/producerFiles: src/app/character/[id]/page.tsx:370-378, src/app/staff/[id]/page.tsx:355-363, src/app/producer/[id]/page.tsx:210-218, consumer src/components/DetailReorderLayout.tsx:67-244.Symptom: DetailSection.label? is never populated on any of the three pages. Because DetailReorderLayout only renders the collapsed-section header (line 183) and the collapse-toggle button in edit mode (line 237) when sec?.label exists, the entire "collapse by default" behavior added in 677895f is unreachable from the UI on the three new scopes. A user who PATCHes { collapsedByDefault: true } from the Settings modal still sees the section expanded with no header. In edit mode the chevron-toggle is silently omitted.Reproduction: Open Settings → Page layout (VN) → expand the character/staff/producer layout sub-accordion → tick "Repliée par défaut" on any section → save → navigate to /character/. The section is rendered open with no collapse header; the page does not honor the persisted state.Severity: Critical. The labels feature commit 1be65ce and the collapse-by-default commit 677895f both regressed end-to-end on the three scopes they were meant to enable.

SettingsButton DetailSectionFrame / DetailSectionResetButton are orphan codeFile: src/components/DetailSectionFrame.tsx (entire file, 400 lines).Symptom: Introduced by commit 34c18ca then abandoned when ec99913 swapped to DetailReorderLayout. grep -rn "DetailSectionFrame" src/ tests/ returns only the file itself. Two exported components plus all helpers are dead code that ships in the bundle.Severity: Critical (bundle bloat + maintenance debt; no user-visible regression but it's a footgun for whoever next edits the layout system).

Activity page pagination — "Next" button shows on the last pageFile: src/app/activity/page.tsx:165-171.Bug:const sysRowsAll = listUserActivity({ ..., limit: offset + PAGE_SIZE + 1 });const sysRows = sysRowsAll.slice(offset, offset + PAGE_SIZE);const sysHasMore = sysRowsAll.length > PAGE_SIZE;At page 1+, sysRowsAll.length is the count of all returned rows (offset + page + 1 at most). > PAGE_SIZE is true whenever the user has more than 50 events ever, even when no further pages exist. Same bug on vnHasMore (line 171).Correct check is sysRowsAll.length > offset + PAGE_SIZE.Reproduction: Seed a DB with exactly 100 system activity rows. Navigate /activity?page=1. The "Next →" button is rendered, but clicking it loads /activity?page=2 which shows 0 rows. Same for VN events.Severity: Critical. Introduced by bbbaeb1. Visible immediately to any user with > 50 activity rows.

UpcomingCard is NOT shared with /recommendations despite commit promiseCommit: 236b148 feat(recs+upcoming+egs): broaden signals, shared UpcomingCard, EGS UX.Files: src/app/recommendations/page.tsx:740-855 builds bespoke card JSX with ...badges…. src/components/UpcomingCard.tsx is imported only by src/app/upcoming/page.tsx.Symptom: The "shared" card promised by the commit title doesn't exist for /recommendations. The two surfaces drift independently — already visible: recommendations cards have no Open VNDB / Map EGS / Add to collection chips that UpcomingCard exposes via data-affordance="…" slots.Severity: Critical (commit-message claim does not match reality; affordances inconsistent across the two grids).High

TagsBrowser breaks the VNDB tree fallback after a single LOCAL↔VNDB tab toggleFile: src/components/TagsBrowser.tsx:51-143.Bug: In the LOCAL branch (lines 77-86), tree is never assigned and stays null. On line 132 setHomeTree(tree) is invoked unconditionally, clobbering the SSR-injected homeTree state to null. When the user later switches back to VNDB mode without an active search/category, the effect takes the skipTreeFetch=true path (lines 88-101) and returns without calling setHomeTree — so state stays null. The render condition at line 227 (mode === 'vndb' && !q && !category && homeTree) is false → falls to the empty state.Reproduction: open /tags?mode=vndb (tree shows) → click Local tab → click VNDB tab. Expected: tree re-displays. Actual: "Aucun résultat" message.Severity: High. Affects the central new feature from 9839f20 after just one tab toggle.

Settings "Spoiler / Content" tab label promises spoiler controls that don't existFile: src/components/SettingsButton.tsx:412-441; i18n at src/lib/i18n/dictionaries.ts:847,3298,5745.Symptom: The English label is "Spoiler / Content", French "Spoilers / Contenu", JA "スポイラー / コンテンツ" — but the tab body only renders three controls (blurR18, hideSexual, nsfwThreshold) and zero spoiler-related toggles. A grep for spoiler in SettingsButton.tsx returns zero matches.Severity: High. The IA promise from 097e46d is half-met; users searching for spoiler controls in the tab named "Spoiler / Content" are sent in a circle.

Cover rotation reset button is hidden behind hover on desktopFile: src/components/CoverRotationButtons.tsx:122-163.Bug: The wrapping  carries md md:group-hover md:group-focus-within. The reset button (the rotation-degree readout chip at lines 154-163) is inside that wrapper, so on desktop it's invisible unless the cover container is hovered.The audit-scope requirement (item 8): "The reset CTA must always be visible — not gated by hover."Test a200ba7 only pins that the button is always mounted (line 58-61 of cover-rotation-ui.test.ts), not that it's visually present.Reproduction: On /vn/ with a rotated cover, view on a desktop (≥ md breakpoint) without hovering the cover. The 90°/180°/270° chip is not visible. Hover to reveal.Severity: High. Direct contradiction of the explicit audit-scope requirement.

Library toolbar: 3 labels are hidden below lg breakpointFile: src/components/LibraryClient.tsx:787-789, 827-829, 846-848.Symptom: hidden lg strips the text labels for:"Sort: drag-reorder mode" / "Exit drag-reorder mode" (custom sort toggle)"Dense view" / "Comfortable view""Select mode" / "Exit select mode"Comment at line 783-786 acknowledges and justifies the choice ("keep the toolbar from wrapping").This violates the user-pinned rule: "Mobile/tablet: no functionality compromise — Never hide labels/controls behind hidden sm."Reproduction: Open / on iPad portrait (834px). Only icons render; users have to tap to test the action.Severity: High. Three labels affected, on the most-used surface in the app.

ShelfReadOnlyControls trigger label is hidden on mobileFile: src/components/ShelfReadOnlyControls.tsx:283.Bug: {dict.title} strips "Display options" / "Options d'affichage" below sm.Violates the same "Mobile/tablet: no functionality compromise" rule. Especially regrettable on /shelf because the controls launcher is icon-only on phones — users won't know they exist.Severity: High.

Cover-picker rotate-left / rotate-right labels hidden on mobileFile: src/components/CoverSourcePicker.tsx:350,361.Bug: {t.coverActions.rotateLeft} and the rotateRight equivalent. The cover edit popover on mobile shows two unlabeled rotate icons.Severity: High.

VnDetailActionsBar — ActionMenu wrapper-span gets doubled padding from [&>] px-3 py-1.5Files: src/components/VnDetailActionsBar.tsx:116-117, 124-125 (PRIMARY_ROW_CLASSES / DROPDOWN_ROW_CLASSES) + src/components/ActionMenu.tsx:199-217 (wrapper span).Bug analysis: The flex rows apply [&>] [&>] [&>].5 to direct children. ActionMenu's outer wrapper is a ; the inner button receives triggerClassName="btn h-9 px-3 py-1.5" (commit 73fbe88). Net result with box-sizing: border-box:Wrapper span: 36px total height, 12px vertical padding → 24px content area.Inner button: forced 36px tall.Button overflows wrapper's content area by 12px, visible because  defaults to overflow: visible.The same [&>*] selector doesn't apply to plain children like  (which renders a  directly), so plain children land at 36px while ActionMenu trigger buttons render at 36px with a 6px overflow on each side. The two clusters visually misalign.Reproduction: Open /vn/ with a VN in collection. Compare the vertical centering of the FavoriteToggleButton (cluster 1) and the Tracking dropdown trigger (cluster 2). The Tracking trigger reads as ~6px lower.Severity: High. The very regression 73fbe88 was supposed to fix.

/tag/[id] empty fallback shows "no results" when local tab has hits — TagWebDetailBlock cross-rendersFile: src/app/tag/[id]/page.tsx:183-249.Symptom: The  is rendered unconditionally (line 183-185) before the tab === 'local' and tab === 'vndb' branches. On the Local tab the user sees:Local matches grid (intended)PLUS the VNDB hierarchy panel (breadcrumb / categoryLabel / properties chips / childGroups)The two panels report contradicting facts (one is local, one is VNDB-side metadata), but visually they read as one continuous tab body. The "tab" affordance becomes misleading because the VNDB block bleeds across both.Severity: High (information architecture; the IA bucket promise from 097e46d is violated here too).Medium

i18n drift fix is incomplete in FRFile: src/lib/i18n/dictionaries.ts:9, 2347, 2348, 2349.Status: Commit f1cf477 migrated only staff.scopeCollection from "Ma collection" to "Locale". Residual personal phrasing in FR:app.tagline: 'Ma collection de Visual Novels'traits.mine: 'Ma collection'traits.mineCountSuffix: 'dans ma collection'traits.mineEmpty: "...d'un VN de ta collection."EN already says "In the collection" (line 4794), JA says "コレクションのみ" (line 7240). The FR fix is partial.Severity: Medium. Affects the FR build only.

SearchClient platform code xbs doesn't exist in VNDBFile: src/components/SearchClient.tsx:27 (COMMON_PLATFORMS).Bug: The platform-filter chip list contains 'xbs' while VNDB's enumeration uses 'xxs' for Xbox Series X/S (see src/lib/platform-label.ts:49). Calling platformLabel('xbs') falls through to the uppercase fallback at line 85, so the chip renders as XBS (not the friendly "Xbox Series X/S" promised by commit 9cfd01e).Even worse: when the user toggles this chip, the search payload sends xbs to VNDB, which won't match any release.tests/platform-label.test.ts:20 correctly verifies xxs → "Xbox Series X/S".Severity: Medium. Commit 9cfd01e claimed to fix "N3D renders as 'Nintendo 3DS'" but missed xbs/xxs skew.

/staff/[id] declares 'timeline' as a canonical section ID but only renders it when voice.length > 0File: src/app/staff/[id]/page.tsx:244-247 and src/lib/staff-detail-layout.ts:17-22.Bug: STAFF_SECTION_IDS = ['timeline', 'voice-credits', 'production-credits', 'extra-credits']. The page only pushes the 'timeline' DetailSection when voice.length > 0. For a staff with only production credits, the canonical id 'timeline' is in draft.order (from the factory's defaults) but no node exists for it. In edit mode the user sees 3 sections to reorder, but a saved layout will persist 4 sections, of which 'timeline' is invisible.The user can move sections around, but can't move 'timeline' (it's not rendered as a sortable card). Confusing.Severity: Medium.

/staff/[id] section 'extra-credits' is always pushed even when nothing to showFile: src/app/staff/[id]/page.tsx:339-346.Symptom: Pushed unconditionally. The  inside its Suspense renders a placeholder skeleton on first paint, then resolves and may return nothing if VNDB has no additional credits beyond the locally known ones. The DetailReorderLayout still allocates spacing for the section and shows the "Edit Layout" affordance below it. If  resolves to nothing, the user sees an empty card-shape gap above the Edit Layout chip.Severity: Medium (subtle empty-state regression depending on data).

Activity page pagination is shared between VN-events and System-events sectionsFile: src/app/activity/page.tsx:154-301.Symptom: Both sections derive from the single ?page=N query param. Clicking "Next →" under one section advances both. A user paginating through 200 VN events while system events has only 30 rows will silently skip past the system tail (the second section disappears once offset >= sysRowsAll.length).Severity: Medium (combined with Finding 3 — the "Next" button itself is wrong, so this is downstream).

EGS chip in UpcomingCard lacks aria-label / titleFile: src/components/UpcomingCard.tsx:216-225.Symptom: All other affordance chips (open-local, open-vndb, add-to-collection, map-egs-to-vndb) carry aria-label and title. The EGS-only-fallback link (lines 216-225) has only the chip text "EGS" — no aria-label, no title. Inconsistent a11y.Severity: Medium (a11y consistency).

Producer detail section has no descriptive label in DetailReorderLayout edit modeFile: src/app/producer/[id]/page.tsx:151-207.Symptom: None of the four producer sections (description, extlinks, works, stats) carry a label field on the DetailSection. In edit mode the operator sees four unlabeled cards and has to guess which is which by content. Reset → reorder works but the user has no idea what they're moving.The dictionaries producerLayout.sectionLabels (lines 2128-2133, 4574-4579, 7020-7025) DEFINE the labels — they're just not threaded into the DetailReorderLayout call site.Same issue affects character page (characterLayout.sectionLabels exists but not passed) and staff page (staffLayout.sectionLabels).Severity: Medium (combined with Finding 1 — the labels are defined in dictionaries but never reach the UI on these three scopes).

Tag flat-view card hover-arrow hidden below mdFile: src/components/TagsBrowser.tsx:435-437.Symptom:  — the arrow-right glyph on tag cards is fully hidden on phones/tablets, and only appears on desktop hover. It's a small affordance (clear "this is a link" indicator), but it loses 100% of touch users.Severity: Medium.

Shelf page: initialPrefs fallback logic is fragileFile: src/app/shelf/page.tsx:244.Bug: initialPrefs={overrides.shelves[activeShelfKey] ? effective : legacyGlobal} — uses the legacy global key (shelf_view_prefs_v1) as fallback when the active shelf has no override. If legacyGlobal and overrides.global diverge (which they can during the migration window between settings PATCHes), the sliders open with the wrong values.Should consistently fall back to overrides.global (which is what resolveShelfPrefs returns anyway with no override).Severity: Medium (only manifests during cross-tab settings PATCH or migration).

Settings tabs subject claims "7 buckets" but lists 8Commit: 097e46d title says "7 operator buckets" but enumerates display, content, library, home, vn-page, account, integrations, automation = 8 tabs. The code at src/components/SettingsButton.tsx:132-141 has 8 entries.Severity: Medium (documentation/commit-message mismatch).

/producer/[id] "stats" section condition contradicts canonical layoutFile: src/app/producer/[id]/page.tsx:200-207.Bug: 'stats' is conditionally pushed only when scrapedInfo && scrapedInfo.relations.length > 0, but PRODUCER_SECTION_IDS declares it canonical. The Settings modal's DetailPageLayoutPanel lists all four canonical sections regardless — if the user enables visible=true on 'stats' but the producer has no scraped relations, the section literally doesn't render. The UI lies.Severity: Medium.Nit

SearchClient language chip uses .toUpperCase() onlyFile: src/components/SearchClient.tsx:446.Symptom: Language chips render as EN, JA, FR. Localized language names exist via languageDisplayName (used elsewhere on /staff/[id] and /producer/[id]).Inconsistent with the platform-label treatment alongside.Severity: Nit.

No tests for DetailReorderLayout component itselfFile: src/components/DetailReorderLayout.tsx (no test counterpart).The factory section-layout.test.ts covers the validator, but the React component's edit-mode UI, save flow, event subscription, and collapse-header rendering are untested. Combined with findings 1 and 19, the dead-feature bugs would have been caught.Severity: Nit (process / coverage).

DetailReorderLayout local type for defaultSectionLayoutV1File: src/components/DetailReorderLayout.tsx:61-65.Bug: Local type declares the field as collapsed?: boolean but the interface at line 43-45 uses collapsedByDefault?: boolean. Reset effectively works because the field is never read off the default, but it's a footgun for the next editor.Severity: Nit.

Activity rows render with flex flex-wrap items-start then time floats right — long titles wrap onto a second row but time stays on firstFile: src/app/activity/page.tsx:230-253.Visual nit: with min-w-0 flex-1 on the title block and a non-shrinking  on the right, a long VN title that wraps to 3 lines will sit beside the  only on its first line; the time looks orphaned from the rest of the row.Severity: Nit.

UpcomingCard cover-link aria-label is just the title (no "Open" verb)File: src/components/UpcomingCard.tsx:113, 128.Cover image is a link but its aria-label = title (no "Open " prefix). Screen readers announce the title alone, which is identical to the text link two lines below, sounding like a duplicate.Severity: Nit.Files for the caller to inspect firstIf you're going to fix one thing per priority bucket:

Critical: src/app/activity/page.tsx:165-171 (pagination math)Critical: src/components/DetailReorderLayout.tsx + the three detail pages (re-thread label)High: src/components/TagsBrowser.tsx:130-132 (don't clobber homeTree from non-VNDB branches)High: src/components/VnDetailActionsBar.tsx:116-117, 124-125 (ActionMenu wrapper height bug)High: src/components/CoverRotationButtons.tsx:131 (reset visibility)agentId: add281098e9510ea6 (use SendMessage with to: 'add281098e9510ea6' to continue this agent)total_tokens: 326535tool_uses: 224duration_ms: 1504398
