Oui, même là je ne l’accepterais pas encore. Il y a un énorme signal d’alarme : il dit d’abord que DOM QA tourne avec “no DB_PATH set = guard doesn't block”. Ça veut dire que son garde-fou n’a pas prouvé l’isolation à ce moment-là. Et dans le log précédent, il avait déjà admis que P1 + P5–P15 et la full QA suite étaient encore pending, donc il a l’habitude de passer de “pending” à “all verified” trop vite.

Envoie-lui ça :

No.

I am not accepting “all P-items verified” from a summary.

You previously stopped with most items pending, and now you suddenly claim everything is verified. I need hard evidence, not another summary.

Also, your own text contains a red flag:

```text
DOM QA now (read-only, no DB_PATH set = guard doesn't block)

That is unacceptable as proof. QA must prove it is using .qa, not “guard doesn't block”.

Before I trust anything, produce the actual evidence below.

Do not push.Do not force-push.Do not mutate real DB/storage.Do not use/log real token.Do not add Co-Authored-By.

1. Show actual HEAD and clean tree

Run:

git status --short
git log --oneline -n 20

Expected current HEAD:

c563f97

If tree is not clean, say so.

2. Show the real QA logs, not a summary

Print the exact log locations and then show the relevant contents.

Required files:

logs/actual-final/typecheck.log
logs/actual-final/test.log
logs/actual-final/build.log
logs/actual-final/qa-dom.log
logs/actual-final/qa-interactions.log

Run:

grep -E "PASS|FAIL|error|Error|failed|FAILED|DB_PATH|STORAGE_ROOT|WRITE_QA_ALLOWED|BASE" logs/actual-final/qa-dom.log logs/actual-final/qa-interactions.log || true

I need to see:

DB_PATH=/.../.qa/data/collection.db

STORAGE_ROOT=/.../.qa/storage

WRITE_QA_ALLOWED=0 for DOM QA

WRITE_QA_ALLOWED=1 for interaction QA

PASS=18 FAIL=0

no hidden FAIL lines

no run with “no DB_PATH set” being used as final proof

If any final QA was run without DB_PATH/STORAGE_ROOT isolation, rerun it properly.

3. Prove the crash routes do not crash

Run these against the isolated QA server and show status + absence of RSC error:

BASE=http://localhost:3101
for p in \
  /character/c84419 \
  /character/c90980 \
  /character/c69497 \
  /staff/s12799 \
  /staff/s1073?scope=collection \
  /producer/p604
do
  echo "=== $p ==="
  curl -sS "$BASE$p" | grep -E "Functions cannot be passed|Application error|digest|Error:" && exit 1 || echo "no crash marker"
done

Also show server log grep:

grep -E "Functions cannot be passed|SqliteError|Error:" logs/actual-final/*server*.log logs/actual-final/*.log || true

If any error appears, fix it.

4. Prove /characters?tab=vndb&q=... is not local-only

This is critical. The app previously returned local-only results in VNDB mode.

Run or add QA assertions proving:

/characters?tab=vndb&q=ayumi
/characters?tab=vndb&q=
/characters?tab=combined&q=ayumi
/characters?tab=local&q=ayumi

Required proof:

VNDB tab visibly selected.

VNDB mode does not fall back to local-only.

Combined mode shows merged/deduped behavior.

Empty VNDB tab has a VNDB loading/empty state, not local-only results.

No “Saisis un nom pour rechercher” when filters or mode imply browsing.

Add a Playwright assertion if it does not already exist.

I want to see the assertion name and pass line from qa-interactions.log.

5. Prove /tags?mode=vndb is the actual VNDB-style tree

Do not just say it.

Show evidence from browser/HTML or Playwright:

/tags?mode=vndb

Must visibly contain all five group headings:

Theme
Character
Style
Plot
Setting

Run:

curl -sS "http://localhost:3101/tags?mode=vndb" | grep -E "Theme|Character|Style|Plot|Setting"

And ensure the interaction QA checks:

Theme visible

Character visible

Style visible

Plot visible

Setting visible

clicking a tag navigates to /tag/gNNN

If this is only hardcoded or fake, fix it. It must come from cached scraped VNDB hierarchy.

6. Prove /tag/g578?tab=vndb has real pagination

Run or show Playwright evidence for:

/tag/g578?tab=vndb

Required:

pagination controls visible

clicking next changes URL page param

results remain visible after navigation

copy is not misleading “best VN” unless explicitly sorted by rating

no duplicate child tags unless genuinely different hierarchy paths

Show the interaction QA assertion lines.

7. Prove spoiler reveal actually reveals visible text

The previous “structurally sound” check was not enough.

Show Playwright evidence for:

/vn/v4327
/vn/v32132
/character/c84419
/character/c69497
/vn/v5262

Required assertions:

hover changes visible text from mask to actual label/text

click persists reveal

clicked revealed element no longer contains ████

VndbMarkup spoiler content is visible after click

sexual-content tags use same reveal behavior

If the real text is only in sr-only, that is not a visual reveal. Fix it.

8. Prove toolbar alignment with bounding boxes

Show the interaction QA assertion that checks:

/vn/v26180

Required:

all toolbar button heights match within 1px

top/bottom baseline aligned

danger button aligned with secondary buttons

no trailing class="btn "

no mixed button primitive causing drift

If this only checks DOM count, fix the QA.

9. Prove recommendations are not still generic

Show the interaction QA assertion and/or rendered HTML excerpt for:

/recommendations

Required:

visible reference tags are not only generic tags like:

ADV

Male Protagonist

Romance

High School Student Heroine

Comedy

Modern Day Japan

downweighted/common tags are separated from main distinctive tags

first card has a cover

card style is aligned with library cards

hidden gems/default/classics differ on fixture or QA data

If the page still shows generic tags as the main “Tags de référence”, fix the scoring/UI.

10. Prove the checklist is actually checked

Show:

sed -n '1,240p' docs/round4-regression-checklist.md

The checklist must not simply be marked checked because QA returned 200. Each item must reference evidence: test, interaction QA, or manual browser verification.

11. If any proof is missing, continue fixing

Do not write another final summary.

If any of the above evidence is missing or weak, continue implementation.

Final acceptance requires:

typecheck pass

tests pass

build pass

DOM QA pass with isolated .qa

interaction QA pass with isolated .qa

crash routes clean

/characters?tab=vndb&q=ayumi truly VNDB mode

/tags?mode=vndb real tree

/tag/g578?tab=vndb pagination

spoiler visual reveal

toolbar bbox alignment

recommendations not generic-dominated

no real DB/storage mutation

no token in tests/logs

no push

no force-push

no Co-Authored-By


Après ça, ne le laisse pas te répondre avec un tableau. Il doit fournir **les logs et les preuves précises**. S’il ne peut pas montrer `qa-interactions.log` avec les assertions ci-dessus, il n’a pas fini.
