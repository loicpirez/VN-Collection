# TODO — full audit (2026-05-30)

Fresh audit of the current codebase, one pass per field. Each row was verified at the cited `file:line`. Columns: `ID | category | issue | file(s) | — | fix | status`.

| ID | category | issue | file(s) | — | fix | status |
| --- | --- | --- | --- | --- | --- | --- |
| A11Y-001 | a11y | role="tablist" mode switcher has no roving tabIndex (both Link tabs stay in tab order) and no Arrow-key handler, so it does not follow the WAI-ARIA tabs keyboard model used elsewhere | src/components/TagsBrowser.tsx:243,255 | — | Add onKeyDown ArrowLeft/Right(+Home/End) on the tablist that moves selection+focus, and set tabIndex={active ? 0 : -1} on each role="tab" | TODO |
| A11Y-002 | a11y | Both role="tab" elements set aria-controls="tags-results-panel" but neither has an id, so the role="tabpanel" cannot reference its owning tab via aria-labelledby | src/components/TagsBrowser.tsx:246,257,299 | — | Give each tab a stable id and set aria-labelledby={activeTabId} on the tabpanel | TODO |
| A11Y-003 | a11y | Markdown edit/preview tablist has no accessible name, no roving tabIndex (both tab buttons remain tabIndex=0), and no Arrow-key handler | src/components/MarkdownNotes.tsx:42,46,57 | — | Add aria-label to the tablist, roving tabIndex on each tab, and an ArrowLeft/Right onKeyDown that switches and moves focus | TODO |
| A11Y-004 | a11y | Source filter chips use role="tablist"/role="tab"+aria-selected but have no aria-controls, no tabpanel, no roving tabIndex, and no Arrow handler; they filter a list rather than swap panels | src/components/VnSourcePicker.tsx:196,254 | — | Re-model as a filter group: role="group" on the row + aria-pressed on each chip, dropping the tab roles (mirror MediaGallery) | TODO |
| A11Y-005 | a11y | Candidate selector uses role="tablist"/role="tab"+aria-selected with no aria-controls, no tabpanel, no roving tabIndex, and no Arrow handler | src/components/ErogePricePanel.tsx:806,814 | — | Complete the tabs pattern (roving tabindex + Arrow handler + tabpanel aria-labelledby) or switch to role="group"+aria-pressed | TODO |
| API-001 | api | DB-touching route loads better-sqlite3 (via @/lib/vndb to vndb-cache to db) but omits export const runtime = 'nodejs', violating the NODEJS_RUNTIME convention in api-route-meta.ts:43 | src/app/api/vndb/auth/route.ts:5 | — | Add export const runtime = 'nodejs'; next to the dynamic export | TODO |
| API-002 | api | DB-touching route (@/lib/steam to @/lib/db) omits export const runtime = 'nodejs' | src/app/api/collection/find/route.ts:5 | — | Add export const runtime = 'nodejs'; | TODO |
| API-003 | api | VNDB/DB-touching route omits export const runtime = 'nodejs' | src/app/api/vndb/stats/route.ts:6 | — | Add export const runtime = 'nodejs'; | TODO |
| API-004 | api | VNDB/DB-touching route omits export const runtime = 'nodejs' (its sibling tags route declares it) | src/app/api/traits/route.ts:6 | — | Add export const runtime = 'nodejs'; | TODO |
| API-005 | api | VNDB/DB-touching route omits export const runtime = 'nodejs' | src/app/api/release/[id]/route.ts:6 | — | Add export const runtime = 'nodejs'; | TODO |
| API-006 | api | VNDB/DB-touching route omits export const runtime = 'nodejs' | src/app/api/character/[id]/route.ts:6 | — | Add export const runtime = 'nodejs'; | TODO |
| API-007 | api | VNDB/DB-touching route omits export const runtime = 'nodejs' while neighbouring tags/route.ts declares it | src/app/api/tags/route.ts:8 | — | Add export const runtime = 'nodejs'; | TODO |
| API-008 | api | VNDB/DB-touching route omits export const runtime = 'nodejs' | src/app/api/staff/route.ts:6 | — | Add export const runtime = 'nodejs'; | TODO |
| API-009 | api | Filesystem-reading route (@/lib/files uses node:fs/promises) omits export const runtime = 'nodejs' | src/app/api/files/[...path]/route.ts:4 | — | Add export const runtime = 'nodejs'; | TODO |
| API-010 | api | DB-touching route (@/lib/vndb-tag-web-cache to db) omits export const runtime = 'nodejs' | src/app/api/tags/web-tree/route.ts:6 | — | Add export const runtime = 'nodejs'; | TODO |
| API-011 | api | game-log PATCH mutates an entry by global body.id without verifying the entry belongs to the path VN; /api/collection/vA/game-log can edit an entry owned by vB | src/app/api/collection/[id]/game-log/route.ts:130 | — | Pass the path id into updateGameLogEntry and reject (404) when the loaded entry's vn_id differs | TODO |
| API-012 | api | game-log DELETE removes an entry by global ?entry= id with no ownership check against the path VN, allowing cross-VN deletion | src/app/api/collection/[id]/game-log/route.ts:156 | — | Have deleteGameLogEntry take the path id and only delete when vn_id matches; 404 otherwise | TODO |
| API-013 | api | steam/sync GET returns HTTP 400 for an upstream Steam outage, conflating a client error with an upstream failure; sibling steam/library returns 502 for the same failure | src/app/api/steam/sync/route.ts:44 | — | Return 502 when the failure is not steam_not_configured; keep 400 only for not-configured/validation | TODO |
| API-014 | api | stock/resolve-titles applies no per-query length cap before forwarding up to 50 q values into searchVn + searchEgsByName (up to 100 upstream calls), unlike other upstream routes which clamp + rate-limit | src/app/api/stock/resolve-titles/route.ts:46 | — | Clamp each q with clampQuery(q, 200) and add tooManyRequests(req, 'stock/resolve-titles', ...) | TODO |
| API-015 | api | traits GET spends a throttled VNDB call per hit but has no rate limit, while its sibling tags/route.ts:18 applies tooManyRequests | src/app/api/traits/route.ts:12 | — | Add tooManyRequests(req, 'traits', { limit: 30, windowMs: 10_000 }) matching the tags route | TODO |
| BUG-001 | bug | resetJob is exported but has zero call sites in src/ or tests/ — a dead export implying an unshipped job-reset feature | src/lib/download-status.ts:243 | — | Remove the unused export, or wire it to the cancel/restart UI it was intended for | TODO |
| BUG-002 | bug | fetchPinnedExtras is exported but has zero call sites in src/ or tests/ — dead export for a pinned-eroge-price-id fetch path that is never invoked | src/lib/erogeprice-meta.ts:620 | — | Remove the unused export, or call it from the stock-refresh path that handles a pinned epId | TODO |
| BUG-003 | bug | probeVndbHealthy is exported but has zero call sites in src/ or tests/ — dead export for a circuit-breaker health probe nothing calls | src/lib/vndb-throttle.ts:156 | — | Remove the unused export, or invoke it from the throttle circuit-breaker resume logic | TODO |
| I18N-001 | i18n | Price-history chart X-axis date ticks call toLocaleDateString() with no locale, rendering in the browser default locale instead of the app locale (component has no locale prop) | src/components/charts/Sparkline.tsx:127 | — | Add a locale prop, thread BCP47[locale] from the ErogePricePanel call site, use Intl.DateTimeFormat(BCP47[locale], { dateStyle: 'medium' }) | TODO |
| I18N-002 | i18n | Price-history chart tooltip date label calls toLocaleDateString() with no locale, ignoring the active locale | src/components/charts/Sparkline.tsx:145 | — | Use the same locale-aware Intl.DateTimeFormat formatter for the labelFormatter date | TODO |
| I18N-003 | i18n | HBarChart value-label fallback d.value.toLocaleString(locale) runs with locale undefined because every stats call site passes neither locale nor formatValue | src/components/charts/BarChart.tsx:41 | — | Require locale and call fmtNum(d.value, locale), or pass locale at each HBarChart call in stats/page.tsx | TODO |
| I18N-004 | i18n | VBarChart value-label fallback d.value.toLocaleString(locale) runs with locale undefined; stats VBarChart calls omit locale/formatValue | src/components/charts/BarChart.tsx:101 | — | Require locale and use fmtNum(d.value, locale), or pass locale at the VBarChart calls in stats/page.tsx | TODO |
| I18N-005 | i18n | Hardcoded English placeholder "e.g. 3676" on the EGS-id input while the adjacent label is localized | src/components/ErogePricePanel.tsx:780 | — | Add an addCandidatePlaceholder key under erogePrice.manualMatch in fr/en/ja and render it | TODO |
| PERF-001 | performance | StockPricesSection statically imports ErogePricePanel, which statically pulls in recharts, shipping the recharts bundle in the VN detail page eager client JS; every other heavy section on that page is next/dynamic | src/components/StockPricesSection.tsx:4 | — | next/dynamic the import: const ErogePricePanel = dynamic(() => import('./ErogePricePanel').then((m) => m.ErogePricePanel), { ssr: false }) | TODO |
| PERF-002 | performance | NotesSectionToggle statically imports MarkdownView, eagerly bundling react-markdown + remark-gfm (~100kB) into the VN detail page even when notes are empty; sibling MarkdownNotes already next/dynamic's it | src/components/NotesSectionToggle.tsx:4 | — | Replace the static import with const MarkdownView = dynamic(() => import('./MarkdownView').then((m) => m.MarkdownView), { ssr: false }) | TODO |
| PERF-003 | performance | Home page mounts LibraryClient twice; the collection fetch is not gated by mode and uses cache:'no-store', so both instances fire a full /api/collection request (collection scan + getStats + annotations) parsed twice; controls-only instance never renders the items it fetches | src/components/LibraryClient.tsx:449 | — | Gate the items fetch on showGrid (controls-only requests only stats), or drop cache:'no-store' here so the two identical GETs dedupe, or hoist the fetch into a shared provider | TODO |
| RESP-001 | responsive | "Add to collection" + button is a 28px (h-7 w-7) essential control with no tap-target utility or min-h-[44px] | src/components/AddMissingVnButton.tsx:52 | — | Add the tap-target utility class (visual size stays, hit area extends to ~48px) | TODO |
| RESP-002 | responsive | Modal close (X) button sizes only the 16px icon; the button has no tap-target/min-h, giving a ~16px hit target | src/components/CompareWithButton.tsx:118 | — | Add tap-target (and small padding e.g. p-1) to the close button, matching EgsPanel/BannerSourcePicker close buttons | TODO |
| RESP-003 | responsive | Kobe match-dialog close (X) button is bare text-muted hover:text-white with no tap-target/min-h around a 16px icon | src/components/kobe/KobeLinkDialog.tsx:103 | — | Add tap-target + p-1 to the close button to reach the WCAG 44px hit area | TODO |
| SEC-001 | security | Unescaped SQL LIKE wildcards in searchLocalStaff: user q from /staff (only trimmed) is built into `%${needle}%` with no %/_ escaping and no ESCAPE clause, unlike every other search builder; parameterized so not classic SQLi but %/_ distorts matching | src/lib/db.ts:5261 | — | Escape before building: const like = `%${needle.replace(/[\\%_]/g, '\\$&')}%`; and append ESCAPE '\\' to each LIKE, mirroring activity.ts:131 | TODO |
| SEC-002 | security | VN cover upload enforces size only via precheckContentLength + post-formData file.size; a chunked request with no/forged Content-Length buffers the whole body in memory before the check (memory DoS); sibling producer/logo + series/image use streaming reparseWithLimit | src/app/api/collection/[id]/cover/route.ts:40 | — | Before formData(), call reparseWithLimit(req, MAX_COVER_BYTES) and map PayloadTooLargeError to 413 | TODO |
| SEC-003 | security | VN banner upload has the same chunked-upload memory-DoS gap: only precheckContentLength + file.size, no streaming reparseWithLimit | src/app/api/collection/[id]/banner/route.ts:47 | — | Use reparseWithLimit(req, MAX_BANNER_BYTES) (413 on PayloadTooLargeError) before formData() | TODO |
| UIUX-001 | uiux | Place delete uses native window.confirm instead of the app's styled useConfirm dialog (inconsistent with every other destructive action) | src/components/PlaceCard.tsx:37 | — | Replace window.confirm with await confirm({ message: t.places.deleteConfirm, tone: 'danger' }) via useConfirm() | TODO |
| UIUX-002 | uiux | Place delete fetch failure is swallowed (.catch(() => setDeleting(false))) — no toast/feedback when DELETE fails, and no success toast | src/components/PlaceCard.tsx:39 | — | Check r.ok and surface the error via useToast().error(...); add a success toast on delete | TODO |
| UIUX-003 | uiux | Place-detail delete uses native window.confirm instead of the styled useConfirm dialog | src/components/PlaceDetailClient.tsx:40 | — | Swap to await confirm({ message: t.places.deleteConfirm, tone: 'danger' }) | TODO |
| UIUX-004 | uiux | Place-detail delete failure is silently swallowed (catch { setDeleting(false) }) — no error surfaced | src/components/PlaceDetailClient.tsx:46 | — | Add useToast() and call toast.error((e as Error).message) in the catch | TODO |
| UIUX-005 | uiux | "Move branch here" uses native window.confirm instead of the styled useConfirm dialog | src/components/AssignProviderDialog.tsx:94 | — | Replace window.confirm with await confirm({ message, tone: 'danger' }) from useConfirm() | TODO |
| UIUX-006 | uiux | Add/Edit Place modal discards unsaved form edits on backdrop-click / Esc with no confirmation (form holds name, address, coords, notes) | src/components/AddEditPlaceModal.tsx:125 | — | Track a dirty flag and confirm before closing when dirty (mirror SeriesMetaEditor/EgsSyncBlock), or guard onClose | TODO |
| UIUX-007 | uiux | Add/Edit Place save button is a hand-rolled bg-accent text-bg primary instead of shared .btn-primary, and shows no spinner while saving | src/components/AddEditPlaceModal.tsx:308 | — | Use className="btn btn-primary" and render Loader2 animate-spin while saving | TODO |
| UIUX-008 | uiux | Assign-provider dialog shows plain "Loading…" text instead of a loading skeleton while the unassigned list fetches | src/components/AssignProviderDialog.tsx:210 | — | Render SkeletonRows withThumb={false} during loading instead of the t.app.loading paragraph | TODO |
| UIUX-009 | uiux | PlaceBrowser initial load has no error handling — a failed /api/places fetch leaves the skeleton spinning forever with no error surfaced | src/components/PlaceBrowser.tsx:65 | — | Wrap reload() in try/catch, set an error state + ErrorAlert, and always clear loading in a finally | TODO |
| UIUX-010 | uiux | After creating a place from an unassigned branch, the follow-up link POST is fire-and-forget (no .ok check, no error feedback) | src/components/PlaceBrowser.tsx:571 | — | Check the link response and surface failures so a failed auto-link isn't silent | TODO |
| UIUX-011 | uiux | ErogePrice "remove candidate" DELETE failure silently rolls back local state with no toast — user can't tell the delete failed | src/components/ErogePricePanel.tsx:737 | — | On the catch, surface an error (file has no useToast; add one or an inline role="alert" like handleSetPrimary's error path) | TODO |
| UIUX-012 | uiux | SavedFilters delete has no .ok check and no catch — a failed DELETE gives no feedback and the preset silently reappears after reload | src/components/SavedFilters.tsx:169 | — | Mirror save(): check r.ok, throw readApiError, and toast.error on failure | TODO |
