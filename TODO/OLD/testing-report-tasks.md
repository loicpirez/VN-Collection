# Testing audit tasks

| ID | Severity | Finding and implementation direction | Location | Status |
| --- | --- | --- | --- | --- |
| TESTA-001 | CRITICAL | SSRF regression coverage exercises direct `safeFetch` redirects but not proxy-enabled initial URLs and redirects. Add proxy-hop allowlist and private-address rejection tests. | `tests/safe-fetch.test.ts`, `src/lib/proxy-fetch.ts` | DONE_WITH_DIFF |
| TESTA-002 | HIGH | Collection listing has no contract test proving private fields are absent from public-shaped responses. Add an exact response-body security test. | `tests/collection-public-dto.test.ts`, `src/app/api/collection/route.ts` | DONE_WITH_DIFF |
| TESTA-003 | HIGH | New stock browser, map, places, and place detail routes do not have route-level loading-state checks. Add file-presence QA and component smoke coverage. | `tests/loading-states.test.ts`, `src/app/{alicenet,map,places,places/[id]}/loading.tsx` | DONE_WITH_DIFF |
| TESTA-004 | HIGH | VN stock navigation race behavior is untested. Add deterministic delayed-response tests proving stale responses cannot replace the active VN. | `tests/stock-prices-section.test.ts`, `src/lib/stock-prices.ts`, `src/components/StockPricesSection.tsx` | DONE_WITH_DIFF |
| TESTA-005 | MEDIUM | Collection `limit` handling has no regression test proving SQL and API bounds. Add database and route tests for defaults, maximums, and invalid values. | `tests/list-collection-for-cards.test.ts`, `tests/api-collection-pagination.test.ts`, `tests/collection-api-client.test.ts`, `tests/performance-query-shapes.test.ts` | DONE_WITH_DIFF |
| TESTA-006 | MEDIUM | The repository has no formal lint script despite relying on style and safety conventions. Add a lint command or document the intentionally limited gate clearly. | `CLAUDE.md` | VERIFIED_EXISTING |
| TESTA-007 | MEDIUM | Existing QA does not cover narrow mobile tutorial placement, VN detail height, or touch-target sizing. Add browser-driven assertions for representative narrow viewports. | `scripts/browser-interactions.mjs`, `tests/qa-script-coverage.test.ts` | DONE_WITH_DIFF |
| TESTA-008 | LOW | Rotated `SafeImage` rendering has no orientation regression coverage. Add component tests for portrait, landscape, and rotated framing calculations. | `tests/safe-image-rotation.test.ts`, `src/components/SafeImage.tsx` | DONE_WITH_DIFF |
| TESTA-009 | HIGH | The place-registry response contract had no regression proving that structured registry rows and string physical-location suggestions remain separate. Add a route-level contract test with both payload shapes populated. | `tests/place-coordinates.test.ts`, `src/app/api/places/route.ts` | DONE_WITH_DIFF |
| TESTA-010 | HIGH | `yarn qa` fetched routes through command substitutions, so route failures incremented `FAIL` only inside discarded subshells. Its curl fallback also appended `000` after a timed-out `200`, printing `200000` while the script could still exit successfully. Keep fetch state in the parent shell, preserve one status code, and pin both contracts. | `scripts/browser-qa.sh`, `tests/qa-script-coverage.test.ts` | DONE_WITH_DIFF |
| TESTA-011 | MEDIUM | Staff-detail DOM QA required a gender-filter chip even when the cached VNDB fixture legitimately omitted gender. Keep the source-level clickability pin and make the live probe conditional on fixture data. | `scripts/browser-qa.sh`, `tests/qa-script-coverage.test.ts`, `tests/field-clickability.test.ts` | DONE_WITH_DIFF |
| TESTA-012 | HIGH | `/api/export/csv` ships a CSV formula-injection defense (`csvEscape`) with zero tests, so dropping a metacharacter from the `/^[=+\-@\t\r]/` class silently re-opens injection. Add a route test asserting `=HYPERLINK(...)` and `@SUM(1)` are emitted quote-prefixed, a comma/quote/newline field is RFC-4180 quoted, and the auth gate denies a non-local request. | `src/app/api/export/csv/route.ts:45` | DONE_WITH_DIFF |
| TESTA-013 | MEDIUM | `/api/export/ics` has untested edge-heavy logic (`fold()` 75-octet UTF-8 wrapping, `ics()` escaping, `compactDate()`) with no importing test, so a folding regression silently drops events. Add a route/unit test feeding a >75-octet multibyte SUMMARY and asserting CRLF+space continuation and correct escaping. | `src/app/api/export/ics/route.ts:13` | DONE_WITH_DIFF |
| TESTA-014 | MEDIUM | `src/lib/stock-query.ts` (`titleQueries` 5-cap + dedupe, `titleQueriesForProvider` Amazon-JP slice, `amazonSearchTerms`) is bounded query-generation logic with no test. Add unit tests for the 5-item cap, sub-2-char/whitespace dropping, case-insensitive dedupe across title/alttitle/titles, and the amazon_jp Japanese filter + 3-item slice. | `src/lib/stock-query.ts:5` | DONE_WITH_DIFF |
| TESTA-015 | MEDIUM | `tests/stock-route.test.ts:47` asserts `expect([404, 500]).toContain(res.status)`, violating one-exact-status, and does not mock `refreshStockForVn`, so the path is undetermined. Mock it to throw `Error('VN not found')` and assert exactly 404 with `{error:'vn not found'}`, and a generic throw to assert exactly 500. | `tests/stock-route.test.ts:47` | DONE_WITH_DIFF |
| TESTA-016 | MEDIUM | `tests/seca-input-length-caps.test.ts` "accepts note at exactly 10000 chars" never seeds `v1`, so the route 404s at the collection check before exercising the boundary, and the `[400,404]` allowlist verifies nothing. Seed `v1`, then assert exactly 200 with a returned `entry` at 10000 and exactly 400 at 10001. | `tests/seca-input-length-caps.test.ts:36` | DONE_WITH_DIFF |
| TESTA-017 | MEDIUM | `tests/stock-summary-client.test.ts` drives the coalescing client with real `setTimeout` waits against `COALESCE_MS=60`, making it flaky and slow, against the deterministic-timers rule. Convert to `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync` with `vi.useRealTimers()` teardown. | `tests/stock-summary-client.test.ts:47` | DONE_WITH_DIFF |
| TESTA-018 | MEDIUM | The SSRF gate on the VNDB read cache (`vndb-cache.ts:doFetch`) and the write/status-push path (`vndb-throttle.ts:throttledFetch`) is proven only by source-string pins, never executed against a loopback/RFC-1918 URL at runtime. Add a runtime test stubbing the network and passing `http://127.0.0.1`/`http://169.254.169.254` through each, asserting it throws before any fetch. | `tests/vndb-cache-ssrf-gate.test.ts`, `tests/vndb-throttle-ssrf-gate.test.ts` | DONE_WITH_DIFF |
| TESTA-019 | LOW | `/api/stock/resolve-titles` has untested security/bounds logic (LIKE-wildcard escaping, `MAX_TITLES=50` slice, `clampQuery`, rate limiter). Add a route test asserting a `q` with `%`/`_` is escaped, >50 `q` params are truncated, and the empty-query path. | `src/app/api/stock/resolve-titles/route.ts:14` | DONE_WITH_DIFF |
| TESTA-020 | LOW | `src/lib/physical-location-input.ts` (`parsePhysicalLocations`, reject-not-truncate with MAX 32/200 + dedupe) is imported by no test, so its documented reject contract is unverified. Add unit tests for 33-entry rejection, 201-char rejection, string-vs-array parsing, non-string-array rejection, and dedupe. | `src/lib/physical-location-input.ts:14` | DONE_WITH_DIFF |
