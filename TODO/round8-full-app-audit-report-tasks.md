# Round 8 full application audit - 2026-06-15

Fresh source inspection covered the VN detail stock section, generic stock page,
AliceNet shop page, places registry, place detail page, map, navigation menus,
display layout settings, cover controls, route guards, client response decoders,
and current coverage gates.

Second app-wide source pass covered the library, wishlist, search, compare,
shelf view, shelf fullscreen, dumped tracker, image rendering primitives,
keyboard shortcuts, guided tour, public-read route policy, backup and file
serving routes, markdown rendering, and route-level responsive risk points.

Fresh verification evidence after this report update:

- `yarn typecheck` passed.
- `yarn build` passed.
- `yarn test:cov` passed with `Statements 100%`, `Branches 100%`, `Functions 100%`, and `Lines 100%`.
- Focused tests for AliceNet decoding, place decoding, and VN detail runtime rendering passed.

| ID | Severity | Finding and implementation direction | Location | Status |
| --- | --- | --- | --- | --- |
| R8-STOCK-001 | CRITICAL | Problem: the VN detail page had lost its in-page stock lookup section, so item-level stock and price checks were not discoverable from the VN itself. Fix: restore the `stock` VN section with the full `StockPanel`, keep it inside the VN detail layout order, and wrap provider-map loading so a place-map failure does not blank the VN page. | `src/app/vn/[id]/page.tsx`, `src/lib/vn-detail-layout.ts`, `tests/bug-regression-source-pins.test.ts` | DONE_WITH_DIFF |
| R8-STOCK-002 | HIGH | Problem: shop freshness badges on the places page used place metadata `updated_at`, so a fresh stock sync could still render stale labels. Fix: derive `stock_updated_at` from `vendor_stock_offer.updated_at`, use it for stale badges, stale filtering, and freshness sorting, and fall back only when a place has no linked stock timestamp. | `src/lib/db.ts`, `src/components/PlaceBrowser.tsx`, `src/components/PlaceCard.tsx`, `tests/places.test.ts`, `tests/PlaceBrowser.test.tsx`, `tests/PlaceCard.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-003 | MEDIUM | Problem: older `/api/places` payloads without `stock_updated_at` had an untested compatibility branch. Fix: keep decoder compatibility by defaulting the missing field to `null` and add a regression test for the old shape. | `src/lib/place-client-shape.ts`, `tests/place-client-shape.test.ts` | DONE_WITH_DIFF |
| R8-STOCK-004 | HIGH | Problem: AliceNet operation controls must remain on the AliceNet shop page, not on generic `/stock` or the places index. Fix: keep `AliceNetClient` mounted only when the place detail has the AliceNet provider label and keep `/stock` on the generic `StockLookupClient`. | `src/components/PlaceDetailClient.tsx`, `src/app/stock/page.tsx`, `src/components/StockLookupClient.tsx`, `tests/alicenet-branding.test.ts` | VERIFIED_EXISTING |
| R8-STOCK-005 | HIGH | Problem: generic `/stock` needs to stay provider-neutral while still showing AliceNet offers as stock results for a selected VN. Fix: keep `StockLookupClient` as the page shell, resolve provider-place links separately, and render only the generic `StockPanel` for the selected VN. | `src/components/StockLookupClient.tsx`, `src/components/StockPanel.tsx` | VERIFIED_EXISTING |
| R8-STOCK-006 | MEDIUM | Problem: `StockPanel` could imply all cached offers are stale even when a refresh found fresh offers from another provider. Fix: show the provider-cached stale hint only when cached offers exist and no fresh offers were found. | `src/components/StockPanel.tsx`, `tests/StockPanel.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-007 | HIGH | Problem: AliceNet operations start as detached background jobs, but the AliceNet page only shows a toast and tells the operator to refresh. Fix: subscribe or poll the download-status job from the AliceNet panel and render the active job label, percent, current item, error count, and cancel action inline. Evidence: the embedded AliceNet shop panel polls the durable status snapshot, shows determinate or preparing progress, current item, error count, Stop and Close controls, and reloads once on completion; lifecycle and unmount paths are covered. | `src/components/AliceNetClient.tsx`, `src/app/api/alicenet/run/route.ts`, `src/components/DownloadStatusBar.tsx`, `tests/AliceNetClient.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-008 | MEDIUM | Problem: AliceNet run errors can still surface as generic API error text, especially queue-full or upstream-failure states. Fix: return stable error codes from `/api/alicenet/run` and map them to localized, action-oriented copy in the client. Evidence: invalid operations, queue conflicts, and durable-lock failures now emit `invalid_operation`, `queue_full`, and `run_unavailable`; the client maps all three to localized copy and never exposes the internal lock error. | `src/app/api/alicenet/run/route.ts`, `src/components/AliceNetClient.tsx`, `src/lib/api-error-read.ts`, `src/lib/i18n/dictionaries.ts`, `tests/alicenet-run-route.test.ts`, `tests/AliceNetClient.branches.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-009 | HIGH | Problem: AliceNet fetches every paginated API page into one client array before filtering and grouping. Virtualized rendering helps DOM cost, but memory, network, and first usable interaction still scale with the whole stock table. Fix: move query, sort, filters, and group windows to `/api/alicenet`, then let the client request only the visible page or virtual window. Evidence: `/api/alicenet` accepts bounded offset, limit, filter, sort, group, query, producer, year, and price inputs; `queryPage` applies them before `LIMIT`, and the client retains only one server page. | `src/components/AliceNetClient.tsx`, `src/app/api/alicenet/route.ts`, `src/lib/db.ts`, `src/lib/db/repositories/alicenet.ts`, `tests/db-stock-coverage.test.ts` | DONE_WITH_DIFF |
| R8-STOCK-010 | MEDIUM | Problem: AliceNet producer filtering uses a native select built from every producer or EGS brand. Large option sets are slow to scan and poor on mobile. Fix: replace it with the same searchable capped combobox pattern used by library facets. Evidence: `AliceNetClient` now passes counted server facets to the bounded, searchable and keyboard-operable `FacetCombobox`; `AliceNetClient` and `FacetCombobox` targeted coverage is 100/100/100/100 across 122 tests. | `src/components/AliceNetClient.tsx`, `src/components/library/FacetCombobox.tsx`, `tests/AliceNetClient.test.tsx`, `tests/AliceNetClient.branches.test.tsx`, `tests/FacetCombobox.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-011 | MEDIUM | Problem: AliceNet bulk clear has inline progress and a stop button, but normal AliceNet sync and match operations rely on the global download bar only. Fix: unify AliceNet operation progress with the bulk-progress visual language and keep a local status strip after starting a server job. Evidence: server operations now use the same local progress-bar language with done/total/percent, preparing state, current item, error count, and Stop while remaining visible in the global download surface. | `src/components/AliceNetClient.tsx`, `src/components/DownloadStatusBar.tsx`, `tests/AliceNetClient.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-012 | LOW | Problem: VN detail stock section silently continued with an empty provider-place map if the map read failed, which avoided a crash but hid why physical location links were missing. Fix: pass a non-blocking diagnostic into `StockPanel` so the page can say place links are temporarily unavailable. Evidence: the server now distinguishes an empty map from a failed read, keeps the stock section mounted, and passes a localized physical-link diagnostic; the runtime regression proves the section survives a thrown DB read. | `src/app/vn/[id]/page.tsx`, `src/components/StockPanel.tsx`, `tests/vn-detail-page-runtime.test.tsx`, `tests/StockPanel.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-013 | MEDIUM | Problem: the generic stock page title resolver logged title failures only to the console, so the visible page could look under-informed without explaining that title enrichment failed. Fix: render a small non-blocking title-resolution warning in the selected VN panel header. Evidence: HTTP, network, and undecodable title failures now set a localized diagnostic while stock remains usable by VN id; aborts and late settlements after unmount remain silent. `StockLookupClient.tsx` reaches 100/100/100/100 targeted coverage. | `src/components/StockLookupClient.tsx`, `src/components/StockPanel.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/StockLookupClient.test.tsx`, `tests/StockPanel.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-014 | MEDIUM | Problem: AliceNet and stock pages rely on manual refresh after background jobs complete to show new matches. Fix: listen for the matching job completion event or poll `/api/download-status` and reload stock snapshots automatically after the relevant job finishes. Evidence: AliceNet reloads exactly once when its tracked job finishes; generic stock checks are request-bound and replace the visible snapshot after each provider response before refreshing the server route. | `src/components/AliceNetClient.tsx`, `src/components/StockPanel.tsx`, `src/lib/download-status.ts`, `tests/AliceNetClient.test.tsx`, `tests/StockPanel.test.tsx` | DONE_WITH_DIFF |
| R8-STOCK-015 | LOW | Problem: stock freshness now uses stock timestamps, but places cards do not expose the exact stock-updated time when data is fresh. Fix: add a localized fresh timestamp chip or tooltip for linked shop cards. | `src/components/PlaceCard.tsx`, `src/lib/locale-number.ts`, `tests/PlaceCard.test.tsx` | DONE_WITH_DIFF |
| R8-PLACES-001 | HIGH | Problem: the places page could previously render stale shop badges after stock sync. Fix: aggregate `MAX(vendor_stock_offer.updated_at)` in place queries and use it through the client shape. | `src/lib/db.ts`, `src/lib/place-client-shape.ts` | DONE_WITH_DIFF |
| R8-PLACES-002 | MEDIUM | Problem: places and unassigned branches are loaded as full arrays before client pagination starts. This is acceptable for the current small registry but will regress with large shop lists. Fix: add server pagination and search parameters to `/api/places` and `/api/places/unassigned`. Evidence: both endpoints now return bounded 60-row windows with validated offset/limit and filtered totals. Places additionally apply tab, type, GPS, freshness, NFKC search, deterministic sort, and return global registry stats. `PlaceBrowser` reloads server windows on controls and page changes while preserving legacy-envelope local behavior. Typecheck and 62 focused behavior tests pass; the complete changed surface reaches 100/100/100/100 across 535 branches. | `src/lib/place-registry-page.ts`, `src/components/PlaceBrowser.tsx`, `src/app/api/places/route.ts`, `src/app/api/places/unassigned/route.ts`, `src/lib/place-client-shape.ts`, `tests/place-registry-page.test.ts`, `tests/PlaceBrowser.test.tsx` | DONE |
| R8-PLACES-003 | LOW | Problem: list-mode place rows use compact icon-only buttons for URL, map, and edit. They have labels, but visual discoverability is low for repeated administration. Fix: show text labels at wide breakpoints or group secondary actions in a labelled action menu. | `src/components/PlaceBrowser.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/PlaceBrowser.test.tsx` | DONE_WITH_DIFF |
| R8-PLACES-004 | MEDIUM | Problem: deleting a place aborts and guards identity correctly, but the destructive action is still visually adjacent to normal edit actions. Fix: move delete into an overflow or danger cluster on the place detail header. | `src/components/PlaceDetailClient.tsx`, `tests/PlaceDetailClient.branches.test.tsx` | DONE_WITH_DIFF |
| R8-PLACES-005 | LOW | Problem: the places stats grid mixes registry counts, stock counts, and branch counts without a visible freshness note. Fix: add one compact caption explaining which counts come from stock offers and which come from place metadata. | `src/components/PlaceBrowser.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/PlaceBrowser.test.tsx` | DONE_WITH_DIFF |
| R8-MAP-001 | HIGH | Problem: manual QA flagged the map as overlaying modals. Source wraps the Leaflet container in `relative isolate z-0`. Progress: browser QA now enables the external map, requires a live Leaflet pane, opens Add Place, checks center-point hit testing belongs to the dialog, and compares modal versus map z-index. Keep open until the final isolated `yarn qa` execution passes. | `src/components/MapCanvas.tsx`, `src/components/MapPageClient.tsx`, `scripts/browser-interactions.mjs` | TODO |
| R8-MAP-002 | HIGH | Problem: map tiles and geocoding are external network calls that need explicit consent. Fix: keep CARTO tiles and Nominatim geocoding behind `MapPrivacyControl`; source inspection confirms `MapCanvas` does not initialize until consent is true and search is disabled without consent. | `src/components/MapPrivacyControl.tsx`, `src/components/MapPageClient.tsx`, `src/components/MapCanvas.tsx` | VERIFIED_EXISTING |
| R8-MAP-003 | LOW | Problem: the map privacy warning can be dismissed, but the restore control becomes a small single button that may be missed. Fix: add a compact settings-link style affordance near the map controls after dismissal. | `src/components/MapPrivacyControl.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/MapPrivacyControl.test.tsx` | DONE_WITH_DIFF |
| R8-MAP-004 | MEDIUM | Problem: map search results render as a listbox of buttons but do not implement roving keyboard focus between options. Fix: add ArrowUp, ArrowDown, Home, End, Escape, and active-descendant semantics. | `src/components/MapPageClient.tsx`, `tests/MapPageClient.branches.test.tsx` | DONE_WITH_DIFF |
| R8-MAP-005 | MEDIUM | DONE: maps with 80 places or fewer preserve eager marker rendering; larger datasets now materialize only markers inside a 35%-padded viewport plus any explicitly focused place. Marker membership resynchronizes on move and zoom while preserving popup updates, focus callbacks, and saved viewport behavior. Verified by global typecheck, 24/24 branch tests, and 100/100/100/100 targeted coverage over 121 statements and 56 branches. | `src/components/MapCanvas.tsx`, `tests/MapCanvas.branches.test.tsx` | DONE |
| R8-MAP-006 | LOW | Problem: popup HTML is safely escaped, but the popup is built as a string and bypasses React accessibility helpers. Fix: keep escaping, then add a test that validates the generated popup contains no raw unescaped place fields and consider a React portal popup wrapper later. | `src/components/MapCanvas.tsx`, `tests/MapCanvas.branches.test.tsx` | DONE_WITH_DIFF |
| R8-NAV-001 | HIGH | Problem: desktop grouped nav is portaled and clamped, but a long group can still become a scrollable menu. Manual QA found this feels like the navbar itself is scrolling. Fix: split the largest group into two columns or subgroups on desktop and add WebKit regression coverage for menu bounds. | `src/components/MoreNavMenu.tsx`, `tests/MoreNavMenu.branches.test.tsx` | DONE_WITH_DIFF |
| R8-NAV-002 | MEDIUM | DONE: navigation labels now respond to the actual configurable header width instead of the viewport alone. The three daily destinations appear at a 72rem header container, category labels at 88rem, and icon-only accessible controls remain below those thresholds. This preserves French/Japanese fit and narrow custom header layouts. Verified by global typecheck, 25/25 navigation tests, and 100/100/100/100 targeted component coverage over 94 statements and 60 branches. | `src/app/layout.tsx`, `src/app/globals.css`, `src/components/MoreNavMenu.tsx`, `tests/navbar-desktop-portal-source.test.ts` | DONE |
| R8-NAV-003 | MEDIUM | Problem: mobile sheet is portaled with full-height scrolling and safe-area padding, which preserves navigation reachability. Fix: keep this pattern and add a focused regression for tapping every group on a narrow viewport. | `src/components/MoreNavMenu.tsx`, `tests/MoreNavMenu.test.tsx` | VERIFIED_EXISTING |
| R8-NAV-004 | LOW | Problem: dropdown menu width is fixed to 224px, so long localized labels truncate or wrap poorly in some locales. Fix: clamp a wider responsive width such as `min(20rem, calc(100vw - 16px))` and test French and Japanese labels. | `src/components/MoreNavMenu.tsx`, `tests/MoreNavMenu.branches.test.tsx` | DONE_WITH_DIFF |
| R8-RESP-001 | HIGH | Problem: VN platform overflow summary was too small on touch viewports. Fix: make the `+N` platform disclosure at least 44px high on narrow screens while keeping compact desktop sizing. | `src/app/vn/[id]/page.tsx`, `tests/vn-detail-compact-artwork-platforms.test.ts` | DONE_WITH_DIFF |
| R8-RESP-002 | MEDIUM | Problem: VN cover edit overlay hid its text label below `sm`, leaving only an icon over cover art. Fix: compact viewports now hide artwork overlays and expose one labelled pencil action in the VN toolbar; its portal is an anchored popover on desktop and a focus-trapped bottom sheet on mobile. Evidence: 98 focused tests pass and the ten-file artwork surface reaches 100/100/100/100 across 429 branches. | `src/components/CoverEditOverlay.tsx`, `src/components/ArtworkActionMenu.tsx`, `src/components/VnDetailActionsBar.tsx`, `tests/ArtworkActionMenu.test.tsx` | DONE |
| R8-RESP-003 | MEDIUM | Problem: AliceNet candidate chips and action buttons used 44px touch targets on phone but shrank on `sm`, which could still affect tablets with touch input. Fix: use pointer media queries rather than breakpoint-only min-height reductions. Evidence: every AliceNet candidate, card, toolbar, and remap-dialog reduction now uses the existing `can-hover` variant (`hover: hover` and `pointer: fine`); coarse-pointer devices retain 44px targets at every viewport width. Source contracts and AliceNet behavior pass 155/155 targeted tests. | `src/components/AliceNetClient.tsx`, `src/components/alicenet/AliceNetLinkDialog.tsx`, `tailwind.config.ts`, `tests/responsive-tap-targets.test.ts` | DONE_WITH_DIFF |
| R8-RESP-004 | LOW | Problem: per-page layout settings now separate spacing and density rows, but each density control has a fixed five-column mini-grid. Fix: add a 320px viewport regression for the settings panel to prove the row does not overflow in translated locales. | `src/components/settings/LayoutSettingsTab.tsx`, `tests/responsive-tap-targets.test.ts` | DONE_WITH_DIFF |
| R8-RESP-005 | MEDIUM | Problem: place list rows pack many chips and action buttons into one wrapping line. This works but can become visually noisy on phone with long addresses. Fix: stack metadata chips above a dedicated action row below 640px. | `src/components/PlaceBrowser.tsx`, `tests/PlaceBrowser.test.tsx` | DONE_WITH_DIFF |
| R8-RESP-006 | LOW | Problem: map size options render as a compact button group and may wrap awkwardly in narrow locales. Fix: add a segmented select or two-row layout below `sm`. | `src/components/MapPageClient.tsx`, `tests/MapPageClient.branches.test.tsx` | DONE_WITH_DIFF |
| R8-UIUX-001 | HIGH | Problem: AliceNet has special controls and data semantics, but its embedded place-detail header still starts with generic stats before explaining that this is the shop mirror. Fix: add a compact explanatory header with source, last fetch, active job, and data freshness before the stats grid. Evidence: the embedded panel begins with the AliceNet title, canonical source link, local-mirror explanation, last-fetch state, and active-job strip before statistics. | `src/components/AliceNetClient.tsx`, `src/components/PlaceDetailClient.tsx`, `tests/AliceNetClient.test.tsx` | DONE_WITH_DIFF |
| R8-UIUX-002 | MEDIUM | Problem: places stats card labels are terse and can read as unrelated numbers. Fix: add short subtitles or tooltips for Total, On map, No coordinates, VN in stock, Branches, and Unassigned. | `src/components/PlaceBrowser.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/PlaceBrowser.test.tsx` | DONE_WITH_DIFF |
| R8-UIUX-003 | MEDIUM | DONE: the empty stock workspace now shows up to six genuinely recent VN checks, three completed stock-batch jobs with localized labels/date/progress/error counts, skeleton loading, explicit empty/error states, retry, and an anchored path to batch tools. A dedicated `recent_checked` queue scope orders by `MAX(fetched_at) DESC` in SQLite and PostgreSQL, while the existing oldest-stock scope remains unchanged. Verified by global typecheck, 77/77 focused tests, and 100/100/100/100 coverage over 438 statements and 276 branches. | `src/components/StockRecentActivity.tsx`, `src/components/StockLookupClient.tsx`, `src/components/StockBatchClient.tsx`, `src/app/api/stock/queue/route.ts`, `src/lib/db/repositories/stock-queue.ts`, `src/lib/i18n/dictionaries.ts`, `tests/StockRecentActivity.test.tsx`, `tests/stock-queue-route-branches.test.ts`, `tests/postgres-stock-queue.test.ts` | DONE |
| R8-UIUX-004 | LOW | Problem: AliceNet filters are open by default unless `filters=0`, which makes the first view dense. Fix: persist the panel state and default closed when there are no active advanced filters. Evidence: the panel now defaults closed, persists `showFilters`, honors explicit URL state, opens for active advanced URL filters and reveals itself when a producer chip applies a criterion; 115 targeted tests cover the component at 100/100/100/100. | `src/components/AliceNetClient.tsx`, `tests/AliceNetClient.test.tsx`, `tests/AliceNetClient.branches.test.tsx` | DONE_WITH_DIFF |
| R8-UIUX-005 | MEDIUM | DONE: each per-page density row now presents responsive Compact/Balanced/Comfortable presets as the primary control. The plus/minus buttons and slider remain available inside a collapsed Precise setting disclosure, eliminating the narrow-layout overlap while preserving arbitrary persisted values and power-user control. Density presets have qualified accessible names so they cannot be confused with the adjacent spacing presets. Verification: 49 settings/i18n tests pass, typecheck passes, and the complete settings tab reaches 100/100/100/100 across 211 statements, 160 branches, 80 functions, and 190 lines. | `src/components/settings/LayoutSettingsTab.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/LayoutSettingsTab.test.tsx`, `tests/LayoutSettingsTab.branches.test.tsx` | DONE |
| R8-UIUX-006 | LOW | DONE: `CoverQuickActions` now offers a `responsive-menu` variant with one 44px ellipsis trigger below `sm`, full labeled keyboard-addressable menu items, and unchanged inline labeled actions from `sm` upward. The VN detail collection cluster uses this variant; the default and destructive tracking modes retain their existing behavior and mutation ownership. Verified by global typecheck, 50/50 action-bar tests, and 100/100/100/100 coverage over 166 statements and 151 branches. | `src/components/CoverQuickActions.tsx`, `src/components/VnDetailActionsBar.tsx`, `tests/CoverQuickActions.branches.test.tsx`, `tests/vn-detail-actions-bar-runtime.test.tsx` | DONE |
| R8-ACCESS-001 | MEDIUM | Problem: nav dropdowns implement arrow-key movement and focus, but their open button click path calls `updateMenuPosition` before `open` and relies on non-null refs. Fix: add a regression test for first open, keyboard open, resize, and scroll reposition. | `src/components/MoreNavMenu.tsx`, `tests/MoreNavMenu.branches.test.tsx` | DONE_WITH_DIFF |
| R8-ACCESS-002 | LOW | Problem: map privacy dismiss button has a 20px visual box with `tap-target`, but the class behavior is not obvious from source alone. Fix: assert computed hit target size in browser QA or replace with explicit 44px width and height. | `src/components/MapPrivacyControl.tsx`, `tests/MapPrivacyControl.test.tsx` | DONE_WITH_DIFF |
| R8-ACCESS-003 | MEDIUM | Problem: AliceNet card status badges were visual text, but batch selection state was only on the overlay checkbox. Fix: include selection state in each card row label or add `aria-describedby` from selected state to the item title. Evidence: every card and list row in selection mode references a localized screen-reader description that updates between selected and not selected; tests assert the relationship and state transition. | `src/components/AliceNetClient.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/AliceNetClient.test.tsx` | DONE_WITH_DIFF |
| R8-ACCESS-004 | MEDIUM | Problem: virtualized AliceNet grids add top and bottom spacers inside list containers, which could be counted as items by assistive technology. Fix: ensure spacers are ignored and prove coherent item counts. Evidence: spacers now combine `role=presentation` with `aria-hidden`; rendered listitems expose valid list semantics through `aria-posinset` and `aria-setsize` rather than invalid `aria-rowcount` on `role=list`. Card and row virtualization regressions prove spacer exclusion and logical positions; AliceNet reaches 100/100/100/100 coverage. | `src/components/AliceNetClient.tsx`, `src/lib/virtual-grid.ts`, `tests/AliceNetClient.branches.test.tsx` | DONE_WITH_DIFF |
| R8-ACCESS-005 | LOW | Problem: place list action icons have accessible names, but repeated rows produce repeated identical labels like Open map. Fix: include the place name in aria labels for row actions. | `src/components/PlaceBrowser.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/PlaceBrowser.test.tsx` | DONE_WITH_DIFF |
| R8-I18N-001 | MEDIUM | Problem: branch labels rendered as generic plural forms such as `branche(s)`. Fix: add singular and plural dictionary keys for place cards and map popups. | `src/lib/i18n/dictionaries.ts`, `src/components/PlaceBrowser.tsx`, `src/components/PlaceCard.tsx`, `src/components/MapPageClient.tsx` | DONE_WITH_DIFF |
| R8-I18N-002 | LOW | Problem: French AliceNet background-job copy missed accents and sounded unfinished. Fix: update the French dictionary strings for launched background operations. | `src/lib/i18n/dictionaries.ts` | DONE_WITH_DIFF |
| R8-I18N-003 | MEDIUM | Problem: AliceNet filter placeholders included literal sample years, including a year that would become stale. Fix: derive year placeholder examples from the current calendar year or remove hardcoded examples. Evidence: both year fields now use a stable localized format hint (`AAAA` in French and `YYYY` in English/Japanese); tests assert the visible placeholders and all 115 AliceNet tests pass. | `src/components/AliceNetClient.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/AliceNetClient.test.tsx` | DONE_WITH_DIFF |
| R8-I18N-004 | LOW | Problem: map popup content is built in TypeScript with inline HTML, making translation coverage harder to audit than React markup. Fix: keep labels passed as localized functions and add tests for all active locales. | `src/components/MapCanvas.tsx`, `tests/MapCanvas.branches.test.tsx` | DONE_WITH_DIFF |
| R8-I18N-005 | LOW | DONE: operator logs now begin with unique stable machine-readable codes instead of mutable English phrases. VN stock enrichment, VN detail upstream/place-map failures, and library decode/settings/load failures share one immutable registry; visible errors remain localized. Verification: 103 focused tests and typecheck pass, and the complete changed surface reaches 100/100/100/100 across 1,087 statements, 1,144 branches, 275 functions, and 900 lines. | `src/lib/operation-log-codes.ts`, `src/components/StockLookupClient.tsx`, `src/app/vn/[id]/page.tsx`, `src/components/LibraryClient.tsx`, `tests/operation-log-codes.test.ts` | DONE |
| R8-I18N-006 | MEDIUM | DONE: shared semantic acronym labels now expose localized VNDB, EGS, and GPS explanations on AliceNet links plus place badges and filters; StockPanel has no visible acronym label requiring replacement. Verified by the affected component suites and `AcronymLabel` at 100/100/100/100 coverage. | `src/components/AcronymLabel.tsx`, `src/components/AliceNetClient.tsx`, `src/components/PlaceBrowser.tsx`, `tests/AcronymLabel.test.tsx` | DONE |
| R8-SEC-001 | HIGH | Problem: AliceNet endpoints expose local stock data and can trigger upstream syncs. Fix: keep `requireLocalhostOrToken` on all AliceNet list and run routes; source inspection confirms both checked routes gate before work begins. | `src/app/api/alicenet/route.ts`, `src/app/api/alicenet/run/route.ts`, `src/lib/auth-gate.ts` | VERIFIED_EXISTING |
| R8-SEC-002 | HIGH | Problem: place URLs and external stock links can be persisted data. Fix: continue using `safeHref` before rendering place URLs and audit all stock/provider outbound links through the same helper. | `src/components/PlaceBrowser.tsx`, `src/components/PlaceDetailClient.tsx`, `src/components/PlaceCard.tsx`, `src/components/StockPanel.tsx`, `src/components/ReleasesSection.tsx`, `src/components/EgsRichDetails.tsx`, `src/lib/safe-href.ts`, `tests/PlaceBrowser.test.tsx` | DONE_WITH_DIFF |
| R8-SEC-003 | MEDIUM | Problem: Leaflet popup strings include place fields. Fix: `buildPopup` currently escapes place strings before string interpolation; add a direct unit test for malicious place names, addresses, and labels. | `src/components/MapCanvas.tsx`, `tests/MapCanvas.branches.test.tsx` | DONE_WITH_DIFF |
| R8-SEC-004 | HIGH | Problem: stock providers involve server-side fetches to external shops. Fix: ensure every provider fetch is resolved from the canonical provider capability catalog and never from arbitrary client-supplied URLs. Evidence: request providers are restricted to `STOCK_PROVIDER_IDS`; exact provider hosts now come only from `STOCK_PROVIDER_ALLOWED_HOSTS`; manual/imported sources must match their declared provider, use HTTP(S), and contain no credentials; mismatches fail before network with a provider diagnostic; catalogue coverage is 100/100/100/100 across 147 targeted tests. | `src/lib/stock.ts`, `src/lib/stock-provider-capabilities.ts`, `src/app/api/vn/[id]/stock/route.ts`, `src/app/api/vn/[id]/stock/sources/route.ts`, `tests/stock-provider-capabilities.test.ts`, `tests/stock-refresh-wave.test.ts` | DONE_WITH_DIFF |
| R8-SEC-005 | MEDIUM | Problem: `activeJobs` for AliceNet is in process memory, so a server restart can lose the one-job guard while the durable download-status layer may still show work. Fix: persist active AliceNet run locks with expiry in SQLite. Evidence: `app_job_lock` acquisition is atomic, owner-bound, expiring, renewable, and released in `finally`; both SQLite and PostgreSQL repositories implement the same contract and route tests cover contention and persistence failure. | `src/app/api/alicenet/run/route.ts`, `src/lib/download-status.ts`, `src/lib/db.ts`, `src/lib/db/repositories/app-job-lock.ts`, `tests/alicenet-run-route.test.ts` | DONE_WITH_DIFF |
| R8-PERF-001 | HIGH | Problem: AliceNet renders through virtualization but still loads all pages sequentially into memory. Fix: implement server-side filtering, sorting, and pagination, then virtualize only the active page or window. Evidence: the client requests one bounded server page and the repository applies filtering, grouping, ordering, count, and pagination before serialization; virtualization is limited to the active page. | `src/components/AliceNetClient.tsx`, `src/app/api/alicenet/route.ts`, `src/lib/db/repositories/alicenet.ts`, `tests/db-stock-coverage.test.ts` | DONE_WITH_DIFF |
| R8-PERF-002 | MEDIUM | Problem: places registry previously paginated after fetching all places and unassigned branches. Fix: the places and unassigned APIs now apply validated search, filters, sort, offset, and bounded limit before serialization; `PlaceBrowser` retains one server window and preserves legacy-envelope compatibility. Evidence: this is the performance facet of `R8-PLACES-002`; 62 focused tests pass and the changed surface reaches 100/100/100/100 across 535 branches. | `src/lib/place-registry-page.ts`, `src/components/PlaceBrowser.tsx`, `src/app/api/places/route.ts`, `src/app/api/places/unassigned/route.ts` | DONE |
| R8-PERF-003 | MEDIUM | DONE: the map switches from eager markers to padded viewport filtering above 80 coordinate-bearing places, removes markers leaving the render window, creates newly visible markers after pan/zoom, and pins an explicit focus marker even when it starts outside the viewport. Bounds are calculated once per synchronization. Verified by global typecheck, 24/24 branch tests, and 100/100/100/100 targeted coverage over 121 statements and 56 branches. | `src/components/MapCanvas.tsx`, `tests/MapCanvas.branches.test.tsx` | DONE |
| R8-PERF-004 | MEDIUM | Problem: `loadVndbWishlistIdsWithinBudget` protected AliceNet from slow VNDB wishlist calls but each paged client load still performed wishlist enrichment on every page request. Fix: cache the wishlist id set per request chain or expose first-page wishlist stats separately. Evidence: a process-local cache now coalesces concurrent reads, reuses successful snapshots for 30 seconds, negatively caches unavailable/auth states for only 5 seconds, and uses a generation guard against stale in-flight writes. Successful wishlist mutations invalidate it; 69 route/cache tests pass and the cache module reaches 100/100/100/100. | `src/app/api/alicenet/route.ts`, `src/app/api/wishlist/[id]/route.ts`, `src/lib/vndb-wishlist-cache.ts`, `tests/vndb-wishlist-cache.test.ts`, `tests/alicenet-routes.test.ts`, `tests/wishlist-route-branches.test.ts` | DONE_WITH_DIFF |
| R8-PERF-005 | LOW | DONE: production profiling confirms the layout editor and all dnd-kit imports remain behind `next/dynamic({ ssr: false })`; they are absent from the Settings shell. The lazy layout chunk is 31,911 bytes raw and 8,482 bytes gzip, so further sub-tab splitting would add requests/loading states for a sub-9KB post-open gain. A source contract now locks the lazy boundary. Verified by a successful production build and 1/1 focused test. | `src/components/SettingsButton.tsx`, `src/components/settings/LayoutSettingsTab.tsx`, `tests/settings-layout-lazy-boundary.test.ts` | DONE |
| R8-TYPE-001 | MEDIUM | Problem: AliceNet integer decoding used an opaque safe-integer guard that left coverage difficult to prove. Fix: keep strict validation but make numeric checks explicit and test safe, unsafe, fractional, non-finite, below-min, and accepted cases through public decoders. | `src/lib/alicenet-client-shape.ts`, `tests/alicenet-client-shape.test.ts` | DONE_WITH_DIFF |
| R8-TYPE-002 | MEDIUM | Problem: place rows were decoded with an optional `stock_updated_at` but all current SQL projections now specify it. Fix: keep the decoder backward-compatible while tightening DB row projection types. | `src/lib/db.ts`, `src/lib/place-client-shape.ts`, `tests/place-client-shape.test.ts` | DONE_WITH_DIFF |
| R8-TYPE-003 | LOW | DONE: AliceNet query parsing is now a pure shared contract covering filter, sort, group, view, filter-panel state, advanced fields, text search, and positive page numbers. The stock page uses a separate typed parser that selects the first query value, validates both VNDB and EGS identifiers, and normalizes case. URL behavior and persisted preference fallbacks remain compatible. Verification: 49 focused tests pass and targeted coverage is 100/100/100/100 across 58 statements, 69 branches, 15 functions, and 49 lines. | `src/components/alicenet-types.ts`, `src/components/AliceNetClient.tsx`, `src/lib/page-query.ts`, `src/app/stock/page.tsx`, `tests/alicenet-query.test.ts`, `tests/page-query.test.ts` | DONE |
| R8-TYPE-004 | MEDIUM | DONE: `readApiErrorDetails` now preserves sanitized message, code, route context, HTTP status, and fallback provenance from validated canonical or legacy payloads. Collection and wishlist emit stable contexts, while AliceNet maps DNS, timeout, refusal, forbidden, not-found, parse, load, link, stop, reset, and clear failures to localized operation-specific messages. Verified by 417 critical tests, 306 additional route tests, full typecheck, and 100/100/100/100 targeted coverage. | `src/lib/api-error-shape.ts`, `src/lib/api-error-read.ts`, `src/components/LibraryClient.tsx`, `src/components/AliceNetClient.tsx`, `src/app/api/collection/route.ts`, `src/app/api/wishlist/route.ts`, `tests/api-error-read.test.ts` | DONE |
| R8-TEST-001 | CRITICAL | Problem: global branch coverage was short by one branch after the interrupted work. Fix: add real public-decoder coverage for missing place `stock_updated_at`, AliceNet integer edge cases, and rerun full coverage to `100%` for statements, branches, functions, and lines. | `tests/place-client-shape.test.ts`, `tests/alicenet-client-shape.test.ts`, `src/lib/place-client-shape.ts`, `src/lib/alicenet-client-shape.ts` | DONE_WITH_DIFF |
| R8-TEST-002 | HIGH | Problem: VN detail runtime tests could finish while lazy stock imports were still resolving, causing an environment teardown error under coverage. Fix: make the shared server-render helper await all mocked dynamic imports. | `tests/vn-detail-page-runtime.test.tsx` | DONE_WITH_DIFF |
| R8-TEST-003 | HIGH | Problem: unit coverage does not prove map modal z-index, Safari nav menus, and AliceNet background jobs. Progress: the isolated browser runner now covers Leaflet hit testing, desktop/mobile navigation in Chromium and WebKit, and AliceNet place-page start/progress/current-item/Stop using intercepted job APIs. WebKit 26.4 is installed. Keep open until final browser execution passes. | `scripts/browser-interactions.mjs`, `src/components/MoreNavMenu.tsx`, `src/components/AliceNetClient.tsx` | TODO |
| R8-TEST-004 | MEDIUM | DONE: integration coverage now proves that an old place metadata timestamp with a fresh linked stock timestamp remains visible when stale places are hidden and never renders a stale badge. The DB contract also proves that an updated out-of-stock offer refreshes `stock_updated_at`, so a successful sync does not look stale merely because it found no available copy. Verification: `yarn vitest run tests/places.test.ts tests/PlaceBrowser.test.tsx tests/PlaceCard.test.tsx` (63 tests passed). | `tests/PlaceBrowser.test.tsx`, `tests/PlaceCard.test.tsx`, `tests/places.test.ts`, `src/lib/db.ts` | DONE |
| R8-TEST-005 | MEDIUM | Problem: generic `/stock` and VN-detail stock share `StockPanel` but require different default provider scopes. Fix: VN detail now passes `physical` explicitly while `StockLookupClient` passes `all`; runtime tests prove the resulting refresh bodies contain only the physical provider on the VN page and both physical and online providers on generic stock. Evidence: StockPanel, stock lookup, and VN page runtime suites pass 66/66 tests. | `src/components/StockLookupClient.tsx`, `src/app/vn/[id]/page.tsx`, `tests/StockPanel.test.tsx`, `tests/StockLookupClient.test.tsx`, `tests/vn-detail-page-runtime.test.tsx` | DONE |
| R8-TEST-006 | LOW | Problem: coverage commands emitted harmless but noisy JSDOM navigation warnings. Fix: identify tests that intentionally trigger navigation and prevent only the simulated document change. Evidence: the Edition info popover still exercises pointer, mouse, and click propagation on its platform, VN, and release links, but test-local `preventDefault` handlers stop JSDOM navigation. The suite passes 26/26 with no navigation warning. | `tests/EditionInfoPopover.branches.test.tsx` | DONE_WITH_DIFF |
| R8-DOC-001 | MEDIUM | Problem: the active TODO directory previously only had archived reports under `TODO/OLD`, so current audit status was hard to find. Fix: add this active round-8 report in `TODO/` with the canonical five-column format. | `TODO/round8-full-app-audit-report-tasks.md` | DONE_WITH_DIFF |
| R8-DOC-002 | LOW | Problem: CLAUDE.md contains historical notes for AliceNet naming and stock provider capabilities, but the current operational split between `/stock`, `/places`, and AliceNet place detail is easy to regress. Fix: add a short architecture note describing which page owns which stock controls. | `CLAUDE.md`, `README.md`, `FEATURES.md`, `tests/alicenet-branding.test.ts`, `src/components/PlaceDetailClient.tsx`, `src/components/StockLookupClient.tsx` | VERIFIED_EXISTING |
| R8-DOC-003 | LOW | Problem: stock freshness semantics are not documented near the SQL query. Fix: document that `stock_count` counts only current in-stock or limited offers while `stock_updated_at` uses the latest linked offer update, including out-of-stock offers. | `src/lib/db.ts`, `TODO/round8-full-app-audit-report-tasks.md` | DONE_WITH_DIFF |
| R8-DATA-001 | HIGH | Problem: the local workspace DB was corrupt during interrupted work, while a copied server DB passed integrity checks. Fix implemented: the operator runbook prohibits automatic replacement, preserves the original DB/WAL/SHM, validates candidate and staged copies, performs an offline same-filesystem replacement, requires application smoke checks and a reversible write, and documents rollback. No database file is modified automatically. | `docs/SQLITE_RECOVERY_RUNBOOK.md`, `README.md` | DONE_WITH_DIFF |
| R8-DATA-002 | MEDIUM | Problem: stock freshness depends on provider stock rows having accurate `updated_at` values. Fix: add a maintenance panel check that shows latest stock update per provider and flags providers that have not updated after a sync job. Evidence: durable stock batches now retain their selected provider IDs on SQLite and PostgreSQL; `/api/maintenance/stock-providers` compares each canonical live provider's latest status write with the latest completed batch that selected it; `/data` renders localized updated, missed-write, and no-batch states in a responsive third maintenance pane. AliceNet remains intentionally separate and keeps its specialized sync/freshness workflow on its own shop page. The strict decoder, authenticated route, both repository backends, persistence, UI lifecycle, malformed payloads, and migration manifest pass 46 focused tests with targeted `100 / 100 / 100 / 100` coverage. | `src/lib/db.ts`, `src/lib/db/repositories/stock-provider-maintenance.ts`, `src/lib/stock-batch-store.ts`, `src/app/api/maintenance/stock-providers/route.ts`, `src/components/DataMaintenance.tsx`, `db/postgres/migrations/0005_stock_batch_providers.sql` | DONE_WITH_DIFF |
| R8-FUNC-001 | HIGH | Problem: collection load failures on deployed hosts currently collapse to generic client errors when response decoding fails. Fix: log the failing response shape in development, return stable API error codes, and show a precise localized retry state in the library. | `src/components/LibraryClient.tsx`, `src/lib/collection-client-shape.ts`, `src/app/api/collection/route.ts`, `tests/LibraryClient.test.tsx`, `tests/collection-list-route-branches.test.ts`, `tests/collection-client-shape.test.ts` | DONE_WITH_DIFF |
| R8-FUNC-002 | MEDIUM | Problem: VN detail membership mutations relied on `router.refresh`, while dependent client islands retained stale collection-only controls. Fix: a typed `vn:collection-changed` contract now carries authoritative membership, a shared hook filters by VN identity, and resident visibility boundaries update the add banner, action clusters, cover overlay, rotation controls, and banner editing immediately; `router.refresh` remains the server reconciliation fallback. Evidence: add/remove, malformed/foreign events, identity changes, server gating, and image surfaces pass 113 focused tests; the nine-file surface reaches 100/100/100/100 across 448 branches. | `src/lib/vn-collection-events.ts`, `src/lib/use-vn-collection-state.ts`, `src/components/VnCollectionVisibility.tsx`, `src/components/CoverQuickActions.tsx`, `src/components/NotInCollectionBanner.tsx`, `src/components/HeroBanner.tsx`, `src/components/CoverHero.tsx` | DONE |
| R8-FUNC-003 | MEDIUM | Problem: AliceNet page can filter to wishlist using VNDB enrichment, but enrichment timeouts silently make all rows look not wishlisted. Fix: surface a wishlist-enrichment unavailable badge when the timeout fallback is used. Evidence: the route emits `wishlist_available: false` for authentication, timeout, or upstream failure, the decoder preserves it, and the stats tile shows a localized unavailable warning instead of presenting zero as confirmed. | `src/app/api/alicenet/route.ts`, `src/components/AliceNetClient.tsx`, `src/lib/alicenet-client-shape.ts`, `tests/AliceNetClient.test.tsx` | DONE_WITH_DIFF |
| R8-FUNC-004 | LOW | DONE: place detail now preserves all three ownership modes. Standard shops render the normal stock browser, AliceNet-only shops render their special controls directly, and mixed shops expose accessible AliceNet/other-branch tabs so neither stock source hides the other. Controls remain on the linked AliceNet shop page. Verification: 16 component tests, 11 i18n parity tests, typecheck, and targeted 100/100/100/100 coverage across 73 statements, 64 branches, 14 functions, and 65 lines. | `src/components/PlaceDetailClient.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/PlaceDetailClient.branches.test.tsx` | DONE |
| R8-FUNC-005 | MEDIUM | Problem: the audit originally assumed stock and map pages both fetched place-provider mappings and recovered differently. Inspection found no map-page consumer: `/stock` is the only client fetch, while VN detail reads the same mapping server-side. Fix: keep the client lifecycle and server read separate, but normalize both to an explicit map plus availability diagnostic instead of adding a single-consumer hook. Evidence: both consumers now preserve stock results, distinguish an empty map from a failed lookup, and render the same localized non-blocking warning through `StockPanel`; abort and server-failure paths have regressions. | `src/components/StockLookupClient.tsx`, `src/app/vn/[id]/page.tsx`, `src/components/StockPanel.tsx`, `tests/StockLookupClient.test.tsx`, `tests/vn-detail-page-runtime.test.tsx` | DONE_WITH_DIFF |
| R8-LIB-001 | HIGH | Problem: library collection load failures still collapse decoder mismatches into `Error`, which is exactly the deployed failure mode seen when `decodeLibraryCollectionResponse` returns null. Fix: add route-specific decode diagnostics, include a sanitized failure code in the response, and show a precise retry message in the library instead of the generic fallback. | `src/components/LibraryClient.tsx`, `src/lib/collection-client-shape.ts`, `src/app/api/collection/route.ts`, `tests/LibraryClient.test.tsx`, `tests/collection-list-route-branches.test.ts`, `tests/collection-client-shape.test.ts` | DONE_WITH_DIFF |
| R8-LIB-002 | HIGH | Problem: clicking library filters was previously reported to crash the page, and the advanced filters drawer is lazy loaded with a large parameter surface. Fix: add a regression test that opens filters, toggles every boolean facet, validates URL state, and confirms no decoder fallback is hit. | `src/components/LibraryClient.tsx`, `src/components/library/MoreFilters.tsx`, `tests/LibraryClient.test.tsx` | DONE_WITH_DIFF |
| R8-LIB-003 | MEDIUM | Problem: aspect filter materialization used a keyless 30-second collection VN id cache. A newly added VN could miss aspect backfill until the TTL expired. Fix: invalidate this cache from collection mutation routes or replace it with a short-lived version keyed by latest collection update time. Evidence: the cache is now an asynchronous repository-backed module with coalescing, defensive copies, TTL, and an invalidation generation. VNDB add, EGS add, and collection delete invalidate after successful membership changes; real-route tests prove the next read reflects the mutation immediately. The cache reaches 100/100/100/100. | `src/app/api/collection/route.ts`, `src/app/api/collection/[id]/route.ts`, `src/app/api/egs/[id]/add/route.ts`, `src/lib/collection-vn-ids-cache.ts`, `tests/collection-vn-ids-cache.test.ts`, `tests/route-collection-id-core.test.ts`, `tests/egs-add-route-branches.test.ts` | DONE_WITH_DIFF |
| R8-LIB-004 | MEDIUM | Problem: virtual grid spacers were anonymous children and Library cards lacked logical list positions. Fix: the grid now exposes `role=list`, spacers combine `role=presentation` with `aria-hidden`, and every rendered card wrapper carries global `aria-posinset` plus total `aria-setsize`; the invalid row count on a non-grid role is removed. Evidence: scrolled-window tests prove spacer exclusion, fewer DOM items than logical items, total size, and a first visible position greater than one; Library reaches 100/100/100/100 across 776 branches and 78 tests. | `src/components/LibraryClient.tsx`, `tests/LibraryClient.test.tsx`, `tests/LibraryClient.branches.test.tsx` | DONE |
| R8-LIB-005 | MEDIUM | Problem: library filter, sort, group, density, and saved-filter controls compete for one wrapping toolbar. This works on desktop but becomes hard to scan on mobile when several filters are active. Fix: split active filters into a summary chip row and move secondary controls into a clearly labelled overflow panel below `sm`. Evidence: the primary search/filter/options row stays compact; every valid active status, search, facet, range, aspect, dump, and tri-state flag now appears in a named removable summary group with explicit negative-state text; malformed status and tri-state URL values are ignored; secondary sort, grouping, density, selection, random, and bulk actions remain in the labelled mobile-only drawer and the desktop wrapping row is unchanged. LibraryClient passes 79 focused tests at targeted `100 / 100 / 100 / 100` coverage. | `src/components/LibraryClient.tsx`, `src/components/SavedFilters.tsx`, `tests/LibraryClient.test.tsx`, `tests/library-display-controls.test.ts` | DONE_WITH_DIFF |
| R8-LIB-006 | LOW | Problem: request de-duplication aborts the shared fetch when the last consumer releases, but controls/grid split rendering lacked a consumer-count regression. Fix: a keyed two-consumer test now proves one collection request is shared, removing `controls-only` leaves its signal active, no duplicate request starts, and the retained `grid-only` consumer renders the eventual payload. Evidence: the complete `LibraryClient.test.tsx` suite passes 62/62 and typecheck is clean. | `src/components/LibraryClient.tsx`, `tests/LibraryClient.test.tsx` | DONE |
| R8-SEARCH-001 | HIGH | Problem: local search errors are logged to the console only; the user sees no visible failure if `/api/collection/find` or `/api/search/textual` fails. Fix: add a small inline error state and retry action for `TextualSearchPanel` in accordion and standalone modes. | `src/components/TextualSearchPanel.tsx`, `src/lib/collection-find-client-shape.ts`, `src/lib/browse-client-shape.ts`, `tests/TextualSearchPanel.branches.test.tsx` | DONE_WITH_DIFF |
| R8-SEARCH-002 | MEDIUM | DONE: VNDB and EGS quick searches now reject successful late responses after their request signal has been aborted, covering transports and mocks that resolve instead of throwing `AbortError`. Regression tests resolve stale VNDB and EGS payloads after a replacement query and prove only the latest results render. Verified by 53 focused tests, global typecheck, and 100/100/100/100 targeted coverage over 359 statements and 330 branches. | `src/components/SearchClient.tsx`, `tests/SearchClient.branches.test.tsx` | DONE |
| R8-SEARCH-003 | MEDIUM | DONE: advanced search now draws from a single canonical full platform catalog, keeps the 14 common choices first, and exposes uncommon systems in a localized native disclosure without duplicate compatibility aliases. Tests pin catalog order/uniqueness, select MS-DOS through the UI, and verify the URL query. Verified by 53 focused tests, global typecheck, and 100/100/100/100 targeted coverage. | `src/components/SearchClient.tsx`, `src/lib/platform-label.ts`, `src/lib/i18n/dictionaries.ts`, `tests/platform-label.test.ts`, `tests/SearchClient.test.tsx` | DONE |
| R8-SEARCH-004 | LOW | Problem: advanced length chips encode VNDB numeric length buckets with compact labels, but there is no inline hint explaining that those are VNDB length categories rather than hours. Fix: add localized helper text near the length field. | `src/components/SearchClient.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/SearchClient.test.tsx` | DONE_WITH_DIFF |
| R8-SEARCH-005 | MEDIUM | DONE: the EGS add route now returns canonical `needs_mapping`, `upstream_unavailable`, `already_exists`, and `egs_game_not_found` errors with route context; duplicate collection entries are rejected before network access, mapped local VNDB targets are added directly, and missing mapped targets do not create synthetic duplicates. SearchClient maps every code plus malformed success and legacy failures to localized FR/EN/JA messages. Verified by 55 tests, typecheck, and 100/100/100/100 coverage over 381 statements and 344 branches. | `src/components/SearchClient.tsx`, `src/app/api/egs/[id]/add/route.ts`, `src/lib/api-error-read.ts`, `src/lib/i18n/dictionaries.ts`, `tests/egs-add-route-branches.test.ts`, `tests/SearchClient.branches.test.tsx` | DONE |
| R8-WISH-001 | HIGH | Problem: wishlist upstream failures can still appear as generic unavailable states, while auth-required and VNDB-upstream-failed are different operator actions. Fix: make `/api/wishlist` return stable typed states for auth missing, upstream unavailable, rate limited, and malformed payload, then render separate messages. | `src/components/WishlistClient.tsx`, `src/app/api/wishlist/route.ts`, `src/lib/vndb-ui-client-shape.ts`, `tests/wishlist-route-branches.test.ts`, `tests/WishlistClient.branches.test.tsx`, `tests/WishlistClient.test.tsx` | DONE_WITH_DIFF |
| R8-WISH-002 | MEDIUM | DONE: `/api/wishlist` now accepts bounded page, page-size, text, language, platform, rating, year, sort, group, hide-owned, and locale parameters; filtering/sorting/pagination and complete-list facets/download IDs are computed server-side. Requests without pagination parameters retain the legacy `{ items }` shape, and the decoder accepts both contracts strictly. Client mutations reconcile paged counts locally so eventual VNDB consistency cannot reinsert a just-removed row. Evidence: 104 focused tests pass and the four Wishlist production surfaces reach 100/100/100/100 over 651 statements and 654 branches. | `src/components/WishlistClient.tsx`, `src/app/api/wishlist/route.ts`, `src/lib/wishlist-pagination.ts`, `src/lib/vndb-ui-client-shape.ts`, `tests/wishlist-pagination.test.ts`, `tests/wishlist-route-branches.test.ts`, `tests/WishlistClient.test.tsx`, `tests/WishlistClient.branches.test.tsx` | DONE |
| R8-WISH-003 | MEDIUM | Problem: deleting many selected wishlist items fired one request per VN through `Promise.all` without visible per-item progress. Fix implemented: deletion is sequential and reports done, total, failed count, percent, and current title in a live region; Stop aborts the active request, completed removals remain applied, unfinished or failed rows remain selected, and rapid double-click plus late-unmount paths are guarded. Evidence: 57 wishlist tests pass and targeted `WishlistClient.tsx` coverage is 100/100/100/100. | `src/components/WishlistClient.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/WishlistClient.test.tsx`, `tests/WishlistClient.branches.test.tsx` | DONE_WITH_DIFF |
| R8-WISH-004 | LOW | DONE: server grouping now occurs before pagination. Year, developer, language, platform, and status buckets remain whole even when a bucket exceeds the nominal page size; grouped pages have variable row counts and unknown metadata is grouped deterministically. Evidence: all grouping modes, unknown buckets, empty groups, page bounds, and oversized-group behavior are covered in the 100/100/100/100 Wishlist suite. | `src/components/WishlistClient.tsx`, `src/lib/wishlist-pagination.ts`, `tests/wishlist-pagination.test.ts`, `tests/WishlistClient.test.tsx` | DONE |
| R8-WISH-005 | MEDIUM | DONE: `wishlistHideOwned` is a sanitized, persisted Display preference with a default of true and a labeled Settings switch. The Wishlist URL can override it explicitly, invalid/missing URL values fall back to the preference, and a visible active-filter chip resets the filter. Evidence: 86 Wishlist/Settings integration tests pass; persistence, URL overrides, switch reset, and chip behavior are covered. | `src/components/WishlistClient.tsx`, `src/components/SettingsButton.tsx`, `src/lib/settings/client.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/SettingsButton.test.tsx`, `tests/display-settings-validation.test.ts`, `tests/WishlistClient.test.tsx` | DONE |
| R8-VN-001 | HIGH | Problem audited: VN detail action groups previously combined unrelated controls in a dense heterogeneous bar. Verified current implementation: primary collection actions remain inline; Tracking, External links, Media, Data, and Mapping use labeled `ActionMenu` groups with consistent 44 px targets and Lucide alignment; destructive removal is isolated in `CoverQuickActions` danger mode. Collection/EGS gates and responsive target contracts are pinned. Evidence: 117 focused toolbar, gating, artwork, safe-link, responsive, and VN runtime tests pass. | `src/components/VnDetailActionsBar.tsx`, `src/components/CoverQuickActions.tsx`, `tests/vn-detail-actions-bar-runtime.test.tsx`, `tests/vn-toolbar-shape.test.ts`, `tests/responsive-tap-targets.test.ts` | VERIFIED_EXISTING |
| R8-VN-002 | MEDIUM | Problem: platform overflow used a hover/focus `<details>` for the hidden platform list, which was weak on touch devices. Fix: `PlatformOverflowDisclosure` now exposes a real button with expanded state and the shared portal popover, including a mobile bottom sheet, explicit close control, focus handling, complete platform names, and unchanged search-filter URLs. Evidence: page runtime and disclosure tests pass; the new component reaches 100/100/100/100. | `src/app/vn/[id]/page.tsx`, `src/components/PlatformOverflowDisclosure.tsx`, `tests/PlatformOverflowDisclosure.test.tsx` | DONE |
| R8-VN-003 | MEDIUM | Problem: cover and banner edit controls were visually heavy over artwork on phones even when labels were hidden. Fix: one responsive artwork surface now owns cover/banner source selection, upload, cover rotation/reset, and banner focal editing; resident pickers and VN-scoped events preserve mutation lifecycles while non-contextual image overlays stay desktop-only. Evidence: malformed and foreign events, dialog ownership, popover dismissal, transforms, and server integration are covered; the changed surface is 100/100/100/100. | `src/components/ArtworkTransformControls.tsx`, `src/components/BannerPickerTrigger.tsx`, `src/components/BannerSourcePicker.tsx`, `src/components/HeroBanner.tsx`, `src/components/CoverRotationButtons.tsx` | DONE |
| R8-VN-004 | MEDIUM | DONE: a genuinely empty cover now shows one 44 px upload action in the cover slot, and an empty banner shows the equivalent action in the banner slot, including hide-images mode. Both delegate to the existing resident source pickers through VN-scoped events, so upload, URL, VNDB, EGS, and gallery choices keep one mutation owner. The banner action follows collection membership events immediately and neither action appears outside the collection. Verified by global typecheck, 68 functional tests, and 100/100/100/100 targeted coverage over 463 statements and 526 branches. | `src/app/vn/[id]/page.tsx`, `src/components/CoverPickerTrigger.tsx`, `src/components/BannerPickerTrigger.tsx`, `src/components/HeroBanner.tsx`, `tests/HeroBanner.test.tsx`, `tests/HeroBanner.branches.test.tsx`, `tests/vn-detail-page-runtime.test.tsx` | DONE |
| R8-VN-005 | MEDIUM | DONE: one compact localized score legend now defines unified, VNDB, EGS, and personal score semantics across the VN score summary and EGS comparison panel. Each source chip is keyboard focusable and exposes a precise tooltip; compare source controls use the matching shared source-choice tooltip contract. Verified by global typecheck, 52 functional and dictionary tests, and 100/100/100/100 targeted coverage over 257 statements and 240 branches. | `src/components/ScoreSourceLegend.tsx`, `src/components/ScoreSection.tsx`, `src/components/EgsPanel.tsx`, `src/components/SourceChoiceTooltip.tsx`, `tests/ScoreSourceLegend.test.tsx`, `tests/ScoreSection.test.tsx`, `tests/EgsPanel.branches.test.tsx` | DONE |
| R8-VN-006 | LOW | DONE: VN title resolution is extracted into a typed shared helper that returns one primary/alternate pair for metadata, the visible heading, recent views, cover alt text, and stock lookup. It promotes the shortest full candidate containing a truncated cache title while preserving a useful alternate; the `Gakuen` to `Josou Gakuen` regression proves browser and on-page titles remain identical. Verified by global typecheck, 20 tests, and 100/100/100/100 coverage over 207 statements and 344 branches. | `src/lib/vn-display-title.ts`, `src/app/vn/[id]/page.tsx`, `tests/vn-display-title.test.ts`, `tests/vn-detail-page-runtime.test.tsx` | DONE |
| R8-COMPARE-001 | HIGH | Problem: the previous seiyuu compare mismatch needs a locked regression, because compare joins can overcount or mis-match voice actor credits when aliases and character links differ between two VNs. Fix: add synthetic compare fixtures for one shared VA, one alias-only VA, and one unrelated VA, then assert the compare summary counts. Evidence: synthetic DB fixtures now prove canonical `sid` matching across different alias ids/names, reject unrelated actors that share a display name, retain per-VN characters and distinct route notes, and sort shared actors deterministically; `compare-credits.ts` is covered at 100/100/100/100. | `src/app/compare/page.tsx`, `src/lib/compare-credits.ts`, `src/lib/db.ts`, `tests/compare-shared-vas.test.ts`, `tests/compare-page-runtime.test.tsx` | DONE_WITH_DIFF |
| R8-COMPARE-002 | MEDIUM | Problem: `FieldCompare` now renders source tabs even when one linked side is blank, but keyboard navigation only handles left and right arrows. Fix: add Home and End handling and tests for empty-side tabs with `egsLinked`. | `src/components/FieldCompare.tsx`, `tests/FieldCompare.test.tsx` | DONE_WITH_DIFF |
| R8-COMPARE-003 | MEDIUM | DONE: compare now renders one semantic, vertically stacked VN card per item below `md`, with repeated field labels, explicit empty states, shared-value highlighting, full cover/title links, and VNDB versus personal rating badges. The dense scrollable matrix remains available from `md` upward only. Shared render helpers keep platforms, developers, tags, staff, and seiyuu identical across both layouts. Verified by global typecheck, 4/4 runtime tests, and 100/100/100/100 targeted coverage over 143 statements and 112 branches. | `src/app/compare/page.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/compare-page-runtime.test.tsx` | DONE |
| R8-COMPARE-004 | LOW | DONE: a shared `SourceChoiceTooltip` now explains Auto fallback order plus VNDB, EGS, and Custom persistence semantics in FR/EN/JA. It wraps compact default/source actions in field, brand, and cover comparisons while preserving their existing labels and mutations; focus-based integration tests cover each surface. Verified by global typecheck, 58 functional tests, and 100/100/100/100 coverage over 262 statements and 298 branches. | `src/components/SourceChoiceTooltip.tsx`, `src/components/FieldCompare.tsx`, `src/components/BrandCompare.tsx`, `src/components/CoverCompare.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/SourceChoiceTooltip.test.tsx`, `tests/FieldCompare.test.tsx`, `tests/BrandCompare.branches.test.tsx`, `tests/CoverCompare.test.tsx` | DONE |
| R8-SHELF-001 | HIGH | Problem: the read-only shelf clips through `ShelfScrollFrame`, but the original right-edge fade needed browser evidence. Progress: browser QA now forces a 900 px viewport, proves max-content extends beyond an overflow frame, checks right-only fade at origin, both fades mid-scroll, and left-only fade at the end. Keep open until final isolated `yarn qa` passes. | `src/components/ShelfScrollFrame.tsx`, `src/components/ShelfSpatialView.tsx`, `scripts/browser-interactions.mjs` | TODO |
| R8-SHELF-002 | MEDIUM | DONE: display rows keep the physical shelf geometry by default, while a new localized Geometric/Compact segmented control can hide empty display cells and reduce the row tracks to occupied items. The preference is validated, persisted globally or per shelf, exposed through a shelf-root data attribute, and applied immediately without a server refresh in regular and fullscreen views. Verified by global typecheck, 81 focused tests, and 100/100/100/100 targeted coverage over 415 statements and 337 branches. | `src/lib/shelf-view-prefs.ts`, `src/components/ShelfSpatialView.tsx`, `src/components/ShelfReadOnlyControls.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/shelf-view-prefs.test.ts`, `tests/shelf-display-overrides.test.ts`, `tests/ShelfReadOnlyControls.test.tsx`, `tests/ShelfReadOnlyControls.branches.test.tsx`, `tests/ShelfSpatialView.test.tsx` | DONE |
| R8-SHELF-003 | HIGH | Problem: the shelf editor info button was reported as unclickable during drag and drop. Fix: stop pointer-start propagation from info and action buttons and add a regression that clicks info while drag handles are present. Evidence: the real trigger already stops pointer, mouse, touch, and click propagation before the draggable parent can receive them; the popover also isolates pointer and mouse starts. A parent-spy regression now proves the trigger remains clickable without initiating drag, all 119 shelf/editor tests pass, and `EditionInfoPopover.tsx` reaches 100/100/100/100 targeted coverage. | `src/components/ShelfLayoutEditor.tsx`, `src/components/EditionInfoPopover.tsx`, `tests/EditionInfoPopover.branches.test.tsx`, `tests/ShelfLayoutEditor.drag.test.tsx` | VERIFIED_EXISTING |
| R8-SHELF-004 | MEDIUM | DONE: a shared nine-level CSS scale now orders map, navigation, fullscreen, status, popover, modal, toast, confirmation, and accessibility surfaces. Shelf fullscreen/editor, desktop and mobile navigation, status bars, portal/context popovers, every global modal family, map canvas, toasts, confirmations, and the skip link use semantic layer classes instead of independent numbers. Portal popovers moved below dialogs while remaining above content; the only remaining numeric fixed layer is the intentionally low quote footer. A source contract locks both strict numeric order and component assignments. Verified by global typecheck, 196 focused tests, and a clean fixed-overlay source scan. | `src/app/globals.css`, `src/components/ShelfSpatialFullscreen.tsx`, `src/components/ShelfLayoutEditor.tsx`, `src/components/ToastProvider.tsx`, `src/components/DownloadStatusBar.tsx`, `src/components/Dialog.tsx`, `src/components/ConfirmDialog.tsx`, `src/components/MapCanvas.tsx`, `src/components/MoreNavMenu.tsx`, `src/app/layout.tsx`, `tests/ui-layer-scale.test.ts` | DONE |
| R8-SHELF-005 | MEDIUM | DONE: physical bundles are now first-class collection units linking two or more owned VN releases to one anchor object. SQLite and PostgreSQL schemas enforce owned-member, anchor, uniqueness, and cascade invariants; transactional creation removes secondary placements, shelf projections hide duplicate members, and placement rejects non-anchor members. The responsive manager supports search, pagination, explicit anchor selection, creation, confirmed dissolution, loading/error states, and immediate shelf/pool reconciliation. Editor, read-only shelf, drag snapshots, and edition popovers consistently render the bundle name and member count while retaining release artwork and the anchor VN link. Verified by global typecheck, 319 shelf-domain tests, 19 focused API/UI/decoder tests, and 100/100/100/100 targeted coverage over 231 statements and 158 branches. | `src/lib/db.ts`, `db/postgres/migrations/0006_physical_bundles.sql`, `src/app/api/physical-bundles/**`, `src/lib/physical-bundle-client-shape.ts`, `src/components/PhysicalBundleDialog.tsx`, `src/components/ShelfLayoutEditor.tsx`, `src/components/ShelfSpatialView.tsx`, `src/components/EditionInfoPopover.tsx`, `tests/physical-bundles.test.ts`, `tests/physical-bundle-routes.test.ts`, `tests/PhysicalBundleDialog.test.tsx` | DONE |
| R8-SHELF-006 | MEDIUM | DONE: the DB projection already selected exact-release artwork with `pkgfront` priority, but the read-only spatial cards discarded it and always rendered the shared VN cover. Shelf cells and face-out display cards now apply release URL, thumbnail, local thumbnail, and sexual rating before VN fallback. A DB regression places two boxed editions of one VN in a normal slot and a display slot and proves their `rel_*` artwork remains distinct; a component regression proves both images reach `SafeImage`. Verified by global typecheck, 60 focused functional tests, and 100/100/100/100 targeted component coverage over 48 statements and 63 branches. | `src/lib/db.ts`, `src/components/ShelfSpatialView.tsx`, `tests/shelf-slot-owned-fields.test.ts`, `tests/ShelfSpatialView.test.tsx` | DONE |
| R8-DUMP-001 | MEDIUM | Problem: dumped ignore is now a first-class toggle and ignored tab, which matches the requested exclude flow. Fix: keep it and add an integration test proving ignored VNs are removed from `all` counts and restored after toggle. | `src/components/DumpIgnoreButton.tsx`, `src/app/dumped/page.tsx`, `src/lib/db.ts` | VERIFIED_EXISTING |
| R8-DUMP-002 | MEDIUM | Problem: dumped tracker cards include progress, shelf deep links, and edition CTAs in one card. On narrow cards, the shelf action can dominate the layout. Fix: move secondary actions into a footer row that wraps below the progress section. | `src/app/dumped/page.tsx`, `src/lib/dumped-links.ts`, `tests/dumped-page-runtime.test.tsx` | VERIFIED_EXISTING |
| R8-DUMP-003 | LOW | Problem: ignored rows use the same card visual density as active missing rows, so exclusion state can be missed when switching tabs. Fix: add a muted ignored-state banner inside ignored cards. | `src/app/dumped/page.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/dumped-page-runtime.test.tsx` | DONE_WITH_DIFF |
| R8-IMG-001 | HIGH | Problem: custom native image implementations could bypass the project skeleton and error-placeholder contract. Fix implemented: all production TSX was audited; native image elements are confined to `SafeImage`, `LoadingImage`, and `HeroBanner`. The hero keeps its primary image transparent behind a skeleton until load, mounts its same-source contain layer only after success, and replaces errors with `ImageOff`. A recursive source contract now rejects new native image files outside those audited implementations, and an explicit lifecycle test covers skeleton-to-visible rendering. Evidence: 49 focused tests pass and `HeroBanner.tsx` is 100/100/100/100. | `src/components/SafeImage.tsx`, `src/components/LoadingImage.tsx`, `src/components/HeroBanner.tsx`, `tests/image-loading-skeletons.test.ts`, `tests/HeroBanner.branches.test.tsx` | DONE_WITH_DIFF |
| R8-IMG-002 | MEDIUM | Problem: lightweight image wrappers track load only per mount, so navigating away and back can still show skeletons for browser-cached images. Fix: promote loaded URL caching to a small module-level cache with bounded size for `LoadingImage`. | `src/components/LoadingImage.tsx`, `tests/image-and-collection-banner-runtime.test.tsx` | DONE_WITH_DIFF |
| R8-IMG-003 | MEDIUM | Problem: `LoadingImage` sets opacity on load but does not use `HTMLImageElement.decode`, so a loaded event can still paint before decode completes on some browsers. Fix: call `decode()` when available before flipping to visible, with an error fallback. | `src/components/LoadingImage.tsx`, `tests/image-and-collection-banner-runtime.test.tsx` | DONE_WITH_DIFF |
| R8-IMG-004 | LOW | DONE: failed or missing `SafeImage` and `LoadingImage` placeholders now expose a localized failure label, append the image title only when non-empty, and remain fully hidden for decorative images. Voluntary hide-images placeholders retain the original image identity. SSR fixtures now provide the real i18n context. Verified by global typecheck, 41 functional tests, and 44 coverage tests at 100/100/100/100 over 111 statements and 109 branches. | `src/components/SafeImage.tsx`, `src/components/LoadingImage.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/SafeImage.test.tsx`, `tests/image-and-collection-banner-runtime.test.tsx`, `tests/image-loading-skeletons.test.ts` | DONE |
| R8-SETTINGS-001 | HIGH | Problem: the settings layout tab previously showed density controls overlapping spacing controls on small widths. Source now uses separate rows, but there is no browser regression for the exact responsive breakpoint and locale mix. Fix: add a mobile settings screenshot or Playwright test around per-page layout controls. | `src/components/settings/LayoutSettingsTab.tsx`, `tests/responsive-tap-targets.test.ts` | DONE_WITH_DIFF |
| R8-SETTINGS-002 | MEDIUM | VERIFIED EXISTING: the two heavy settings domains are already isolated into `LayoutSettingsTab` and `IntegrationsSettingsTab`, loaded through separate `next/dynamic` boundaries with SSR disabled and stable skeleton fallbacks. Each chunk mounts only when its tab is active; lightweight Display, Content, Library, Account, Automation, and Shortcuts controls remain inline to avoid unnecessary request fragmentation. Verification also updated the responsive density-track source contract from the obsolete five-column overlapping layout to the current three-column minus/slider/plus layout. Global typecheck and 36 lazy-boundary, tab-a11y, skeleton, and behavior tests pass. | `src/components/SettingsButton.tsx`, `src/components/settings/LayoutSettingsTab.tsx`, `src/components/settings/IntegrationsSettingsTab.tsx`, `tests/settings-layout-lazy-boundary.test.ts`, `tests/settings-per-page-layout-skeleton.test.ts` | VERIFIED_EXISTING |
| R8-SETTINGS-003 | LOW | DONE: the unified per-page layout panel now resolves the currently open route scope and, when that scope has an explicit width, shows a localized live status row with the page name, Custom chip, and active preset beside the width controls. The status appears and disappears immediately as the override is created or reset, with no persistent badge clutter on pages that inherit defaults. | `src/components/settings/LayoutSettingsTab.tsx`, `src/lib/page-space.ts`, `tests/LayoutSettingsTab.test.tsx` | DONE |
| R8-SETTINGS-004 | MEDIUM | DONE: page width and scoped density consumers were audited around the single `DisplaySettingsProvider`; the root `PageSpaceFrame` covers every route, every card-density surface is source-checked for a matching `DensityScopeProvider`, and VN/other detail layouts retain their dedicated live layout events. A shared-tree interaction regression now changes width and density through `set()` and proves the open frame attributes, max-width CSS variable, and scoped density CSS variable update without navigation or refresh. Verified by global typecheck, 70 focused tests, and 100/100/100/100 targeted coverage over 233 statements and 181 branches. | `src/lib/settings/client.tsx`, `src/components/PageSpaceFrame.tsx`, `src/components/DensityScopeProvider.tsx`, `src/components/VnDetailLayout.tsx`, `tests/PageSpaceFrame.test.tsx`, `tests/density-scope-coverage.test.ts`, `tests/VnDetailLayout.test.tsx` | DONE |
| R8-TOUR-001 | MEDIUM | DONE: the guided tour now covers 20 destinations including canonical local search, compare, stock, places, AliceNet through its shop-list flow, and map. A typed navigation registry is the single source for grouped navbar routes and tour destinations, while AliceNet remains absent from global navigation and no dynamic shop identifier is hardcoded. FR/EN/JA content, the full route sequence, navbar behavior, and registry completeness are covered. Global typecheck and 30 focused tests pass. | `src/lib/navigation-registry.ts`, `src/components/TutorialTour.tsx`, `src/components/MoreNavMenu.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/navigation-registry.test.ts`, `tests/TutorialTour.test.tsx`, `tests/MoreNavMenu.branches.test.tsx` | DONE |
| R8-TOUR-002 | LOW | Problem: tour auto-start is correctly limited to the home page, avoiding deep-link hijacks. Fix: keep this behavior and add a regression for `/stock`, `/places`, and `/vn/...` not being redirected by first-run tour state. | `src/components/TutorialTour.tsx`, `tests/TutorialTour.test.tsx` | VERIFIED_EXISTING |
| R8-TOUR-003 | LOW | DONE: the non-modal tour now has a disabled-at-start Back control plus a collapsible, touch-safe ordered step list with direct navigation and `aria-current=step`. Focus and live announcements remain intact after backward and direct jumps. Global typecheck and 30 focused behavior, accessibility, navigation, and registry tests pass. | `src/components/TutorialTour.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/TutorialTour.test.tsx`, `tests/tutorial-tour-a11y.test.ts` | DONE |
| R8-KEYS-001 | MEDIUM | DONE: typed shortcut sections now cover shelf, map, compare, search, and stock with only visible, reversible operations. Stable `data-shortcut` hooks wire shelf fullscreen/options, map add/reset, compare add/run, search source tabs, and stock refresh; batch navigation scrolls to its semantic section. The global `g <key>` prefix now correctly wins over conflicting page keys. FR/EN/JA help is complete in both contextual help and settings. Global typecheck and 368 focused registry, handler, and component tests pass. | `src/components/KeyboardShortcuts.tsx`, `src/lib/shortcut-registry.ts`, `src/components/ShelfSpatialFullscreen.tsx`, `src/components/ShelfLayoutEditor.tsx`, `src/components/ShelfReadOnlyControls.tsx`, `src/components/MapPageClient.tsx`, `src/components/CompareVnPicker.tsx`, `src/components/SearchClient.tsx`, `src/components/StockPanel.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/KeyboardShortcuts.test.tsx`, `tests/shortcut-registry.test.ts` | DONE |
| R8-KEYS-002 | LOW | DONE: the global `/` shortcut targets the shared `data-vn-search`, native search type/input mode, and search role contracts. Audit of the newly scoped pages found the map combobox missing that marker; it now participates without changing its combobox semantics. Focus/select and fallback behavior remain covered, and the broader keyboard/component run passes 368 tests after global typecheck. | `src/components/KeyboardShortcuts.tsx`, `src/components/MapPageClient.tsx`, `tests/KeyboardShortcuts.test.tsx`, `tests/MapPageClient.branches.test.tsx` | DONE |
| R8-I18N-007 | MEDIUM | Problem: most date surfaces now use shared locale helpers, but raw upstream date strings remain in rich EGS details. Fix: parse and format EGS registration or update dates through `formatIsoDateString` or a locale-aware helper. | `src/components/EgsRichDetails.tsx`, `src/lib/locale-number.ts`, `tests/EgsRichDetails.branches.test.tsx` | DONE_WITH_DIFF |
| R8-I18N-008 | MEDIUM | DONE: `platformLabel` now accepts the typed UI locale, applies French and Japanese names where platform terminology differs, preserves official brand names and uppercase unknown-code fallbacks, and keeps English as the compatibility default. Locale is passed explicitly through search, wishlist, releases, owned editions, edition popovers, shelf views/editor, stats, VN/release detail, and compare. Verified by global typecheck, 329 focused surface tests across two runs, and 100/100/100/100 helper coverage. | `src/lib/platform-label.ts`, `src/components/SearchClient.tsx`, `src/components/WishlistClient.tsx`, `src/components/OwnedEditionsSection.tsx`, `src/components/EditionInfoPopover.tsx`, `src/components/ReleasesSection.tsx`, `src/components/ShelfLayoutEditor.tsx`, `src/app/stats/page.tsx`, `src/app/shelf/page.tsx`, `src/app/vn/[id]/page.tsx`, `src/app/release/[id]/page.tsx`, `src/app/compare/page.tsx`, `tests/platform-label.test.ts` | DONE |
| R8-I18N-009 | LOW | Problem: tour route for the current year uses `new Date().getFullYear()` in the client. That follows local time, but tests and SSR can observe different calendar years around midnight. Fix: inject the server locale date or share one current-year helper. | `src/components/TutorialTour.tsx`, `src/components/KeyboardShortcuts.tsx`, `src/lib/locale-number.ts`, `tests/locale-number.test.ts` | DONE_WITH_DIFF |
| R8-I18N-010 | MEDIUM | DONE: FR/EN/JA dictionaries now contain one shared acronym glossary and compare/search source controls render it through the semantic `AcronymLabel` component while retaining canonical accessible names. Ten targeted files passed with 208 tests, dictionary parity passed, typecheck passed, and the new component reached 100/100/100/100 coverage. | `src/lib/i18n/dictionaries.ts`, `src/components/AcronymLabel.tsx`, `src/components/SearchClient.tsx`, `src/components/FieldCompare.tsx`, `tests/AcronymLabel.test.tsx` | DONE |
| R8-ACCESS-006 | MEDIUM | Problem: `TextualSearchPanel` hides failed local-search states and has no live region for completed hit count changes in accordion mode. Fix: add visible error and `aria-live` count announcements. | `src/components/TextualSearchPanel.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/TextualSearchPanel.branches.test.tsx` | DONE_WITH_DIFF |
| R8-ACCESS-007 | MEDIUM | Problem: `WishlistClient` selects and bulk deletes items, but selected count and deletion progress are not announced as a live region outside the floating action bar. Fix: add a polite status region tied to selection and mutation state. | `src/components/WishlistClient.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/WishlistClient.test.tsx` | DONE_WITH_DIFF |
| R8-ACCESS-008 | MEDIUM | DONE: `card-action-overlay` is now the shared visibility contract for VN-card actions, list-card actions, media actions, and cover editing. Actions remain visible and interactive by default for coarse/touch pointers, hide only under a hover-capable fine-pointer media query, and return on group hover, group focus, or direct focus-visible. The cover overlay keeps the compact mobile toolbar flow, remains 44 px on touch tablets, and compacts to 36 px only for hover-capable pointers. Ten focused files pass 102 tests, including a dedicated four-surface CSS contract. | `src/app/globals.css`, `src/components/VnCard.tsx`, `src/components/ListCardActions.tsx`, `src/components/MediaGallery.tsx`, `src/components/CoverEditOverlay.tsx`, `tests/card-action-overlay.test.ts`, `tests/responsive-tap-targets.test.ts` | DONE |
| R8-ACCESS-009 | LOW | Problem: `Tooltip` is keyboard dismissible, but it uses absolute positioning and `whitespace-nowrap`, which can overflow narrow screens. Fix: clamp tooltip width, allow wrapping, and add a viewport-edge test. | `src/components/Tooltip.tsx`, `tests/display-components-runtime.test.tsx` | DONE_WITH_DIFF |
| R8-ACCESS-010 | MEDIUM | Problem: `MarkdownView` renders tables in horizontally scrollable blocks, but table captions and header associations are not enforced for imported markdown. Fix: add accessible table wrappers or document markdown limitations in user-facing help. | `src/components/MarkdownView.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/MarkdownView.test.tsx` | DONE_WITH_DIFF |
| R8-SEC-006 | CRITICAL | DONE: public reads now have an explicit deployment policy. `VN_PUBLIC_READ_AUTH=token` applies the existing localhost-or-token gate to every GET/HEAD API request, `upstream` records that the reverse proxy owns authentication without adding a second browser challenge, and an unset or invalid value preserves local compatibility while showing the public-host warning. `/api/health` remains exempt for deployment probes. The warning is based on configured read protection rather than the unrelated admin-token presence. Verified by global typecheck and 62/62 focused policy, proxy, warning, route, loopback, trusted-proxy, and health tests. | `src/lib/api-route-meta.ts`, `src/proxy.ts`, `src/app/layout.tsx`, `src/lib/public-read-warning.ts`, `src/app/api/health/route.ts`, `tests/public-read-auth.test.ts`, `tests/public-read-warning.test.ts`, `tests/root-composition-runtime.test.tsx` | DONE |
| R8-SEC-007 | HIGH | Problem: mutating API requests are protected by the CSRF proxy, but route tests did not cover multipart upload paths through the proxy. Fix: add proxy-level tests for JSON mutation allowed, form mutation rejected, multipart same-origin allowed, and cross-site multipart rejected. Evidence: a no-mock integration suite calls the real root proxy for collection JSON, form, cover multipart, and banner multipart requests; same-origin upload and JSON paths continue, while simple forms return 415 and cross-site multipart returns 403. Proxy plus guard reach 100/100/100/100. | `src/proxy.ts`, `src/lib/csrf.ts`, `src/components/CoverUploader.tsx`, `src/components/BannerControls.tsx`, `tests/proxy-csrf-integration.test.ts`, `tests/proxy-runtime.test.ts`, `tests/csrf.test.ts` | DONE_WITH_DIFF |
| R8-SEC-008 | HIGH | Problem: backup export and restore are gated by localhost or token, and restore has content-length plus SQLite magic checks. Fix: keep this and add a regression that oversized uploads are rejected before buffering. | `src/app/api/backup/route.ts`, `src/app/api/backup/restore/route.ts`, `src/lib/read-limited-body.ts` | VERIFIED_EXISTING |
| R8-SEC-009 | HIGH | Problem: file serving is gated and forces SVG attachments with restrictive CSP, which protects against inline SVG script execution. Fix: keep this and add regression coverage for path traversal, SVG content type, content disposition, and filename sanitization. | `src/app/api/files/[...path]/route.ts`, `src/lib/files.ts` | VERIFIED_EXISTING |
| R8-SEC-010 | MEDIUM | Problem: `safeHref` protects rendered links, but not every component using data-derived outbound URLs has an explicit test asserting unsafe schemes are suppressed. Fix: add component contract tests for release links, place links, stock offer links, and EGS rich links. | `src/lib/safe-href.ts`, `src/components/ReleasesSection.tsx`, `src/components/PlaceDetailClient.tsx`, `src/components/PlaceBrowser.tsx`, `src/components/PlaceCard.tsx`, `src/components/StockPanel.tsx`, `src/components/EgsRichDetails.tsx`, `tests/ReleasesSection.branches.test.tsx`, `tests/PlaceDetailClient.branches.test.tsx`, `tests/PlaceBrowser.test.tsx`, `tests/PlaceCard.test.tsx`, `tests/StockPanel.test.tsx`, `tests/EgsRichDetails.branches.test.tsx` | DONE_WITH_DIFF |
| R8-SEC-011 | MEDIUM | Problem: the SSRF allowlist contains many provider domains. That is necessary for stock features but risky to grow casually. Fix: add a provider-domain ownership test that requires each allowed host to be referenced by a provider capability or documented image source. | `src/lib/url-allowlist.ts`, `src/lib/stock-provider-capabilities.ts`, `tests/url-allowlist.test.ts` | DONE_WITH_DIFF |
| R8-SEC-012 | MEDIUM | DONE: provider-map and unassigned-branch reads participate in the optional global read-auth policy through the proxy and also invoke the shared helper inside their handlers, so direct route execution cannot bypass token mode. Upstream mode remains single-challenge by design, preserving Safari compatibility behind reverse-proxy authentication. Verified by global typecheck and 62/62 focused tests. | `src/app/api/places/provider-map/route.ts`, `src/app/api/places/unassigned/route.ts`, `src/lib/api-route-meta.ts`, `src/proxy.ts`, `tests/public-read-auth.test.ts`, `tests/places-readonly-routes.test.ts` | DONE |
| R8-SEC-013 | MEDIUM | DONE: native Safari uses polling instead of the long-lived download-status EventSource after Basic Auth navigation. Historical Nginx evidence identified the second prompt root cause: iOS authenticated the document, but its separate networking process requested `favicon.ico` and both Apple touch icon names without credentials. A bounded Nginx snippet returns a cacheable 204 only for those discovery names while pages, arbitrary static paths, and APIs remain authenticated. The active vhost was rechecked with anonymous icon 204 and root, arbitrary-path, and API 401 responses. A clean native Safari session then displayed one challenge, loaded Library after one valid authentication, remained prompt-free after the delayed icon window, and navigated to Wishlist without another challenge. The optional app read-auth `upstream` mode also avoids a duplicate application challenge. | `src/components/DownloadStatusBar.tsx`, `ops/nginx/vndb-public-icons.conf`, `README.md`, `tests/DownloadStatusBar.test.tsx`, `tests/nginx-basic-auth-contract.test.ts`, active Nginx vhost | DONE |
| R8-PERF-006 | HIGH | DONE: the long local result surfaces are server-windowed and now share validated pagination primitives. AliceNet uses offset metadata with derived `has_more`; Places uses the same bounded offset window; Wishlist extends shared one-based page metadata with its group-aware flag. Shared factories and decoders prevent response-shape drift. Search is deliberately provider-windowed rather than locally paginated: quick VNDB, quick EGS, and advanced VNDB requests use shared explicit limits of 30, 40, and 50, while the server caps advanced windows/pages at 100. No surface repeatedly loads a complete long list. Verified by global typecheck, 325 pagination/consumer tests, 69 search tests, and 100/100/100/100 coverage over the shared pagination contract. | `src/lib/server-pagination.ts`, `src/lib/search-result-limits.ts`, `src/lib/wishlist-pagination.ts`, `src/lib/place-registry-page.ts`, `src/lib/alicenet-client-shape.ts`, `src/components/SearchClient.tsx`, `tests/server-pagination.test.ts`, `tests/search-result-limits.test.ts` | DONE |
| R8-PERF-007 | MEDIUM | DONE: `SafeImage` now subscribes to a shared `IntersectionObserver` pool keyed by root margin and threshold. Multiple images share one browser observer, each target still triggers once, unmount cleanup is idempotent, and an empty pool disconnects itself. The no-observer fallback remains immediate. Verification: 20 focused tests pass and targeted coverage is 100/100/100/100 across 128 statements, 98 branches, 19 functions, and 111 lines. | `src/components/SafeImage.tsx`, `src/lib/intersection-observer-pool.ts`, `tests/SafeImage.test.tsx`, `tests/intersection-observer-pool.test.ts` | DONE |
| R8-PERF-008 | MEDIUM | DONE: Settings already deferred layout and integrations tabs. VN detail now keeps cover source, banner source, and VN-to-EGS mapping dialogs behind client-only dynamic owners. Lightweight triggers preserve stable geometry and preload on pointer or keyboard focus; the owners retain the first scoped request while the chunk loads, open immediately after mount, reset across VN identities, and remount mapping cleanly on later opens. Direct dialog defaults remain backward compatible. Verified by global typecheck, 135/135 focused tests, 100/100/100/100 targeted coverage over both new owners, and a successful production Next.js build. | `src/components/SettingsButton.tsx`, `src/components/LazyArtworkPickers.tsx`, `src/components/LazyMapVnToEgsButton.tsx`, `src/components/CoverSourcePicker.tsx`, `src/components/BannerSourcePicker.tsx`, `src/components/MapVnToEgsButton.tsx`, `src/components/VnDetailActionsBar.tsx`, `tests/vn-detail-dialog-lazy-boundary.test.ts` | DONE |
| R8-PERF-009 | LOW | DONE: the panel now queries the bounded local title endpoint first. In secondary accordion mode it skips the 50-row notes/custom-description/quote query only when at least six normalized primary or alternate titles exactly match or prefix-match the query. Explicit standalone local search always queries textual fields, so completeness is preserved when the user chooses that source. Abort ownership rejects both late local and late textual responses. Verified by global typecheck, 24 focused tests, and 100/100/100/100 coverage over 96 statements and 98 branches. | `src/components/TextualSearchPanel.tsx`, `src/lib/textual-search-policy.ts`, `tests/TextualSearchPanel.branches.test.tsx`, `tests/textual-search-policy.test.ts` | DONE |
| R8-PERF-010 | MEDIUM | DONE_WITH_DIFF: compare remains a bounded SSR matrix because the route accepts at most four VN and already caps tags, staff, and voice credits; moving synchronous SQLite reads into client accordions would add hydration and duplicate-fetch cost. Off-screen mobile cards and the desktop matrix now use `content-visibility: auto` with stable intrinsic sizes, while `SafeImage` keeps image requests lazy. A render-budget contract locks the four-VN and rich-list caps plus both paint-containment paths; the focused compare suite passes 12/12 tests. | `src/app/compare/page.tsx`, `tests/compare-render-budget.test.ts`, `tests/compare-shared-vas.test.ts` | DONE_WITH_DIFF |
| R8-TYPE-005 | MEDIUM | DONE: the shared `ApiErrorBody` contract is discriminated by `ok: false` and requires sanitized `error`, stable `code`, and stable `context`; its strict decoder remains backward-compatible with legacy `{ error, code? }` routes. Central upstream/internal helpers, normalized DB failures, AliceNet routes, collection, and wishlist now emit the canonical body. Modules reached 100/100/100/100 coverage, AliceNetClient reached 100/100/100/100, route snapshots pass, and typecheck passes. | `src/lib/api-error-shape.ts`, `src/lib/api-error-read.ts`, `src/lib/api-error.ts`, `src/lib/alicenet-api-error.ts`, `tests/api-error-shape.test.ts`, `tests/upstream-error-helper.test.ts`, `tests/internal-error-helper.test.ts` | DONE |
| R8-TYPE-006 | MEDIUM | DONE: URL integer, finite-number, canonical boolean, and enum parsing now live in one typed contract that distinguishes missing from invalid values. Library retains strict 400 behavior for malformed filters, Wishlist and Places retain bounded fallbacks, and Search neutralizes invalid advanced state. Stock already uses the typed `page-query` parser. Verified by global typecheck, 148 consumer tests, and 100/100/100/100 coverage across all 45 parser branches. | `src/lib/query-params.ts`, `src/lib/page-query.ts`, `src/app/api/collection/route.ts`, `src/lib/wishlist-pagination.ts`, `src/lib/place-registry-page.ts`, `src/components/SearchClient.tsx`, `src/components/WishlistClient.tsx`, `tests/query-params.test.ts` | DONE |
| R8-TYPE-007 | LOW | Problem: `PUBLIC_READ_ROUTE` is a documentation marker, not an enforceable route policy. Fix: add a static test that every public read route imports the marker and every mutating route imports `requireLocalhostOrToken`. | `src/lib/api-route-meta.ts`, `src/app/api/places/[id]/stock/route.ts`, `tests/api-route-policy.test.ts` | DONE_WITH_DIFF |
| R8-TEST-007 | HIGH | Problem: 100 percent unit coverage does not include browser evidence for Safari nav overflow, map modal z-index, settings overlap, shelf fade, and VN mobile cover controls. Progress: the browser runner already covered narrow settings and VN detail; it now adds Chromium plus WebKit navigation geometry/navigation, Leaflet modal hit testing, and shelf clipping/fade transitions. Keep open until all scenarios pass during final isolated QA. | `scripts/browser-interactions.mjs`, `src/components/MoreNavMenu.tsx`, `src/components/MapCanvas.tsx`, `src/components/settings/LayoutSettingsTab.tsx`, `src/components/ShelfScrollFrame.tsx`, `src/components/HeroBanner.tsx` | TODO |
| R8-TEST-008 | MEDIUM | DONE: a minimal anonymized deployment fixture now reproduces a complete library payload, provider status-versus-batch freshness, structured wishlist upstream failure, and canonical-ID seiyuu matching across aliases. The fixture contains only synthetic `v9…/s9…/c9…` identities and does not depend on the copied private database. The focused regression set passes 29/29 tests. | `tests/fixtures/deployed-regressions.ts`, `tests/deployed-regressions.test.ts`, `src/lib/db/repositories/stock-provider-maintenance.ts` | DONE |
| R8-TEST-009 | LOW | Problem: coverage output still includes harmless jsdom navigation warnings, which makes real failures harder to spot. Fix: identify navigation-triggering tests and mock navigation explicitly. | `tests/MoreNavMenu.branches.test.tsx`, `vitest.config.ts` | DONE_WITH_DIFF |
| R8-TEST-010 | MEDIUM | Problem: route security policy relies on source discipline and scattered tests. Fix: add one audit test that scans every `src/app/api/**/route.ts` for runtime, public-read marker, and localhost or token gate where appropriate. | `src/app/api/places/[id]/stock/route.ts`, `tests/api-route-policy.test.ts` | DONE_WITH_DIFF |
| R8-MD-001 | LOW | Problem: active TODO reports use a consistent table shape, but archived reports mix prose, partial tables, and old status labels. Fix: leave archives untouched, but add an index file that points to active reports and explains the current task table format. | `TODO/README.md`, `TODO/`, `TODO/OLD/` | DONE_WITH_DIFF |
| R8-MD-002 | LOW | Problem: markdown rendering is styled defensively, but imported markdown can still contain very wide code or tables that are only horizontally scrollable. Fix: add markdown fixture tests for tables, code blocks, links, and nested lists under mobile width. | `src/components/MarkdownView.tsx`, `tests/MarkdownView.test.tsx` | DONE_WITH_DIFF |
