# Bug audit tasks

| ID | Severity | Finding and implementation direction | Location | Status |
| --- | --- | --- | --- | --- |
| BUGA-001 | HIGH | Root metadata title composition can duplicate the product name on map and places routes because child metadata already includes it. Keep route titles semantic and let the root template append the product name once. | `src/app/layout.tsx`, `src/app/map/page.tsx`, `src/app/places/page.tsx` | DONE_WITH_DIFF |
| BUGA-002 | HIGH | VN stock results can race when navigating between VN pages: the section does not reset or abort stale work before applying a late response. Add request cancellation or sequence guards and reset loading state on VN change. | `src/components/StockPricesSection.tsx` | DONE_WITH_DIFF |
| BUGA-003 | HIGH | Collection listing accepts a `limit` value but the backing query path does not consistently enforce it. Apply a bounded SQL limit and test the route contract. | `src/app/api/collection/route.ts`, `src/lib/db.ts` | VERIFIED_EXISTING |
| BUGA-004 | MEDIUM | Rotated `SafeImage` sizing uses `Math.min`, shrinking images too aggressively and producing inconsistent framing. Calculate bounds from orientation and container aspect ratio. | `src/components/SafeImage.tsx` | DONE_WITH_DIFF |
| BUGA-005 | MEDIUM | The settings description says header page spacing is disabled by default while the stored default evaluates to enabled. Align default behavior and copy. | `src/lib/settings/client.tsx`, `src/components/SettingsButton.tsx` | DONE_WITH_DIFF |
| BUGA-006 | MEDIUM | Map markers can receive non-finite latitude or longitude values from persisted place data. Validate coordinates at the boundary and omit invalid markers with a visible diagnostic. | `src/components/MapPageClient.tsx`, `src/components/MapCanvas.tsx`, `src/lib/db.ts` | DONE_WITH_DIFF |
| BUGA-007 | MEDIUM | Stock marketplace parsing relies on English presentation strings for provider-specific structured results. Preserve structured availability and source-native labels. | `src/lib/stock.ts` | DONE_WITH_DIFF |
| BUGA-008 | LOW | Shelf fullscreen focus restoration runs against an initial mount path and can restore focus unexpectedly. Restore only after a real fullscreen close transition. | `src/components/ShelfSpatialFullscreen.tsx` | DONE_WITH_DIFF |
| BUGA-009 | HIGH | Successful VNDB wishlist mutations can surface as `502` because the proxy wrapper reconstructs HTTP `204` responses with a body, which the platform rejects. Return bodyless responses for bodyless status codes and cover the regression. | `src/lib/proxy-fetch.ts`, `tests/safe-fetch.test.ts` | DONE_WITH_DIFF |
| BUGA-010 | MEDIUM | Rotated VN hero artwork applied cover-oriented scaling to both the blurred background and contained foreground layers, causing contained artwork to over-scale. Compute layer-specific rotation styles and pin the integration contract. | `src/components/HeroBanner.tsx`, `tests/safe-image-rotation.test.ts` | DONE_WITH_DIFF |
| BUGA-011 | HIGH | Library Filters can crash when the place registry is non-empty because `/api/places` returns structured rows while autocomplete consumers render that field as strings. Return a separate `known_places` string list and update every consumer. | `src/app/api/places/route.ts`, `src/components/LibraryClient.tsx`, `src/components/EditForm.tsx`, `src/components/OwnedEditionsSection.tsx`, `tests/place-coordinates.test.ts` | DONE_WITH_DIFF |
| BUGA-012 | HIGH | Map rendering statically imports a helper from the Leaflet canvas module, evaluating browser-only Leaflet code during SSR and forcing a client-render fallback. Move viewport persistence into a browser-safe helper module and keep Leaflet behind the dynamic boundary. | `src/components/MapPageClient.tsx`, `src/components/MapCanvas.tsx`, `src/lib/map-view-storage.ts`, `tests/bug-regression-source-pins.test.ts` | DONE_WITH_DIFF |
| BUGA-013 | LOW | `LoadingImage` removes its skeleton after an image error but leaves an invisible image and no fallback, producing blank producer-logo and quote-avatar slots. Render an accessible placeholder for failed loads. | `src/components/LoadingImage.tsx`, `tests/image-loading-skeletons.test.ts` | DONE_WITH_DIFF |
| BUGA-014 | HIGH | Place coordinates only require finite numbers, so impossible latitude or longitude values can be stored and passed to map rendering. Enforce legal geographic ranges and reject malformed coordinate field types instead of clearing them silently. | `src/lib/place-coordinates.ts`, `src/app/api/places/route.ts`, `src/app/api/places/[id]/route.ts`, `tests/place-coordinates.test.ts` | DONE_WITH_DIFF |
| BUGA-015 | MEDIUM | Shelf create and resize routes pass fractional dimensions into a DB helper that silently floors and clamps them. Validate explicit dimensions at the HTTP boundary so malformed edits fail visibly. | `src/app/api/shelves/route.ts`, `src/app/api/shelves/[id]/route.ts`, `src/lib/db.ts`, `tests/api-strict-input-validation.test.ts` | DONE_WITH_DIFF |
| BUGA-016 | HIGH | Banner multi-field patches persist focal position before validating rotation. Validate the complete request before the first write so a `400` response never leaves a partial banner adjustment. | `src/app/api/collection/[id]/banner/route.ts`, `tests/cover-rotation.test.ts` | DONE_WITH_DIFF |
| BUGA-017 | MEDIUM | Reading-goal mutations silently replace invalid explicit years with the current year, causing writes to the wrong annual goal. Keep GET fallback behavior but reject malformed POST years. | `src/app/api/reading-goal/route.ts`, `tests/api-strict-input-validation.test.ts` | DONE_WITH_DIFF |
| BUGA-018 | MEDIUM | Case-insensitive VN id validators are followed by case-sensitive database lookups, writes, or upstream calls on reading queue, series, EGS mapping, AliceNet, wishlist, VNDB status, EGS sync, and selective-download paths. Normalize accepted ids before lookup, persistence, logging, and outbound use, with persistence-layer normalization for internal callers. | `src/app/api/reading-queue/route.ts`, `src/app/api/series/[id]/vn/[vnId]/route.ts`, `src/app/api/egs/[id]/vndb/route.ts`, `src/app/api/alicenet/[code]/link/route.ts`, `src/app/api/wishlist/[id]/route.ts`, `src/app/api/vn/[id]/vndb-status/route.ts`, `src/app/api/egs/sync/route.ts`, `src/app/api/collection/full-download/route.ts`, `src/lib/db.ts`, `tests/canonical-vn-id-routes.test.ts` | DONE_WITH_DIFF |
| BUGA-019 | LOW | Saved-filter creation validates trimmed name and parameter strings but then persists the raw parameters and applies a second, stricter name truncation in the DB helper. Align the boundary with the real stored limits and persist validator output without silent truncation. | `src/app/api/saved-filters/route.ts`, `src/lib/db.ts`, `tests/saved-filter-validation.test.ts` | DONE_WITH_DIFF |
| BUGA-020 | MEDIUM | Several GET routes parse numeric controls with `parseInt`, truncation, or permissive `Number` coercion, so values such as `12junk` and fractional limits can silently reach SQLite or upstream requests. Centralize strict safe-integer query parsing with bounded defaults and use it for activity, stock recent, collection character search, EGS search, tags, and traits. | `src/lib/api-query.ts`, `src/app/api/activity/route.ts`, `src/app/api/stock/recent/route.ts`, `src/app/api/collection/characters/route.ts`, `src/app/api/egs/search/route.ts`, `src/app/api/tags/route.ts`, `src/app/api/traits/route.ts`, `tests/api-query.test.ts`, `tests/api-input-caps.test.ts` | DONE_WITH_DIFF |
| BUGA-021 | MEDIUM | Shared VN-id validation intentionally accepts uppercase input, but collection subroutes and VN aspect/list routes pass accepted raw IDs into case-sensitive storage, activity labels, synthetic release checks, or upstream work. Normalize accepted route IDs once before use and pin every shared-validator caller. | `src/lib/vn-id-shape.ts`, `src/lib/vn-id.ts`, `src/app/api/collection/[id]`, `src/app/api/vn/[id]/aspect/route.ts`, `src/app/api/vn/[id]/lists/route.ts`, `tests/canonical-vn-id-dynamic-routes.test.ts` | DONE_WITH_DIFF |
| BUGA-022 | MEDIUM | Browser-persisted map view state accepts finite but geographically invalid coordinates and writes unnormalized viewport values. Reuse the canonical coordinate guard for reads and writes, clamp zoom before persistence, and reject corrupt storage rows. | `src/lib/map-view-storage.ts`, `tests/map-view-storage.test.ts` | DONE_WITH_DIFF |
| BUGA-023 | MEDIUM | Per-place stock browsing silently converts failed or malformed `/api/places/[id]/stock` responses into a resolved empty state, hiding operational errors. Surface structured API failures, reject invalid success payloads, and cancel mount requests on navigation. | `src/components/PlaceVnBrowser.tsx`, `tests/bug-regression-source-pins.test.ts` | DONE_WITH_DIFF |
| BUGA-024 | MEDIUM | Map and place-modal Nominatim searches do not cancel the preceding request, so an older response can overwrite newer results or restore results after the operator clears them. Track one request owner per surface and abort stale searches before applying state. | `src/components/MapPageClient.tsx`, `src/components/AddEditPlaceModal.tsx`, `tests/map-privacy.test.ts` | DONE_WITH_DIFF |
| BUGA-025 | MEDIUM | Mapping pickers do not consistently cancel search work when a modal closes, and the AliceNet remap dialog allows older VNDB search responses to overwrite newer hits. Track one active request per picker, abort stale work, and clear search progress on close. | `src/components/LinkToVndbButton.tsx`, `src/components/MapVnToEgsButton.tsx`, `src/components/MapEgsToVndbButton.tsx`, `src/components/alicenet/AliceNetLinkDialog.tsx`, `tests/search-request-cancellation-source.test.ts` | DONE_WITH_DIFF |
| BUGA-026 | MEDIUM | The lazy list-membership picker retains registry rows, checked memberships, and the count chip when its `vnId` prop changes in place, so client navigation can display and mutate the previous VN's state. Reset the per-VN cache and abort obsolete loads on identity changes. | `src/components/ListsPickerButton.tsx`, `tests/client-fetch-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-027 | MEDIUM | Stock-batch quick-add scope pagination is not owned by the component lifecycle. Navigating away leaves page fetches active, and repeated clicks can launch duplicate reads for the same scope. Abort active scope requests on unmount, reject duplicate scope loads, and disable the active affordance. | `src/components/StockBatchClient.tsx`, `tests/client-fetch-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-028 | HIGH | EGS manual refresh reports success and dispatches downstream refresh work even when its upstream read fails because `load()` absorbs the failure. VNDB and EGS integration panels also allow older reads to overwrite newer state. Give each panel one abortable read owner and only announce EGS success after a successful refresh. | `src/components/EgsPanel.tsx`, `src/components/VndbStatusPanel.tsx`, `tests/integration-panel-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-029 | HIGH | VN stock reads and provider refreshes do not share a reliable response owner. An initial GET or superseded provider refresh can overwrite a newer snapshot, clear the active spinner, and trigger a stale router refresh after abort. Same-route VN navigation can also retain the preceding VN's snapshot and controls because prop identity is not rehydrated. Own both request channels, invalidate reads before writes, reset VN-scoped state on identity change, and commit completion state only from the active controller. | `src/components/StockPanel.tsx`, `tests/stock-panel-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-030 | HIGH | EGS mapping and Eroge Price panels initialize their server payload only once. Same-route VN navigation or a refreshed server snapshot can leave the prior VN's match, candidate tabs, related-title links, and transient manual-match controls visible. Rehydrate all VN-scoped state from the new identity and clear derived title matches before resolving the new bundle. | `src/components/EgsPanel.tsx`, `src/components/ErogePricePanel.tsx`, `tests/same-route-hydration-source.test.ts` | DONE_WITH_DIFF |
| BUGA-031 | HIGH | Detail-page editable client islands retain drafts and optimistic state across identity changes. A same-route VN or release navigation can expose the preceding item's favorite state, VNDB pending state, synopsis draft, activity note draft, game-log editor, or release-owned state. Reset prop-backed and transient state whenever the owning VN or release changes and reject completion work from obsolete mutation owners. | `src/components/FavoriteToggleButton.tsx`, `src/components/VndbStatusPanel.tsx`, `src/components/CustomSynopsis.tsx`, `src/components/ActivityTimeline.tsx`, `src/components/GameLog.tsx`, `src/components/ReleaseOwnedToggle.tsx`, `tests/same-route-hydration-source.test.ts` | DONE_WITH_DIFF |
| BUGA-032 | MEDIUM | Wishlist refreshes do not have a single response owner. A slower mount or manual GET can overwrite a newer snapshot or reintroduce rows removed while that read was active. Own wishlist reads with one abort controller, invalidate reads before deletion, and skip stale completion state. | `src/components/WishlistClient.tsx`, `tests/wishlist-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-033 | MEDIUM | Reusable organizer and display client islands initialize prop-backed state once and assume App Router remounts them. Same-route navigation, history traversal, or refreshed server props can retain previous compare chips, cover-picker controls, shelf display overrides, series metadata drafts, brand-overlap selectors, or generic detail-layout state. Rehydrate each island when its canonical props change. | `src/components/CompareVnPicker.tsx`, `src/components/CoverSourcePicker.tsx`, `src/components/ShelfReadOnlyControls.tsx`, `src/components/SeriesMetaEditor.tsx`, `src/components/BrandOverlapPicker.tsx`, `src/components/DetailReorderLayout.tsx`, `tests/reusable-island-hydration-source.test.ts` | DONE_WITH_DIFF |
| BUGA-034 | HIGH | Search treats URL parameters as mount-only defaults. Browser back/forward or same-route navigation can change `/search?...` while the visible query, source tab, advanced filters, and results remain from the preceding URL. Advanced POST searches also have no response owner, allowing an older submission to overwrite a newer result set. Distinguish URL writes owned by the component from external history changes, rehydrate controls for external changes, and abort superseded advanced requests. | `src/components/SearchClient.tsx`, `tests/search-url-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-035 | MEDIUM | Shelf editor reconciliation reads are only partially owned. Active-shelf reads have no teardown cleanup, while pool and shelf-list reloads are unowned and some resize/delete paths bypass the shared helpers entirely. A slower obsolete snapshot can overwrite newer shelf state. Give each reconciliation channel one abort controller, clean all channels up on unmount, and route every pool reload through the owned helper. | `src/components/ShelfLayoutEditor.tsx`, `tests/shelf-editor-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-036 | HIGH | VN artwork and aspect controls allow stale mutation completions from the preceding VN to commit into reused or unmounted detail islands after same-route navigation. Capture the owning VN for each mutation, invalidate owners on teardown, reset transient state on identity changes, and reject obsolete success, error, and completion work. | `src/components/CoverSourcePicker.tsx`, `src/components/HeroBanner.tsx`, `src/components/BannerControls.tsx`, `src/components/CoverUploader.tsx`, `src/components/AspectOverrideControl.tsx`, `src/components/CoverCompare.tsx`, `src/components/CoverRotationButtons.tsx`, `src/components/CoverHero.tsx`, `src/components/BannerSourcePicker.tsx`, `src/components/SetBannerButton.tsx`, `src/components/MediaGallery.tsx`, `tests/artwork-mutation-identity-source.test.ts` | DONE_WITH_DIFF |
| BUGA-037 | HIGH | VN source-preference selectors initialize optimistic state once and accept late mutation completions after same-route navigation or teardown. Rehydrate controls from canonical props, invalidate obsolete owners, and suppress stale success and rollback work. | `src/components/SourceSwitcher.tsx`, `src/components/FieldCompare.tsx`, `src/components/BrandCompare.tsx`, `src/components/PlaytimeCompare.tsx`, `tests/source-preference-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-038 | HIGH | Lightweight VN detail actions can apply late mutation completion work after the owning VN changes or unmounts, retaining stale queue, add-banner, smart-status, wishlist, collection-toggle, or list-removal UI state and triggering obsolete refreshes. Reset identity-scoped state, invalidate owners on teardown, and reject stale completion work. | `src/components/QueueButton.tsx`, `src/components/NotInCollectionBanner.tsx`, `src/components/SmartStatusHint.tsx`, `src/components/CoverQuickActions.tsx`, `src/components/VnListMemberships.tsx`, `tests/vn-action-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-039 | HIGH | Route editing owns multiple asynchronous reads and writes without a VN identity guard. Same-route navigation can retain route drafts or let a preceding VN's add, patch, reorder, or delete completion overwrite the new route section. Reset VN-scoped drafts, invalidate obsolete owners, and guard every reconciliation path. | `src/components/RoutesSection.tsx`, `tests/routes-section-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-040 | CRITICAL | The VN edit form preserves several preceding-VN drafts across same-route navigation and can schedule an auto-save for the new VN using stale notes, edition, location, download, or tracking values. Fully reseed every field and ref on VN identity changes, suppress the transitional auto-save render, flush the preceding VN intentionally, and reject obsolete mutation completion work. | `src/components/EditForm.tsx`, `tests/edit-form-identity-source.test.ts` | DONE_WITH_DIFF |
| BUGA-041 | HIGH | Reusable series helper islands retain dismissed, draft, and pending state across VN or series identity changes and accept late link, create, or removal completions after teardown. Reset identity-scoped state, invalidate owners, and guard confirmation and mutation completion work. | `src/components/SeriesAutoSuggest.tsx`, `src/components/SeriesAddVnForm.tsx`, `src/components/SeriesRemoveVn.tsx`, `tests/series-helper-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-042 | MEDIUM | Producer detail refresh and logo controls retain transient state and accept late completions after producer identity changes or teardown. Reset state, invalidate owners, and suppress obsolete refresh, upload, remove, and info-fetch work. | `src/components/ProducerRefreshButton.tsx`, `src/components/ProducerLogoUpload.tsx`, `tests/producer-detail-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-043 | HIGH | List-management islands retain preceding-list drafts or accept late completion work after same-route navigation, confirmation dialogs, or teardown. Reset list-scoped state, invalidate obsolete owners, and suppress stale creation, metadata, menu, membership-add, and membership-remove reconciliation. | `src/components/ListMetaEditor.tsx`, `src/components/ListCardActions.tsx`, `src/components/CreateListForm.tsx`, `src/components/ListAddVnForm.tsx`, `src/components/ListRemoveVn.tsx`, `tests/list-management-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-044 | HIGH | Reading-goal reads and writes can complete after a year change, while the series registry accepts teardown completions and has no in-flight lock around create or delete actions. Bind reading-goal work to the active year, invalidate registry work on teardown, and prevent duplicate registry mutations. | `src/components/ReadingGoalCard.tsx`, `src/components/SeriesManager.tsx`, `tests/tracking-registry-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-045 | HIGH | Reusable VN, staff, scoped-refresh, and asset-download action controls accept late completions after identity changes or teardown and can queue duplicate mutations before React rerenders. Reset identity-scoped state, invalidate stale owners, and add immediate in-flight locks. | `src/components/AddMissingVnButton.tsx`, `src/components/StaffDownloadButton.tsx`, `src/components/RefreshScopeButton.tsx`, `src/components/DownloadAssetsButton.tsx`, `tests/reusable-action-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-046 | CRITICAL | Recycled VN cards and session controls retain stale add, menu, or running-timer state across VN identity changes, while delayed confirmations or mutations can update the replacement card or session. Rehydrate card state, reset VN timers, invalidate obsolete owners, and lock mutations synchronously. | `src/components/VnCard.tsx`, `src/components/CardContextMenu.tsx`, `src/components/SessionPanel.tsx`, `src/components/PomodoroTimer.tsx`, `tests/card-session-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-047 | HIGH | Home-section persistence captures stale rollback state, ignores reset broadcasts, and allows overlapping writes or teardown completions in the page editor. Track the live section snapshot, handle resets, serialize optimistic writes, and reject editor completion work after teardown. | `src/components/HomeSectionMenu.tsx`, `src/components/HomeLayoutEditorTrigger.tsx`, `tests/home-layout-persistence-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-048 | HIGH | Settings server writes are fire-and-forget and can race, while layout panels broadcast optimistic success before persistence completes and do not roll back failed drafts. Serialize setting writes, return explicit save outcomes, guard settings reads, and emit layout events only after confirmed writes. | `src/components/SettingsButton.tsx`, `src/components/settings/LayoutSettingsTab.tsx`, `tests/settings-layout-persistence-source.test.ts` | DONE_WITH_DIFF |
| BUGA-049 | HIGH | EGS sync and maintenance operations accept teardown completions, can queue duplicate work before rerender, and allow unowned maintenance reloads to overwrite newer snapshots. Guard component lifetime, lock mutations synchronously, and route maintenance reads through one abortable owner. | `src/components/EgsSyncBlock.tsx`, `src/components/DataMaintenance.tsx`, `tests/data-operations-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-050 | HIGH | Saved-filter loading and mutations retain obsolete request paths: teardown aborts only the initial read, later reloads are not owner-checked, and rapid create/delete actions can overlap. Route reads through one abortable owner, guard lifetime, and lock mutations synchronously. | `src/components/SavedFilters.tsx`, `tests/saved-filters-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-051 | HIGH | Place-card and place-detail deletion actions accept delayed confirmation or network completion after the owning place changes or unmounts. Bind each destructive action to its place identity, reset transient state on reuse, and lock deletion synchronously. | `src/components/PlaceCard.tsx`, `src/components/PlaceDetailClient.tsx`, `tests/place-action-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-052 | CRITICAL | Place registry and modal workflows use mount-only hydration, unowned reloads, and unguarded link, geocode, save, or move completion paths. Reusing a dialog or navigating away can overwrite newer place state, link the wrong branch, or refresh an obsolete surface. Own every async channel, reseed place-scoped drafts, and suppress stale completion work. | `src/components/PlaceBrowser.tsx`, `src/components/AssignProviderDialog.tsx`, `src/components/AddEditPlaceModal.tsx`, `tests/place-dialog-lifecycle-source.test.ts`, `tests/client-fetch-lifecycle-source.test.ts`, `tests/bug-regression-source-pins.test.ts` | DONE_WITH_DIFF |
| BUGA-053 | CRITICAL | VNDB and EGS mapping pickers retain preceding-item queries and mapping state across reusable island identity changes, while hydration, search, confirmation, and pin or promotion mutations can commit after teardown or replacement. Reseed identity-scoped state, abort reads, guard every completion, and lock mutations synchronously. | `src/components/MapEgsToVndbButton.tsx`, `src/components/MapVnToEgsButton.tsx`, `src/components/LinkToVndbButton.tsx`, `tests/mapping-action-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-054 | CRITICAL | The lazy list-membership picker owns reads but not optimistic writes. Rapid toggles and create-then-add can overlap, roll back newer memberships, or refresh a recycled card after its VN identity changes. Track the live membership snapshot, lock each list mutation and creation synchronously, and reject stale completion work. | `src/components/ListsPickerButton.tsx`, `tests/lists-picker-mutation-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-055 | CRITICAL | Cache cleanup and import or restore surfaces allow destructive work, delayed confirmations, and reload completion to outlive their component or overlap with a second submission. Own cache reads, lock destructive operations synchronously, abort uploads on teardown or supersession, and suppress obsolete completion work. | `src/components/CachePanel.tsx`, `src/components/ImportPanel.tsx`, `src/components/DropImport.tsx`, `tests/data-destructive-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-056 | HIGH | Selective collection download drains paginated rows without an abort owner and queues submissions without a synchronous lock. Filter changes or teardown can commit obsolete rows, and rapid clicks can queue duplicate full-download work. Abort superseded loads, reset scoped selection, lock submit immediately, and suppress stale completion work. | `src/components/SelectiveFullDownload.tsx`, `tests/selective-download-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-057 | CRITICAL | Bulk edit and delete loops have no synchronous operation lock, teardown cleanup, or selected-set identity guard. Rapid actions can overlap, and delayed confirmation or row completion can mutate or reconcile after the visible selection has changed. Abort on selection change or teardown, lock immediately, snapshot selected ids, and reject obsolete completion work. | `src/components/BulkActionBar.tsx`, `tests/bulk-action-lifecycle-source.test.ts`, `tests/bulk-action-progress.test.ts` | DONE_WITH_DIFF |
| BUGA-058 | HIGH | Bulk asset download exposes Stop but only flips a local boolean after the active request finishes, while start and retry entry points lack a synchronous lock and paginated collection reads outlive teardown. Use one run owner, abort active and pagination requests immediately, lock every entry path, and suppress obsolete completion work. | `src/components/BulkDownloadButton.tsx`, `tests/bulk-download-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-059 | CRITICAL | AliceNet inventory operations use mount-only reads, boolean-only Stop controls, and teardown-unsafe remap, reset, clear, and bulk-clear mutations. Reusing a dialog or leaving the page can commit obsolete snapshots, duplicate pipeline work, or reconcile a stale row. Own every async channel, abort active requests, lock mutations synchronously, and suppress obsolete completion work. | `src/components/AliceNetClient.tsx`, `src/components/alicenet/AliceNetLinkDialog.tsx`, `tests/alicenet-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-060 | HIGH | Stock-batch job creation and cancellation rely on render-time state, so rapid starts or stops can overlap, late responses can commit after teardown, and clearing the editable queue drops the only Stop handle for a running background job. Lock submission and cancellation synchronously, abort obsolete requests, preserve the active job handle, and poll its canonical status until completion. | `src/components/StockBatchClient.tsx`, `tests/stock-batch-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-061 | CRITICAL | Owned-edition reconciliation and mutations are not bound to the active VN identity. Sibling-triggered reloads can overwrite newer snapshots, rapid add/save/remove clicks can overlap, and delayed removal confirmation or network completion can mutate a section after same-route VN reuse or teardown. Route reloads through one abortable owner, lock mutations synchronously, reseed transient state, and reject obsolete completion work. | `src/components/OwnedEditionsSection.tsx`, `tests/owned-editions-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-062 | CRITICAL | Eroge Price candidate pinning, addition, canonical reload, and removal are not bound to one active mutation owner. Rapid interactions can overlap optimistic updates, stale rollbacks can restore obsolete candidate state, and completion work can update a recycled VN panel after teardown. Abort obsolete requests, lock the candidate mutation channel synchronously, and reject stale completions. | `src/components/ErogePricePanel.tsx`, `tests/eroge-price-panel-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-063 | HIGH | Search EGS-only collection insertion has no synchronous request lock or teardown owner. Rapid result clicks can queue duplicate insertion requests, and a delayed completion can update state or navigate after the search page has unmounted. Lock submission before fetch, abort on teardown, and suppress obsolete completion work. | `src/components/SearchClient.tsx`, `tests/search-egs-add-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-064 | CRITICAL | Stock detail mutations are split across render-time booleans without VN-scoped request ownership. Refresh, source add/remove, cache clear, alias add/remove, and suggestion aliases can overlap, overwrite newer snapshots, or commit after reusable-VN replacement and teardown. Serialize snapshot writers, own alias writes separately, abort every obsolete channel, and route suggestions through the canonical alias action. | `src/components/StockPanel.tsx`, `tests/stock-panel-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-065 | CRITICAL | EGS integration reads are owner-checked but panel refresh, unlink, and modal linking still depend on render-time booleans and unowned mutation requests. Rapid actions can overlap, delayed unlink can reconcile a replaced VN, and modal closure can allow an obsolete link completion to update the parent. Add panel operation locks, abortable unlink ownership, and an abortable synchronously locked picker link channel. | `src/components/EgsPanel.tsx`, `tests/egs-panel-mutation-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-066 | CRITICAL | VNDB status label toggles, list clearing, and nested detail saves are not owned across same-route VN replacement or teardown. Rapid actions can overlap, delayed confirmation can clear a replaced VN, and dirty editor drafts can survive into a different VN identity. Add abortable synchronous mutation locks at both levels, disable competing controls, and reseed editor drafts on identity change. | `src/components/VndbStatusPanel.tsx`, `tests/vndb-status-mutation-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-067 | CRITICAL | Release-owned toggles and shelf edition-popover metadata refreshes are not request-owned. Rapid clicks can duplicate the two-step collection-plus-edition insertion, and a delayed popover refresh can reconcile a recycled tile after identity replacement or teardown. Add abortable synchronous locks, reset identity-scoped state, and suppress obsolete completion work. | `src/components/ReleaseOwnedToggle.tsx`, `src/components/EditionInfoPopover.tsx`, `tests/shelf-inventory-action-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-068 | CRITICAL | Lightweight VN actions guard some stale completions with identity strings but do not own active writes. Rapid clicks can duplicate collection, wishlist, favorite, queue, smart-status, banner-add, and card-add requests, while teardown leaves network work alive. Add synchronous locks, per-action abort owners, teardown cancellation, and stale completion suppression across shared action surfaces. | `src/components/CoverQuickActions.tsx`, `src/components/FavoriteToggleButton.tsx`, `src/components/QueueButton.tsx`, `src/components/NotInCollectionBanner.tsx`, `src/components/SmartStatusHint.tsx`, `src/components/VnCard.tsx`, `tests/vn-action-lifecycle-source.test.ts`, `tests/card-session-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-069 | HIGH | Card context-menu PATCH requests retain only a VN identity string and remain alive after card replacement or menu teardown. The favorite action also leaves its optimistic heart state applied when an owned PATCH fails. Add abort ownership, stale completion suppression, and rollback for owned favorite failures. | `src/components/CardContextMenu.tsx`, `tests/card-session-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-070 | CRITICAL | VN detail journaling surfaces use identity strings and render-time booleans instead of one owned mutation channel. Synopsis save or clear, activity add or delete, and game-log add, edit, or delete can overlap or complete after same-route VN replacement and teardown. Abort obsolete requests, serialize writes synchronously, guard delayed confirmations, and reject stale completions. | `src/components/CustomSynopsis.tsx`, `src/components/ActivityTimeline.tsx`, `src/components/GameLog.tsx`, `tests/detail-editor-mutation-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-071 | HIGH | VN detail list-membership chips guard stale completion strings but do not own active removal requests. Rapid remove clicks can queue multiple DELETE writes, and teardown leaves obsolete network work alive. Add an abortable synchronous lock and suppress stale completion work. | `src/components/VnListMemberships.tsx`, `tests/vn-action-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-072 | CRITICAL | Compact VN artwork controls guard stale identity strings but leave mutation requests alive and rely on render-time busy state. Rapid clicks can overlap banner upload/reset, cover upload/remove, aspect persistence, rotation, banner promotion, or cover-source preference writes, while teardown leaves obsolete network work active. Add per-surface abort owners, synchronous locks, and stale completion suppression. | `src/components/BannerControls.tsx`, `src/components/CoverUploader.tsx`, `src/components/AspectOverrideControl.tsx`, `src/components/CoverRotationButtons.tsx`, `src/components/SetBannerButton.tsx`, `src/components/CoverCompare.tsx`, `tests/artwork-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-073 | CRITICAL | Hero-banner rotation, focal-position save, and focal-position reset share optimistic visual state but rely on render-time busy state and identity-only guards. Rapid actions can overlap or finish after reusable-VN replacement and teardown, allowing obsolete reconciliation and rollback work. Serialize banner PATCH writes through one abortable owner and reject stale completions. | `src/components/HeroBanner.tsx`, `tests/hero-banner-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-074 | CRITICAL | Banner-source selection and media-gallery promotion retain identity-only guards and render-state busy checks. Rapid upload, reset, source-selection, or tile-promotion actions can overlap, while teardown leaves obsolete banner and cover writes active. Serialize writes per surface, abort obsolete requests, and suppress stale reconciliation. | `src/components/BannerSourcePicker.tsx`, `src/components/MediaGallery.tsx`, `tests/artwork-picker-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-075 | CRITICAL | Cover-source selection has five render-state-gated write paths and compound cover-plus-source-pref operations without one active request owner. Rapid clicks or teardown can overlap cover uploads, resets, EGS preference changes, rotations, and source picks, while best-effort preference writes continue after the owning VN is obsolete. Thread one abortable mutation owner through each complete operation and suppress stale reconciliation. | `src/components/CoverSourcePicker.tsx`, `tests/cover-source-picker-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-076 | CRITICAL | VN source-preference selectors for generic fields, rich text, brands, and playtime retain identity-only guards and transition-state gating. Rapid option clicks can overlap PATCH writes, stale rollbacks can restore obsolete choices, and teardown leaves network work active. Add abortable synchronous locks, owned rollback snapshots, and disabled competing controls across source-comparison surfaces. | `src/components/SourceSwitcher.tsx`, `src/components/FieldCompare.tsx`, `src/components/BrandCompare.tsx`, `src/components/PlaytimeCompare.tsx`, `tests/source-preference-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-077 | CRITICAL | Route editing relies on render-time `busy` state while create, patch, reorder, delete, and note writes remain active after VN replacement or teardown. Rapid interactions can overlap writes, mutation-triggered reloads can reconcile obsolete snapshots, and failed reorders do not restore their owned optimistic snapshot immediately. Serialize the route mutation channel synchronously, abort obsolete work, thread ownership through reloads, and restore failed optimistic reorders. | `src/components/RoutesSection.tsx`, `tests/routes-section-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-078 | CRITICAL | Wishlist single-row and bulk deletions are not owned by the component lifecycle or one shared mutation lock. A delayed confirmation can delete an obsolete selection, row and bulk writes can overlap, refresh can race active deletion, and teardown can leave DELETE requests applying stale completion work. Serialize deletion writes, abort obsolete work, bind confirmation to the live selection snapshot, and suppress stale reconciliation. | `src/components/WishlistClient.tsx`, `tests/wishlist-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-079 | CRITICAL | Library custom-order reset and drag persistence are unowned. Reset ignores non-OK responses, reorder writes are fire-and-forget, rapid drags can overlap PATCH requests, teardown leaves writes active, and optimistic reorder replaces the page snapshot with only the visible filtered subset. Add one abortable synchronous owner, validate responses, roll back owned failures, disable sorting while saving, and merge reordered visible rows into the complete local snapshot. | `src/components/LibraryClient.tsx`, `src/components/SortableGrid.tsx`, `tests/library-custom-order-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-080 | CRITICAL | Shelf editor writes rely on render-time `busy` state while slot placement, front-display placement, unplacement, create, rename, resize, and delete requests remain active across teardown and shelf navigation. Rapid interactions can overlap writes, delayed prompt or confirmation work can target a replaced shelf, and obsolete mutation completions can reconcile the wrong visible shelf. Add one synchronous abortable mutation owner, bind shelf-scoped actions to the active shelf id, and suppress stale completion or rollback work. | `src/components/ShelfLayoutEditor.tsx`, `tests/shelf-editor-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-081 | CRITICAL | Read-only shelf display controls fire overlapping settings PATCH requests for slider changes and reset actions, ignore non-OK responses, broadcast optimistic state without an owned rollback, and leave active network work alive across shelf replacement or teardown. Serialize writes, track a confirmed hierarchy snapshot, cancel obsolete generations, validate responses, and roll back only the latest failed optimistic intent. | `src/components/ShelfReadOnlyControls.tsx`, `tests/shelf-readonly-persistence-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-082 | CRITICAL | Settings-modal server writes can start from the serialized queue after component teardown, and VNDB pull-status submission relies on render-time state with no abort owner. Rapid pull clicks can duplicate work, closing or unmounting leaves completion work active, and stale save or pull results can toast, refresh, or update modal state after ownership is lost. Add mounted guards, abort ownership, synchronous pull locking, and stale-completion suppression. | `src/components/SettingsButton.tsx`, `tests/settings-layout-persistence-source.test.ts` | DONE_WITH_DIFF |
| BUGA-083 | CRITICAL | Series metadata upload and save actions rely on identity-only guards and render-time state. Upload and save can overlap, rapid submissions can duplicate writes, same-route series replacement leaves network work active, and obsolete completion can update or refresh a recycled editor. Serialize editor mutations through one abort owner, abort on identity replacement or teardown, and disable competing controls while saving. | `src/components/SeriesMetaEditor.tsx`, `tests/series-meta-editor-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-084 | CRITICAL | Generic, VN, and series detail-layout editors persist settings with render-time saving state and no abort owner. Rapid saves can overlap, teardown leaves PATCH work active, and stale completion can apply layout broadcasts or refresh obsolete surfaces. Add one synchronous abortable save owner per host, abort on replacement or teardown, and reject obsolete completion work. | `src/components/DetailReorderLayout.tsx`, `src/components/VnDetailLayout.tsx`, `src/components/SeriesDetailLayout.tsx`, `src/app/character/[id]/page.tsx`, `src/app/staff/[id]/page.tsx`, `src/app/producer/[id]/page.tsx`, `src/app/series/[id]/page.tsx`, `tests/detail-layout-persistence-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-085 | CRITICAL | Maintenance refresh, EGS synchronization, and series-registry mutations suppress some teardown state updates but leave active requests alive. Obsolete work can continue server-side, same-surface replacement can release the wrong lock, and competing EGS controls can compute against a username still being persisted. Add abort ownership, owner-checked lock release, teardown cancellation, and disable competing controls. | `src/components/DataMaintenance.tsx`, `src/components/EgsSyncBlock.tsx`, `src/components/SeriesManager.tsx`, `tests/data-operations-lifecycle-source.test.ts`, `tests/tracking-registry-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-086 | HIGH | Home-layout writes, game-list export generation, and download-status fallback polling leave active fetches alive after teardown or fallback replacement. Home writes can release a newer lock, exports can update an unmounted control, and superseded polling snapshots can overwrite newer SSE or polling state. Add abort ownership, owner-checked completion, and teardown cancellation. | `src/components/HomeLayoutEditorTrigger.tsx`, `src/components/ExportGameListButton.tsx`, `src/components/DownloadStatusBar.tsx`, `tests/home-layout-persistence-lifecycle-source.test.ts`, `tests/infrastructure-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-087 | CRITICAL | Reusable asset-download, scoped-refresh, producer, staff, add-to-collection, and series-link actions retain identity guards and some boolean locks but leave requests active across identity replacement or teardown. Obsolete work can continue server-side and old completion can release a newer request lock. Add per-surface abort owners, synchronous locking, teardown cancellation, and owner-checked release. | `src/components/DownloadAssetsButton.tsx`, `src/components/RefreshScopeButton.tsx`, `src/components/ProducerLogoUpload.tsx`, `src/components/ProducerRefreshButton.tsx`, `src/components/StaffDownloadButton.tsx`, `src/components/AddMissingVnButton.tsx`, `src/components/SeriesAddVnForm.tsx`, `src/components/SeriesRemoveVn.tsx`, `tests/reusable-action-lifecycle-source.test.ts`, `tests/producer-detail-lifecycle-source.test.ts`, `tests/series-helper-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-088 | CRITICAL | List-management create, edit, menu, membership-add, and membership-remove surfaces retain render-time busy gates or identity-only guards. Rapid actions can duplicate prompts or writes, and active requests survive same-route replacement or teardown. Serialize each surface through an abortable owner, bind delayed dialogs to that owner, cancel obsolete requests, and release only the owning lock. | `src/components/CreateListForm.tsx`, `src/components/ListMetaEditor.tsx`, `src/components/ListCardActions.tsx`, `src/components/ListAddVnForm.tsx`, `src/components/ListRemoveVn.tsx`, `tests/list-management-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-089 | HIGH | Reading-goal saves, place deletion, saved-filter writes, and cache cleanup retain synchronous gates but leave active requests alive after identity replacement or teardown. Some delayed confirmations also own only a boolean lock, so cancellation cannot stop an obsolete destructive request. Add abortable mutation owners, pass signals through writes, and release locks only from the current owner. | `src/components/ReadingGoalCard.tsx`, `src/components/PlaceCard.tsx`, `src/components/PlaceDetailClient.tsx`, `src/components/SavedFilters.tsx`, `src/components/CachePanel.tsx`, `tests/tracking-registry-lifecycle-source.test.ts`, `tests/place-action-lifecycle-source.test.ts`, `tests/saved-filters-lifecycle-source.test.ts`, `tests/data-destructive-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-090 | CRITICAL | List-picker optimistic toggles, list creation, and series auto-suggestion joins or create-then-link writes retain boolean gates but leave active requests alive across VN replacement or teardown. The compound create flows can also continue their second write after ownership is lost. Add abortable owners per mutation channel, pass signals through complete operations, and release only the current owner. | `src/components/ListsPickerButton.tsx`, `src/components/SeriesAutoSuggest.tsx`, `tests/lists-picker-mutation-lifecycle-source.test.ts`, `tests/series-helper-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-091 | CRITICAL | Bidirectional VNDB/EGS mapping pins and synthetic-VN promotion retain synchronous boolean gates but leave active writes alive across reusable-dialog identity replacement or teardown. Synthetic promotion also owns a delayed confirmation without a cancellable request generation. Add abortable mutation owners, bind confirmation to the owner, pass signals through writes, and release only the current owner. | `src/components/MapVnToEgsButton.tsx`, `src/components/MapEgsToVndbButton.tsx`, `src/components/LinkToVndbButton.tsx`, `tests/mapping-action-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-092 | CRITICAL | VNDB clear-all and EGS unlink acquire their mutation locks only after awaiting confirmation. Rapid actions can open duplicate destructive dialogs or start competing writes while confirmation is pending, and the nested VNDB editor can release a replacement owner through a loose ref check. Acquire cancellable owners before confirmation and release only the exact controller. | `src/components/VndbStatusPanel.tsx`, `src/components/EgsPanel.tsx`, `tests/vndb-status-mutation-lifecycle-source.test.ts`, `tests/egs-panel-mutation-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-093 | CRITICAL | VN edit-form autosave, collection membership, and series membership writes have no synchronous request owners. A scheduled PATCH can race collection removal, delayed confirmation can target an obsolete VN, rapid series actions can overlap before render state catches up, and active requests survive identity replacement or teardown. Add separate abortable collection and series channels, preserve the intentional detached navigation flush, and release only exact owners. | `src/components/EditForm.tsx`, `tests/edit-form-mutation-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-094 | CRITICAL | Synopsis clear, activity deletion, game-log deletion, and hero collection removal reserve their mutation locks only after awaiting confirmation. Rapid actions can open duplicate destructive dialogs or let another write enter the surface while a dialog is pending. Reserve the abortable owner before confirmation, validate the same owner after the delay, and release it on cancellation. | `src/components/CustomSynopsis.tsx`, `src/components/ActivityTimeline.tsx`, `src/components/GameLog.tsx`, `src/components/CoverQuickActions.tsx`, `tests/detail-editor-mutation-lifecycle-source.test.ts`, `tests/vn-action-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-095 | CRITICAL | Stock alias and source deletion plus series deletion reserve mutation ownership only after awaiting confirmation, while provider-assignment writes have a boolean gate but no abort owner. Duplicate dialogs can overlap later writes and closing a provider dialog leaves link, unlink, or move requests active. Acquire exact abort owners before delayed dialogs, cancel obsolete provider writes on teardown, and release only the owning generation. | `src/components/StockPanel.tsx`, `src/components/SeriesManager.tsx`, `src/components/AssignProviderDialog.tsx`, `tests/stock-panel-request-lifecycle-source.test.ts`, `tests/tracking-registry-lifecycle-source.test.ts`, `tests/place-dialog-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-096 | CRITICAL | Route deletion and custom-order reset reserve their mutation channels only after confirmation, while AliceNet reset and row-clear use boolean prompt locks without assigning abort owners until after confirmation. Rapid interactions can open competing dialogs or leave obsolete delayed work outside the teardown cancellation graph. Acquire exact owners before awaiting dialogs and release them on cancellation. | `src/components/RoutesSection.tsx`, `src/components/LibraryClient.tsx`, `src/components/AliceNetClient.tsx`, `tests/routes-section-lifecycle-source.test.ts`, `tests/library-custom-order-lifecycle-source.test.ts`, `tests/alicenet-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-097 | CRITICAL | Wishlist bulk deletion leaves the confirmation window unlocked, Pomodoro playtime logging has no abort owner, and series unlink plus shelf rename/delete assign controllers only after delayed dialogs. Duplicate dialogs and obsolete delayed completion work can overlap writes or survive teardown. Reserve exact abort owners before awaiting confirmation or prompt, release on cancellation, and suppress stale work. | `src/components/WishlistClient.tsx`, `src/components/PomodoroTimer.tsx`, `src/components/SeriesRemoveVn.tsx`, `src/components/ShelfLayoutEditor.tsx`, `tests/wishlist-request-lifecycle-source.test.ts`, `tests/card-session-lifecycle-source.test.ts`, `tests/series-helper-lifecycle-source.test.ts`, `tests/shelf-editor-request-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-098 | CRITICAL | Place-modal saves and home-section persistence still rely on boolean locks without abort ownership. Closing a place modal, switching its identity, or unmounting a home section leaves PATCH or POST writes active; late completion can release obsolete locks or refresh stale surfaces. Add exact abort owners, pass signals through writes, localize place API failures, and release only the active generation. | `src/components/AddEditPlaceModal.tsx`, `src/components/HomeSectionMenu.tsx`, `tests/place-dialog-lifecycle-source.test.ts`, `tests/home-layout-persistence-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-099 | CRITICAL | The VN release section treats loaded rows as permanent across same-route VN changes and owns per-row edition toggles with only a boolean lock. A recycled section can show the preceding VN's releases, leave POST or DELETE work alive after replacement, or dispatch an obsolete ownership event. Reseed on VN identity changes, abort mutation owners on replacement or teardown, pass signals through writes, and disable competing row actions. | `src/components/ReleasesSection.tsx`, `tests/releases-section-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-100 | HIGH | Place-browser create-and-link continuation and integration proxy tests leave active POST requests alive after target replacement or teardown. Branch linking can reconcile an obsolete branch, while rapid proxy-test clicks can queue duplicate requests because transition state is not a synchronous lock. Add exact abort owners, synchronous locks, signals, and owner-checked completion cleanup. | `src/components/PlaceBrowser.tsx`, `src/components/settings/IntegrationsSettingsTab.tsx`, `tests/place-dialog-lifecycle-source.test.ts`, `tests/proxy-test-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-101 | HIGH | Reusable map, EGS-rich, and quote read islands can apply or retain obsolete snapshots. Map geocoding does not verify its controller is still current after parsing, EGS-rich details do not clear the prior VN snapshot while loading or after failure, and quotes skip fetching entirely once a preceding VN has populated state. Add exact map-owner checks and reseed VN-scoped read state on identity replacement. | `src/components/MapPageClient.tsx`, `src/components/EgsRichDetails.tsx`, `src/components/QuotesSection.tsx`, `tests/read-island-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-102 | HIGH | Per-place stock browsing and EGS cover-candidate loading abort replaced requests but still apply decoded response state without checking whether cleanup canceled that generation. A response that resolves during replacement can briefly overwrite the new place or candidate grid. Recheck the owning signal immediately before committing decoded state. | `src/components/PlaceVnBrowser.tsx`, `src/components/CoverSourcePicker.tsx`, `tests/read-island-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-103 | HIGH | Compare-with, recommendation-seed, and similar-seed pickers retain transient rows, text, open state, and selections when App Router reuses the client island for a new VN or URL seed. A same-route navigation can expose choices from the preceding identity. Reset transient picker state and abort active searches whenever the canonical VN or seed identity changes. | `src/components/CompareWithButton.tsx`, `src/components/VnSeedPicker.tsx`, `src/components/SimilarSeedPicker.tsx`, `tests/picker-identity-lifecycle-source.test.ts` | DONE_WITH_DIFF |
| BUGA-104 | HIGH | Desktop grouped-navbar dropdowns remain inside the sticky blurred header stacking context and are not viewport-clamped. Safari can paint category items under the page or let the rightmost menu underflow beyond the usable viewport. Render desktop category menus through a body portal with fixed clamped coordinates and preserve outside-click plus keyboard navigation. | `src/components/MoreNavMenu.tsx`, `tests/navbar-desktop-portal-source.test.ts` | DONE_WITH_DIFF |
| BUGA-105 | MEDIUM | The dumped tracker has no durable way to exclude irrelevant games. Add a collection-level ignore flag, exclude ignored rows from active tracker totals, expose a recovery tab, and make the toggle available from both the VN edit form and dumped cards. | `src/lib/db.ts`, `src/lib/types.ts`, `src/app/api/collection/[id]/route.ts`, `src/components/EditForm.tsx`, `src/components/edit-form/OwnedEditions.tsx`, `src/components/DumpIgnoreButton.tsx`, `src/app/dumped/page.tsx`, `tests/dumped-vs-library-filter.test.ts` | DONE_WITH_DIFF |
| BUGA-106 | MEDIUM | Cover rotation controls can exceed narrow poster bounds and banner action clusters compete for width on compact detail layouts. Constrain cover controls inside their artwork container and let banner controls occupy a responsive, right-aligned wrapping surface without obscuring the hero. | `src/components/CoverRotationButtons.tsx`, `src/components/HeroBanner.tsx`, `tests/cover-rotation-ui.test.ts` | DONE_WITH_DIFF |
| BUGA-107 | HIGH | The library collection endpoint returns valid VNDB relation summaries without publisher arrays, but the strict client decoder requires `publishers` on every relation and rejects the entire HTTP 200 response. Model relation publishers as optional source data, normalize missing arrays for card rendering, and validate arrays when they are present. | `src/lib/types.ts`, `src/lib/vn-persisted-json-shape.ts`, `src/lib/collection-client-shape.ts`, `src/components/RelationsSection.tsx`, `tests/collection-client-shape.test.ts`, `tests/vn-persisted-json-shape.test.ts` | DONE_WITH_DIFF |
