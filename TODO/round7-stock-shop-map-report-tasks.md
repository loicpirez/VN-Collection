# Round 7 stock, shop, and map audit tasks

| ID | Severity | Finding and implementation direction | Location | Status |
| --- | --- | --- | --- | --- |
| STKMAPA-001 | HIGH | `/stock` refreshed only physical providers by default even though this page is the generic stock workspace. Default the standalone stock page to all configured provider groups while keeping VN detail focused on physical providers. | `src/components/StockLookupClient.tsx`, `src/components/StockPanel.tsx`, `tests/StockLookupClient.test.tsx`, `tests/StockPanel.branches.test.tsx` | DONE_WITH_DIFF |
| STKMAPA-002 | HIGH | AliceNet mirror operations were not integrated into `/stock`, making the operator update AliceNet from a separate feature surface while the stock page still showed cached AliceNet offers. Embed the AliceNet client in `/stock` and keep individual VN pages free of mirror-wide controls. | `src/components/StockLookupClient.tsx`, `src/components/AliceNetClient.tsx`, `README.md`, `FEATURES.md`, `CLAUDE.md`, `tests/alicenet-branding.test.ts` | DONE_WITH_DIFF |
| STKMAPA-003 | MEDIUM | AliceNet cached offers used the same stale label as live provider snapshots, so a fresh per-VN stock refresh looked broken when the AliceNet mirror itself was older. Use an AliceNet-specific stale hint that points to the mirror sync control. | `src/components/StockPanel.tsx`, `src/lib/i18n/dictionaries.ts`, `tests/StockPanel.branches.test.tsx` | DONE_WITH_DIFF |
| STKMAPA-004 | HIGH | `GET /api/alicenet` blocked the local mirror snapshot on live VNDB wishlist enrichment. In QA this delayed the route by seconds and made `/stock` feel unavailable when the upstream wishlist call stalled. Bound wishlist enrichment and render stock rows without wishlist badges when the live annotation is slow or unavailable. | `src/app/api/alicenet/route.ts`, `tests/alicenet-routes.test.ts` | DONE_WITH_DIFF |
| STKMAPA-005 | MEDIUM | `/places` displayed an unassigned-branch count while staying on an empty registered-places tab when the registry had no shops. Auto-open the unassigned branch view only for the zero-place, non-empty-branch state so AliceNet is immediately actionable. | `src/components/PlaceBrowser.tsx`, `tests/PlaceBrowser.test.tsx` | DONE_WITH_DIFF |
| STKMAPA-006 | LOW | Stock and place labels still used first-person French phrasing on the audited surfaces. Replace those labels with neutral collection and wishlist wording without changing the underlying filters. | `src/lib/i18n/dictionaries.ts` | DONE_WITH_DIFF |
| STKMAPA-007 | MEDIUM | Map privacy and layer ordering needed re-verification after the shop work. Browser QA confirmed the map page keeps external tiles disabled by default, has no horizontal overflow at 390 px, and source inspection confirms the Leaflet canvas is isolated at `z-0` while place dialogs use the shared `z-[1000]` modal layer. | `src/components/MapPageClient.tsx`, `src/components/MapCanvas.tsx`, `src/components/MapPrivacyControl.tsx`, `src/components/AddEditPlaceModal.tsx`, `tests/map-privacy.test.ts` | VERIFIED_EXISTING |
| STKMAPA-008 | LOW | The available project SQLite snapshots explain the observed stale-data confusion: the QA DB has AliceNet mirror rows but no generic provider offers or registered place rows. Keep the UI explicit about which sync owns each cached source and continue validating against the reproducible QA DB path. | `.qa/data/collection.db`, `src/components/StockPanel.tsx`, `src/components/AliceNetClient.tsx`, `src/components/PlaceBrowser.tsx` | VERIFIED_EXISTING |
| STKMAPA-009 | HIGH | Public-read route markers were exported from route modules, which violates Next route export contracts and breaks generated route typechecks after those routes are visited. Keep the marker as an imported top-level read instead of a public route export. | `src/app/api/{collection,lists,places,reading-queue,saved-filters,series,shelves}/**/route.ts`, `src/lib/api-route-meta.ts` | DONE_WITH_DIFF |
