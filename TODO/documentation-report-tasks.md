# Documentation audit tasks

| ID | Severity | Finding and implementation direction | Location | Status |
| --- | --- | --- | --- | --- |
| DOCA-001 | HIGH | The API inventory in `CLAUDE.md` does not enumerate every route file, including newer stock, place, display, and recovery surfaces. Regenerate the route table from the filesystem and document all real endpoints. | `CLAUDE.md`, `tests/claude-architecture-inventory.test.ts` | DONE_WITH_DIFF |
| DOCA-002 | HIGH | Refresh-scope documentation describes a request shape that no longer matches the implemented method and body contract. Update docs from the route implementation and add a contract test. | `CLAUDE.md`, `src/app/api/refresh/scope/route.ts`, `tests/claude-architecture-inventory.test.ts`, `tests/refresh-scope-route.test.ts` | DONE_WITH_DIFF |
| DOCA-003 | HIGH | Schema documentation is stale relative to the SQLite bootstrap tables and indexes. Reconcile the schema overview with direct inspection of `db.ts`. | `CLAUDE.md`, `src/lib/db.ts`, `tests/claude-architecture-inventory.test.ts` | DONE_WITH_DIFF |
| DOCA-004 | MEDIUM | Stock provider documentation does not clearly distinguish exact structured providers, search-only links, diagnostics, and unfinished capabilities. Document the capability matrix from code. | `README.md`, `FEATURES.md`, `CLAUDE.md`, `src/lib/stock.ts`, `tests/stock-provider-capabilities.test.ts` | DONE_WITH_DIFF |
| DOCA-005 | MEDIUM | Tutorial documentation and comments still describe a smaller tour than the current registry. Align docs, labels, and tests with the actual step set. | `TUTORIAL.md`, `src/components/TutorialTour.tsx`, `tests/tutorial-tour-a11y.test.ts` | DONE_WITH_DIFF |
| DOCA-006 | MEDIUM | Historical task snapshots and the active audit backlog are not clearly separated. Keep historical files intact and point active work to this `TODO/README.md`. | `TODO/`, `README.md` | DONE_WITH_DIFF |
| DOCA-007 | LOW | AliceNet naming was mixed across UI, docs, code identifiers, and old terminology. Use one canonical user-facing label and document migration compatibility where it is intentionally retained. | `README.md`, `FEATURES.md`, `CLAUDE.md`, `tests/alicenet-branding.test.ts` | DONE_WITH_DIFF |
| DOCA-008 | MEDIUM | Canonical docs still point at the pre-extraction stock capability module and describe the old settings-tab arrangement. Document the current module boundary and the consolidated eight-tab settings UI, then pin both contracts. | `CLAUDE.md`, `FEATURES.md`, `TUTORIAL.md`, `tests/stock-provider-capabilities.test.ts`, `tests/settings-tabs-a11y.test.ts` | DONE_WITH_DIFF |
| DOCA-009 | HIGH | Canonical docs intentionally preserved the old stock-browser compatibility names after the product was renamed. Update the route inventory, schema map, environment reference, and feature guide to the canonical AliceNet identifiers while documenting the isolated forward migration. | `README.md`, `FEATURES.md`, `CLAUDE.md`, `TUTORIAL.md`, `tests/alicenet-branding.test.ts`, `tests/claude-architecture-inventory.test.ts` | DONE_WITH_DIFF |
| DOCA-010 | LOW | Cover-source picker documentation still describes inferred initial-tab selection and trusted EGS banners even though the implementation deliberately opens Custom and must gate persisted remote candidates. Align the feature reference with the actual UI and trust boundary. | `FEATURES.md`, `TUTORIAL.md`, `CLAUDE.md` | DONE_WITH_DIFF |
| DOCA-011 | LOW | Canonical feature docs omit the shared root portal contract for custom dialog layouts and the route-journal page window. Document both so future UI work preserves modal stacking and long-list behavior. | `FEATURES.md`, `TUTORIAL.md`, `CLAUDE.md` | DONE_WITH_DIFF |
| DOCA-012 | HIGH | `CLAUDE.md` documents `GET /api/alicenet` as a flat stats snapshot that "never touches the AliceNet website", but the route returns `{ items, stats, pending, last_fetch }` (nested stats) and makes a VNDB network call via `fetchAuthenticatedWishlist()` to compute `in_wishlist`. Replace the shape and reword the no-network claim. | `CLAUDE.md:390`, `src/app/api/alicenet/route.ts:24` | DONE_WITH_DIFF |
| DOCA-013 | MEDIUM | The AliceNet "Download all" flow is documented as four steps but `runDownloadAll()` runs six, and three user-facing single ops (`match-vndb-from-egs`, `retry-vndb-aggressive`, `search-egs-no-vndb`) are undocumented. Update to six steps and list the standalone operations. | `README.md:118`, `FEATURES.md:877`, `src/components/AliceNetClient.tsx:600` | DONE_WITH_DIFF |
| DOCA-014 | MEDIUM | AliceNet filter tabs are documented as five but the client renders eight (All, Matched, VNDB, EGS only, Unmatched, None found, In collection, In wishlist). Update README/FEATURES and the CLAUDE manual-QA note to the actual eight. | `README.md:119`, `FEATURES.md:904`, `CLAUDE.md:1864`, `src/components/AliceNetClient.tsx:834` | DONE_WITH_DIFF |
| DOCA-015 | MEDIUM | FEATURES says the Wishlist tab is items in the local collection with `status='planning'` and that `listAliceNetStock()` joins to populate `in_wishlist`; both are wrong (the filter keys on `in_wishlist===1` computed from live VNDB Label 5, and the join only yields `in_collection`). Rewrite to the VNDB-Label-5 source and the separate `in_collection` tab. | `FEATURES.md:905`, `src/app/api/alicenet/route.ts:35`, `src/components/AliceNetClient.tsx:735` | DONE_WITH_DIFF |
| DOCA-016 | MEDIUM | The keyboard-shortcut reference tables are stale subsets: `ROUTE_SHORTCUTS` defines 23 `g <key>` shortcuts plus page-scoped ones, but TUTORIAL lists 10 and FEATURES lists 4. Regenerate both tables from `shortcut-registry.ts`. | `FEATURES.md:1046`, `TUTORIAL.md:439`, `src/lib/shortcut-registry.ts:23` | DONE_WITH_DIFF |
| DOCA-017 | MEDIUM | All three docs say `VNCOLL_DISABLE_ACTIVITY=true` disables the audit log, but the gate only honors the literal `'1'`, so `=true` is a no-op. Correct README/FEATURES/CLAUDE to `=1` (or widen the gate to accept `true`). | `README.md:253`, `FEATURES.md:715`, `CLAUDE.md:1139`, `src/lib/activity.ts:77` | DONE_WITH_DIFF |
| DOCA-018 | LOW | The `/data` export row ships four formats (JSON, CSV, ICS, plus a plain-text archive game-list at `GET /api/export/game-list`), but FEATURES and TUTORIAL only document three. Add the plain-text archive export to both lists. | `FEATURES.md:609`, `TUTORIAL.md:348`, `src/app/data/page.tsx:138` | DONE_WITH_DIFF |
| DOCA-019 | LOW | CLAUDE.md Safety rules claim "Both lockfiles exist for legacy reasons but yarn is canonical", but only `yarn.lock` is present. Drop the "both lockfiles exist" clause; keep yarn-is-canonical. | `CLAUDE.md:136` | DONE_WITH_DIFF |
| DOCA-020 | LOW | The CLAUDE manual-QA checklist lists nine settings tabs with separate Home and VN-page, and TUTORIAL/FEATURES reference "Settings -> Home tab" as top-level, but `SETTINGS_TABS` has eight and Home is an inner sub-tab of the Layout (vn-page) tab. Clarify that Home is a sub-tab under Layout. | `CLAUDE.md:1860`, `TUTORIAL.md:24`, `src/components/SettingsButton.tsx:68` | DONE_WITH_DIFF |
