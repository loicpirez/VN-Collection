# Security audit tasks

| ID | Severity | Finding and implementation direction | Location | Status |
| --- | --- | --- | --- | --- |
| SECA-001 | CRITICAL | Proxy-enabled outbound requests reuse a proxy agent across redirects without validating every hop. Resolve and reject off-allowlist or private-address targets before each proxied request. | `src/lib/proxy-fetch.ts` | DONE_WITH_DIFF |
| SECA-002 | CRITICAL | Proxy-enabled provider requests bypass the direct `safeFetch` allowlist path on the initial URL. Route both generic and stock proxy wrappers through a validating hop resolver. | `src/lib/proxy-fetch.ts` | DONE_WITH_DIFF |
| SECA-003 | HIGH | Cover and banner source fields accept path-like absolute URLs in code paths that do not consistently apply the SSRF allowlist. Normalize remote inputs through one validated boundary. | `src/lib/files.ts`, `src/app/api/**/image/**` | DONE_WITH_DIFF |
| SECA-004 | HIGH | Public collection listing responses expose private annotation-shaped data, including notes, download URLs, custom descriptions, and physical locations. Define a minimal public DTO or localhost-gate the endpoint. | `src/app/api/collection/route.ts`, `src/lib/db.ts` | DONE_WITH_DIFF |
| SECA-005 | HIGH | Downloaded and uploaded files are served through predictable asset paths without an explicit access boundary. Decide whether assets are private and enforce localhost or token access where required. | `src/app/api/files/[...path]/route.ts`, `tests/files-route-path-traversal.test.ts` | DONE_WITH_DIFF |
| SECA-006 | MEDIUM | Map geocoding and tiles send collection-derived location data to third parties without a privacy explanation or opt-in. Add an explicit map privacy control and document the network boundary. | `src/components/MapPrivacyControl.tsx`, `src/components/MapPageClient.tsx`, `src/components/AddEditPlaceModal.tsx`, `src/components/MapCanvas.tsx`, `tests/map-privacy.test.ts` | DONE_WITH_DIFF |
| SECA-007 | MEDIUM | Leaflet marker icons load from `unpkg.com`, adding an undeclared runtime dependency and privacy leak. Bundle marker assets locally. | `src/components/MapCanvas.tsx`, `public/leaflet/` | DONE_WITH_DIFF |
| SECA-008 | HIGH | Remote image downloads can persist SVG or mislabeled non-image bytes and serve them back inline. Sniff supported raster signatures before storage and reject every unsupported payload. | `src/lib/files.ts`, `src/app/api/files/[...path]/route.ts` | DONE_WITH_DIFF |
| SECA-009 | MEDIUM | Advanced VNDB search forwards unbounded language and platform arrays, accepts fractional values for integer controls, and buffers JSON without a stream cap. Bound the request body, cap and deduplicate multi-value filters, and reject fractional integer fields. | `src/app/api/search/advanced/route.ts`, `tests/search-advanced-validation.test.ts` | DONE_WITH_DIFF |
| SECA-010 | LOW | Printable QR labels compose their encoded origin directly from forwarded headers. Normalize protocol and host input before embedding an origin in generated QR payloads. | `src/app/labels/page.tsx`, `src/lib/qr-origin.ts`, `tests/qr-origin.test.ts` | DONE_WITH_DIFF |
| SECA-011 | HIGH | Shared JSON parsing buffers mutation bodies without a streamed byte cap, and collection import trusts header prechecks before parsing. Apply the existing stream limiter to JSON helpers and import parsing, then move remaining direct JSON consumers onto bounded paths. | `src/lib/api-body.ts`, `src/app/api/stock/summary/route.ts`, `src/app/api/collection/import/route.ts`, `tests/api-body-readjsonobject.test.ts` | DONE_WITH_DIFF |
| SECA-012 | HIGH | Multiple mutation routes coerce truthy values into booleans, and the VNDB status route forwards loosely typed arrays, notes, dates, and votes upstream. Reject malformed input at each HTTP boundary, cap label batches, and preserve exact booleans only. | `src/app/api/collection/[id]/route.ts`, `src/app/api/collection/[id]/owned-releases/route.ts`, `src/app/api/route/[routeId]/route.ts`, `src/app/api/series/[id]/vn/[vnId]/route.ts`, `src/app/api/vn/[id]/vndb-status/route.ts`, `tests/api-strict-input-validation.test.ts` | DONE_WITH_DIFF |
| SECA-013 | HIGH | Bulk and numeric mutation routes silently drop malformed array members, truncate oversized stock batches, or coerce numeric strings. Reject malformed payloads before applying any partial action and validate bounded integer fields explicitly. | `src/app/api/collection/order/route.ts`, `src/app/api/lists/[id]/items/route.ts`, `src/app/api/collection/full-download/route.ts`, `src/app/api/egs/sync/route.ts`, `src/app/api/stock/batch/route.ts`, `src/app/api/vn/[id]/erogamescape/route.ts`, `src/app/api/collection/[id]/game-log/route.ts`, `src/app/api/vn/[id]/stock/sources/route.ts`, `tests/api-strict-input-validation.test.ts` | DONE_WITH_DIFF |
| SECA-014 | MEDIUM | Mirrored-image download failures include the full upstream URL in thrown errors. Redact path and query material before errors can reach background-job logs or persisted diagnostics. | `src/lib/files.ts`, `tests/image-source-validation.test.ts` | DONE_WITH_DIFF |
| SECA-015 | HIGH | Remaining mutation routes floor fractional values or silently filter malformed provider and apply rows. Reject invalid stock-provider selections, Steam sync rows, manual activity timestamps, and AliceNet batch controls before any work starts. | `src/app/api/vn/[id]/stock/route.ts`, `src/app/api/steam/sync/route.ts`, `src/app/api/collection/[id]/activity/route.ts`, `src/app/api/alicenet/**/route.ts`, `tests/api-strict-input-validation.test.ts` | DONE_WITH_DIFF |
| SECA-016 | HIGH | Owned-edition nested aspect overrides are validated after the edition row update and silently coerce malformed dimensions, aspect keys, and notes. Validate the nested object first and commit edition plus override changes transactionally; apply the same note validation to VN-level aspect overrides. | `src/app/api/collection/[id]/owned-releases/route.ts`, `src/app/api/vn/[id]/aspect/route.ts`, `tests/api-owned-releases-date.test.ts`, `tests/aspect-route-and-activity.test.ts` | DONE_WITH_DIFF |
| SECA-017 | MEDIUM | List create, update, and reorder routes silently ignore malformed optional fields or accept duplicate ordering identifiers. Reject invalid optionals and duplicate reorder payloads before writing. | `src/app/api/lists/route.ts`, `src/app/api/lists/[id]/route.ts`, `src/app/api/lists/[id]/items/route.ts`, `src/app/api/reading-queue/route.ts`, `src/app/api/saved-filters/route.ts`, `src/app/api/shelves/route.ts`, `tests/api-input-caps.test.ts`, `tests/seca-input-length-caps.test.ts` | DONE_WITH_DIFF |
| SECA-018 | HIGH | Place registry mutations silently erase malformed optional values and accept arbitrary URL schemes that are rendered directly into clickable anchors. Validate optional metadata, normalize HTTP(S) URLs with `safeHref`, and gate persisted legacy URLs again at render time. | `src/app/api/places/route.ts`, `src/app/api/places/[id]/route.ts`, `src/components/PlaceCard.tsx`, `src/components/PlaceBrowser.tsx`, `src/components/PlaceDetailClient.tsx`, `tests/place-coordinates.test.ts`, `tests/safe-href.test.ts` | DONE_WITH_DIFF |
| SECA-019 | MEDIUM | AliceNet matching routes silently coerce malformed `retry_none` and `aggressive` values to `false`. Add a shared optional-boolean parser and reject malformed retry controls before invoking upstream work. | `src/lib/alicenet-route-input.ts`, `src/app/api/alicenet/match-next/route.ts`, `src/app/api/alicenet/search-egs-no-vndb/route.ts`, `tests/alicenet-route-input.test.ts` | DONE_WITH_DIFF |
| SECA-020 | MEDIUM | Collection and owned-edition physical-location tags silently truncate oversized arrays and tag values; owned-edition purchase locations silently truncate too. Reject oversized metadata visibly before persistence so operators do not lose annotations without feedback. | `src/app/api/collection/[id]/route.ts`, `src/app/api/collection/[id]/owned-releases/route.ts`, `tests/collection-patch-validation.test.ts`, `tests/api-owned-releases-date.test.ts` | DONE_WITH_DIFF |
| SECA-021 | MEDIUM | Shelf unplacement, route reordering, and series unlink boundaries are weaker than their corresponding add paths: malformed VN ids can reach delete helpers and duplicate route ids are accepted. Apply symmetric identifier validation and reject duplicate order payloads. | `src/app/api/shelves/[id]/slots/route.ts`, `src/app/api/shelves/[id]/displays/route.ts`, `src/app/api/collection/[id]/routes/route.ts`, `src/app/api/series/[id]/vn/[vnId]/route.ts`, `tests/shelf-vn-id-validation.test.ts`, `tests/api-strict-input-validation.test.ts` | DONE_WITH_DIFF |
| SECA-022 | HIGH | EGS-only collection adds fetch and persist upstream data before parsing the local mutation body, and silently map malformed explicit statuses to `planning`. Parse and validate the requested status before upstream work or database writes. | `src/app/api/egs/[id]/add/route.ts`, `tests/api-strict-input-validation.test.ts` | DONE_WITH_DIFF |
| SECA-023 | MEDIUM | Place-provider linking accepts an invalid explicit `from_place_id` as if no source place was supplied and stores unbounded labels. Reject malformed optional source ids and cap provider labels before persistence. | `src/app/api/places/[id]/link/route.ts`, `tests/places-route-validation.test.ts` | DONE_WITH_DIFF |
| SECA-024 | MEDIUM | Stock alias mutations treat every action other than the exact delete slug as an add, so malformed actions silently create aliases. Require an explicit `add` or `delete` action. | `src/app/api/vn/[id]/stock/aliases/route.ts`, `tests/stock-aliases-route.test.ts` | DONE_WITH_DIFF |
| SECA-025 | MEDIUM | Scoped refresh mutations silently drop malformed `params` containers and non-string values before resolving cache patterns. Reject malformed explicit params instead of executing a broader parameterless refresh. | `src/app/api/refresh/scope/route.ts`, `tests/refresh-scope-route.test.ts` | DONE_WITH_DIFF |
| SECA-026 | HIGH | Settings credential writes bypass the existing token-shape validator: Steam keys are silently truncated, Steam IDs accept non-SteamID64 lengths, and VNDB token validation is duplicated. Route credential fields through the canonical validator and deduplicate disabled stock-provider IDs. | `src/app/api/settings/route.ts`, `src/lib/input-validators.ts`, `tests/settings-route-validation.test.ts` | DONE_WITH_DIFF |
| SECA-027 | MEDIUM | Shelf, VN-route, and series mutation boundaries validate permissive lengths and then silently truncate names; shelf rename also ignores malformed explicit names, and series descriptions persist the pre-normalized input. Align each public limit with persistence and store validator output. | `src/app/api/shelves/route.ts`, `src/app/api/shelves/[id]/route.ts`, `src/app/api/collection/[id]/routes/route.ts`, `src/app/api/route/[routeId]/route.ts`, `src/app/api/series/route.ts`, `src/app/api/series/[id]/route.ts`, `tests/metadata-route-validation.test.ts` | DONE_WITH_DIFF |
| SECA-028 | MEDIUM | Steam manual linking accepts app ids outside the safe bounded range, persists untrimmed names, and allows names that the DB helper silently truncates. Validate app ids and normalized names at the route boundary, and normalize ids on unlink. | `src/app/api/steam/link/route.ts`, `tests/steam-link-validation.test.ts` | DONE_WITH_DIFF |
| SECA-029 | HIGH | Stock, media, and EGS client surfaces render dynamic upstream or persisted URLs directly into anchors. Apply `safeHref` again at render time so malformed legacy rows and upstream payloads cannot emit unsafe clickable schemes. | `src/components/StockPanel.tsx`, `src/components/StockPhysicalLocations.tsx`, `src/components/MediaGallery.tsx`, `src/components/EgsPanel.tsx`, `tests/safe-href.test.ts` | DONE_WITH_DIFF |
| SECA-030 | LOW | Proxy-test and source-preference validation errors reflect caller-provided text into JSON responses even though static diagnostics are sufficient. Return bounded static errors for invalid provider names and preference values. | `src/app/api/proxy/test/route.ts`, `src/app/api/collection/[id]/source-pref/route.ts`, `tests/reflected-error-validation.test.ts` | DONE_WITH_DIFF |
| SECA-031 | MEDIUM | Numeric API boundaries frequently require integers but not safe integers, allowing values above JavaScript's exact range to be rounded before SQLite lookup, ordering, or mutation. Require `Number.isSafeInteger` consistently for path identifiers, query identifiers, reorder arrays, and integer mutation controls. | `src/app/api/**/route.ts`, `tests/safe-integer-route-validation.test.ts` | DONE_WITH_DIFF |
| SECA-032 | HIGH | Eroge Price and EGS-rich client panels render persisted or upstream URLs into clickable anchors without the shared render-time scheme gate. Apply `safeHref` to retailer, game, official, brand, FANZA, and EGS detail links so malformed legacy or upstream values cannot emit unsafe schemes. | `src/components/ErogePricePanel.tsx`, `src/components/EgsRichDetails.tsx`, `tests/safe-href.test.ts` | DONE_WITH_DIFF |
| SECA-033 | HIGH | JSON collection import casts an untrusted decoded upload to the export type, validates only top-level arrays and ids, and can enter a partial restore transaction with malformed nested metadata, enums, timestamps, or series rows. Decode and normalize the complete backup contract before mutation, cap every imported table, and reject ambiguous duplicate identifiers. | `src/lib/collection-import.ts`, `src/app/api/collection/import/route.ts`, `tests/collection-import-validation.test.ts` | DONE_WITH_DIFF |
| SECA-034 | MEDIUM | Revoking map third-party network consent clears visible geocoding results but leaves active Nominatim requests running. Abort active geocoding immediately when consent is revoked and on unmount. | `src/components/MapPageClient.tsx`, `src/components/AddEditPlaceModal.tsx`, `tests/map-privacy.test.ts` | DONE_WITH_DIFF |
| SECA-035 | HIGH | EGS cover candidate listing returns raw persisted or upstream banner and VNDB image URLs directly to the browser before the trusted-host gate. Filter remote candidates through `isAllowedHttpTarget` and allow only local `/api/files` paths otherwise. | `src/app/api/egs-cover/[id]/candidates/route.ts`, `tests/egs-cover-raw.test.ts` | DONE_WITH_DIFF |
| SECA-036 | MEDIUM | Collection GET silently downgrades malformed sort, order, dumped, series, and year query inputs, accepts contradictory ranges, and lets an otherwise unused `group=aspect` query trigger full aspect materialization. Reject malformed public filters and run expensive aspect hydration only for an actual aspect filter. | `src/app/api/collection/route.ts`, `tests/api-collection-pagination.test.ts` | DONE_WITH_DIFF |
